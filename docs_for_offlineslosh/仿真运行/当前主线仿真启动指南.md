# 当前主线仿真启动指南

更新：2026-09-18。适用于 `feat/spmpc-liquid-control` 的 `raw_mpcc` / `planned_slosh` 开发验证，`formal=false`。本次文档整理没有启动新仿真。运行前先读[当前状态](../当前实验状态与文档索引.md)。

## 当前固定条件

| 项目 | 本批条件 |
| --- | --- |
| 控制代码 | `c82a09db` 对应实现；后续文档提交不改变控制逻辑 |
| 预测窗口 | N40、30 Hz、约 1.33 s |
| B0 / Full | `raw_mpcc` / `planned_slosh`；Full contour=1、曲率变化权重=0.0001、slosh 权重=5 |
| 任务 | 同一 S 路线任务，45 s 硬截止，位置/朝向容差 5 cm / 0.1 rad |
| 执行器 | `--identified-actuator`，按控制器的 delay/tau/gain 配置仿真适配器 |
| 上层计划 | 已校验的 `feasible_seed_plan.json`，`REPLAYED_FEASIBLE_SEED_NOT_REOPTIMIZED` |
| 评价 | odom 驱动液体模型；不作为独立液面真值；全过程异常另行核对 |
| 隔离环境 | `/data/a/scout_sim_replacement`；地图由 runner 显式指定 |

入口为 [run_trajectory_gazebo_smoke.py](../../scripts/run_trajectory_gazebo_smoke.py)。使用控制包 `src/scout_apps/control/spmpc_local_planner`，与历史 R8 仿真 fork 分开。旧指南中的 N60、60 s 和 N=9 模板不作为本批默认条件。

## 运行物与复现

最近补跑使用 `/tmp/scout_full_clean3_overlay_20260918/setup.bash`。原 `/tmp/scout_handoff_20260916_6XlFYy/ros_devel` 已不存在。该私有入口恢复自 Full R2 归档的节点和控制库；生成求解器、配置和任务按补跑 manifest 核对。完整说明见[三次无异常样本的运行物口径](../仿真实验分析/01_对比结果/20260918_B0与Full无异常样本对比.md)。

临时目录可能被清理。使用前核对入口、动态依赖与归档 hash；缺失时按归档恢复并记录实际依赖，不自动换成工作区旧二进制。归档位置：

```text
/data/a/scout_sim_replacement/results/20260918_b0_full_repeat3/full_02/case/runtime/
/data/a/scout_sim_replacement/results/20260918_full_clean3_supplement/manifest.json
/data/a/scout_sim_replacement/results/20260918_full_clean3_supplement/isolated_runtime.json
/data/a/scout_sim_replacement/results/20260918_full_clean3_supplement/restored_setup.bash
```

以下为单次 Full 模板。`NEW_FULL_CASE` 必须换成未使用的结果目录；默认 ROS/Gazebo 端口 `11892/11926` 应为空。

```bash
cd /home/a/scout_ws
source /tmp/scout_full_clean3_overlay_20260918/setup.bash
python3 scripts/run_trajectory_gazebo_smoke.py \
  --setup /tmp/scout_full_clean3_overlay_20260918/setup.bash \
  --output /data/a/scout_sim_replacement/results/NEW_FULL_CASE \
  --profile planned_slosh --identified-actuator \
  --task /data/a/scout_sim_replacement/results/20260917_startup_repair/raw_r01/task.json \
  --plan /data/a/scout_sim_replacement/results/20260917_startup_repair/feasible_seed_plan.json
```

B0 使用 `--profile raw_mpcc`，保留相同 `--task`、执行器适配和运行物，去掉 `--plan`，另用新结果目录。runner 的最终 `passed` 不代替 45 s 截止及全过程无异常验收；统计口径见[完整重复实验](../仿真实验分析/01_对比结果/20260918_B0与Full三组重复对比.md)。

## 运行与记录边界

- 每次独立启动和关闭 ROS/Gazebo，先录包再运动；核对实际参数、代码/运行物身份、任务和计划 hash，结束后检查端口释放与 bag 闭合。
- 不修改隔离环境的地图、world、launch 和脚本；仅停止本次启动的进程，不使用 `pkill` / `killall`。
- 全部尝试均保留。当前已有 B0 三次与 Full 三次无异常样本；Full 为累计六次尝试中保留三次，本页不自动安排追加试验。
- 原始数据留在 results；分析写入[仿真实验分析](../仿真实验分析/README.md)。后续先处理已知异常和验证边界，避障继续后置。
