# 同计划下层液体代价：三次对照

2026-09-19。本次按授权只补 `cost0_02/03` 两次，合并此前 `cost0_01`，与三次归档 Full 比较。**没有修改控制代码、控制参数或上层计划；没有补跑 Full、追加候选或开启时间优化。**

**两组各三次，Full 平均峰值/P95/RMS 降低 14.61%/24.80%/13.35%，平均到点慢 0.241 s。P95/RMS 的逐次范围完全分离，峰值范围仍有重叠，严格三项全改善未通过。** 这是小样本开发结果，不能证明峰值差异一定来自随机波动，也不能宣称统计显著。

## 条件与统计口径

沿用[单次消融](./20260919_同计划下层液体代价单次消融.md)的全部条件：共同蓝色几何和 progress 速度参考、同一 task/plan、28 维模型、控制运行物和物理设置。唯一实际参数差异为 `variants.B_slosh.w_slosh: 5 → 0`，保留液体状态与观测，在线硬液面约束继续关闭。三次 cost0 都由 bag 中 effective_config 证实权重为 0，其余运行参数与 Full 全量一致。

运输窗口仍为 `[0,t_stop)`，单位 mm，评价量为 odom 驱动的液体模型响应。全部六次均纳入，未筛选；两次新增各一次，480 s/次、总仿真进程预算 960 s，无重试。Full 为归档记录，**不是新跑或随机交错的三组配对**。

| 运行 | 运输峰值 / mm | P95 / mm | RMS / mm | 到点 / s |
| --- | ---: | ---: | ---: | ---: |
| cost0_01 | 0.539239 | 0.278454 | 0.125266 | 27.955 |
| cost0_02 | 0.712339 | 0.331796 | 0.138220 | 27.914 |
| cost0_03 | 0.524110 | 0.279717 | 0.122719 | 28.255 |
| full_01 | 0.540832 | 0.227137 | 0.112230 | 28.315 |
| full_02 | 0.477365 | 0.223009 | 0.110845 | 28.298 |
| full_03 | 0.498026 | 0.219080 | 0.111554 | 28.234 |
| cost0 均值 | 0.591896 | 0.296655 | 0.128735 | 28.041 |
| full 均值 | 0.505408 | 0.223075 | 0.111543 | 28.282 |

## 重复性与判定

采用“每一次 Full 均低于三次 cost0 各指标最小值”的严格开发口径；即逐指标比较 `max(Full) < min(cost0)`。该口径描述观察到的样本分离，不是置信区间或显著性检验。均值改善不能替代逐次全通过。

| 指标 / mm | cost0 范围 | Full 范围 | 严格逐次判定 |
| --- | ---: | ---: | --- |
| 峰值 | 0.524110～0.712339 | 0.477365～0.540832 | 未通过 |
| P95 | 0.278454～0.331796 | 0.219080～0.227137 | 通过 |
| RMS | 0.122719～0.138220 | 0.110845～0.112230 | 通过 |

此前主比较中的峰值增加约 0.00159 mm（0.30%），小于这次看到的组内峰值跨度；因此不能凭它断言 Full 稳定恶化。另一方面，峰值范围重叠仍然存在，也不能据均值下降宣布峰值稳定改善。P95/RMS 在本组六次中则保持一致方向。

## 执行、归档与范围

新增两次均在 45 s 内正常到点并持续停车，运输及停车后 5 s 观察窗完整，原运输/停车液面上限均通过。全程异常状态为 0；新增合计 1662 个 odom 求解拍无车辆/液体时间戳错配、无命令合同违规。运行前后冻结哈希一致，ROS/Gazebo 端口已释放，bag 正常闭合。逐次初始定位、身份与核验结果保存于同名 JSON。

原始数据：`/data/a/scout_sim_replacement/results/20260919_same_plan_cost0_repeat2_145556`。`protocol.json` 在运行前冻结范围与预算；`run_once.py` 复用单次脚本，仅增加输出编号；`cost0_02/03` 保存完整 bag、实际参数、日志、运行物及 assessment；`compare.py` 和 `comparison.json` 汇总全部六次。原 cost0 和 Full 目录只读复用，地址及 assessment 哈希写入同名 JSON。

本轮至此停止。结果支持同计划下层液体代价对 P95/RMS 的额外贡献；**峰值一致优势仍未建立**。固定原始 S 几何的整套方法公平对照、时间开关缩时收益和实物液面效果仍未验证，`formal=false`。
