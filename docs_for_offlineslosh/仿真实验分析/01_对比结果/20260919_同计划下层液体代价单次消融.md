# 同计划下层液体代价：单次消融

2026-09-19。按“快速、不要冗余设计、记好记录”的要求，只新增一次关闭下层液体代价的仿真，复用已归档的三次 Full。没有重新优化上层、追加重复、调参或开启时间搜索。

**同计划对照已跑通，但尚未证明下层液体代价能让三项指标稳定全改善。** 预先指定 `full_01` 为主比较对象：Full 的 P95/RMS 下降 18.43%/10.41%，峰值上升 0.30%（仅约 0.00159 mm），到点慢 0.360 s。不能把这样的小峰值差异认定为稳定恶化，也不能忽略它并宣布三指标全通过。

## 对照条件

两组使用完全相同的蓝色上层几何、progress 速度参考、task/plan、控制二进制、模型、非液体代价、运动限制和执行配置。保留 28 维液体模型及观测链，在线液面硬约束原本就关闭；**唯一实际参数差异为 `variants.B_slosh.w_slosh: 5 → 0`**。bag 内 effective_config 确认权重为 0，运行时参数全量比较确认其余字段完全一致。

这组称为“同计划关闭下层液体代价”，**不是原始 `raw_mpcc` B0**。上层防晃规划仍供双方使用；本实验仅检验下层液体代价的额外贡献，不能分离上层路径/速度优化各自收益，也不代表双方固定原始 S 几何的整套方法对照已完成。参考相同不意味着实际速度和实际轨迹相同。

## 结果

运输统计使用现有 `[0,t_stop)` 口径；单位 mm，液面为 odom 驱动的模型响应。Full 的三次旧记录全部列出，未挑选有利样本。

| 运行 | 运输峰值 / mm | P95 / mm | RMS / mm | 到点 / s |
| --- | ---: | ---: | ---: | ---: |
| 关闭下层液体代价（新增 1 次） | 0.539239 | 0.278454 | 0.125266 | 27.955 |
| full_01（归档） | 0.540832 | 0.227137 | 0.112230 | 28.315 |
| full_02（归档） | 0.477365 | 0.223009 | 0.110845 | 28.298 |
| full_03（归档） | 0.498026 | 0.219080 | 0.111554 | 28.234 |
| Full 三次归档均值（辅助） | 0.505408 | 0.223075 | 0.111543 | 28.282 |

Full 三次均值相对新 cost0：峰值下降 6.27%、P95 下降 19.89%、RMS 下降 10.95%，到点增加 1.17%。**这只是 3 次归档 Full 对 1 次新 cost0 的描述统计，不是新跑的三组配对，也不代表统计显著。** 三次 Full 的 P95/RMS 均低于 cost0，但 `full_01` 的峰值略高，因此逐次三指标全改善判定为否。

## 执行与身份核验

新增运行正常到点 27.955 s、持续停稳 27.954333 s，均在 45 s 内；运输和停车后 5 s 观察窗完整，运输/停车 0.90/0.08 mm 上限及 0.001 mm 容差通过。全程异常状态为 0；828 个 odom 求解拍无车辆/液体时间戳错配、无命令合同违规，QP 上限均为 20；启动初值来自同一上层计划。

两组 task、plan、地图、控制节点/库、生成模型和执行器适配器哈希一致。运行前后的冻结文件核对通过；ROS/Gazebo 端口释放，bag 正常闭合。初始定位存在正常微小差异，逐次位姿保存在同名 JSON，未因结果修改初态或重试。新运行耗时约 129.36 s；预算 480 s。

## 最小实现与复现

现有 [runner](../../../scripts/run_trajectory_gazebo_smoke.py) 增加 `--slosh-weight` 参数，仅适用于 `planned_slosh`，要求有限非负值。通过原有单次 overlay 注入，未更改控制器或默认权重；启动后核对实际加载值。验证包含 Python 编译检查、CLI 入口和上述真实单次闭环，不新增测试框架。

从[启动指南](../../仿真运行/当前主线仿真启动指南.md)的 Full 模板出发，使用新的输出目录，追加 `--slosh-weight 0` 即可复现。没有参数时仍使用原 Full 权重 5。

原始数据：`/data/a/scout_sim_replacement/results/20260919_same_plan_cost0_144122`。

- `protocol.json`：运行前冻结的范围、主比较对象、预算与哈希。
- `run_once.py`、`compare.py`：复用现有 runner、窗口和执行审核；单次执行与结果比较。
- `cost0_01/case/`：完整 bag、实际参数、日志、运行物和 summary。
- `cost0_01/assessment.json`、`comparison.json`：身份、窗口、异常及逐次差异。
- 归档 Full：`/data/a/scout_sim_replacement/results/20260919_slosh_early_brake_120624/full_01..03`，只读复用，没有重复运行。

本次止于如实记录：下层液体代价的 P95/RMS 显示正向效果，峰值改善还不稳定；原始 S 路径上的整套方法公平对照和时间开关缩时收益仍未验证。`formal=false`，不作为实物液面证据。
