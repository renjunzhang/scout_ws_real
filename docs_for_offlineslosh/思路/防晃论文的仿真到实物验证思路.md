# 防晃论文路线图：先仿真，再实物

- 更新日期：2026-08-23
- 当前分支：`offline-slosh-plan-online-tracking`
- 当前阶段性基线：`bcc54c7e 阶段性冻结：闭合正式配对统计分析合同`（未 push）
- 当前工作：**仿真 4 前置冻结：C4 多 seed development readiness 已完成；正在闭合 C1 任务可靠性和公平匹配**
- 总体状态：**seed 8601 与 8701--8705 C4 development 闭环均通过；C1/正式 session 未冻结使正式 readiness 仍为 `NO_GO`，正式 C0--C4 仿真和实物均未放行**

> 当前执行边界（2026-08-23）：不做正式 C0--C4 多种子实验。development/pilot 只用于修复 C4 稳定性、冻结 C1 和物化正式 session，不能写成论文正式性能结果。

## 0. 当前进度快照

| 阶段                   | 状态                   | 已做到哪里                                                                                   | 还缺什么                                          |
| ---------------------- | ---------------------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| 共用控制器代码         | **完成**         | 唯一最终命令出口、22 维双通道求解器、执行前沿、终端恢复判断和失败关闭均已接通                | 实车阶段重新标定参数                              |
| 仿真 1：独立 Plant     | **完成**         | 仿真专用独立 C++ Scout＋液体 Plant v1 已冻结，液体真值不进入控制器                           | 不能把该参数版本外推为实车或 CFD 真值             |
| 仿真 2：实验配置       | **部分完成**     | P2/V3/recovery、C0/C2/C3/C4/IS、正式 seeds/Williams 顺序和统计合同已冻结                     | C1 公平匹配和正式 hash-bound session 尚未冻结     |
| 仿真 3：轨迹与恢复规则 | **完成** | 全相位 recovery、held-out gate、完整 V3、Full SQP、预测域 B_exec、seed 8601 两次闭环及 8701--8705 readiness 均通过 | 不代表正式性能结论；实物 release 未放行 |
| 仿真 4：C0--C4 对比    | **未开始**       | 六种条件入口与 trial-level 分析合同已接通，严格 C3 定义和 C4 development readiness 完成 | C1 freeze、正式 session 和人工审查 |
| 实物线路               | **未开始**       | 只保留了接口和标定清单                                                                       | Scout、Nokov、IMU、RGB 和真实液面实验全部待做     |

当前已经确认的开发结果：

- P2 离线计划共 1070 个离散点、约 35.63 s；内部低阶模型的 Q95 从 0.8898 mm 降到 0.5615 mm。它只说明离线计划生成正常，不是独立 Plant 的正式防晃结论。
- 最终 recovery 数据共 44,940 条完整 rollout；fit/tune/held-out 各 14,980 条，seed 和 rollout_id 两两互斥。类别数依次为 14,282/698、14,279/701、14,281/699（正/负），没有修改标签或合成类别。
- 冻结 held-out gate 只评价一次并通过：coverage 0.7297，false-safe 5/699=0.00715，95% Wilson 上界 0.01663；它是低最差-bin-coverage的开发 gate，不是安全证书。
- 完整 P2 V3 共 1070 行，含 121-step `publish_zero_settle_hold_v2` 尾段、逐相位 recovery radii/execution bounds 和 `bounded_tracking_recovery_policy_v1`；生产 loader 通过。
- seed 8601 的精确回归确认旧根因：clock phase 9 的窗口 `[8,10]` 中，phase 10 虽是 9D 最优，但当前命令队列越过冻结 B_exec；前置过滤后 phase 10 被排除、phase 9 被选择，ACADOS 不再收到 phase 10 的硬不可行参数图像。
- B_exec 修复后的旧 RTI 两次 development/诊断运行都在 cycle 10 复现 `DELAY_AUGMENTED_ACADOS_SOLVE_FAILED_-2_RESIDUAL_REJECTED`。warm-start 的 stage/terminal 约束均有正余量，但 RTI 输出的 stationarity/inequality residual 分别为 23.2993/0.306303。
- Full SQP 使用 `FUNNEL_L1PEN_LINESEARCH`＋`HPIPM BALANCE`，内层 QP 四项容差为 `1e-9`，外层 KKT 合同保持 `1e-6`。solver ID 为 `delay_augmented_phase_acados_full_sqp_funnel_bexec_path_qp1e9_v4`，配置 hash 为 `340f38d007e11d3553d639ec24a376ba3f38c614954596931d6efbbfc5550841`。
- 14 项 terminal B_exec 已从归一化平方约束改为严格等价的双边参数仿射约束；OCP 每个预测 stage 同时约束完整 14 维执行状态，保证命令在队列逐格前移、半径逐相位收紧时仍保持 B_exec 不变。未修改 beta、residual authority、V3、recovery 或 Plant。
- `PhaseClock` 已将 1 ns 量化容差只用于 artifact `upper_bound` 查找，不修改原始时间审计。新 probe 的 1001 个有效 clock 从 0 单调到 1000，最大步进为 1，不再出现 `0,0,2` 跳相。
- 修正 terminal 约束尺度后，单次 probe 的 1001 次 ACADOS 调用全部成功，但 cycle 1002 暴露出旧“一步队尾约束”不能保证命令后续移位兼容；完整预测域 B_exec 修复后该停止消失。
- seed 8601 两次完整 development 闭环各有 1061 次 Full SQP：`solver_failures=0`、`controlled_stops=0`、`sequence_completed=true`、`task_success=true`，全部 current/terminal execution gate 通过；除 wall-time 列外，两份 cycle CSV 逐行一致。
- 两次共 2122 个 backend 样本的完整 wall time 为 P50 `2.2207 ms`、P95 `3.0568 ms`、P99 `5.3270 ms`、max `9.8945 ms`，30 Hz 的 `33.333 ms` 预算零超期。全周期最大四项 KKT 残差为 `9.998e-7/4.178e-9/0/9.942e-10`，均满足冻结合同。
- 严格 C3 已与 C4 共用同一 22D Full SQP、V3、B_exec、恢复动作和最终命令事务，唯一差异是 terminal empirical gate 为 monitor-only；seed 8601 在 monitor reject 后继续放行并于下一周期因无兼容候选失败关闭，验证了消融语义。
- C4 readiness seeds 8701--8705 已在完整 horizon B_exec 可达性前置见证修复后全部完成，证据位于 `/data/a/spmpc_exec_identification/phase_rejoin_c4_horizon_witness_readiness_dev_20260823.KJ3QZE`。
- C1 scale 1.0/0.78 的 task-only pilots 未形成跨 seed 完成可靠的候选，`pilot_tuned_and_frozen` 保持 false；不得使用任何液面指标调 C1。
- 连续基线 runner 现已显式绑定全部非负权重并在 summary 中记录，alpha-state 测量航向也会在 `±π` 处提升到连续求解分支；seed 8801 仍在首个急弯进入持续旋转/停滞，因此根因已从“配置未绑定”收敛到普通 MPCC 的任务闭环设计，不能靠解除 safety latch 或查看液面指标冻结 C1。
- 进一步用温和 Bézier路径和初始航向对齐的直线路径做了单 seed task-only 证伪：C1 仍分别在约 2.31 m 和 2.54 m 后停滞；Full SQP 与 direct-omega 替代后端也未形成可靠闭环。由此排除“只换一条简单路径即可”的解释，下一步必须先在直线、定曲率圆弧和曲率换向三类独立回归上闭合普通 comparator，再决定是否重建正式路径资产。
- 正式 seeds 3101--3116、16-block Williams 顺序、0.5 mm 主效应门、10,000 次 paired bootstrap 和 C0/C1 任务非劣界已写入可执行合同；模板审计仍明确返回 `NO_GO`。
- 聚焦构建、73 项 C++ 测试和 5 项 codegen Python 测试通过；`git diff --check` 通过。
- 当前没有正式 C0--C4 性能数据，因此**还不能判断完整方法的防晃性能是否正式正向**。

下一步按顺序做：

1. C4 在新 development seeds 上的稳定性已经闭合，继续保持 gate/V3/B_exec/KKT 不变；
2. 修复普通连续 MPCC 的急弯任务闭环，只用完成时间、tracking 和运动平滑指标冻结 C1；
3. C1 通过后物化 hash-bound 正式 session，使 readiness 从 `NO_GO` 变为 `READY_NOT_EXECUTED`；
4. 在人工审查许可前不运行正式 C0--C4 多种子实验；实物 release 仍须重新标定真实 Scout/液体参数。

### 整体路线

状态符号：`✅` 表示当前线路的完成条件已满足；`△` 表示已有开发实现或部分证据，但 release gate 仍未满足；`○` 表示尚未开始或尚无可验收结果。

```text
仿真和实物共用的准备工作
✅ 第1阶段  冻结可复现基线
   ↓
✅ 第2阶段  统一最终命令出口
   ↓
△  第3阶段  预计发布时间和双通道执行模型代码已完成，正式实车参数待标定
   ↓
✅ 第4阶段  在线周期显式选择时实际调用22维求解器，默认旧行为不变
   ↓
线路一：仿真，先完成
✅ 仿真1  建立最小独立C++“假Scout＋假液体”并通过smoke/pilot
△ 仿真2  P2/V3、严格C3、正式seed/order/statistics已冻结；C1和正式session仍未冻结
✅ 仿真3  全相位数据、held-out gate、Full SQP、seed 8601及C4 development readiness均已通过
○ 仿真4  完成C0–C4正式仿真
   ↓
仿真完成点
   ├→ 保留一篇只声称“仿真有效”的备用论文
   └→ 若继续冲击RA-L：进入实物线路
                    ↓
线路二：实物
○ 实物1  标定Scout、IMU和RGB
○ 实物2  完成实车基础验证
○ 实物3  重新生成实物专用轨迹和接回尾段的判断规则
○ 实物4  用实车参数重跑仿真，再做只观察、不控制的检查
○ 实物5  完成正式实物对比
   ↓
   ├→ 实物通过：形成带真实液面证据的RA-L稿件
   └→ 实物未通过：使用仿真备用稿，不声称真实液面降低
```

这里的核心关系是：

> **控制器代码和模型形式共用；仿真参数、实车参数、离线轨迹、恢复规则和正式数据分别冻结，不能混用。**

两条线路分别保留参数、轨迹和数据，但不是同时开工：**先完整做通仿真，再开始实物。**本文所说的“冻结”，就是把代码、参数和规则固定并记录下来，之后不能根据正式结果随意改动。

## 1. 为什么要分成两条线路

仿真和实物面对的“被控制对象”本来就不同：

| 内容           | 仿真线路                | 实物线路                     |
| -------------- | ----------------------- | ---------------------------- |
| 控制器内部代码 | 同一套 C++ 代码         | 同一套 C++ 代码              |
| 内部模型形式   | 同一套线/角双通道模型   | 同一套线/角双通道模型        |
| 内部模型参数   | 仿真版本参数            | 用实车数据辨识的参数         |
| 外部被控对象   | 独立的仿真 Scout 和液体 | 真实 Scout 和真实液体        |
| 离线防晃轨迹   | 按仿真参数生成          | 按实车参数重新生成           |
| 恢复判定规则   | 用独立仿真数据验证      | 用实车参数和独立数据重新验证 |
| 最终结论       | 方法在仿真中有效        | 真实液面是否降低             |

控制器内部模型要保持同一种含义，但参数不必相同。仿真中的“假 Scout＋假液体”反而不能与控制器内部模型完全一样，否则相当于控制器自己出题、自己判卷。

## 2. 共用准备第4阶段已经完成，但仍是正式 NO-GO

当前问题可以用一句话说明：

> 22维执行增广求解器已经由显式配置进入真实在线周期；当前缺口已经转为正式仿真资产，而不是在线 factory 接线。

本工作树已经闭合五件事：

1. 在线控制器能够选择并启动新的执行增广求解器；
2. 使用当前机器人状态、最终发布命令历史和两路命令队列构造每周期初态；
3. 补齐相对离线轨迹的正式目标、参数和在线修正边界；
4. 接入终端“能否重新接回防晃尾段”的判断，以及当前执行状态匹配条件；
5. 保持唯一最终命令出口和异常停止规则，完成在线闭环回归。

第 4 阶段完成条件：

- [X] 在线控制周期实际调用新求解器，而不是只在独立测试中运行；
- [X] 第一拍新命令按线、角各自延迟影响联合终点预测；
- [X] 命令历史不完整、时间异常或配置不匹配时拒绝正常控制；
- [X] 在线求解结果、终端判断、保存动作和最终发布命令可以完整追溯；
- [X] 原 opt-in 22 维 backend 的接线回归和既有 30 Hz 聚焦检查通过，并形成阶段记录；
- [X] 完整 SQP development 闭环已形成两次成功耗时分布，P99/max 均低于 30 Hz 预算；正式实物 release 仍因真实参数未标定而 NO-GO。

新的 backend 仍是严格 opt-in：默认 `continuous_mpcc_acados`、`delay_augmented_phase/enabled=false`，且默认 recovery hash 为空，所以旧行为不变、误开启会被拒绝。独立 plant、开发参数 v1、全相位 recovery、开发 held-out V3 和 C0--C4/IS 统一 runner 已经建立。cycle 10 的 phase 10 硬不可行问题已通过“22D B_exec 资格过滤后再做 9D 评分”从因果结构上修复；求解后的 current/terminal gate 保留。Full SQP 又通过数学等价的 terminal 双边仿射 B_exec 和完整预测域 B_exec 约束闭合了尾段 KKT/候选不变性，seed 8601 两次完整 development 闭环成功。严格 C3 已冻结为“与 C4 使用同一优化器、只把 empirical gate 改为 monitor-only”；当前剩余控制侧缺口是 C1 尚未公平匹配，因此仍不能运行或宣称正式 C0--C4 结果。

本轮针对“尽量与实物对齐”补了四项基础合同：

1. V3 名义轨迹的尾段改为“最终发布命令先归零，命令队列和底盘响应随后真实衰减并停稳”，不再要求发布零命令的同一时刻车速也瞬间归零；
2. 离线计划从冻结路径出发，经过与在线控制器相同含义的 22 维双通道执行模型，保留路径终点的真实四元数朝向；
3. 恢复规则按完整 recovery trial 划分 fit、tune 和 held-out，独立液体真值不进入控制器，统计单位不能用 MPC 周期扩样；
4. recovery sampler 和 production coordinator 共用同一“冻结反馈律＋22 维发布速率事务”，避免离线标签使用在线无法发布的命令跃变；
5. 正式仿真配置绑定程序、路径、plant、planner、名义轨迹、恢复规则、条件定义和随机种子，并在启动前逐项核对文件 hash。

这些只是把仿真证据链和单 seed development 闭环做正确，还不是论文正向结果。全相位开发数据、独立 held-out gate、完整 V3、四项 KKT 和两次 seed 8601 闭环已经形成，但 gate 的冻结最低 coverage 只有 0.10，且尚无正式多种子 C0--C4 比较。不得把单 seed 任务完成、held-out 分类通过或开发时延达标改写成完整方法防晃性能正式正向。

## 3. 线路一：先把仿真完整做通

### 仿真1：建立独立的仿真 Scout 和液体

控制器发出最终命令后，仿真对象再独立模拟：

```text
最终命令
→ 线/角两路延迟
→ 响应惯性、增益、死区和饱和
→ Scout真实运动
→ 液体响应
```

仿真对象至少要允许参数偏差、噪声和时间抖动。液体仿真最好比控制器内部的低阶模型更复杂；如果暂时做不到，也必须改变参数并加入未建模误差，不能使用完全相同的方程和参数自证。

### 仿真2：冻结一次仿真实验

把一次仿真需要的内容固定下来：

- 代码版本；
- 仿真地图和路径；
- 控制器参数；
- 仿真 Scout 和液体参数；
- 启动、录包和分析程序；
- 失败规则和结果指标。

以后看到结果再调参数，就必须生成新的仿真版本，旧结果不能与新结果混在一起。

### 仿真3：生成仿真专用轨迹和恢复规则

根据仿真版本的执行参数生成完整离线防晃轨迹：

```text
运动 → 减速 → 抵消余晃 → 静置保持
```

恢复规则也只用仿真线路的数据建立，并用另一批完全没参与调参的数据检验。它只说明“在当前仿真范围内是否还能接回尾段”，不叫安全保证。

当前这一阶段逐项状态如下：

| 内容                          | 当前状态                                                                                                                        |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 路径索引的离线防晃速度计划    | **完成（开发资产）**：P2 共 1070 点、35.63 s，输入路径、计划和报告 hash 已记录                                            |
| 双通道执行增广传播            | **完成**：sampler、22 维 solver、coordinator 使用同一执行合同和最终命令历史                                               |
| sampler/coordinator 恢复动作  | **完成**：共同使用 `bounded_tracking_recovery_policy_v1` 和发布速率事务；外部液体真值不入动作                           |
| dataset/audit 成对发布        | **完成**：C++ 两文件和 Python 三文件 bundle 均先暂存再成组发布，失败回滚                                                  |
| recovery rollout 原始数据     | **完成（开发证据）**：最终 44,940 条，全相位、6 个互斥 seed、3 个互斥 split                                               |
| recovery fit/tune/held-out    | **完成（低覆盖开发 gate）**：held-out 只评价一次并 PASS；不是安全证书                                                     |
| 真实停稳尾段                  | **完成**：V3 含 121-step `publish_zero_settle_hold_v2`                                                                  |
| 完整 V3 nominal/recovery 文件 | **完成（开发资产）**：1070 行，生产 builder/loader 均 PASS，hash 已封存                                                   |
| 单 seed development 闭环      | **完成**：seed 8601 两次各 1061 次 Full SQP 全部通过，任务完成、无 solver failure/controlled stop，四项 KKT 满足 `1e-6` |
| C1 公平匹配定义               | **未完成 / NO-GO**：scale 1.0/0.78 pilot 均未形成跨 seed 完成可靠候选；只能用任务指标继续冻结                             |
| 严格 C3 消融定义              | **完成**：与 C4 共用 Full SQP、V3、B_exec、恢复动作和命令事务，仅 empirical gate 为 monitor-only                          |
| C4 多 seed readiness          | **完成（development）**：完整 horizon B_exec 可达性前置见证修复后，8701--8705 全部完成                                  |
| 正式 seed/order/statistics    | **完成（未执行合同）**：3101--3116、16-block Williams、paired bootstrap 和任务非劣界已冻结                                |
| 正式 hash-bound session       | **未完成 / NO-GO**：模板保留空路径/hash，closed-loop summary 仍明确标记 development-only                                  |
| C0--C4/IS 正式多种子闭环      | **未执行（按计划）**：不能把单元测试、development gate 或 pilot 当作性能比较                                              |

#### 仿真第 3 阶段开发证据记录（2026-08-22）

最终证据根目录：

`/data/a/spmpc_exec_identification/phase_rejoin_p2_recovery_final_dev_v1_20260822.cElpPI`

关键结果：

- fit：14,980 条，14,282 正、698 负，seed 8301/8302；
- tune：14,980 条，14,279 正、701 负，seed 8401/8402；
- held-out：14,980 条，14,281 正、699 负，seed 8501/8502；
- phase-bin 0--999：14,000 条；phase-bin 1000--1069：980 条；三个 split 都完整覆盖；
- held-out：TA=10,421、false-safe=5、TR=694、FR=3,860；accepted coverage=0.72971，admission rate=0.69599，false-safe/负类=0.007153，95% Wilson 上界=0.016634；
- 最差 bin 是 1000--1069：coverage=0.27571；false-safe=0/280，95% Wilson 上界=0.013534；
- dataset SHA-256：`50403ae5ce897162108a831968f368d9a92f5bd72ff2fe0587cc0df8214befc7`；
- held-out report SHA-256：`068d9c78dfa34044df28fd43b00cecf0ba6aab489ac939192e57454150316115`；
- V3 文件 SHA-256：`afddb130746be46340e395743ec1673c1f94b6dc3fa738efc25bc1139897e88e`；canonical recovery hash：`3948d6cd7f9cbb70b1e94f5fed569853ab0ea9b5a19174d3ca92c8f8ab15b1be`。

审计过程中先后淘汰了三批开发证据：旧 V1 的恢复动作没有执行在线发布速率事务；V2 修正速率后 44,940 条全部为正类；后续 7701/7702 pilot 和 780x--800x 开发数据仅用于冻结 90 周期可执行扰动与 phase-bin/coverage 计划。最终 830x--850x 与这些 seed 完全隔离。没有修改外部液面标签阈值伪造平衡。

旧证据中的 seed 8601 两次运行逐字节一致。B_exec 修复后的 development 运行和增加诊断后的同 seed 重放在控制轨迹、cycle 10 phase 选择及失败状态上语义一致；由于第二次增加了候选窗口和 residual 诊断列，两个输出文件的 schema/字节不再相同，不能把它们记作最终的“两次逐字节一致”验收。

新证据目录：

`/data/a/spmpc_exec_identification/phase_rejoin_seed8601_bexec_fix_dev_20260822.5x9pe6`

cycle 10 的结果为 clock=9、候选窗口 `[8,10]`、B_exec 拒绝 1 个候选、selected phase=9、phase lead=0。完整 22D 初态、warm-start terminal 和余量记录在 `seed8601_run_b/summary.json`：warm-start 的最差 execution normalized error 为 0.49408（余量 0.50592），terminal empirical metric 为 0.30371（余量 0.69629），最小线速度 residual 余量为 0.06221 m/s；这些已排除“phase 9 与当前队列同样硬不可行”。但 ACADOS 结果的 `nlp_status=0`、`qp_status=0` 时 stationarity residual=23.2993、inequality residual=0.306303，远高于冻结复核阈值，因而被正确失败关闭。两次均为 1 次 solver failure、0 次 recovery、1 次 controlled stop，`sequence_completed=false`、`task_success=false`。因此第 3 阶段的“数据、gate、资产、B_exec 前置资格过滤和失败证据”完成，“任务完成且 solver 无失败”未完成。

#### 完整 SQP 正确性 backend 中间诊断（2026-08-22，历史记录）

> 本节到“FUNNEL、B_exec 一步不变性与 PhaseClock”均保留当时的失败定位过程；其中“当前 NO-GO/未运行”已经被后面的“Full SQP 与完整预测域 B_exec 最终闭合”取代，不代表本文档顶部的最终状态。

为区分“单步 RTI 没有收敛”与“原问题确实不可行”，生成脚本同时产生两个独立 capsule：

- 正确性候选：`delay_augmented_phase_acados_full_sqp_v1`，`SQP`＋`MERIT_BACKTRACKING`＋`HPIPM BALANCE`，四项 KKT 阈值 `1e-6`，最多 20 次；配置 hash `930a3b05749b7708cab1a58fc3331ea99e28402e0fe0a4548e84f0e0d62dbef2`（已把 `scale_x/scale_u` 与 HPIPM mode 并入 hash）；
- 旧行为对照：`delay_augmented_phase_acados_rti_reference_v1`，单步 `SQP_RTI`；配置 hash `d11da9d02d6a21242877b7c1ea4f4cfa1328fbcd95e4cfab873722b76e82d9a3`。

两者都由同一脚本生成；N=10、22 维模型、Gauss--Newton、现有 QP 硬约束、V3、gate 半径、B_exec、residual authority 和成本权重均未修改。`residualsAdmissible` 现在严格检查 stationarity/equality/inequality/complementarity 四项，而不是只要求后两项有限。失败证据才保存 raw `x[0:N]`、`u[0:N-1]`、完整 64 项逐 stage 参数图像和独立 C++ 约束审计，正常运行不写大体积轨迹。

新的只读证据目录：

`/data/a/spmpc_exec_identification/phase_rejoin_seed8601_sqp_probe_20260822.S9cifq`

真实 seed 8601 在 cycle 1 成功，cycle 2 首次失败并随后进入既有失败关闭路径，未到原 cycle 10：

- `nlp_status=2`（20 次迭代上限），`qp_status=0`；最终四项残差为 2.2000915、1.786e-13、0、4.716e-6；
- warm-start 四项残差为 25.02295、5.55e-17、0、0；第一次完整步把 stationarity 降到 2.17744，此后 iteration 2--20 的 `alpha` 固定为 0.05764801，stationarity 缓慢回升，complementarity 从 3.94e-6 增至 4.72e-6；
- 独立 C++ 审计的具名最大 primal 硬约束违反项为 `NONE`、违反量 0；terminal empirical metric=0.002479，14 项 B_exec 最差 normalized error=0.07111，均通过；
- raw ACADOS 状态与独立 causal rollout 的最大差异为 stage 10、state index 9 的 1.0954e-13，低于 `1e-6` 合同；raw state/control 量级分别不超过 3.44845/0.599783；冻结成本归一化尺度依次为 position 0.15、yaw 1、progress 0.1、v 0.8、omega 1.2、eta 0.00275037、eta_dot 0.0859380、a 0.6、alpha 1.2、v_s 0.8；
- 本次失败周期 ACADOS 内部 solve time 为 6.257 ms，整个 backend wall time 为 6.742 ms。由于闭环在 cycle 2 已失败，不能计算有代表性的 P50/P95/P99/max，也不能据这一条样本放行 30 Hz 实物实时预算。

所以当前没有“违反的 empirical/B_exec 约束及其 Jacobian”：所有具名 primal 违反量都是 0，真正超标的是 KKT stationarity 和 complementarity。完整 SQP 的 stationarity 分量归属、对应 Lagrangian/Jacobian 尺度尚未完成定位；按“完整 SQP 仍失败则立即停止”的规则，本轮没有继续修改 globalization、步长、迭代上限、容差或任何资产参数。也因此没有执行精确 cycle 10 的 RTI/SQP 同快照对照、seed 8601 两次完整闭环和耗时分位统计；这些都是明确的后续 NO-GO 项，而不是可以省略的验收项。

**无量纲缩放与伴随量定位（2026-08-22 后续纠正）**：把 codegen 的 `scale_x/scale_u` 应用于 OCP 决策变量后，同一 seed 8601 cycle 2 snapshot 的 stationarity 从 2.20009 降到 0.0158641。随后发现该结果仍受求解器配置缺陷污染：Full SQP 显式使用 `HPIPM SPEED_ABS`，而该模式设置 `comp_dual_sol_eq=0`，不计算 dynamics equality costate。`qp_solver_cond_N=N=10` 在 partial-condensing 中实际表示“不做 condensing”，不是“满 condensing”；此前据此作出的“§7.3 分支 4、真实 terminal 跟踪残差”结论撤销。

Full SQP 已通过生成脚本改为 `HPIPM BALANCE`，RTI reference 保持 `SPEED_ABS`，OCP、权重、V3、gate 半径、B_exec 和 residual authority 均未改变。修复后同一快照的 stage 0..9 `pi` 全部非零，terminal x 代价梯度 `-1.583e-2` 被 incoming `pi[9]` 正确抵消，x 分量 residual 降至约 `-4.2e-6`。这证明 `-2.38 mm` terminal x 误差本身不是 KKT 违反。

新的独立 C++ 完整分解使用 `res_stat_N = cost_grad_N - incoming_pi[N-1] - ineq_adj_N`，并同时覆盖 empirical 9D gate 与 14 项 execution bounds。当前最大项转为 terminal `eta_x_dot`（index 7）：`cost_grad=-4.5166448e-3`、`incoming_pi=-4.4738975e-3`、`ineq_adj=3.9132e-9`，得到 `res_stat=-4.2751176e-5`，与 acados 逐位一致。四项残差为 stationarity `4.2751e-5`、equality `5.13e-11`、inequality `0`、complementarity `6.85e-7`；20 次后仍为 `NLP_STATUS_2`，iteration 2 起 `alpha=0.057648`，stationarity 单调下降但未达到 `1e-6`。

因此当前准确状态是：**缺失 π 的工程缺陷已经修复，但冻结的 20 次 Full SQP KKT 合同仍未闭合。** 按“修复明确 dual 错误后完整 SQP 仍失败则停止”的规则，本轮不继续调 globalization、迭代上限、容差或资产参数，也不运行 seed 8601 两次完整闭环；这些仍是明确 NO-GO 项。

**FUNNEL、B_exec 一步不变性与 PhaseClock 后续诊断（2026-08-22）**：同一 cycle 2 快照表明 MERIT 的失败步应用问题可由当前 acados 支持的 `FUNNEL_L1PEN_LINESEARCH` 避开；配合 `HPIPM BALANCE` 后，四项 KKT 均低于 `1e-6`。为避免 residual-authority 合法的发布命令在下一周期使 selector 无候选，stage 硬约束增加了“新命令相对名义队列尾项仍位于冻结 B_exec”不变性。随后发现的 cycle 57 失败并非新 OCP 不可行，而是整数纳秒控制时钟与 `dt=0.0333333333` 资产时间轴的亚纳秒比较误差；PhaseClock lookup-only 的 1 ns 容差已使完整 probe 全程 clock 最大步进恢复为 1。

只读失败证据位于：

`/data/a/spmpc_exec_identification/phase_rejoin_seed8601_sqp_funnel_dev_20260822.kg2bNH/seed8601_phase_clock_probe.YBqi59`

该 probe 推进至尾段 cycle 993 才首次失败。求解后所有 stage/terminal 硬约束和独立 causal rollout 仍通过，equality/inequality/complementarity 分别为 `4.69e-9/0/5.79e-7`，但 stationarity 在 20 次内振荡并最终为 `1.255e-4`。最大分量是 terminal state index 16，即 `angular_pending_1`；对应 execution beta=`0.0007806014 rad/s`，解后误差只占 beta 的 `0.24897`，该约束并不 active，但平方归一化约束对无量纲状态的 Jacobian 仍约为 `765.46`。因此当前 NO-GO 已收敛为“极小 B_exec 半径的平方约束数值尺度放大对偶误差”，而不是论文方法错误、硬约束不可行或 PhaseClock 跳相。

在不修改 beta、V3 或成功定义的前提下，下一项结构性候选是把 14 个一维 B_exec 平方不等式改写成严格等价的双边线性 box；经验 9D ellipsoid 保持非线性，独立 C++ 归一化审计保持不变。依照本轮“Full SQP 在新周期仍失败则立即停止”的规则，本次没有实施该候选，也没有运行两次 seed 8601 验收。仿真 3 的“资产与局部正确性”完成，但“单 seed 完整任务成功”仍未完成。

#### Full SQP 与完整预测域 B_exec 最终闭合（2026-08-22）

上述 cycle 993 NO-GO 已按结构原因闭合。terminal 14 项 B_exec 改为严格等价的 `error-beta<=0`、`-error-beta<=0` 双边参数仿射约束后，Full SQP 的小半径 Jacobian 放大消失；把 QP 四项容差收紧到 `1e-9` 后，外层 `1e-6` KKT 合同获得明确误差预算。该版本曾连续完成 1001 次 ACADOS 求解，但在 cycle 1002 被前置 selector 拒绝：cycle 998 发布的角速度命令随后移到 `angular_pending_3`，而对应 phase 1000 的 beta 已收紧到 `0.000719077435 rad/s`，实际误差 `0.00105380837 rad/s`、归一化误差 1.4655。

这说明旧约束只保证“命令刚进入队尾时兼容”，没有保证它在后续预测阶段逐格前移时仍兼容。最终 OCP 因此在每个 path stage 对 14 个 execution state 全部施加与该 stage 参数图像绑定的双边 B_exec，共 28 项；发布 envelope/residual authority 仍为原 6 项，stage 总约束数 34，terminal 为 empirical 1 项加 B_exec 28 项。参数 schema/hash、beta、V3、residual authority、cost、recovery、Plant 和成功定义均未改变；solver config hash/ID 因求解器语义变化而更新。

最终只读 development 证据目录：

`/data/a/spmpc_exec_identification/phase_rejoin_seed8601_stage3_closed_dev_20260822.ShBSbL`

两次 seed 8601 结果完全一致（仅真实 wall-time 列不同）：

- 每次 1061 次 Full SQP，原始状态全部为 `DELAY_AUGMENTED_ACADOS_OK`；
- `solver_failures=0`、`controlled_stops=0`、`recovery_actions=0`；
- `sequence_completed=true`、`task_success=true`，完成原因为 `PHASE_TAIL_RELEASED`；
- current/terminal execution compatibility 与 terminal empirical gate 全部通过，外部 Plant 液体真值仍未进入控制器；
- 每次最大 KKT 残差均为 stationarity `9.9978e-7`、equality `4.1775e-9`、inequality `0`、complementarity `9.9421e-10`，最大 SQP/QP 迭代数为 4/13；
- 两次合并 2122 个完整 backend wall-time 样本：P50 `2.2207 ms`、P95 `3.0568 ms`、P99 `5.3270 ms`、max `9.8945 ms`，相对 30 Hz 的 `33.333 ms` 预算零超期。

因此仿真第 3 阶段的 seed 8601 development gate 为 **GO**，严格 C3 消融定义也已冻结；完整 horizon B_exec 可达性前置见证修复后，8701--8705 也已全部通过 development readiness。当前 **NO-GO** 是：C1 普通 MPCC 尚不能可靠完成 P2、公平匹配未冻结、正式 session 尚未 hash-bound、正式 C0--C4/IS 未执行，planar_r03 仍不是实车参数，且没有论文级正式正向防晃结论。

### 仿真4：完成C0–C4正式仿真

这是后续阶段。只有 C4 新 development seeds 稳定通过、C1 公平匹配冻结、正式 session readiness=`READY_NOT_EXECUTED` 且人工审查许可后，才允许开始 seeds 3101--3116 的正式矩阵。

名义路径比较：

- C0 普通 MPCC；
- C1 与完整方法完成时间和平滑程度相近的普通控制器；
- C2 只回放离线防晃轨迹；
- C4 完整方法。

受控偏离比较：

- C3 允许在线修正，但不检查能否接回离线尾段；
- C4 完整方法。

同时改变延迟、液体频率和阻尼，检查结果是否稳定。**独立仿真液面必须下降**；控制器内部计算的液面只用于解释原因，并检查变化趋势是否一致。还要报告完成时间、路径误差、动作平滑性，以及控制器能否每秒运行 30 次且不超时。

### 仿真线路完成后得到什么

仿真线路完成后，至少留下一个完整、可复现的仿真研究：

> 考虑线/角差分执行延迟的相位重接控制，在独立底盘和液体仿真中降低了晃动，并减少了破坏离线防晃尾段的在线修正。

如果实物最终没有成功，这套结果可以独立整理成一般应用型控制或机器人论文。但只能声称“仿真中有效”，不能声称真实液面已经降低。

## 4. 线路二：仿真通过后再做实物

### 实物1：标定 Scout 和测量时间轴

使用最终容器、液位和载荷，记录最终发布命令、Nokov、odom、IMU 和 RGB，辨识：

- 线速度和角速度延迟；
- 响应惯性和增益；
- 死区、饱和和可能的方向差异；
- ROS、Nokov、IMU 和 RGB 的时间偏差。

### 实物2：完成实车基础验证

详细技术文档把这一步叫 `G0`。人话就是检查：

1. Scout 的执行延迟是否稳定；
2. 从读取状态到预测终点的整段预测是否足够准确；
3. 第一拍在线修正在实车上是否真的高于噪声；
4. IMU 和 RGB 的时间、幅值和相位是否可信。

四项全部通过，才是“可以继续”；任一项不通过就停止，先修正延迟、模型或传感器。这一步通过不代表防晃性能已经成功。

### 实物3：重新生成实物专用资产

实物线路继续使用相同控制器代码，但必须替换为实车参数，并重新生成：

- 实物专用离线防晃轨迹；
- 执行状态匹配条件；
- 恢复判定规则；
- 实物实验配置和版本记录。

仿真线路的轨迹和恢复规则不能直接拿来上车。

### 实物4：重跑仿真，再做只观察、不控制的检查

先用实车标定参数配置控制器，再重跑仿真，确认代码修改后仍然正向。外部仿真 Scout 和液体仍要保留独立的参数偏差与未建模误差，不能与控制器内部模型完全相同。随后在实车上只观察控制器判断、不让它实际控制车辆，确认接回尾段的判断、时间索引和最终命令记录正确。

这两项通过后，才允许正式实物实验。

### 实物5：正式实物对比

名义主实验：

- C0、C1、C2、C4；
- 使用同一路径、容器、液位、起始条件和安全限制；
- 主要比较独立 RGB 液面高度，同时检查完成时间和路径误差。

恢复实验：

- C3、C4；
- 只做预先审查过的小幅、可重复偏离；
- 大量危险或明显不可恢复的情况留在仿真和录包回放中。

最终防晃结论只由独立 RGB 实物结果给出，控制器内部液面只能解释原因。

## 5. 两条线路共用什么，必须分开什么

### 共用

- 控制器 C++ 代码和双通道模型形式；
- 最终命令出口和记录方式；
- C0–C4 的定义；
- 分析指标和失败统计原则；
- “异常时停止，不偷偷放宽规则”的原则。

### 分开

- 执行和液体参数；
- 离线防晃轨迹；
- 执行状态匹配条件和恢复规则；
- 仿真冻结版本与实物冻结版本；
- 用于调参、独立验证和最终结论的数据。

任何一条线路只要根据验证结果改了模型或阈值，就必须生成新版本，并重新采集独立验证数据。

### 仿真无法代替实物的部分

以下内容不在仿真里伪造“已对齐”：

- RGB、Nokov、IMU 和 ROS 的多时钟偏差；
- Scout 在不同电量、载荷和地面下的真实延迟、死区和方向差异；
- 相机观察平面的有符号液面相位与 max-LCR 标定；
- 驱动器/CAN 的真实执行回执和现场安全边界。

仿真只复用它们的**数据结构、时间含义、双通道模型形式和失败关闭逻辑**。具体数值与正式结论必须留到实物重新标定和验证。

## 6. 暂时不做什么

- DualSPHysics/CFD；
- 动态障碍和在线重规划；
- 第二容器和大量路径；
- 鲁棒不变集、递归可行性等重理论扩展；
- 无法公平复现的外部防晃基线。

核心仿真和第一条实物路径通过后，再考虑第二冻结路径或可信的 input-shaping/fixed-profile 对照。

## 7. 代码中几个英文词的人话解释

| 代码中的词                 | 人话解释                                         |
| -------------------------- | ------------------------------------------------ |
| `plant`                  | 仿真中的“假 Scout＋假液体”                     |
| `delay-augmented solver` | 考虑真实执行延迟的在线优化器                     |
| `session`                | 一次实验的完整配置单                             |
| `runner`                 | 按配置单启动控制器、传感器和录包的程序           |
| `B_exec`                 | 当前命令队列和底盘响应能否接回离线轨迹的条件     |
| `held-out gate`          | 用完全独立的数据检验过的恢复判定规则             |
| `release`                | 代码、参数、轨迹和分析方法都固定的实验版本       |
| `G0=GO`                  | 实车基础验证通过，允许继续；不是防晃效果已经成功 |

## 8. 详细文档入口

- [正式方法闭环实施方案](../方案/20260821_PhaseRejoining正式方法闭环实施方案.md)
- [第1阶段：基线冻结与架构决定](../方案/20260821_PhaseRejoining_WP0基线冻结与架构决定.md)
- [第2阶段：唯一最终命令事务闭环记录](../方案/20260821_PhaseRejoining_WP1唯一最终命令事务闭环记录.md)
- [Methods 章节组织思路](../Methods/Methods章节组织思路.md)
- [实验章节组织思路](../Experiments/实验章节组织思路.md)
- [仿真实验方案](../仿真实验/仿真实验方案.md)
- [实物实验方案](../实物实验/实物实验方案.md)
- [实物标定与参数冻结清单](../实物需要标定和确定的参数/实物标定与参数冻结清单.md)

当前只需记住一句话：

> **恢复资产、Full SQP、严格 C3、C4 development readiness 和正式统计合同已经接通；现在先闭合 C1 任务可靠性与公平冻结，再物化正式 session，最后才允许进入仿真 4。**
