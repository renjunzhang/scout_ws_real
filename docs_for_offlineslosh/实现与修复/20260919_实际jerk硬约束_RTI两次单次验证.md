# 实际 jerk 硬约束：RTI 2 次单次验证

2026-09-19。用户要求先试 RTI=2，通过后再跑一次 Gazebo 并与 B0、FP-AS 画图。**本次耗时预检查通过，但 S 路径未完成，因此没有进入 Gazebo，也没有生成新的有效三方法对比图。**

只把试验参数从 RTI=3 改为2，复用同一计划、可执行文件及求解库；哈希核对一致，有效参数清单唯一差异为迭代次数。实际纵向 jerk 上限仍为1 m/s³，在线液面仍为软代价，未改代码、未重编、未重新规划。

| 指标 | RTI=2，本次一次 | RTI=3，归档一次 |
| --- | ---: | ---: |
| S 模型闭环完成 | **否** | 是，40.167 s |
| 首次求解失败 | **25.600 s** | 无 |
| 实际模型 jerk 峰值 | 1.000 m/s³，未超限 | 1.000 m/s³，未超限 |
| 求解调用耗时 P95 | 17.534 ms | 27.538 ms |
| 求解调用耗时最大值 | **25.873 ms** | 34.170 ms |
| 最大预测动力学缺陷 | 1.138e-6 | 8.475e-9 |

本次在25.600 s报 `ACADOS_SOLVE_FAILED_4`；日志为 QP `ACADOS_MINSTEP`（状态3，第13次QP迭代）。随后按既有故障停车逻辑处理，在45.033 s报任务期限失败；行驶4.186 m，距终点约0.981 m。缺陷和 jerk 数值没有超限，**不能把这次失败归为实际 jerk 超限**，也不能仅凭错误码判定问题不可行。

本次25.873 ms低于预先登记的30.333 ms计算上限（30 Hz周期减3 ms发布预留），但只是未包含ROS处理开销的模型预检查；运行未完成，不能称为实时控制已通过。RTI=3仍是已有唯一跑通的新约束模型案例，尚有最大耗时超预算的缺口。

**按条件停止：** 不追加参数档位、不重试、不启动Gazebo或实车。用户要的B0／FP-AS／新Full曲线对比仍待一个合格的新Full执行记录；不能将失败的短程曲线当成完成任务的性能比较。

原始记录：`/data/a/scout_sim_replacement/results/20260919_actual_jerk_rti2`，包含冻结协议、命令／哈希、进程结果、CSV、JSON及求解日志。本次模型运行总墙钟13.240 s；[机器可读汇总](./20260919_实际jerk硬约束_RTI两次单次验证.json)。未触碰Testing或隔离仿真环境文件。
