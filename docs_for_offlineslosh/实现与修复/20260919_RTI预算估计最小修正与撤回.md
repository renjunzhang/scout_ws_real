# RTI预算估计：最小修正单次失败，已撤回

2026-09-19。**只改预算估计后试了一次，仍有预算停车，随后QP失败，任务未完成。已撤回该策略改动，保留逐次耗时诊断；没有追加运行或进入Gazebo。当前问题尚未解决。**

最小候选：下一次RTI由“本轮最慢已观测耗时”改为“最近一次耗时”估计，避免早期尖峰持续影响后续判断。固定三次RTI、30.333 ms求解预算、3 ms发布预留、实际模型jerk上限1 m/s³、液面软代价全部不变。复用同一task／plan、Release构建和生成求解库，不重新规划、不扫参。

| 一次模型验证 | 结果 |
| --- | --- |
| 任务完成 | 否，45.033 s报告期限失败 |
| 最大求解调用 | 26.040 ms，未超过本次预算 |
| 实际模型jerk峰值 | 1.000 m/s³，未超限 |
| 预算停车 | 25.900 s、27.067 s各一次 |
| QP失败 | 27.800 s，`ACADOS_MINSTEP`，第12次QP迭代 |

逐次诊断现在明确显示：

- 25.900 s：第二次RTI实际12.229 ms；准备第三次时仅剩12.094 ms，因此拒绝启动。
- 27.067 s：第一次RTI实际14.531 ms；准备第二次时仅剩13.269 ms，因此拒绝启动。
- 两次拒绝在当前观测下，用原先最大值或最近值都会发生。这说明仅处理旧尖峰估计不足以解决问题。

本次首拍确有一处“最近值”允许第三次正常完成、原最大值会拒绝的局部决策差异，但没有转化成整任务通过。它不是两种策略完整闭环的严格对照，不能按两次实验停车数3→2宣称改善。

尖峰的acados内部计时分别12.221／14.524 ms，与RTI墙钟接近；耗时发生在求解调用内部。此计时仍可能含操作系统调度影响，不能据此断言全是QP计算，也不能把恢复后的QP失败直接归因为预算估计改动。

**最终保留：** 原保守预算估计、三次迭代要求、所有约束和发布超时拒绝；仅新增native `_iterations.csv`，逐次记录估计、剩余预算、实际墙钟与acados耗时，拒绝启动时耗时为`-1`。ROS消息接口不变。撤回后不再跑第二次S路径，不能将历史42.433 s完成当成当前版本的新验证。

候选代码提交`65ac0dab`，native／ROS构建及73个定向用例通过，但闭环门槛未过。最终保留版本重新构建，并通过34个求解／预算／回放用例。运行候选和最终保留版本身份分开归档。

结果目录：`/data/a/scout_sim_replacement/results/20260919_rti_recent_budget`；包含全部状态、逐次耗时、失败日志、运行物哈希和[机器可读报告](./20260919_RTI预算估计最小修正与撤回.json)。新Full与B0／FP-AS的有效对比图仍缺合格新运行，不拼接失败曲线。

未修改工作区`Testing/`或隔离仿真环境文件，未启动实车。jerk仍是30 Hz执行器模型网格约束，不是连续物理jerk保证；没有新增真实液面收益证据。
