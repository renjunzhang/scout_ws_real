# FP-AS S 路径 28 秒对比尝试

2026-09-19。按用户要求，将 FP-AS 固定运输时间设为 28 s，尝试接近归档 Full 的实际 28.282 s。**单次离线求解在 600.033 s 超时，未生成合格计划，未进入模型闭环或 Gazebo。因此本次没有得到接近同耗时的效果对比，不能据此判断 FP-AS 在 28 秒下不可行或效果更差。**

## 可报告的结果

高度均为 odom 驱动液体模型输出；不是独立液面测量。到点时间是实际执行结果，不能用设定时长替代。

| 方法 | 状态 / 样本 | 实际到点 s | 运输峰值 mm | P95 mm | RMS mm | 停车 5 s 峰值 mm |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Full | 归档三次均值 | 28.282 | 0.505408 | 0.223075 | 0.111543 | 5.447e-6 |
| FP-AS，设定 40 s | 归档单次通过 | 40.399 | 0.269344 | 0.148104 | 0.077576 | 0.015029 |
| FP-AS，设定 28 s | 本次离线超时，无执行样本 | — | — | — | — | — |

Full 三次到点范围为 28.234～28.315 s，峰值范围为 0.477365～0.540832 mm。原 FP-AS 40 s 结果只能说明更慢时的模型表现，不能推算成 28 s 的闭环结果。归档极小停车值是模型衰减数值，不代表真实测量精度。

## 本次做了什么

- 复用最终 v8 实现，只改任务标识和 `transport_duration: 40 → 28`；仅一个候选、一次求解，无时长或参数搜索。使用现有冷启动初值生成器，没有直接执行压缩后的旧轨迹。
- 冻结曲线与 40 s 版本逐项相同，长 5.812509 m。保留 30 Hz、真实延迟 / 响应 / 命令历史、45 s 期限、5 s 停车窗，以及运输 / 停车 0.90 / 0.08 mm 限值和 0.001 mm 验收容差。控制与规划代码、默认配置和实物接口均未改。
- 运行物、模型与配置哈希核对一致。此项指与前次 FP-AS 一致，不表示与旧 Full 控制库相同；由于离线未通过，本次未重新运行执行检查。
- 600 s 墙钟限制触发后，仅终止本次求解进程组，返回 `-15`。最新落盘事件为第 550 次迭代（578.445 s），最大约束乘子约 6.90e11，涉及固定路径位移约束。stdout 因缓冲只保留到第 535 次，其原始 / 对偶残差为 1.87e-5 / 6.07e10；这些不是终止时的最终残差。
- 没有明确定位到可据此修复的实现或配置错误，按方案停止，不自动重试。外部 SIGTERM 没有经过求解异常保存出口，故没有完整拒绝候选；保留任务、几何、源码哈希、全部已落盘日志与进程结果。

## 结论边界和缺口

当前缺口首先是 **28 s 单候选在现有预算内未收敛**，还不能进入效果排名。若继续，应先定位固定路径约束的数值收敛问题，而不是更改液体限制或堆重复实验。

即使后续时间接近，Full 名义优化路径约 5.008 m，FP-AS 固定路径约 5.813 m，几何仍不同；旧 Full 与外部基线的控制库版本亦不同。所有现有高度由共同模型估计，仿真执行器参数匹配，仍缺既有实物中接近液体共振的约 5 Hz 结构振动；真实低速制动、阻尼、初始残振和独立液面的同步测量尚未核验。

本次新增 Gazebo **0 次**，未补跑 Full / B0，未启动实车；`Testing/` 及隔离环境文件未修改，端口 11892 / 11926 空闲。

原始记录：`/data/a/scout_sim_replacement/results/20260919_fp_as_s_28s_195318`。结构化指标、哈希、日志摘要和收尾见[同名 JSON](./20260919_FP_AS_S路径28秒对比尝试.json)；已有成功结果见[40 秒报告](./20260919_FP_AS_S路径续验.md)，简要讨论见[GPT Pro 讨论稿](../情况介绍/20260919_四种防晃方法仿真现状_GPT_Pro讨论稿.md)。
