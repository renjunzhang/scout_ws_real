# 时间优化开关：单候选验证

2026-09-19。按“简单测试，不冗余”的要求，仅尝试 **40 → 36 s**，没有改控制代码、权重、物理限制或防晃门槛。

**结论：开关启用与失败回退已实测；本次没有得到更短的可执行计划，缩时闭环收益尚未验证。** 默认仍关闭时间优化。

后续按用户授权只试 38 s，已真实收敛并完成一次闭环，实际到点 24.256 s、比关闭组均值快 14.24%，防晃门槛通过，见[38 s 闭环记录](./20260919_时间优化38秒单候选闭环验证.md)。本页保留首次 36 s 超时结果。

| 项目 | 结果 |
| --- | --- |
| 40 s 基准 | 复用此前真实收敛的计划，重新检查三指标资格，不重复数值求解 |
| 36 s 候选 | 一次求解；600 s 墙钟预算耗尽，未收敛，未导出可执行计划 |
| 最终选择 | 正确回退 40 s；selected_plan 与原计划逐字节及 SHA256 一致，生产 C++ 加载检查通过 |
| 新闭环运行 | 0 次；未重复验证已有基准，也未执行失败候选 |
| 重试/额外候选 | 0 次 |

使用现有离线入口，参数为 `--optimize-time --times 40 36 --process-seconds 600 --total-seconds 720`。基准为 `20260919_slosh_early_brake_120624` 的防晃优先 Full；只改变候选的名义运输时长，保留 RMS 归一化尺度、1.80 m 末端包络、45 s 截止和原三指标门槛。

求解过程中进入可行性恢复阶段，最终以进程超时结束。一次局部求解超时不能证明 36 s 不可行，也不能据此宣称开启时间优化没有收益。搜索入口返回 `selected` 表示选中了有效基准，不表示更短候选成功。

临时运行物曾在预检查后消失，第一次入口调用在数值搜索前拒绝；随后从原归档恢复同哈希控制二进制，只重建小型 C++ 检查器。环境问题和唯一一次数值尝试均记录在原始目录。

原始数据：`/data/a/scout_sim_replacement/results/20260919_time_switch_single_140512`，包含 `experiment.json`、`search/manifest.json`、`T_1080/solve.log`（位于 search 下）、候选 task、回退计划及 `assessment.json`。本报告的同名 JSON 保存进程结果和计划身份。

当前三次默认 Full 的[防晃结果](./20260919_防晃优先与可选时间优化.md)保持不变；本次未完成同路径 B0/Full 对照，不改变其公平性边界。后续不自动追加候选或仿真。
