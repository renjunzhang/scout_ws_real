# 2026-09-10 SPMPC 实物降晃与来源对照交接

> 用途：新对话恢复项目状态；不需要重读整段聊天或重新分析所有 bag。
> **2026-09-11 最新决策：** 用户希望先获得内部 slosh 指标的可重复正向结果，接受完整方案包含硬 jerk 约束；RGB 与 Smooth/NoState 拆分后置。先按[实验思路第 0 节](../../实物实验注意事项/对比试验/解决问题的思路/20260910_C03固定条件三组对比与液体权重小范围调参思路.md#0-当前安排先比较-b0-与完整组合方案2026-09-11)比较开发 B0（液体关、硬 jerk 关）与 full（IMU、w=1、硬 jerk=0.6），各一包筛选、有改善则原参数各补一包；无改善再考虑 full w=0.5。共同 C03、v_ref=0.2、2 秒预测时域，统一 IMU 监视器全任务 RMS，兼报峰值、耗时和跟踪。结果仅支持组合方案的内部模型指标改善，不单独归因于液体机制；目前尚无本轮新结果。
> 本次只记录思路，基于 `diag/lt-dwa-collision-tracking @ c5d803b`；无 tracked diff，两个既有未跟踪项保留。普通无 RGB B0/full 入口已存在，但旧 `ablation-rgb` 函数不适用，0.5 权重与共同非 RGB 录制验收接续尚需整理。**不再默认从 `01_smooth_retry1` 继续六包**，下述为 9 月 10 日历史状态。本次未改代码、未录包、不 push。
>
> 初稿核验日期：2026-09-10；仓库 `/home/geist/scout_ws`，分支 `diag/lt-dwa-collision-tracking`，当时 HEAD `cb4e9d2`。
> 初稿写入前无 tracked diff，只有根目录两个既有未跟踪文件 `VALIDATE_ONLY=false`、`bash`，不要删除或纳入提交。初稿为既有交接文件，本次按用户要求归档，下面历史实验结论保留。
>
> **2026-09-10 收工状态：** 用户决定次日继续；本次记忆更新基于同分支 HEAD `f82eb67`，写入前无 tracked diff，仅保留上述两个未跟踪项。今晚不再实验、不 push。

- **已实现、软件已验证：** `319e180` 修复录制验收到达时间与 RGB 开关；`d8b64b1` 修复 RealSense inactive 轮询、补静态诊断与稳定配置；`f82eb67` 接通三组 RGB、液体权重、trial ID/phase，57 项相关软件测试及三组 `--validate-only` 通过。三组入口已可用，不是明日待开发项。
- **已通过质量门：** 稳定相机组合两轮 60 秒静态 PASS；`d8b64b1` 的 201955 full/IMU/J0.6 实车质量包四项 PASS，RGB 最大间隔 34.0 ms、回调 P95 18.6 ms、求解/对齐/故障零速均为 0。配置、产物与限制见 [RGB 修复报告](../../实物实验注意事项/对比试验/实物对比试验分析/20260910_RGB静态掉帧修复与重复验收.md)。201955 单列质量 smoke，不计入新协议六包，不证明降晃收益。
- **失败与暂停位置：** 新协议仅尝试了 `01_smooth`（204926），起点位置误差 8.2～8.5 cm 超过 8 cm 门限，未形成有效运动窗口；四项自动报告全部 FAIL、无 PASS 标记，保留原包。当前三组有效对比包为 **0/6**，`01_smooth_retry1` 尚未执行，详见[方案第 6.3 节](../../实物实验注意事项/对比试验/解决问题的思路/20260910_C03固定条件三组对比与液体权重小范围调参思路.md#63-2026-09-10-收工记录与次日恢复)。
- **9 月 10 日计划（现已后置）：** 固定 IMU、J0.6、v_ref=0.2、2 秒预测时域，三组交错各两次，再按结果决定是否加入 0.5 权重，候选冻结后用新包复验。尚无新协议的效果结论，未证明 J0.6 最优；不增加来源切换、J1 或延迟仿真前置，不恢复 5 Hz 标定。

收工时录制、控制器及路径发布者已退出；用户自启的基础传感器与 NOKOV 服务仍运行，本次未关闭它们。该运行状态仅是当晚快照，次日重新核对。第 4 节保留初稿建议的时间边界。

## 1. 主线与当前结论

用户关注论文方法和实物对比，论文目录为 `docs/论文书写/草稿/spmpc_paper_core`。9 月 11 日近期目标改为先寻找完整组合方案相对开发 B0 的内部 slosh 收益；液体预测、状态记忆是否提供超过通用平滑的额外收益留到后续拆分，不追求无限扩展诊断。

**全时域命令加速度变化约束有效，但完整 S-MPCC 尚未表现出稳定、可重复的降晃收益，历史反向结果的唯一原因未找到。** 模型曾在预测计划中主动保留约 5 Hz 动作，离线特定去频带扰动反而增大预测液体代价；只能说明当前模型偏好，不能证明实物有益。激励幅相、执行模型和当前液体初态之间的差距尚未被唯一归因。

不能认定 IMU、延迟、单一权重或 ACADOS 求解失败就是根因；本轮录制/调度问题也不能解释全部历史负结果。约 5 Hz 的车体实测运动与液体约 5.15 Hz 模态频率是不同物理量，“车体结构共振”仍是机制假设。

## 2. 已实现与脚本边界

全时域 `Δa_cmd` 软惩罚与硬约束、首拍真实发布命令历史衔接、终端停车锁存均已实现。普通 MPC 控制与终端接管分开，安全停车优先，不得裁剪历史或关闭首拍硬约束来掩盖不可行。

入口：[run_spmpc_ablation_smoke.sh](../../../src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_ablation_smoke.sh)，共同 profile：[spmpc_ablation_profile.sh](../../../src/scout_apps/control/spmpc_local_planner/scripts/lib/spmpc_ablation_profile.sh)。复用共享运行引擎与旧 recorder。

| 条件 | 液体预测/代价 | 每轮 OCP 液体初态 | jerk 硬约束 |
|---|---|---|---|
| `smooth` | 关闭 | 不参与液体优化 | 开启 |
| `nostate` | 保留 | 四个液体状态置零 | 开启 |
| `full` | 保留 | 使用选定 observer 状态 | 开启 |
| `b0` | 关闭 | 不参与液体优化 | 关闭 |
| `no_jerk` | 保留 | 使用 observer 状态 | 关闭 |

这些开发组均保留共同非液体软平滑权重 `w_accel=0.3, w_du_a=0.1, w_alpha=0.1`。B0 不是旧冻结 B0 的完整参数；`b0 --jerk-max 0.6` 仍关闭硬约束，带该硬约束的无液体基线就是 `smooth`。NoState 不是“B0 加监控”：未来液体响应仍参与优化，只清零 OCP 液体初态，后台评价监视器不清零。

**三组 RGB 接口已在 f82eb67 实现：** 显式传 `--experiment ablation-rgb` 使用独立协议 `SMPCC_C03_ABLATION_RGB_DEV_V1`，支持 `smooth/nostate/full`，强制 RGB、Tracker0 和双监视器；`--trial-id` 必填，`--phase` 默认 `screening`，Smooth 权重为 0，NoState/full 可选 1 或 0.5。旧来源对照入口的 full-only 限制保留；未指定新协议时，仅加 `--observer-source` 或 `--record-rgb` 仍不能录 smooth/nostate。现有 recorder 与四项自动验收继续复用，逐包命令见[方案第 6.2 节](../../实物实验注意事项/对比试验/解决问题的思路/20260910_C03固定条件三组对比与液体权重小范围调参思路.md#62-已实现三组-rgb-入口及逐包命令)。

新地图/C03 要显式传 `--scene 20260907_c03`，默认还是旧 C02。新地图 env：

```text
/home/geist/scout_maps/real/20260907_mocap_exec/map_carto_20260907_mocap_exec_v1_freeze.env
/home/geist/fixed_paths/real/20260907_spmpc_mocap_execution_chain/candidates/mocap_compact_s_C03.json
```

共同速度 `v_ref=0.20, v_safe_max=0.25 m/s`，30 Hz、N=60，**2 秒是 MPC 预测时域**。显式执行器、legacy delay off、fail_closed、common_epoch 保留。IMU/odom 切换仅改变液体初态来源，车体定位仍用原 odom/TF 链。四类记录为 RGB、NOKOV、IMU/odom 双液体监视器；不要另造第三套在线 NOKOV 液体监视器。

## 3. 已完成实验，不要重复提取

- 9 月 7 日新地图 C03 四组无 RGB：full、smooth、nostate、b0 运行通过，但完整方法内部高度未获得一致收益。见[C03 四组分析](../../实物实验注意事项/对比试验/实物对比试验分析/20260907_C03新地图四组消融内部液面高度分析.md)。
- 9 月 8 日 full/J0.8、J0.6、J1，各一包无 RGB，运行/jerk 均通过。J0.6 相对同晚 J1 的 IMU 全任务 RMS 低 19.1%，峰值高 12.2%，odom RMS 高 13.1%，没有一致最优档。见[三档分析](../../实物实验注意事项/对比试验/实物对比试验分析/20260908_C03完整方法jerk三档对比分析.md)。
- 9 月 9 日 full、J1 来源对照，IMU→odom→odom→IMU，各两包，代码 b0b322e；完整分析已提交 cb4e9d2。见[来源对照分析](../../实物实验注意事项/对比试验/实物对比试验分析/20260909_C03完整方法IMU与odom液体初态来源对比分析.md)。

9 月 9 日全任务 RMS 摘要：

| 包 | IMU monitor mm | odom monitor mm | RGB 位移 px |
|---|---:|---:|---:|
| IMU1 / 143940 | 1.128 | 0.804 | 12.612 |
| odom1 / 144405 | 0.994 | 0.811 | 12.085 |
| odom2 / 144833 | 0.832 | 0.716 | 9.383 |
| IMU2 / 145248 | 0.933 | 0.867 | 10.679 |

odom 组两包均值分别低 11.3%、8.6%、7.8%，但同一 IMU 方法重复间的 IMU/RGB 指标自身下降 17.3%/15.3%；中段 RGB 均值只低 3.7%，第一对中段 RGB 没改善。IMU 起点偏差约 5～6 cm、odom 约 1～2 cm，不能把组均值差直接归因于来源。

四包 jerk 契约均 PASS、求解失败为 0。IMU 两包运行/source PASS；odom1 回调 P95=30.313 ms 超 `<30 ms` 门，odom2 起步 +0.083661 s 出现一次 TF 对齐失败及故障零速，另有连续两拍回调超时。四包 RGB 原生最大间隔 100.445～134.753 ms 超 100 ms 门，均 FAIL。双监视器/NOKOV 覆盖正常，NOKOV 88.15～89.99 Hz。不得把这一批写成整体验收通过。

内部高度是运动驱动液体模型的响应，不是液面真值。RGB 本次按统一检测器提取像素位移；当天毫米标尺未核验，未声称真实毫米降晃率。统一窗口为全任务、几何路径 10%～90%、到达后 5 s；监视器用 state_stamp，RGB 用 header.stamp，不用 bag 到达时间或逐包拟合时移。

权威数据与缓存目录：

```text
/home/geist/slosh_bags/real/20260909_spmpc_observer_source_smoke_v1_20260907_c03/full/H0/
  source_comparison_20260909_b0b322e/
    acceptance_summary.json
    metrics/comparison.json
    metrics/metrics.csv
    metrics/*_extracted.json
    metrics/analyze_source_comparison.py
    rgb/summary.json
    rgb/*_pixels.csv
    rgb/rgb_Hpx_comparison.png
    rgb/rgb_extract.py
```

优先读报告/JSON，重复计算使用小话题及图像坐标缓存，不重扫原始大 bag。`_rosparam.yaml` 是 planner 启动前快照，可能残留上包 source；实际来源看包内 observer/solver-input/audit，不能据旧参数快照判错源。`tracking.end_offset_cm` 是最后有效 MPC 快照到终点的距离，不是最终停车误差。

## 4. 初稿交接时的建议

已提交[总方案](../../实物实验注意事项/对比试验/解决问题的思路/20260907_全时域加速度变化硬约束与NoState三组开发验证思路.md)仍建议先补有效来源配对。之后用户讨论论文方法验证，助手给出的最新建议是：

1. 先处理 RGB 掉帧、回调超门、起步 TF 对齐，短 smoke 验证；这些修复截至交接**尚未实施**，异常因果尚未定位，不要直接归因于带宽或 IMU。
2. 不必等 IMU/odom 选出赢家；可以固定 IMU 来源、相同速度/起点/其他配置，以 J0.6 做 Smooth-only、NoState、full 三组，每组至少两次，共同保留 RGB、NOKOV 和双监视器。先补齐第 2 节所述三组 RGB 入口/验收缺口。
3. 这三组回答“液体预测、状态记忆是否有额外收益”，不能证明“0.6 比 1.0 好”；若论文还需要后者，再补 full/J1 对照，不将选出 jerk 胜者设成三组方法验证的必经前置。

**以上保留初稿交接时的建议，不是已经完成的录制。** 同日用户已在顶部链接的新方案中选择 J0.6 作共同实验条件，仍未证明它是最佳值；后续按新方案继续，不自动启动实车或未经要求重构控制器。

## 5. 协作要求与恢复步骤

- 用中文，简短直接，先给结论；用户明确反对冗余分析，重复提取可交给低 token 子代理，但不要为简单核对额外开代理。
- 已暂停 C03 5 Hz 模型深度对比/完整五状态动捕参考标定支线，不将其恢复为当前前置。旧平滑直发命令下已有约 5 Hz 成分；不重复证明“不用 MPC 也有振动”。
- 保留重力补偿；此前两个旧直线包补偿前后该频带差约 3.3%，这不是关闭补偿的证据。
- 不回放发布机器人命令、不执行 `--run`。实车由用户操作。录制脚本已有安全及 dirty runtime 检查，保留。
- 用户要求时中文本地 commit，不擅自 push；来源对照实现及报告提交为 b0b322e、cb4e9d2。此次后续已获用户明确要求，将脚本修复、测试、新思路及既有交接一并本地归档，不 push。
- 共享工作区可能有别的 Codex 窗口，先看 git status/diff，保留陌生改动。不要继承旧终端会话、子代理状态或临时进程假设。
- memory skill 实际名称 `memory-writer`，路径 `/home/geist/.codex/skills/memory-writer/SKILL.md`，项目记忆在已有 docs 树，不是独立隐藏数据库。

新会话最小恢复：读本文件顶部最新决策及方案第 0 节 → `git status --short` / 最新日志及必要 diff → 核对并整理 B0/full 无 RGB 逐包入口与共同内部指标验收。优先复用已有脚本，显式暴露关键对比参数；不把 RGB 诊断或旧三组六包恢复为当前前置。实车由用户操作，每包归位同一点与朝向、静置至少 30 秒且液面静止；运动与非 RGB 质量失败保留并停。9 月 10 日起点超 8 cm 的无效 Smooth 包仍不纳入效果比较；不要照搬当晚临时位姿校正，不重分析旧 bag，不恢复 5 Hz 标定支线。
