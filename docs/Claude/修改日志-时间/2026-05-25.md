# 2026-05-25 修改日志

## SloshPriorityMPC 论文执行方案与 Ferrari 指标分析

### 新增文档

- 新增 `docs/Claude/修改方案-时间-简介/2026-05-24_SloshPriorityMPC物理保真度与残振治理方案.md`
  - 梳理 SloshPriorityMPC 后续论文主线。
  - 明确 P1 yaw-offset 激励耦合、P2 terminal residual、P3 zeta 物理标定、P4 Ferrari-style 指标、P5 实物扩样本的角色。
  - 明确不动 OSCRS、不动 RGB 主评价口径、不用 `/slosh/height` 自证真实液面。

- 新增 `docs/Claude/修改方案-时间-简介/2026-05-24_论文执行TODO_6步.md`
  - 整理 RA-L 投稿前 6 步执行 TODO。
  - 当前 baseline 数据口径为固定 P2 路径、C/D/F n=3。
  - 明确 RGB 主指标使用 `max(left, center, right)`，主评价窗口排除 terminal 前 1 秒。

- 新增 `docs/Claude/修改方案-时间-简介/2026-05-24_P2_terminal_residual_TODO.md`
  - 单独拆出 terminal residual 治理方案。
  - 分为 Phase A terminal slosh ramp 和 Phase B free-decay residual prediction。
  - 明确 terminal 治理只作为独立诊断/后续改进，不混入主 tracking 效果统计。

### 新增脚本

- 新增 `src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_ferrari_indices.py`
  - 批量读取 bag 中 `/slosh/height` 和 RGB infer CSV。
  - 默认 RGB 口径为 `max_lcr = max(h_mm_left, h_mm_center, h_mm_right)`。
  - 默认评价窗口为 `main = TRACKING start -> first terminal - 1.0s`。
  - 输出 per-bag Ferrari-style 指标：
    - `gamma_model_pct`
    - `gamma_opt_pct`
    - `rgb_p95_mm / rgb_rms_mm / rgb_peak_mm`
    - `model_p95_mm / model_rms_mm / model_peak_mm`
    - `U_p95_mm / U_max_mm`
  - 保留 `smooth_corr` 和 `residual` 窗口作为可选对照口径。

### 验证

- 已运行：

```bash
python3 -m py_compile src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_ferrari_indices.py
```

- 已用 `/data/a/slosh_bags/real/20260522_fixed_path_cost_d200` 和 v157 RGB infer 结果跑通分析。
- 输出目录：

```text
/data/a/slosh_bags/real/20260522_fixed_path_cost_d200/ferrari_indices_max_lcr_main
```

### 注意

- `docs/.obsidian/workspace.json` 是 Obsidian 自动工作区状态，不属于本次提交内容。
## P3 ζ 离线保真度对比脚本

- 新增 `scripts/analysis/analyze_zeta_fidelity_ablation.py`。
- 目的：在不修改在线 MPC 的前提下，用同一批 bag 的 `/slosh/ax_est`、`/slosh/ay_est` 重放二阶晃液模型，对比 `ζ=0.05` 与 Ferrari 物理 `ζ` 是否改善 RGB 保真度。
- 默认窗口：`TRACKING -> first terminal - 1s`，避免 terminal approach jerk 问题混入主效果统计。
- RGB 主指标：`max(left, center, right)`，与当前 Ferrari-style maximum wall-rise 口径一致。
- 输出：per-bag、group summary、A_rank summary、图和 Markdown 报告。

### 运行结果

- 数据集：`/data/a/slosh_bags/real/20260522_fixed_path_cost_d200`
- RGB：`red_visual_analysis_20260524/relabel_20260524/red_infer_hsv_manual_v157`
- 输出：`/data/a/slosh_bags/real/20260522_fixed_path_cost_d200/zeta_fidelity_ablation_20260525`
- 主要结果：
  - `ζ=0.05` replay：p95 `A_rank=0.711538`，`gamma=134.739`，`RMSE=0.833365mm`，`corr=0.298876`
  - Ferrari `ζ=0.116962` replay：p95 `A_rank=0.8`，`gamma=167.759`，`RMSE=0.869873mm`，`corr=0.268075`
- 判断：Ferrari ζ 改善了 p95 排序一致性，但绝对保真度指标变差，且模型对 RGB 幅值低估更严重；因此暂不建议把 Ferrari ζ 接入在线默认，只保留为离线 ablation / 论文物理参考。

## SloshPriorityMPC 对比实验设计收敛

- 新增 `docs/Claude/修改方案-时间-简介/2026-05-25_SloshPriorityMPC对比实验设计.md`。
- 明确本轮不做 P1 `ω²·d / ω̇·d` 偏置耦合补偿，不做 P2 terminal residual。
- 后续主线改为固定当前 MPC 结构，做 C/D/E/F 的 2×2 因子对比：
  - C：无 slosh cost，普通 ax/jerk 平滑；
  - D：只加 modal slosh cost；
  - E：只加强 ax/jerk 平滑；
  - F：modal slosh cost + ax/jerk 平滑。
- 更新 `2026-05-24_论文执行TODO_6步.md`，删除继续推进 P1/P2 的主线安排，改成录包、RGB/Ferrari 指标分析和 C/D/E/F 差分统计。

## 论文 baseline 对比实验计划

- 新增 `docs/Claude/修改方案-时间-简介/2026-05-25_SloshPriorityMPC论文baseline对比实验计划.md`。
- 明确外部 baseline 选择逻辑：
  - 主 baseline：TOPPRA-style acceleration-limited retiming、Ruckig-style jerk-limited retiming、Smooth MPC；
  - 工程 baseline：TEB；
  - 可选传统下界：DWA；
  - related work / future baseline：RTEB、TOPP-DWR。
- 明确 TEB 不应作为唯一主 baseline，因为它会改变几何路径；固定路径 retiming baseline 更适合证明液体搬运中的激励控制差异。
- 根据 Ferrari 2026 的 Assigned Path / Point-to-Point 结构，将 baseline 计划改成双任务设计：
  - Task A：固定 P2_s_curve 路径跟踪，作为主结果和主消融；
  - Task B：同起终点点到点导航，作为工程泛化 / supplementary。
- 按最新讨论收敛正文主表：TOPPRA-style、Ruckig-style、Smooth MPC/E、SloshPriorityMPC/F；C/D 转为 ablation，TEB/DWA 只放 appendix 或 related work。
