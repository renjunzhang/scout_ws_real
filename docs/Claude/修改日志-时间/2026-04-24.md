# 2026-04-24 修改日志

## 1. 风险自适应后续改进计划整理

新增文件：

- `docs/Claude/修改方案-时间-简介/2026-04-24_Risk-Adaptive Scheduling改进计划.md`

记录内容：

- 当前 `risk_scheduler` 的问题不在“完全失效”，而在“主指标对齐不够”
- 当前更像持续风险整形器，而不是峰值最小化器
- 后续优先级明确为：
  1. 先补重复实验
  2. 再把调度输入改成真实参考速度
  3. 再拆 `Q_eta / eta_bar / v_ref` 三路调度做消融

## 2. 文献检索 prompt 整理

在方案文件中补了 4 组可直接使用的 prompt，用于后续检索：

- adaptive MPC / anti-slosh 文献
- risk scheduling / gain scheduling 论证方式
- anti-slosh baseline
- adaptive speed planning / reference shaping 文献

用途：

- 不直接推动代码改动
- 主要服务论文 related work、方法论证和消融设计

## 3. 当前口径

今天没有继续修改 `risk_scheduler` 代码。

当前判断：

- 现在更重要的是先把主实验数据录扎实
- 不应在证据不足时继续同时改多项调度参数
- 下一步最合理的代码级改动候选，是把 `risk_scheduler` 的 `v_ref` 输入从固定巡航速度改成真实参考速度源

## 4. 风险自适应实验协议收紧

将 04-24 方案进一步收紧为可执行协议，新增内容包括：

- `PROP` 成功判据：
  - `p95/RMS` 改善 `>= 10%`
  - `peak` 恶化 `<= 5%`
  - `task time` 增加 `<= 10%`
- 增加 `FAS_Q5_slow` baseline，用于排除“只是跑慢”的解释
- 将代码级后续修改明确拆成：
  - `P1-A: reference-consistent scheduling`
  - `P1-B: physical normalization correction`
- 明确当前主实验中：
  - `adaptive_v_ref` 预期是主作用通道
  - `t_risk` 不应混入 motion-phase 主结论

今天仍未修改 `risk_scheduler` 代码，以上内容仅用于冻结后续实验与改进顺序。

## 5. Anti-Slosh MPC 结构改进方案整理

新增文件：

- `docs/Claude/修改方案-时间-简介/2026-04-24_Anti-Slosh_MPC结构改进方案.md`

记录内容：

- 当前问题不是“液体晃动融入 MPC 的方向错误”，而是第一版结构太弱
- 现有 slosh cost 只惩罚 `eta_x / eta_y`，没有惩罚 `eta_dot`
- 当前控制器应定位为 `slosh-aware tracking MPC`，还不是强 `anti-slosh MPC`
- 后续代码改动应从整体结构推进，不做临时补丁

推荐第一轮代码改动：

- 增加 `Q_slosh_eta_dot`
- 默认值为 `0.0`
- 保证 `Q_slosh=0` 和旧实验入口行为不变
- 先只验证 `eta_dot` 代价是否能改善 `/slosh/height RMS/peak` 与视觉 `p95/RMS`

后续代码修改日志要求：

- 每次修改代码后，都在本文件继续记录：
  - 修改范围
  - 修改原因
  - 验证命令
  - 验证结果

## 6. P1 代码改动：加入 `Q_slosh_eta_dot`（2026-04-25）

### 背景

32 包实物实验分析结果显示，当前 anti-slosh MPC 在 P2_s_curve 和 P3_mixed 两条路径上均无法稳定优于 NOM 基线（视觉口径），`/slosh/height` 口径也只在 P3 上有部分支持。

分析原因：现有代价只惩罚 `eta_x / eta_y`（液体模态位移），不惩罚 `eta_x_dot / eta_y_dot`（模态速度）。控制器可以在短时间内将 eta 压回零，但允许高速振荡，导致 MPC 视窗内实际液体相位控制能力弱。

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/types.h` | `MPCParams` 新增 `Q_slosh_eta_dot = 0.0` |
| `src/cost_function.cpp` | `evaluate()` 和 `getQuadraticCost()` 中增加 eta_dot 代价项 |
| `src/local_planner_ros.cpp` | 读取参数 `mpc/Q_slosh_eta_dot`；启动 log 增加输出 |
| `config/mpc_params.yaml` | 新增 `Q_slosh_eta_dot: 0.0`，实物入口默认关闭 |
| `config/mpc_params_sim.yaml` | 新增 `Q_slosh_eta_dot: 0.0` |
| `launch/slosh_experiment.launch` | 新增 `Q_slosh_eta_dot` launch arg 和 param |
| `launch/slosh_experiment_sim.launch` | 新增 `Q_slosh_eta_dot` launch arg 和 param |

说明：仿真和实物入口保持同步新增参数，避免两套实验口径分叉；默认值均为 `0.0`，不改变旧行为。

### 实现口径

```text
J_slosh =
  Q_slosh_eta     * (eta_x² + eta_y²)          [原有，Q_slosh_eta = Q_slosh * h_coeff²]
  + Q_slosh_eta_dot * (eta_x_dot² + eta_y_dot²) [新增，直接 QP 权重，不经 h_coeff 缩放]
```

`Q_slosh_eta_dot` 默认 `0.0`，保证旧行为不变。

### 验证命令

```bash
catkin_make --pkg scout_local_planner  # 编译通过 ✓
```

静态行为检查（仿真启动后）：
```bash
roslaunch scout_local_planner slosh_experiment_sim.launch Q_slosh:=0 Q_slosh_eta_dot:=0.0
```

仿真消融实验（P2_s_curve）：
```bash
# FAS_Q5 基线（保持不变）
roslaunch scout_local_planner slosh_experiment_sim.launch Q_slosh:=5 Q_slosh_eta_dot:=0.0

# FAS_Q5_DOT 小值测试
roslaunch scout_local_planner slosh_experiment_sim.launch Q_slosh:=5 Q_slosh_eta_dot:=0.05
```

指标：观察 `/slosh/height` 的 `peak / p95 / RMS` 是否下降。

### 验证结果

仿真 8 包（P2_s_curve + P3_mixed × 4 条件）+ 3 包诊断包。结论：

- `Q_slosh_eta_dot=0.05`：激励可比条件下（P2_s_curve）η̇_rms 不降反升（+0.145 mm/s），h 无改善
- `Q_slosh_eta_dot=0.20`：h 有改善（Δh_peak=−0.75 mm），但 η̇_rms 仍升高（+0.949 mm/s）；P0 直线对照包证明改善来自激励相位偏移，不是阻尼
- 真实机制：惩罚 η̇² 使优化器把激励移到振荡零点（η 小、η̇ 大），比值 η̇/(ω₀η) 从 0.76 升至 0.98

P1 结论：代码保留，但机制为相位偏移而非阻尼；不作为主要 anti-slosh 结构。进入 P2。

---

## 7. P2 代码改动：加入 terminal slosh cost（2026-04-25）

### 背景

P1 实验和对 32 包实物数据的机制分析表明：
- `Q_slosh_eta`（η-only 代价）在 S 弯路径上可构成性加剧晃动（FAS_Q5 的 `/slosh/height` peak 比 NOM 高 19%）
- 原因是 η-only 惩罚使优化器将激励时机移到液体位移峰值附近，结果是平均 η 更高
- 解决方案：在预测窗口末端同时惩罚 η 和 η̇（总模态能量 E = ω₀²η² + η̇²），逼迫 MPC 规划出"到达末端时液体位移和速度同时小"的轨迹

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/types.h` | `MPCParams` 新增 `terminal_factor_slosh_eta`、`terminal_factor_slosh_eta_dot`，默认 0.0 |
| `src/cost_function.cpp` | terminal ramp 块中叠加 slosh 终端放大系数（η 和 η̇ 独立，仅在 base cost > 0 时生效） |
| `src/local_planner_ros.cpp` | 读取 `mpc/terminal_factor_slosh_eta` 和 `mpc/terminal_factor_slosh_eta_dot` |
| `config/mpc_params_sim.yaml` | 新增两个参数，默认 0.0 |
| `config/mpc_params.yaml` | 新增两个参数，默认 0.0 |
| `launch/slosh_experiment_sim.launch` | 新增两个 launch arg 和对应 param |
| `launch/slosh_experiment.launch` | 新增两个 launch arg 和对应 param |
| `scripts/run_sim_fixed_path_bag.sh` | 新增 `FAS_Q5_TERM` 条件；MPC_ARGS 中传入两个新参数 |

### 实现口径

terminal ramp 最后 `terminal_ramp_steps` 步内，在现有 ec/etheta/v 放大的基础上，叠加：

```
if terminal_factor_slosh_eta > 0 AND Q_slosh_eta > 0:
    Q_ETA_X *= 1 + alpha × (terminal_factor_slosh_eta − 1)
    Q_ETA_Y *= 1 + alpha × (terminal_factor_slosh_eta − 1)

if terminal_factor_slosh_eta_dot > 0 AND Q_slosh_eta_dot > 0:
    Q_ETA_X_DOT *= 1 + alpha × (terminal_factor_slosh_eta_dot − 1)
    Q_ETA_Y_DOT *= 1 + alpha × (terminal_factor_slosh_eta_dot − 1)
```

两个 factor 独立控制，默认 0.0，旧行为完全不变。

### 验证命令

```bash
catkin_make  # 编译通过 ✓
```

仿真验证（P2 第一轮）：

```bash
# NOM 基线
PATH_ID=P2_s_curve CONDITION=NOM RUN_ID=01 START_DELAY=0 \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

# FAS_Q5 对照
PATH_ID=P2_s_curve CONDITION=FAS_Q5 RUN_ID=01 START_DELAY=0 \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

# FAS_Q5_TERM（P2，Q_slosh=5, Q_slosh_eta_dot=0.02, factor_eta=10, factor_eta_dot=10）
PATH_ID=P2_s_curve CONDITION=FAS_Q5_TERM RUN_ID=01 START_DELAY=0 \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

成功判据：

- `/slosh/height RMS` 相比 FAS_Q5 下降，且 ay_rms 差 < 10%
- η̇_rms 不升高（区别于 P1 的相位偏移）
- `task_time` 增加 ≤ 15%
- `success_ratio` ≥ 0.97

### 验证结果

仿真 3 包（P2_s_curve：NOM / FAS_Q5 / FAS_Q5_TERM，2026-04-25）。分析方法：提取 TRACKING 阶段（排除 terminal recovery 占比影响）。

| 指标 | NOM | FAS_Q5 | FAS_Q5_TERM |
|------|-----|--------|-------------|
| h_peak (mm) | 8.74 | 7.92 | **7.34** |
| h_p95 (mm) | 6.55 | 6.46 | **5.35** |
| h_rms (mm) | 2.96 | 3.21 | **2.85** |
| η̇_rms (mm/s) | 16.12 | 15.90 | **13.53** |
| η̇/(ω₀η) | 1.207 | 1.099 | **1.053** |
| ay_rms (m/s²) | 0.842 | **1.009** | 0.844 |
| omega_rms (rad/s) | 0.524 | **0.583** | 0.461 |
| task_time (s) | 16.58 | 16.16 | 17.15 |
| success_ratio | 0.971 | 0.984 | 0.984 |

**激励可比性（TERM vs FAS_Q5，TRACKING 段）**：omega_rms 差 20.8%，ay_rms 差 16.3%，均超出 10% 限制。h 改善不能独立归因于 terminal cost。

**P2 成功判据逐项**：

- h_rms 下降（vs FAS_Q5）：delta=−0.36 mm ⚠️ 激励不可比，不可归因
- h_peak 不恶化：delta=−0.57 mm ✓
- η̇_rms 不升高：15.90 → 13.53 mm/s（−14.9%）✓ **P1 区别标志满足**
- task_time 增加 ≤ 15%：6.1% ✓
- success_ratio ≥ 0.97：0.984 ✓

**机制发现**：FAS_Q5（η-only）的 omega_rms 比 NOM 高 11%（0.583 vs 0.524），与 P1 结论一致——η-only 代价使优化器选择更高转速产生峰值相位激励。FAS_Q5_TERM 的 omega_rms 反而降至 0.461（低于 NOM），说明 terminal energy cost 渗透至整段路径规划，不只作用于末端段。

**P2 结论**：初步有效信号，但激励不可比。η̇_rms 下降满足 P2 核心特征（区别于 P1），但 terminal_factor=10 影响力过强。

**下一步**：录制 factor=3 的小值对照（`TERM_S`），验证激励可比性能否恢复。

---

## 8. P2 run04 旧迷宫路径复测记录（2026-04-26）

### Bag 与启动命令

本轮在旧迷宫地图中手动启动 MPC，路径话题为 `/scout/global_path`。

NOM：

```bash
roslaunch scout_local_planner slosh_experiment_sim.launch \
  Q_slosh:=0 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=0.0 \
  terminal_factor_slosh_eta_dot:=0.0 \
  risk_scheduler_enable:=false \
  input_shaping_enable:=false \
  enable_slosh_box_constraint:=false \
  slosh_speed_governor_enable:=false
```

Bag：

```text
/data/a/slosh_bags/sim/20260426/20260426_NOM_run04.bag
```

FAS_Q5_TERM_f3：

```bash
roslaunch scout_local_planner slosh_experiment_sim.launch \
  Q_slosh:=5 \
  Q_slosh_eta_dot:=0.02 \
  terminal_factor_slosh_eta:=3 \
  terminal_factor_slosh_eta_dot:=3 \
  risk_scheduler_enable:=false \
  input_shaping_enable:=false \
  enable_slosh_box_constraint:=false \
  slosh_speed_governor_enable:=false
```

Bag：

```text
/data/a/slosh_bags/sim/20260426/20260426_FAS_Q5_TERM_f3_run04.bag
```

### 数据完整性

这两个 bag 均包含核心分析 topic：

- `/slosh/height`
- `/slosh/state`
- `/terminal/mode`
- `/mpc_status`
- `/mpc/status_val`
- `/odom`
- `/cmd_vel`
- `/scout/global_path`

因此本轮可以分析 `/slosh/height` 与模态状态，不再是上一组旧地图 bag 那种缺少 slosh topic 的无效记录。

### 分析口径

主结论采用严格主段：

```text
mpc_status == TRACKING 且 terminal/mode == NONE
```

该口径排除 `IDLE / SETTLING / REACHED / terminal recovery` 对液面统计的影响。另检查全包统计用于确认是否存在明显求解失败或终点未到达。

### 主段结果

| 指标 | NOM_run04 | FAS_Q5_TERM_f3_run04 | 变化 |
|---|---:|---:|---:|
| `/slosh/height` peak | 7.86 mm | 8.97 mm | +14.0% |
| `/slosh/height` p95 | 4.68 mm | 4.27 mm | -8.9% |
| `/slosh/height` RMS | 2.39 mm | 2.24 mm | -6.1% |
| η RMS | 1.31 mm | 1.23 mm | -6.1% |
| η̇ RMS | 11.51 mm/s | 11.09 mm/s | -3.7% |
| ay RMS | 0.463 m/s² | 0.443 m/s² | -4.3% |
| omega RMS | 0.423 rad/s | 0.414 rad/s | -2.1% |
| cmd_v RMS | 1.297 m/s | 1.254 m/s | -3.3% |
| cmd_w RMS | 0.964 rad/s | 0.962 rad/s | -0.2% |
| tracking solve success | 1.000 | 1.000 | 持平 |
| solve mean | 4.51 ms | 4.48 ms | 持平 |

终点检查：

| 指标 | NOM_run04 | FAS_Q5_TERM_f3_run04 |
|---|---:|---:|
| final-to-path-end distance | 0.123 m | 0.134 m |
| final yaw error | 1.36 deg | 1.25 deg |

### 关键限制：路径不是同一条

本轮 NOM 与 FAS_Q5_TERM_f3 的 `/scout/global_path` 不一致：

| 路径项 | NOM_run04 | FAS_Q5_TERM_f3_run04 |
|---|---:|---:|
| path length | 19.43 m | 20.41 m |
| path start yaw | 0.228 rad | -0.307 rad |
| path end | `(-0.992, 4.522)` | `(-1.990, 4.653)` |
| path kappa p95 | 8.44 1/m | 7.59 1/m |

因此这轮不是严格 A/B。虽然 ay/omega 激励 RMS 接近，但路径长度、起点朝向、终点位置不同，液面差异不能完全归因于 MPC 代价项。

### 结论

FAS_Q5_TERM_f3 在本轮中没有引入明显求解问题，tracking 段求解成功率为 1.0，终点也正常到达。

液面指标表现为：

- RMS 与 p95 略有下降，幅度约 6% 到 9%；
- η 与 η̇ RMS 也略有下降；
- peak 反而升高，从 7.86 mm 增至 8.97 mm。

本轮结论只能记为：`FAS_Q5_TERM_f3` 在旧迷宫路径下有轻微 RMS/p95 改善信号，但 peak 变差，且路径不可比，因此不能判定 terminal slosh cost 已稳定有效。

### 下一步要求

后续验证必须使用同一条固定路径 replay，不要每次重新点目标生成 `/scout/global_path`。建议固定路径后重录：

- `NOM`
- `FAS_Q5`
- `FAS_Q5_TERM_f3`

只有在同一路径、同一初始位姿、同一统计口径下，才能判断 P2 结构是否真正抑制 `/slosh/height`。

---

## 9. P2 近似同路径三包分析与方案主线调整（2026-04-26）

### Bag

本轮使用开阔场景生成的 P2_s_curve 近似同路径三包：

| 条件 | Bag |
|---|---|
| `NOM` | `/data/a/slosh_bags/sim/20260426/NOM_P2171958.bag` |
| `FAS_Q5` | `/data/a/slosh_bags/sim/20260426/NOFAS_Q5_172452.bag` |
| `FAS_Q5_TERM_f3` | `/data/a/slosh_bags/sim/20260426/NOFAS_Q5_TERM_f3172847.bag` |

三包均包含核心 topic：

- `/slosh/height`
- `/slosh/state`
- `/terminal/mode`
- `/mpc_status`
- `/mpc/status_val`
- `/odom`
- `/cmd_vel`
- `/scout/global_path_fixed`

路径不是 bit-level 相同，但几何近似一致，按趋势机制分析处理。

### 分析口径

主段：

```text
mpc_status == TRACKING 且 terminal/mode == NONE
```

### 主段结果

| 指标 | NOM | FAS_Q5 | FAS_Q5_TERM_f3 |
|---|---:|---:|---:|
| h_peak | 7.239 mm | 7.488 mm | 7.189 mm |
| h_p95 | 6.273 mm | 6.162 mm | 6.233 mm |
| h_rms | 3.130 mm | 3.039 mm | 3.165 mm |
| eta_rms | 1.719 mm | 1.669 mm | 1.738 mm |
| eta_dot_rms | 11.633 mm/s | 13.725 mm/s | 13.930 mm/s |
| ay_rms | 1.227 m/s² | 1.110 m/s² | 1.215 m/s² |
| omega_rms | 0.633 rad/s | 0.563 rad/s | 0.639 rad/s |
| solve success | 1.000 | 0.991 | 1.000 |

路径检查：

| 项 | NOM | FAS_Q5 | FAS_Q5_TERM_f3 |
|---|---:|---:|---:|
| path length | 8.984 m | 9.014 m | 8.999 m |
| path hash | `77666350e1` | `9bc40123df` | `fc8f2e844b` |

### 机制结论

本轮按近似同路径解释，`FAS_Q5` 和 `FAS_Q5_TERM_f3` 均未形成稳定 anti-slosh 效果。

关键原因：

- `FAS_Q5` 的 h_rms 只比 NOM 低约 2.9%，但 eta_dot_rms 从 11.633 mm/s 升至 13.725 mm/s，说明更像相位移动，不是阻尼。
- `FAS_Q5_TERM_f3` 的 h_peak 略低，但 h_rms 高于 NOM，eta_dot_rms 最高，说明 terminal cost 没有稳定降低整段模态能量。
- 当前代价仍主要惩罚液体状态 `eta`，没有直接约束产生晃动的激励源：`ay`、`alpha`、`jerk` 和曲率突变处速度。

因此当前控制器应继续定义为：

```text
slosh-aware tracking MPC
```

还不能定义为有效的：

```text
anti-slosh MPC
```

### 方案文档更新

已重写：

```text
docs/Claude/修改方案-时间-简介/2026-04-24_Anti-Slosh_MPC结构改进方案.md
```

新的主线：

1. 不继续盲目调大 `Q_slosh`、`Q_slosh_eta_dot` 或 terminal factor。
2. P1 `Q_slosh_eta_dot` 和 P2 terminal slosh cost 保留为消融项，不作为主机制。
3. 下一步先补齐诊断：`eta_norm / eta_dot_norm / modal_energy / excitation`。
4. 之后实现默认关闭的 `slosh-aware speed cap`，从源头降低 `ay / alpha / 曲率突变速度`。
5. 成功判据改为同时要求 `h_rms/h_p95`、`eta_dot_rms`、`modal_energy_rms` 改善，而不是只看 h_peak。

当前下一步代码方向：

```text
先证明能降低激励源，再证明能降低模态能量，最后才看液面 peak。
```

---

## 10. P3A 诊断 topic 接入（2026-04-26）

### 背景

§9 的近似同路径三包显示：

```text
NOM eta_dot_rms    = 11.633 mm/s
FAS_Q5 eta_dot_rms = 13.725 mm/s
```

`FAS_Q5` 的 h_rms 只轻微下降，但 eta_dot_rms 明显升高，说明当前 η-only 代价更像相位移动而不是阻尼。进入 P3A：先补齐模态能量和激励源诊断，不改变控制行为。

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 P3A 诊断 publisher 成员 |
| `src/local_planner_ros.cpp` | 发布 `eta_norm / eta_dot_norm / modal_energy / modal_energy_norm / excitation_ay_abs / excitation_alpha_abs` |
| `scripts/run_sim_fixed_path_bag.sh` | 录包 topic 列表加入 P3A 诊断 topic |
| `scripts/record_slosh_experiment.sh` | 实物录包 topic 列表加入 P3A 诊断 topic |
| `scripts/record_slosh_debug.sh` | debug 录包 topic 列表加入 P3A 诊断 topic |
| `scripts/extract_slosh_metrics.py` | 从 `/slosh/state` 计算 `eta_norm_rms / eta_dot_norm_rms`，并读取未来 bag 中的 `modal_energy` topic |

新增 topic：

```text
/slosh/eta_norm
/slosh/eta_dot_norm
/slosh/modal_energy
/slosh/modal_energy_norm
/slosh/excitation_ay_abs
/slosh/excitation_alpha_abs
```

计算口径：

```text
eta_norm = sqrt(eta_x^2 + eta_y^2)
eta_dot_norm = sqrt(eta_x_dot^2 + eta_y_dot^2)
modal_energy = omega0^2 * eta_norm^2 + eta_dot_norm^2
modal_energy_norm = sqrt(modal_energy)
excitation_ay_abs = abs(ay_est_used)
excitation_alpha_abs = abs(alpha_est_used)
```

### 行为边界

本次只新增诊断输出和录包/离线统计支持：

- 不改变 MPC 状态维度。
- 不改变 cost function。
- 不改变 `/cmd_vel`。
- 不改变 `Q_slosh=0` 默认行为。
- 不启用 speed cap。

### 验证命令

```bash
python3 -m py_compile src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
bash -n src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh
bash -n src/scout_apps/control/scout_local_planner/scripts/record_slosh_debug.sh
catkin_make --pkg scout_local_planner
```

离线旧 bag 检查：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py \
  /data/a/slosh_bags/sim/20260426/NOM_P2171958.bag \
  /data/a/slosh_bags/sim/20260426/NOFAS_Q5_172452.bag \
  /data/a/slosh_bags/sim/20260426/NOFAS_Q5_TERM_f3172847.bag
```

### 验证结果

- Python 语法检查通过。
- 三个 shell 录包脚本 `bash -n` 通过。
- `catkin_make --pkg scout_local_planner` 编译通过。
- 旧 bag 可从 `/slosh/state` 复算 `eta_dot_rms`。
- 旧 bag 中没有新发布的 `/slosh/modal_energy_norm`，因此 `energy_norm_rms=nan` 属于预期；新 bag 录制后该字段应有值。

旧三包复算结果：

| 条件 | h_rms | eta_dot_rms | solve success |
|---|---:|---:|---:|
| NOM | 0.003130 m | 0.011633 m/s | 1.000 |
| FAS_Q5 | 0.003039 m | 0.013725 m/s | 0.991 |
| FAS_Q5_TERM_f3 | 0.003165 m | 0.013930 m/s | 1.000 |

### 下一步

下一包仿真/实物录制后，优先检查：

```bash
rostopic echo /slosh/modal_energy_norm
rostopic echo /slosh/excitation_ay_abs
rostopic echo /slosh/excitation_alpha_abs
```

若新 topic 正常进入 bag，再进入 P3B：默认关闭的 `slosh-aware speed cap`。

---

## 11. P3B 曲率前馈 slosh-aware speed cap 接入（2026-04-26）

### 背景

P3A 诊断链路已通过 `/data/a/slosh_bags/sim/full_all.bag` 验证，新 topic 已进入 bag，`energy_norm_rms` 可被离线脚本读取。

进入 P3B：不继续调大 `Q_slosh` 或 terminal factor，而是在速度规划入口加入默认关闭的曲率前馈限速，从源头降低横向加速度激励。

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 `slosh_speed_cap_*` 参数缓存和 debug publisher |
| `src/local_planner_ros.cpp` | 在生成参考点前按前方曲率限制 `v_des_cmd`；发布 `speed_cap_active / speed_cap_v_limit` |
| `config/mpc_params.yaml` | 新增 `slosh_speed_cap` 默认关闭参数 |
| `config/mpc_params_sim.yaml` | 新增 `slosh_speed_cap` 默认关闭参数 |
| `launch/slosh_experiment.launch` | 暴露 P3B launch arg |
| `launch/slosh_experiment_sim.launch` | 暴露 P3B launch arg |
| `scripts/run_sim_fixed_path_bag.sh` | 新增 `SPEED_CAP`、`FAS_Q5_SPEED_CAP` 条件，并传入 P3B 参数 |
| `scripts/record_slosh_experiment.sh` | 录制 P3B debug topic |
| `scripts/record_slosh_debug.sh` | 录制 P3B debug topic |
| `scripts/extract_slosh_metrics.py` | 输出 `speed_cap_active_ratio / speed_cap_v_limit_*` |

新增参数：

```yaml
slosh_speed_cap:
  enable: false
  ay_limit: 0.8
  dkappa_limit_weight: 0.0
  min_v: 0.4
  preview_distance: 1.0
```

新增 topic：

```text
/slosh/speed_cap_active
/slosh/speed_cap_v_limit
```

### 实现口径

在 `TRACKING` 且非 `goal_stop_pending`、非 `settling_active` 时生效：

```text
v_cap_kappa = sqrt(ay_limit / max(abs(kappa_preview), eps))
v_des_cmd = min(v_des_cmd, max(min_v, v_cap_kappa))
```

如果 `dkappa_limit_weight > 0`，额外启用曲率变化限速：

```text
alpha_budget = dkappa_limit_weight * vehicle.alpha_max
v_cap_dkappa = sqrt(alpha_budget / max(dkappa_preview, eps))
v_des_cmd = min(v_des_cmd, max(min_v, v_cap_dkappa))
```

### 行为边界

本次 P3B 是速度参考层限速：

- 默认关闭，旧行为不变。
- 不改变 MPC 状态维度。
- 不改 cost function。
- 不启用 hard constraint。
- 不依赖当前 `eta`，只看路径几何风险。
- 与旧的 `slosh_speed_governor` 分离；旧 governor 仍默认关闭。

### 验证命令

```bash
python3 -m py_compile src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
bash -n src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh
bash -n src/scout_apps/control/scout_local_planner/scripts/record_slosh_debug.sh
catkin_make --pkg scout_local_planner
git diff --check
```

### 验证结果

- Python 语法检查通过。
- `run_sim_fixed_path_bag.sh` / `record_slosh_experiment.sh` / `record_slosh_debug.sh` 语法检查通过。
- `catkin_make --pkg scout_local_planner` 编译通过。
- `git diff --check` 通过。

### 推荐启动命令

P3B 单独限速，不加 `Q_slosh`：

```bash
PATH_ID=P2_s_curve CONDITION=SPEED_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_AY_LIMIT=0.8 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

P3B + FAS_Q5：

```bash
PATH_ID=P2_s_curve CONDITION=FAS_Q5_SPEED_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_AY_LIMIT=0.8 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

### 下一步验证判据

先录：

```text
NOM / SPEED_CAP / FAS_Q5 / FAS_Q5_SPEED_CAP
```

主判断：

- `speed_cap_active_ratio` 不应接近 1.0，否则说明全程慢跑。
- `task_time` 增加不超过 15%。
- `excitation_ay_abs_mean`、`eta_dot_rms`、`energy_norm_rms` 应下降。
- 只有当 `h_rms/h_p95` 与 `modal_energy` 同时下降，才认为 P3B 有效。

## 12. 2026-04-26 仿真录包脚本支持“固定终点 + 当前位姿生成模板路径”

### 背景

固定 JSON replay 模式要求车体位姿与路径起点严格对齐。仿真启动后存在定位初始化动作，导致起点对齐不稳定，影响 P2/P3 路径录包可比性。

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `scripts/run_sim_fixed_path_bag.sh` | 新增 `PATH_MODE=template_goal`，默认 `replay` 行为不变 |

### 行为

`PATH_MODE=template_goal` 时：

1. 先启动 `template_fixed_path_generator.py`。
2. 启动 MPC。
3. 向 `/scout/goal` 发布固定终点。
4. 模板路径从当前车体位姿生成，输出到 `/scout/global_path_fixed`，同时保存到 `/data/a/fixed_paths/sim/${PATH_ID}.json`。

默认终点来自 2026-04-26 RViz 选择点：

```text
x=-3.6119120121002197
y=3.955589771270752
qz=0.9992705515413127
qw=0.03818854307669733
```

### 推荐命令

```bash
PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=NOM RUN_ID=01 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=SPEED_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_AY_LIMIT=0.8 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=FAS_Q5 RUN_ID=01 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=FAS_Q5_SPEED_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_AY_LIMIT=0.8 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

### 验证

```bash
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
```

结果：通过。

## 13. 2026-04-26 P3B.2 限速器从“硬裁剪”改为“门控裁剪”

### 背景

P2_s_curve 仿真包显示：

| 条件 | tracking | h_rms | eta_dot_rms | energy_norm_rms | 结论 |
|---|---:|---:|---:|---:|---|
| NOM | 8.60 s | 2.552 mm | 14.381 mm/s | 0.0461 | 基线 |
| CAP 0.8 | 14.60 s | 1.805 mm | 9.745 mm/s | 0.0325 | 有效但过慢 |
| CAP 1.2 | 12.55 s | 2.476 mm | 11.737 mm/s | 0.0441 | 折中不足 |
| CAP 2.0 | 11.05 s | 2.920 mm | 10.279 mm/s | 0.0512 | 液面反而变差 |
| CAP 3.0 + dk0.35 | 20.65 s | 1.120 mm | 7.374 mm/s | 0.0206 | 慢速通过，不能算有效 |

判断：第一版 `slosh_speed_cap` 是硬裁剪 `v_des_cmd`，路径曲率连续存在时容易变成全程慢跑。继续调 `ay_limit/dkappa_weight` 不如修改限速结构。

### 修改范围

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 `activation_ratio / max_slowdown_ratio` 参数缓存 |
| `src/local_planner_ros.cpp` | 限速器改为门控触发，并增加相对降速下界 |
| `config/mpc_params.yaml` | 新增 P3B.2 默认参数 |
| `config/mpc_params_sim.yaml` | 新增 P3B.2 默认参数 |
| `launch/slosh_experiment.launch` | 透传 P3B.2 参数 |
| `launch/slosh_experiment_sim.launch` | 透传 P3B.2 参数 |
| `scripts/run_sim_fixed_path_bag.sh` | 透传并打印 P3B.2 参数 |

### 新参数

```yaml
slosh_speed_cap:
  activation_ratio: 0.9
  max_slowdown_ratio: 0.75
```

含义：

- `activation_ratio`：只有几何算出的 `v_cap < activation_ratio * v_des_cmd` 才触发，避免很小裁剪导致全程 active。
- `max_slowdown_ratio`：单次裁剪后 `v_des_cmd` 不低于原值的该比例，避免“慢速通过”伪改善。

### 行为边界

- `slosh_speed_cap/enable=false` 时旧行为不变。
- 不改 MPC 状态维度。
- 不改 cost function。
- 不新增 hard constraint。
- `/slosh/speed_cap_v_limit` 现在记录最终生效的速度上限，便于离线判断实际降速强度。

### 验证

```bash
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
catkin_make --pkg scout_local_planner
git diff --check
```

结果：全部通过。

## 18. 2026-04-26 P4 控制变化率消融首轮结果

时间：2026-04-26 22:27 CST

### 输入 bag

```text
SMOOTH_DOMEGA run01: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run01_221549.bag
SMOOTH_DOMEGA run02: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run02_221924.bag
SMOOTH_DOMEGA run03: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run03_222227.bag
SMOOTH_CTRL run01:   /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run01_221728.bag
SMOOTH_CTRL run02:   /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run02_222107.bag
SMOOTH_CTRL run03:   /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run03_222344.bag
```

基线：

```text
NOM: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_NOM_run01_182713.bag
```

### 条件定义

```text
NOM:            R_da=0.5, R_domega=4.0
SMOOTH_DOMEGA:  R_da=0.5, R_domega=6.0
SMOOTH_CTRL:    R_da=1.0, R_domega=6.0
```

### 结果均值 vs NOM

```text
SMOOTH_DOMEGA:
tracking_time:       +5.4%
h_rms:              +14.1%
h_peak:              +6.5%
eta_dot_rms:         -9.7%
modal_energy_norm:  +12.1%
cmd_dwz_rms:        -12.9%
solve_success_ratio: 0.991

SMOOTH_CTRL:
tracking_time:      +25.2%
h_rms:              +22.5%
h_peak:             +20.6%
eta_dot_rms:         -6.3%
modal_energy_norm:  +20.0%
cmd_dwz_rms:        +19.8%
solve_success_ratio: 0.992
```

### 判断

- `SMOOTH_DOMEGA` 有部分正向信号：角速度命令变化率下降，`eta_dot_rms` 下降，任务时间仍在可接受范围内。
- 但 `h_rms`、`h_peak`、`modal_energy_norm` 均升高，不能判定为有效抑晃。
- `SMOOTH_CTRL` 明确失败：任务时间显著增加，液面高度和模态能量都恶化，且角速度命令变化率没有降低。
- 当前 P4 参数消融不支持继续简单增大 `R_da/R_domega`。

### 下一步

- 停止 `SMOOTH_CTRL` 方向。
- `SMOOTH_DOMEGA` 只保留为弱信号，不直接进入实物验证。
- 下一步优先补充相位/滞后诊断：分析 `ay_est`、`alpha_z`、`eta`、`eta_dot`、`height` 的时序关系，确认“控制更平滑但高度更大”的原因。
- 若继续做参数消融，只做小范围 `R_domega=5.0` 对照，不再扩大权重。

## 19. 2026-04-26 P4 相位/滞后诊断

时间：2026-04-26 22:27 CST

### 本次修改

在 `extract_slosh_metrics.py` 增加离线相位/滞后诊断：

- 计算 signed `ay_est -> eta_y / eta_y_dot` 的最佳滞后相关。
- 计算 signed `alpha_est -> eta_x / eta_x_dot` 的最佳滞后相关。
- 计算 envelope `abs(ay_est) / abs(alpha_est) -> height / modal_energy_norm` 的最佳滞后相关。
- 正滞后定义：响应信号落后于激励信号。
- 默认扫描窗口 `±2.0 s`，步长 `0.05 s`。

### 运行命令

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py \
  --csv /tmp/p2_p4_lag_20260426.csv \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_NOM_run01_182713.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run01_221549.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run02_221924.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run03_222227.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run01_221728.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run02_222107.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run03_222344.bag
```

### 关键结果

均值 vs NOM：

```text
NOM:
ay_abs_mean:              0.620
alpha_abs_mean:           0.825
abs_ay_to_height_corr:    0.673
abs_ay_to_energy_corr:    0.663

SMOOTH_DOMEGA:
ay_abs_mean:              0.739  (+19.1%)
alpha_abs_mean:           0.971  (+17.7%)
abs_ay_to_height_corr:    0.815
abs_ay_to_energy_corr:    0.819
abs_ay_to_height_lag_s:  -0.033 s

SMOOTH_CTRL:
ay_abs_mean:              0.742  (+19.7%)
alpha_abs_mean:           1.013  (+22.8%)
abs_ay_to_height_corr:    0.757
abs_ay_to_energy_corr:    0.764
abs_ay_to_height_lag_s:  -0.017 s
```

### 判断

- P4 首轮失败的直接原因不是求解失败；`solve_success_ratio` 均约 `0.99`。
- `SMOOTH_DOMEGA` 虽降低了 `cmd_dwz_rms` 和 `eta_dot_rms`，但同时提高了 `ay_est/alpha_est` 激励强度。
- `abs(ay_est)` 与 `height/modal_energy_norm` 的相关性在平滑条件下更强，且最佳滞后接近 `0 s`，说明当前 `/slosh/height` 主要受瞬时横向激励幅值驱动。
- 因此，“单纯提高控制变化率惩罚”不是可靠抑晃路径；它可能通过改变跟踪相位/转弯时序降低 `eta_dot`，但没有降低主导高度的横向激励幅值。

### 下一步

- 不继续扩大 `R_da/R_domega`。
- 优先检查轨迹跟踪误差和曲率跟踪是否因平滑变差，导致实际 `ay_est = v * omega` 或等效横向激励升高。
- 若要继续 P4，应从“降低实际横向激励峰值/曲率跟踪误差”入手，而不是继续叠加平滑权重。

## 20. 2026-04-27 P4 轨迹跟踪与实际激励诊断

时间：2026-04-27 CST

### 本次修改

在 `extract_slosh_metrics.py` 增加 TRACKING 段轨迹/激励诊断：

- 从 `/tf` 读取 `map -> odom` 与 `odom -> base_footprint/base_link`，组合得到 map 下车体位姿。
- 使用 map 下车体位姿计算相对 `/mpc/reference_path` 的最近距离误差与航向误差。
- 从 `/odom.twist` 计算实际激励代理：`odom_ay = v * omega`、`odom_kappa = omega / v`。
- CSV 新增 `track_dist_*`、`track_heading_err_*`、`odom_ay_*`、`odom_kappa_abs_p95` 等字段。

注意：`/odom.pose` 与 TF 链中的 `odom -> base_footprint` 在仿真 bag 中不一致；轨迹误差必须使用 TF 链计算，不能直接拿 `/odom.pose` 与 map 路径比较。

### 运行命令

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py \
  --csv /tmp/p2_p4_tracking_20260427.csv \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_NOM_run01_182713.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run01_221549.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run02_221924.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_DOMEGA_run03_222227.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run01_221728.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run02_222107.bag \
  /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SMOOTH_CTRL_run03_222344.bag
```

### 关键结果

均值 vs NOM：

```text
NOM:
track_dist_p95:       1.720 m
heading_err_p95:     53.715 deg
odom_ay_abs_p95:      1.629 m/s^2
odom_kappa_abs_p95:   0.948 1/m
odom_wz_rms:          0.392 rad/s

SMOOTH_DOMEGA:
track_dist_p95:       1.355 m  (-21.2%)
heading_err_p95:     53.067 deg (-1.2%)
odom_ay_abs_p95:      2.233 m/s^2 (+37.1%)
odom_kappa_abs_p95:   1.444 1/m (+52.3%)
odom_wz_rms:          0.466 rad/s (+18.9%)

SMOOTH_CTRL:
track_dist_p95:       1.310 m  (-23.8%)
heading_err_p95:     53.947 deg (+0.4%)
odom_ay_abs_p95:      1.967 m/s^2 (+20.7%)
odom_kappa_abs_p95:   1.549 1/m (+63.4%)
odom_wz_rms:          0.469 rad/s (+19.6%)
```

### 判断

- “平滑权重导致几何贴合明显变差”不成立；`track_dist_p95` 反而下降，`heading_err_p95` 基本不变。
- 晃动恶化与实际激励峰值升高一致：`odom_ay_abs_p95`、`odom_kappa_abs_p95`、`odom_wz_rms` 在两个平滑条件下都高于 NOM。
- 当前 P4 失败更像是：控制变化率惩罚让命令更平滑或更慢，但为了完成同一条 S 曲线路径，车辆在局部段产生更高曲率/角速度峰值，最终抬高横向激励。
- 因此，下一步不应继续提高 `R_da/R_domega`；应转向直接限制“实际横向激励/路径曲率峰值”，例如速度规划层按 `v*omega` 或 `v^2*kappa` 做局部限速，而不是全程平滑控制。

### 验证

```bash
python3 -m py_compile src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
```

结果：通过。

## 17. 2026-04-26 日志顺序更正说明

本文件尾部存在第 14/15/16 节追加顺序交错的问题，保留原内容不删除，避免误删历史实验记录。后续阅读以以下顺序为准：

```text
13. P3B.2 门控裁剪
14. P3B 结论与 P3C dkappa_only
15. P3C.2 dkappa_threshold + short preview
16. P4 控制变化率消融入口
```

本次最新代码修改对应第 16 节：

- launch 支持 `mpc_R_da / mpc_R_domega` 覆盖。
- `run_sim_fixed_path_bag.sh` 支持 `SMOOTH_DOMEGA / SMOOTH_CTRL`。
- `extract_slosh_metrics.py` 输出 `cmd_dvx_rms_mps2 / cmd_dwz_rms_radps2`。

### P3C.2 首轮 bag 结果

录制包：

```text
/data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_DKAPPA_CAP_run03_212051.bag
/data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_DKAPPA_CAP_run04_212217.bag
```

对比：

| 条件 | tracking | h_rms | h_peak | eta_dot_rms | energy_norm | speed_cap_ratio |
|---|---:|---:|---:|---:|---:|---:|
| NOM | 8.60 s | 2.552 mm | 7.253 mm | 14.381 mm/s | 0.0461 | 0.000 |
| DKAPPA run01 无阈值 | 9.95 s | 2.852 mm | 6.285 mm | 10.443 mm/s | 0.0500 | 0.955 |
| DKAPPA run02 无阈值 | 8.55 s | 2.778 mm | 7.744 mm | 15.932 mm/s | 0.0503 | 0.948 |
| DKAPPA run03 threshold=8 | 9.55 s | 3.111 mm | 9.578 mm | 15.763 mm/s | 0.0557 | 0.787 |
| DKAPPA run04 threshold=8 | 9.50 s | 3.096 mm | 8.279 mm | 11.771 mm/s | 0.0544 | 0.778 |

判断：

- `dkappa_threshold=8.0` 确实降低了触发比例：约 0.95 → 0.78。
- 但 `h_rms`、`h_peak`、`modal_energy_norm` 均比 NOM 变差。
- 说明当前 “窗口最大 dkappa + 速度裁剪” 仍不是稳定 anti-slosh 结构。
- 不建议继续在 `dkappa_threshold / dkappa_limit_weight` 上盲调；下一步应转向直接约束控制变化率/激励平滑，或重新审查速度参考生成导致的相位变化。

## 16. 2026-04-26 P4 控制变化率消融入口

### 背景

P3B/P3C 速度参考裁剪已验证不能稳定降低 `/slosh/height` 和 `modal_energy_norm`。下一步不直接新增 cost function，而是先利用现有 MPC 代价中的：

```text
R_da * Δa² + R_domega * Δomega²
```

做 P4 参数消融，判断“控制变化率平滑”是否有 anti-slosh 信号。

当前参数事实：

```text
mpc_params.yaml:     R_da=0.5, R_domega=4.0
mpc_params_sim.yaml: R_da=0.5, R_domega=4.0
```

### 修改

| 文件 | 修改内容 |
|---|---|
| `launch/slosh_experiment.launch` | 新增 `mpc_R_da / mpc_R_domega` arg 并覆盖 `mpc/R_da / mpc/R_domega` |
| `launch/slosh_experiment_sim.launch` | 新增 `mpc_R_da / mpc_R_domega` arg 并覆盖 `mpc/R_da / mpc/R_domega` |
| `scripts/run_sim_fixed_path_bag.sh` | 新增 `SMOOTH_DOMEGA / SMOOTH_CTRL` 条件，并透传 `mpc_R_da / mpc_R_domega` |
| `scripts/extract_slosh_metrics.py` | 从 `/cmd_vel` 计算 `cmd_dvx_rms_mps2 / cmd_dwz_rms_radps2` |
| `config/mpc_params_sim.yaml` | 用户已将仿真 `R_domega` 对齐为 4.0 |

### 条件定义

```text
NOM:
  R_da=0.5
  R_domega=4.0

SMOOTH_DOMEGA:
  R_da=0.5
  R_domega=6.0

SMOOTH_CTRL:
  R_da=1.0
  R_domega=6.0
```

### 推荐验证命令

继续使用固定终点模板路径，不 replay 旧曲线：

```bash
PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=SMOOTH_DOMEGA RUN_ID=01 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=SMOOTH_CTRL RUN_ID=01 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

判断目标：

- `cmd_dwz_rms_radps2` 下降，证明角速度变化率确实更平滑。
- `eta_dot_rms` 下降。
- `modal_energy_norm_rms` 下降。
- `h_rms` 不升，`h_peak/h_p95` 不明显恶化。
- `tracking_time <= NOM * 1.15`。
- `solve_success_ratio >= 0.97`。

### 验证结果

```bash
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
python3 -m py_compile src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
catkin_make --pkg scout_local_planner
git diff --check
```

结果：全部通过。

### P3C 首轮 bag 结果

录制包：

```text
/data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_DKAPPA_CAP_run01_210633.bag
/data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_DKAPPA_CAP_run02_210807.bag
```

对比 NOM：

| 条件 | tracking | h_rms | h_peak | eta_dot_rms | energy_norm | speed_cap_ratio |
|---|---:|---:|---:|---:|---:|---:|
| NOM | 8.60 s | 2.552 mm | 7.253 mm | 14.381 mm/s | 0.0461 | 0.000 |
| DKAPPA run01 | 9.95 s | 2.852 mm | 6.285 mm | 10.443 mm/s | 0.0500 | 0.955 |
| DKAPPA run02 | 8.55 s | 2.778 mm | 7.744 mm | 15.932 mm/s | 0.0503 | 0.948 |

判断：

- `dkappa_only` 没有稳定改善 `/slosh/height` 或 `modal_energy`。
- `speed_cap_ratio` 仍约 0.95，说明当前 `getMaxCurvatureRateAhead()` 的前方窗口最大值在 P2 S 弯上仍导致近似全程触发。
- P3C 当前版本不是最终方案；下一步应把触发从“窗口内最大 dkappa”改成“局部换向事件/阈值触发”，或至少加入 `dkappa_threshold` 与更短 preview。

## 15. 2026-04-26 P3C.2 增加 dkappa 阈值和短 preview

### 背景

P3C 首轮 `DKAPPA_CAP` 虽然设置了 `mode=dkappa_only`，但 `speed_cap_ratio` 仍约 0.95。原因是当前 `getMaxCurvatureRateAhead()` 取前方窗口内最大 `dkappa`，P2 S 弯中窗口内长期存在曲率变化，导致近似全程触发。

### 修改

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 `slosh_speed_cap_dkappa_threshold_` |
| `src/local_planner_ros.cpp` | 只有 `dkappa_preview > dkappa_threshold` 才启用 dkappa cap |
| `config/mpc_params.yaml` | 新增 `slosh_speed_cap/dkappa_threshold`，默认 0 保持旧行为 |
| `config/mpc_params_sim.yaml` | 新增 `slosh_speed_cap/dkappa_threshold`，默认 0 保持旧行为 |
| `launch/slosh_experiment.launch` | 透传 `slosh_speed_cap_dkappa_threshold` |
| `launch/slosh_experiment_sim.launch` | 透传 `slosh_speed_cap_dkappa_threshold` |
| `scripts/run_sim_fixed_path_bag.sh` | `DKAPPA_CAP / FAS_Q5_DKAPPA_CAP` 默认 `dkappa_threshold=8.0`、`preview_distance=0.30` |

### 推荐验证命令

```bash
PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=DKAPPA_CAP RUN_ID=03 \
  SLOSH_SPEED_CAP_DKAPPA_LIMIT_WEIGHT=0.15 \
  SLOSH_SPEED_CAP_DKAPPA_THRESHOLD=8.0 \
  SLOSH_SPEED_CAP_PREVIEW_DISTANCE=0.30 \
  SLOSH_SPEED_CAP_ACTIVATION_RATIO=0.9 \
  SLOSH_SPEED_CAP_MAX_SLOWDOWN_RATIO=0.80 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

目标：

- `speed_cap_ratio` 明显低于 0.95。
- tracking 时间不超过 NOM 的 15%。
- `eta_dot_rms` 下降。
- `h_rms` 和 `modal_energy_norm` 不恶化。

### 验证结果

```bash
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
catkin_make --pkg scout_local_planner
git diff --check
```

结果：全部通过。

### 下一包建议

先用原来最有效但过慢的 `ay_limit=0.8`，配合新门控裁剪验证是否能降低时间代价：

```bash
PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=SPEED_CAP RUN_ID=05 \
  SLOSH_SPEED_CAP_AY_LIMIT=0.8 \
  SLOSH_SPEED_CAP_ACTIVATION_RATIO=0.9 \
  SLOSH_SPEED_CAP_MAX_SLOWDOWN_RATIO=0.75 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

判断目标：

- tracking 时间接近 `9.5~11.0 s`。
- `eta_dot_rms / energy_norm_rms` 低于 NOM。
- `h_rms` 不高于 NOM。
- `speed_cap_v_limit` 不应长期低于 `v_des_cmd` 的 75%。

## 14. 2026-04-26 P3B 结论与 P3C dkappa-only 限速接入

### P3B 重复验证结论

P3B.2 同参数重复包：

```text
run05: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SPEED_CAP_run05_184720.bag
run07: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SPEED_CAP_run07_205414.bag
run08: /data/a/slosh_bags/sim/20260426/20260426_P2_s_curve_SPEED_CAP_run08_205559.bag
```

参数：

```text
SLOSH_SPEED_CAP_AY_LIMIT=0.8
SLOSH_SPEED_CAP_ACTIVATION_RATIO=0.9
SLOSH_SPEED_CAP_MAX_SLOWDOWN_RATIO=0.75
```

结果均值 vs NOM：

```text
tracking_time: +10.7%
h_rms:         +7.2%
h_peak:        -0.2%
eta_dot_rms:   -17.0%
energy_norm:   +5.0%
```

判断：

- 纯曲率限速能降低 `eta_dot_rms`。
- 但 `h_rms` 和 `modal_energy_norm` 没有稳定下降。
- `speed_cap_active_ratio` 仍约 0.95，说明限速近似全程参与。
- P3B 不再作为主线继续调参。

### 本次修改

增加 P3C `dkappa_only` 模式：保留原 `SPEED_CAP` 行为，新增只按曲率变化/换向段限速的模式。

| 文件 | 修改内容 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 `slosh_speed_cap_mode_` |
| `src/local_planner_ros.cpp` | 读取 `slosh_speed_cap/mode`；`dkappa_only` 时跳过纯 `kappa` 分支 |
| `config/mpc_params.yaml` | `slosh_speed_cap.mode: curvature` 默认值 |
| `config/mpc_params_sim.yaml` | `slosh_speed_cap.mode: curvature` 默认值 |
| `launch/slosh_experiment.launch` | 新增 `slosh_speed_cap_mode` arg |
| `launch/slosh_experiment_sim.launch` | 新增 `slosh_speed_cap_mode` arg |
| `scripts/run_sim_fixed_path_bag.sh` | 新增 `DKAPPA_CAP / FAS_Q5_DKAPPA_CAP` 条件 |
| `docs/Claude/修改方案-时间-简介/2026-04-24_Anti-Slosh_MPC结构改进方案.md` | 更新 P3B 失败结论和 P3C 主线 |

### 行为边界

- 默认 `mode=curvature`，旧 `SPEED_CAP` 行为不变。
- `mode=dkappa_only` 只跳过纯曲率限速，不改 MPC 状态、代价函数或 hard constraint。
- `DKAPPA_CAP` 默认 `Q_slosh=0`，用于单独验证换向激励抑制。
- `FAS_Q5_DKAPPA_CAP` 用于验证 η-only 代价与换向限速叠加是否有额外收益。

### 推荐验证命令

```bash
PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=DKAPPA_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_DKAPPA_LIMIT_WEIGHT=0.15 \
  SLOSH_SPEED_CAP_ACTIVATION_RATIO=0.9 \
  SLOSH_SPEED_CAP_MAX_SLOWDOWN_RATIO=0.80 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh

PATH_MODE=template_goal PATH_ID=P2_s_curve CONDITION=FAS_Q5_DKAPPA_CAP RUN_ID=01 \
  SLOSH_SPEED_CAP_DKAPPA_LIMIT_WEIGHT=0.15 \
  SLOSH_SPEED_CAP_ACTIVATION_RATIO=0.9 \
  SLOSH_SPEED_CAP_MAX_SLOWDOWN_RATIO=0.80 \
  START_DELAY=30 APPROACH_START_ENABLE=false \
  rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

### 验证结果

```bash
bash -n src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
catkin_make --pkg scout_local_planner
git diff --check
```

结果：全部通过。
