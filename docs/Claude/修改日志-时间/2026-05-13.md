# 2026-05-13 OSCRS L2.5 接手与诊断字段修正

## 接手审查

- 已阅读 `CLAUDE.md`、L2.5 阶段总结、GFRS 模块化方案和 2026-05-12 修改日志。
- 已审查当前工作区 diff，当前未提交内容包含 fixed replay 接线、candidate_report latch、tail_protect/tail_gate 解耦、generation policy 可见诊断、takeover 语义兼容等改动。
- 继续遵守边界：不把 `/slosh/height` 当作实物液面主指标；不通过放宽 `eta_lim_mm` 或降低 `prediction_v_max` 制造 takeover。

## 修复: tail_protect report 字段未写回 pipeline row

问题：

```text
diagnostics.py 已输出 row 字段 tail=...
但 pipeline.py 未设置 tail_protect_applied。
因此 tail_protect_enable=true 时，candidate_report 仍可能显示 tail=0。
```

修复：

```text
pipeline.py 在每条 row 中写入 tail_protect_applied = tail_protect_enable && name != original。
```

说明：

```text
tail_protect 属于 G 层候选生成策略；tail_gate 属于 F 层 terminal reject。
本次只修正诊断字段，不改变候选选择、不改变 hard gate、不改变 eta_lim_mm / prediction_v_max。
```

## 修复: AntiSloshParams 与 pipeline 权重字段名不一致

问题：

```text
pipeline.py 读取 oscrs_score_w_energy / oscrs_score_w_eta_dot / oscrs_score_w_terminal。
AntiSloshParams 中仍保留 rosparam 名 oscrs_score_w_energy_rms / oscrs_score_w_eta_dot_rms / oscrs_score_w_terminal_E。
```

修复：

```text
AntiSloshParams 字段名与 runtime adapter / pipeline 对齐。
```

## 验证

```bash
python3 -m py_compile <all scripts/*.py>
git diff --check
python3 scripts/oscrs/tests/test_tail_gate.py
python3 scripts/oscrs/tests/test_tail_protect.py
python3 scripts/oscrs/tests/test_selector_score.py
python3 scripts/oscrs/tests/test_generation_policy.py
python3 scripts/oscrs/tests/test_pipeline_tail_report.py
python3 scripts/validate_georef_oscrs_bag.py \
  /data/a/slosh_bags/sim/20260513/20260513_P2_s_curve_GEOREF_OSCRS_ACTIVE_runsmoke_tail_gate03_005046.bag \
  --mode oscrs --require-non-original --require-takeover
python3 scripts/check_oscrs_takeover.py \
  /data/a/slosh_bags/sim/20260513/20260513_P2_s_curve_GEOREF_OSCRS_ACTIVE_runsmoke_tail_gate03_005046.bag
```

`pytest` 当前环境不可用：`No module named pytest`，因此本轮直接执行 smoke test 入口。

最新 bag 行为验收仍通过：

```text
reports=1 active=1 takeover=1 fallback=0 selected=strong fb=0 safety_alarm=0 VERDICT=PASS
```

注意：脚本对旧 report 的 `takeover=0` 做兼容推断；该 bag 最后一条 summary 仍显示旧字段 `takeover=0`，但满足 `active=1 && fb=0 && selected!=original`，因此行为验收计为 takeover。

## 方案文档: OSCRS 候选多样性与 Homotopy-lite G 层

新增：

```text
docs/Claude/修改方案-时间-简介/2026-05-13_OSCRS候选多样性与Homotopy-lite_G层方案.md
```

内容：

```text
1. 明确当前 GeoRef smoothing 候选可能发生 candidate collapse；
2. 参考 src/mpc_planner 的 Planner / ControllerModule / ModuleData / GuidanceConstraints / guidance_planner.yaml；
3. 建议 L2.6 先做 diversity diagnostics，再做默认关闭的 lateral_offset / corner_profile 等 homotopy-lite G family；
4. 真 PRM / homotopy class generator 仅作为 L3 预留；
5. 明确不直接搬 T-MPC solver，不改变 eta_lim_mm / prediction_v_max，不用 /slosh/height 作为实物主指标。
```

### 用户审阅后的修订

根据审阅意见，已更新方案文档：

```text
1. 在 L2.6 前置新增 Step 0: phase4 模型保真度闭环；
2. 将 generator 优先级调整为 corner_profile > offset > speed_proxy > tail_safe；
3. Phase A diversity diagnostics 补充 div_ax_spread、div_ax_pulse_spread、div_eta_x_spread、div_eta_y_spread；
4. 新增 dominant_excitation 与 diversity_aligned，用于判断候选差异是否覆盖主激励维度；
5. 在 Phase A 和 Phase B 之间新增决策 gate，避免还没证明 G 层是瓶颈就实现新 generator；
6. 将论文英文口径改为“借鉴 multi-stage candidate-evaluation-decision structure，替换 homotopy candidates 为 reference-shape / excitation-profile candidates”。
```

边界保持：

```text
不改运行代码；
不提交 git；
不改变 eta_lim_mm / prediction_v_max；
不使用 /slosh/height 替代 RGB 视觉主指标。
```

## Step 0 实现: phase4 模型保真度 ablation

新增离线分析脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/phase4_model_fidelity_ablation.py
```

用途：

```text
1. 读取 phase4 visual summary 中的 run01/run02 RAW / FIXED_MILD / OSCRS_MEDIUM_ACTIVE；
2. 使用 RGB 视觉 CSV 的 h_mm_smooth_corr 作为实物液面主指标；
3. 对 bag 中 /slosh/ax_est、/slosh/ay_est、/odom yaw rate 做离线 replay；
4. 比较 M0/M1/M2/M3 四组模型的单包保真度和 run 内 pairwise A_rank。
```

ablation 组：

```text
M0_online_recorded:
  使用已录 /slosh/height，扣 static median 后作为当前在线模型基线。

M0b_replay_observer:
  observer height_coeff + 当前 damping + 当前 parabola 设置，离线 replay /slosh/ax_est 和 /slosh/ay_est。

M1_ferrari_height_coeff:
  Ferrari closed-form height_coeff + 当前 damping + 当前 parabola 设置。

M2_parabola_off:
  Ferrari closed-form height_coeff + 当前 damping + parabola off。

M3_ferrari_full:
  Ferrari closed-form height_coeff + Ferrari physics damping + parabola on。
```

输出：

```text
docs/Claude/分析数据/phase4_model_fidelity_20260513/model_fidelity_ablation_phase4.csv
docs/Claude/分析数据/phase4_model_fidelity_20260513/model_selection_fidelity_phase4.csv
docs/Claude/分析数据/phase4_model_fidelity_20260513/A_rank_by_model_phase4.csv
docs/Claude/分析数据/phase4_model_fidelity_20260513/A_rank_by_model_phase4.md
```

本轮运行结果：

```text
M0_online_recorded:       A_rank_p95=1.000 (6/6)
M0b_replay_observer:      A_rank_p95=0.500 (3/6)
M1_ferrari_height_coeff:  A_rank_p95=0.500 (3/6)
M2_parabola_off:          A_rank_p95=0.500 (3/6)
M3_ferrari_full:          A_rank_p95=0.833 (5/6)
```

解读：

```text
1. 录包中的在线 /slosh/height 与 RGB p95 排序一致；
2. 但用 /slosh/ax_est、/slosh/ay_est 离线重放 observer 口径只有 A_rank_p95=0.500；
3. 因此需要区分“在线 observer 状态输出”与“离线 rollout 重放”；
4. 只切 Ferrari height_coeff 不足以改善 p95 排序；
5. Ferrari physics damping + Ferrari height_coeff 的组合在 phase4 p95 排序上通过 0.80 门槛；
6. rms 排序仍只有 0.667，不能把模型写成全面可信；
7. 该结论仍以 RGB 视觉 CSV 为真值，/slosh/height 只是 M0 模型变体。
```

## Step 1 实现: candidate diversity diagnostics

目标：

```text
只补充 G 层候选多样性诊断，不改变候选生成、不改变 F/R/S gate、不改变 OSCRS score / selection。
用于回答“候选是否塌缩”以及“候选差异是否覆盖主激励维度”。
```

新增模块：

```text
src/scout_apps/control/scout_local_planner/scripts/oscrs/diversity.py
```

新增 report-only summary 指标：

```text
div_path
div_kappa
div_dkappa
div_ay
div_ax
div_ax_pulse
div_sH
div_sE
div_eta_x
div_eta_y
dominant_excitation = eta_x / eta_y / balanced
diversity_aligned = 0 / 1
candidate_collapse = 0 / 1
```

实现细节：

```text
1. slosh_rollout.py 新增 predicted_excitation_profile()，复用 forward_profile 输出 ax/ay/v 诊断。
2. rollout_slosh_metrics() 输出 slosh_eta_x_p95 / slosh_eta_y_p95。
3. pipeline.py 每条 row 写入 predicted_ax_p95 / predicted_ax_peak / predicted_ay_peak。
4. diagnostics.py 在 candidate_report summary 中追加 diversity 字段，在 row 中追加 axp/axpk/aypk/ex/ey。
5. types.py 同步补齐 SloshMetrics / CandidateEval 文档字段，避免 dataclass 与 dict row 漂移。
```

边界：

```text
1. 不放宽 eta_lim_mm；
2. 不降低 prediction_v_max；
3. 不把 /slosh/height 当实物主指标；
4. 不实现 corner_profile / lateral_offset 等新 generator；
5. 本轮仍保持“Phase A 诊断先行”，后续是否进入 Phase B 由 RAW / GeoRef / OSCRS 多包对比和模型保真度 gate 决定。
```

新增测试：

```text
src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_candidate_diversity_metrics.py
```

覆盖：

```text
1. eta_x 主激励且 ax/eta_x spread 足够时 diversity_aligned=1；
2. eta_x 主激励但只有 ay spread 足够时 diversity_aligned=0；
3. candidate_report 包含 dominant_excitation / diversity_aligned / candidate_collapse / axp / ex 等字段。
```

验证：

```bash
python3 -m compileall -q src/scout_apps/control/scout_local_planner/scripts
git diff --check
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_tail_gate.py
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_tail_protect.py
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_selector_score.py
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_generation_policy.py
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_pipeline_tail_report.py
python3 src/scout_apps/control/scout_local_planner/scripts/oscrs/tests/test_candidate_diversity_metrics.py
python3 src/scout_apps/control/scout_local_planner/scripts/validate_georef_oscrs_bag.py \
  /data/a/slosh_bags/sim/20260513/20260513_P2_s_curve_GEOREF_OSCRS_ACTIVE_runsmoke_tail_gate03_005046.bag \
  --mode oscrs --require-non-original --require-takeover
```

最新 bag 验收仍通过：

```text
reports=1 active=1 takeover=1 fallback=0 selected=strong fb=0 safety_alarm=0 VERDICT=PASS
```

## 新增: 晃液模型与 RGB 真实值分析录包方案

新增问题方案文档：

```text
docs/Claude/遇到的问题与解决方案/2026-05-13_晃液模型真实值分析录包方案.md
```

核心内容：

```text
1. 明确 RGB 视觉液面是真实值，/slosh/height 只作为 observer/model 信号；
2. 小车行为按 static -> straight_ax -> turn -> P2/P3 策略对比推进；
3. 先辨识 eta_x / eta_y 单轴，再判断 ax+ay 耦合与 OSCRS 策略排序；
4. A_rank_p95 < 0.70 时不进入新 G 层 generator，实现优先级回到 R 层模型保真度；
5. 继续禁止通过放宽 eta_lim_mm 或降低 prediction_v_max 制造 takeover。
```

新增命令清单生成脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/generate_slosh_model_truth_protocol.py
```

用途：

```text
生成实物/RGB 真值分析的录包命令与操作清单。
脚本不自动控制小车，只生成 rosbag record 与 fixed-path 对比命令，避免把未验证的开环动作固化成自动脚本。
```

## 更新: L2.5 阶段性总结同步 Step 0/Step 1 状态

更新文档：

```text
docs/Claude/总结/2026-05-13_OSCRS_L2.5模块化阶段性总结.md
```

同步内容：

```text
1. 加入 divdiag01 新 bag 验证结果；
2. 加入 candidate diversity 关键字段与解释；
3. 加入 Step 0 模型保真度 ablation 结果；
4. 加入 Step 1 diversity diagnostics 当前结论；
5. 更新下一步建议为 RGB 真值录包与 RAW/GeoRef/OSCRS 策略排序验证；
6. 记录当前提交 a7d30af。
```

## 更新: 晃液模型真实值分析方案补充实物命令

更新文档：

```text
docs/Claude/遇到的问题与解决方案/2026-05-13_晃液模型真实值分析录包方案.md
```

参考：

```text
docs/重要文档/20260506有效性验证方案.md
```

补充内容：

```text
1. 实物基础系统启动命令：CAN/base、nanoscan、定位、IMU、RealSense；
2. 单轴模型辨识命令：observer-only local planner + 手动 /cmd_vel 激励 + rosbag record；
3. RAW_REAL / GEOREF_TUNED_MILD_REAL / GEOREF_OSCRS_ACTIVE_MILD_REAL 策略对比命令；
4. 固定目标发送、validate_georef_oscrs_bag.py、extract_slosh_metrics.py、compute_ferrari_indices.py 检查命令；
5. 明确单轴 high 组只在开阔场地、刹停距离充足时执行。
```

## 修正: 实物模型真值录包统一使用 record_slosh_experiment.sh

问题：

```text
晃液模型真实值分析方案中，单轴模型辨识部分给了裸 rosbag record 命令。
这容易漏掉 record_slosh_experiment.sh 已经维护好的 /liquid_measurement、/anti_slosh_path、costmap、诊断等 topic。
```

修正：

```text
1. 单轴 static / straight_ax / turn 包统一改为 ./scripts/record_slosh_experiment.sh；
2. 裸 rosbag record 明确降级为排障备用，不作为推荐正式录包方式；
3. generate_slosh_model_truth_protocol.py 同步改为输出 record_slosh_experiment.sh 命令。
```

## 新增: 0424 旧 bag 模型/RGB 排序复核

背景：

```text
.tmp_red_group_compare_0424 是旧 MPC / 旧控制方案数据，不能用于证明 OSCRS 效果。
但液体 observer / slosh model 口径可用于复核模型指标与 RGB 真值是否同向排序。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/red_group_0424_model_truth_summary.py
```

输入：

```text
.tmp_red_group_compare_0424/visual_metric_summary.csv
.tmp_red_group_compare_0424/slosh_internal_metric_summary.csv
```

输出：

```text
docs/Claude/分析数据/0424_model_truth_20260513/0424_joined_model_visual_metrics.csv
docs/Claude/分析数据/0424_model_truth_20260513/0424_model_visual_pairwise_rank.csv
docs/Claude/分析数据/0424_model_truth_20260513/0424_model_visual_A_rank_by_group.csv
docs/Claude/分析数据/0424_model_truth_20260513/0424_model_visual_A_rank_overall.csv
docs/Claude/分析数据/0424_model_truth_20260513/0424_model_visual_A_rank_overall.md
```

运行结果：

```text
peak: A_rank=0.576923
p95 : A_rank=0.440000
rms : A_rank=0.629630
```

解读：

```text
0424 旧数据支持“当前模型排序保真度不足”的判断，尤其 p95 低于随机水平。
因此仍应先做模型保真度闭环，不应仅靠扩 G 层候选改善 OSCRS。
```

## 新增: 0424 旧 bag 曲线叠加图与三层保真度报告

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/red_group_0424_model_fidelity_report.py
```

功能：

```text
1. 从 bag 读取 /slosh/height；
2. 从 .tmp_red_group_compare_0424 读取 RGB h_smooth_corr CSV；
3. 按 tracking_start..tracking_end+2s 过滤；
4. 以 0.15 s 插值窗口配对；
5. 输出 model-vs-RGB overlay 图；
6. 输出 Ferrari-style 保真度、A_rank、threshold sweep recall。
```

运行：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/analysis/red_group_0424_model_fidelity_report.py \
  --root .tmp_red_group_compare_0424 \
  --out-dir docs/Claude/分析数据/0424_model_fidelity_20260513
```

输出：

```text
docs/Claude/分析数据/0424_model_fidelity_20260513/MODEL_FIDELITY_REPORT.md
docs/Claude/分析数据/0424_model_fidelity_20260513/model_fidelity_summary.csv
docs/Claude/分析数据/0424_model_fidelity_20260513/model_selection_fidelity.csv
docs/Claude/分析数据/0424_model_fidelity_20260513/model_gate_fidelity_threshold_sweep.csv
docs/Claude/分析数据/0424_model_fidelity_20260513/model_gate_fidelity_threshold_summary.csv
docs/Claude/分析数据/0424_model_fidelity_20260513/plots/*_model_vs_rgb_overlay.png
docs/Claude/分析数据/0424_model_fidelity_20260513/plots/scatter_p95.png
```

结果：

```text
bags=31
pair_dt_p95_ms_over_bags=24.3032
mean_gamma_model_pct=-39.6625
worst_U_max_mm=4.07356

A_rank_peak=0.500
A_rank_p95=0.500
A_rank_rms=0.800  # ties=44, non-tie pair 少

tau=1.0 mm recall=0.164942
tau=1.5 mm recall=0.0377219
tau=2.0 mm recall=0.0145867
tau=4.0 mm recall=0
```

结论：

```text
0424 旧 MPC 数据不能证明 OSCRS 效果，但能作为模型保真度证据。
按 RGB 真值看，/slosh/height 整体偏低估，p95 排序 A_rank=0.5，
且对高液面片段的 threshold recall 很低。
```

## 新增: 0424 当前模型 vs Ferrari 变体保真度对比

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/red_group_0424_ferrari_fidelity.py
```

参考文档：

```text
docs/重要文档/论文参考总结/Scout当前晃液模型与Ferrari2026模型差异总结.md
```

功能：

```text
1. 复用 .tmp_red_group_compare_0424 的 RGB visual CSV 与 bag；
2. 从 bag 读取 /slosh/ax_est、/slosh/ay_est、/odom、/slosh/height、/mpc_status；
3. 以 RGB |h_smooth_corr| 为真值；
4. 对 current observer 与 Ferrari 变体离线 replay；
5. 输出 model-vs-RGB scatter、p95 A_rank bar、单包保真度和 pairwise A_rank。
```

对比变体：

```text
M0_recorded_slosh_height:
  bag 中已录 /slosh/height。

M0b_replay_observer:
  observer height_coeff + 当前 zeta + parabola。

M1_ferrari_height_coeff:
  Ferrari closed-form height_coeff + 当前 zeta + parabola。

M2_ferrari_no_parabola:
  Ferrari closed-form height_coeff + 当前 zeta，不加 parabola。

M3_ferrari_full:
  Ferrari closed-form height_coeff + Ferrari physics damping + parabola。
```

运行：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/analysis/red_group_0424_ferrari_fidelity.py \
  --root .tmp_red_group_compare_0424 \
  --out-dir docs/Claude/分析数据/0424_ferrari_fidelity_20260513
```

输出：

```text
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/FERRARI_FIDELITY_REPORT.md
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/ferrari_variant_fidelity_summary.csv
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/ferrari_variant_selection_pairs.csv
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/ferrari_variant_A_rank_summary.csv
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/plots/scatter_p95_by_variant.png
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/plots/scatter_peak_by_variant.png
docs/Claude/分析数据/0424_ferrari_fidelity_20260513/plots/A_rank_p95_by_variant.png
```

参数：

```text
omega_n=31.2443
height_coeff_observer=1.8187
height_coeff_ferrari=1.54102
zeta_current=0.05
zeta_ferrari=0.116962
```

p95 结果：

```text
M0_recorded_slosh_height: A_rank=0.5000, mean_gamma=-39.6625%, mean_U_p95=0.6652 mm
M0b_replay_observer:      A_rank=0.5758, mean_gamma=-22.3243%, mean_U_p95=0.5869 mm
M1_ferrari_height_coeff:  A_rank=0.5806, mean_gamma=-44.3495%, mean_U_p95=0.7762 mm
M2_ferrari_no_parabola:   A_rank=0.5806, mean_gamma=-44.4582%, mean_U_p95=0.7782 mm
M3_ferrari_full:          A_rank=0.4643, mean_gamma=-63.6315%, mean_U_p95=0.8571 mm
```

结论：

```text
1. 只替换 Ferrari height_coeff 对 p95 排序只有小幅提升，仍未达到 0.70 门槛；
2. Ferrari height_coeff 更小，导致 0424 上低估 RGB 真值更严重；
3. 关闭 parabola 几乎不改变结果，说明该项不是这批数据的主差异；
4. Ferrari full 在 0424 上 p95 A_rank=0.4643，不能改善排序；
5. 当前不能把 Ferrari closed-form 直接切为在线默认模型，只能继续作为 ablation/oracle。
```

## 新增: 20260513_model_truth 实物单动作模型/RGB 保真度分析

数据：

```text
/data/a/slosh_bags/real/20260513_model_truth
```

说明：

```text
该批数据包含 static、straight_ax_low/mid/high、turn_left/right。
没有 RAW / GeoRef / OSCRS 实物策略对比，因此只能分析模型保真度和动作激励响应，
不能用于证明 OSCRS 策略有效性。
```

视觉提取：

```text
使用 red_liquid_3rulers.yaml 和 red_liquid_infer_from_bag.py。
每包 zero-correction-frames=30，smooth-frames=5。
6 个 bag 均完成红液视觉提取，有效帧 100%。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analysis/model_truth_20260513_fidelity.py
```

运行：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/analysis/model_truth_20260513_fidelity.py \
  --bag-dir /data/a/slosh_bags/real/20260513_model_truth \
  --visual-dir docs/Claude/分析数据/20260513_model_truth_analysis/visual_debug \
  --out-dir docs/Claude/分析数据/20260513_model_truth_analysis
```

输出：

```text
docs/Claude/分析数据/20260513_model_truth_analysis/MODEL_TRUTH_FIDELITY_REPORT.md
docs/Claude/分析数据/20260513_model_truth_analysis/model_truth_fidelity_summary.csv
docs/Claude/分析数据/20260513_model_truth_analysis/model_truth_rank_pairs.csv
docs/Claude/分析数据/20260513_model_truth_analysis/model_gate_fidelity_threshold_sweep.csv
docs/Claude/分析数据/20260513_model_truth_analysis/plots/model_vs_rgb_p95_summary.png
docs/Claude/分析数据/20260513_model_truth_analysis/plots/*_model_vs_rgb_overlay.png
```

关键结果：

```text
bags=6
pair_dt_p95_ms_over_bags=24.3441
mean_gamma_model_pct=-282.487
worst_U_max_mm=6.96542
weak_A_rank_p95=0.333333
```

单包 p95：

```text
static_01:            visual=0.118 mm, model≈0
straight_ax_low_01:   visual=6.729 mm, model=1.835 mm
straight_ax_mid_01:   visual=3.277 mm, model=3.974 mm
straight_ax_high_01:  visual=3.556 mm, model=4.911 mm
turn_left_mid_01:     visual=2.151 mm, model=2.401 mm
turn_right_mid_01:    visual=5.778 mm, model=2.391 mm
```

threshold sweep 全包 recall：

```text
tau=0.5 mm recall=0.437877
tau=1.0 mm recall=0.185286
tau=1.5 mm recall=0.158147
tau=2.0 mm recall=0.131749
tau=3.0 mm recall=0.0950413
tau=4.0 mm recall=0.0136054
tau=6.0 mm recall=0
```

结论：

```text
1. 时间配对质量可接受；
2. static 视觉噪声基线约 p95=0.118 mm；
3. /slosh/height 在 straight_ax_low 和 turn_right 上明显低估 RGB 真值；
4. straight low/mid/high 的视觉排序并不随 ax 强度单调，可能受初始残振/操作时序影响；
5. 该批数据进一步说明当前模型不能单独支撑 hard gate 或候选排序；
6. 后续需要补录 RAW / GeoRef / OSCRS 策略对比，且每包开始前需要更严格静止归零。
```

## 调整: 20260513_model_truth 红液视觉上沿参数复核

问题：

```text
用户指出当前 HSV 识别受反光影响，debug 图中确实存在管壁高光、透明液体区域和红色透色边缘抬高上沿的问题。
```

试验：

```text
1. sat_min=110 + top_boundary_quantile=0.5:
   过严，静止段和低液面主体会被漏检，不采用。

2. 保持默认 HSV，改 top_boundary_quantile=0.5，morph_kernel=7:
   静止噪声显著下降，同时保留动态主体液面。
```

新输出：

```text
docs/Claude/分析数据/20260513_model_truth_analysis_q50/
docs/Claude/分析数据/20260513_model_truth_analysis_q50/HSV_TUNING_NOTE.md
docs/Claude/分析数据/20260513_model_truth_analysis_q50/MODEL_TRUTH_FIDELITY_REPORT.md
```

q50 参数：

```text
sat_min=70
val_min=35
morph_kernel=7
top_boundary_quantile=0.5
```

q50 结果：

```text
static_01:            visual_p95=0.025 mm
straight_ax_low_01:   visual_p95=5.777 mm, model_p95=1.835 mm
straight_ax_mid_01:   visual_p95=3.661 mm, model_p95=3.974 mm
straight_ax_high_01:  visual_p95=3.333 mm, model_p95=4.911 mm
turn_left_mid_01:     visual_p95=2.194 mm, model_p95=2.401 mm
turn_right_mid_01:    visual_p95=4.944 mm, model_p95=2.391 mm
```

判断：

```text
1. 原 q0.2 口径确实偏容易被上方反光/透色边缘影响；
2. q50 口径更稳，static p95 从 0.118 mm 降到 0.025 mm；
3. 修正后 straight_ax_low 和 turn_right 仍明显高于 /slosh/height，模型低估结论不消失；
4. 后续红液验证建议默认使用 top_boundary_quantile=0.5，并保留 debug 图人工抽查。
```

## Slosh-priority MPC 有效性验证方案

新增文档：

```text
docs/重要文档/20260513_SloshPriorityMPC有效性验证方案.md
```

内容：

```text
1. 将“晃动抑制 MPC”定义为 slosh-aware objective rebalancing，而不是单纯增大 Q_slosh；
2. 设计 A/B/C/D/E 五组实物消融：
   A BASE
   B SOFT_SLOSH_ONLY
   C SMOOTH_SPEED_RELAXED
   D SLOSH_PRIORITY_MPC
   E SLOSH_DOMINANT
3. 明确 C 组用于剥离速度松弛和控制平滑本身的贡献；
4. 明确 D 组才是主结果，不能表述为单独 slosh cost 的效果；
5. 指出实物 slosh_experiment.launch 需暴露 Q_v/R_da/R_domega/terminal_factor_slosh_* 等完整参数；
6. 规定 RGB 视觉液面为主指标，/slosh/height 仅作模型内部诊断；
7. 增加 cost contribution check，避免 Q_slosh_eta_dot 过小无效或过大主导奇怪行为。
```

修改前检查：

```text
用户指定实物入口为：
roslaunch scout_local_planner slosh_experiment.launch

检查 src/scout_apps/control/scout_local_planner/launch/slosh_experiment.launch 后确认：
1. 当前实物入口可直接覆盖 Q_slosh / Q_slosh_eta_dot / global_path_topic / energy_profile_enable / risk_scheduler_enable 等；
2. 当前实物入口不能覆盖 Q_v / Q_contour / Q_etheta / R_a / R_da / R_domega / terminal_factor_slosh_*；
3. 文档已补充 A(BASE) 和 B(SOFT_SLOSH_ONLY) 当前可直接运行命令；
4. C/D/E 仍需新增 slosh_priority_mpc_experiment.launch 或等价参数覆盖入口后再执行。
```

实现：

```text
文件：
src/scout_apps/control/scout_local_planner/launch/slosh_experiment.launch

变更：
1. 新增实物 launch 参数：
   mpc_Q_lag
   mpc_Q_contour
   mpc_Q_etheta
   mpc_Q_v
   mpc_R_a
   mpc_R_omega
   mpc_R_da
   mpc_R_domega
   terminal_factor_slosh_eta
   terminal_factor_slosh_eta_dot
2. 在 mpc_params.yaml 加载后、节点初始化前写入对应 ROS 参数；
3. 默认值按当前实物 mpc_params.yaml 设置，裸启动行为不变；
4. docs/重要文档/20260513_SloshPriorityMPC有效性验证方案.md 已更新为当前 slosh_experiment.launch 直接运行 A/B/C/D/E 五组命令。
```

实现后状态：

```text
roslaunch scout_local_planner slosh_experiment.launch

现在可直接运行 A/B/C/D/E 五组 slosh-priority MPC 有效性验证；
无需新增 slosh_priority_mpc_experiment.launch。
```
