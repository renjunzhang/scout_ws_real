# 2026-06-09

## SPMPC 模块化避障后续改进方案

- 新增 `docs/实物实验注意事项/后续改进/20260609_SPMPC局部规划器模块化避障改进方案.md`，明确 SPMPC 后续应定位为 slosh-aware integrated local MPC planner：reference/global path 作为 guidance，而不是固定路径 executor。
- 方案建议借鉴 `src/mpc_planner` 的模块生命周期与能力声明思想，但不直接复刻完整 obstacle-aware / topology-driven MPC 框架；优先做 backend feature guard、CorridorModule、StaticObstacleModule，再考虑 dynamic obstacle、homotopy 和 navigation plugin。
- 记录 acados 下的 superset OCP 思路：codegen 固定包含可选 corridor/obstacle/slack slot，运行时通过 enable flag、权重和参数填充开启/关闭模块，避免运行时动态增删符号结构。

## SPMPC 主线后端运行时口径落地

- 将 `spmpc_local_planner` 的默认 `solver_backend` 从 `primitive` 改为 `continuous_mpcc_acados`，覆盖 `SolverParams` 内置默认值、`config/planner/common.yaml` 以及 `spmpc_experiment.launch` / `spmpc_point_to_point.launch` / `spmpc_fixed_path.launch` 三个入口默认值；未改 launch arg 名称。
- 明确 `primitive` 仅是 fallback/debug rollout sampling + cost ranking，不是 MPCC/mainline；`continuous_mpcc_direct_omega_legacy` 仅作为 RouteB diagnostic/legacy continuous MPCC。
- 新增 backend policy guard：未知 backend 直接 `ROS_FATAL`，不再静默 fallback 到 `primitive`；非主线 backend 禁止 slosh-enabled variant、obstacle、homotopy 和 hard corridor 组合；主线 `continuous_mpcc_acados` 对尚未接入的 obstacle/homotopy/hard corridor 也 fail fast。
- 启动时强制打印 backend、role、variant、mode 和关键 feature flags，便于确认运行时实际口径；`/spmpc/solver_backend` diagnostics 保持发布。
- 将 point-to-point 默认 `obstacle_enable` / `homotopy_enable` 暂时关闭，避免默认主线切到 continuous MPCC 后出现“参数开启但 OCP 未消费”的静默误用。
- 验证：`rg` 确认 SPMPC 配置/launch 中无 `primitive` 默认后端残留；`catkin_make --pkg spmpc_local_planner` 通过；默认 fixed-path / point-to-point launch 均打印 `backend=continuous_mpcc_acados role=SPMPC mainline continuous MPCC`；未知 backend、`primitive+B_ours`、`primitive+obstacle_enable=true`、`continuous_mpcc_direct_omega_legacy+B_ours` 均按预期 `ROS_FATAL`；`continuous_mpcc_direct_omega_legacy+B0` 可作为 diagnostic 启动。
- 项目红线验证：`catkin_make --pkg scout_local_planner` 仍失败，原因是 build 目录下没有 `scout_apps/control/scout_local_planner/all` 目标规则（`没有规则可制作目标`），已按实际输出记录，未改动该包；因此未继续运行 `run_s_curve_smoke_test.sh`。
- 补充修正：`corridor_enable` 在 continuous / RouteB 后端下同样纳入 guard，避免软走廊开关开启但 OCP 中没有 `J_corridor` 的静默空操作；同时将 `SolverParams`、fixed-path、point-to-point 默认 `corridor_enable` 置为 false。primitive 仍可在显式 debug/fallback B0 场景使用其已有 rollout corridor cost。
- 补充验证：`catkin_make --pkg spmpc_local_planner` 通过；默认 fixed-path / point-to-point launch 均打印 `corridor=false`；手动设置 `continuous_mpcc_acados + corridor_enable=true` 与 `continuous_mpcc_direct_omega_legacy + corridor_enable=true` 均按预期 `ROS_FATAL`。

## RouteB B0 成功到点与入弯高晃动基线

- 新增 `docs/实物实验注意事项/实物实验前的仿真/20260609_RouteB_B0成功到点与入弯高晃动基线记录.md`，固化 `/data/a/scout_spmpc_experiments/raw/2026-06-09/00_routeb_baseline/spmpc_routeb/20260609_021546_spmpc_B0_P2_routeb_B0_alpha8_run1` 的分析口径：该 run 是 `continuous_mpcc_direct_omega_legacy + B0` 成功到点基线，不是 alpha-state / `alpha_max=8.0` 结论。
- 记录关键结果：`progress_s 0.0002 -> 0.9783`，`GOAL_REACHED=802`，`solver_time_ms mean/max=1.76/6.68`，`ACADOS_DIRECT_OMEGA_SOLVE_FAILED_4=15`；t≈35s 后停车解释为 terminal reached，不是弯处 creep。
- 记录 B0 入弯高晃动基线：`/spmpc/slosh_height` p50/p95/p99/max = `0.096/2.882/6.007/10.284 mm`，峰值集中在起步高曲率段，可作为后续 `B_slosh` / `B_ours` 对照。
- 初步检查 slosh artifact：bag 未记录 `/slosh/eta` / `/slosh/eta_dot`，`/spmpc/slosh_horizon_summary` 因 B0 非 slosh OCP 全 0；`/spmpc/slosh_height` 与 `/spmpc/debug/slosh_state` 的 `eta_norm` 拟合 `R2=0.99999`，单帧近零对应 modal state 过零，更像瞬时 modal proxy 而非 topic 故障，但不能直接当 RGB true slosh 指标。

## alpha-state B0 主线 fresh-sim 复现

- 新增 `docs/实物实验注意事项/实物实验前的仿真/20260609_alpha_state_B0主线复现记录.md`，记录 fresh sim 下 `continuous_mpcc_acados + B0`、默认 `alpha_max=1.2`、`corridor/obstacle/homotopy=false` 的主线复现。
- 产物路径：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1`；meta git hash 为 `310ef0d`，bag 为同目录下 `20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1.bag`。
- 结果：未到点，`GOAL_REACHED=0`，`progress_s 0.0 -> 0.1575` 后冻结；机器人实际 odom 位移约 `4.70 m`、累计距离约 `10.07 m`，偏离 reference 后 progress 不再增加，不是 RouteB 的成功到点。
- 关键指标：`B0_ACADOS_OK=1699`，`ACADOS_SOLVE_FAILED_4=57`，`solver_time_ms mean/max=5.37/11.21`；`warm_start_status=OK` 全程，说明不是 warm-start invalid，而更像 alpha-state OCP / objective / state-control layout / closed-loop command 交互问题。
- 按 fresh-sim SOP：本次只跑 1 case，运行后停止仿真并等待 30s；后续仿真实验由用户手动执行，Claude 只给命令与离线分析。

## 同一路径 replay 与 slosh monitor 补查

- 用户手动补跑 alpha-state replay：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro_replay/20260609_134237_spmpc_B0_P2_alpha_B0_replay_same_path_run1/20260609_134237_spmpc_B0_P2_alpha_B0_replay_same_path_run1.bag`。同一 generated path 下 `continuous_mpcc_acados + B0` 仍未到点，`progress_s 0.00493 -> 0.11672`，`B0_ACADOS_OK=1416`，`ACADOS_SOLVE_FAILED_4=80`，terminal 全程 `TRACKING`；t=5~30s 平均 `cmd_v=0.021 m/s`，表现为更早的 progress freeze。
- 用户手动补跑 RouteB replay：`/data/a/scout_spmpc_experiments/raw/2026-06-09/00_routeb_baseline/spmpc_routeb_replay/20260609_134622_spmpc_B0_P2_routeb_rate8_on_alpha_path_run1/20260609_134622_spmpc_B0_P2_routeb_rate8_on_alpha_path_run1.bag`。同一路径下 `continuous_mpcc_direct_omega_legacy + B0` 成功到点，`progress_s 0.00165 -> 0.98043`，first `GOAL_REACHED≈34.17s`，`GOAL_REACHED=838`；进一步排除路径文件/仿真环境本身不可走。
- 两条 replay 对比确认 alpha-state 的首帧/前段角速度能力是关键差异：alpha-state 起步 `cmd_omega≈0.041 rad/s`，RouteB 起步 `cmd_omega≈1.056 rad/s`。结合代码，嫌疑集中在 alpha-state 的 `omega` 状态 + `alpha=omega_dot` 硬约束、从实测 omega 单步积分下发命令，以及 `u_prev_[1]` 在 warm-start 中可能把 alpha 当 previous_omega 使用的语义错位。
- 用户手动补跑 slosh monitor：`/data/a/scout_spmpc_experiments/raw/2026-06-09/00_routeb_baseline/spmpc_routeb_slosh_monitor/20260609_134900_spmpc_B0_P2_routeb_B0_slosh_monitor_check_run1/20260609_134900_spmpc_B0_P2_routeb_B0_slosh_monitor_check_run1.bag`。`/slosh/state` 与 `/slosh/debug` 已记录；`/spmpc/slosh_height` p50/p95/p99/max=`0.088/4.601/7.866/13.537 mm`，`/slosh/height` 转 mm 后 p50/p95/p99/max=`0.064/2.376/4.466/13.984 mm`。
- slosh artifact 结论更新：`/spmpc/slosh_height` 对 `/spmpc/debug/slosh_state eta_norm` 拟合 `R2=0.999994`；`/slosh/height` 对 `/slosh/state eta_norm` 拟合 `R2=0.998193`。两者各自都是瞬时 modal proxy；单帧近零对应 eta_norm 过零，不是 height topic 单独故障。两套 observer 逐帧不同步，论文/对比应使用同一窗口 p95/max/envelope 并与 RGB true height 单独对齐。
- 用户手动补跑 alpha-state `alpha_max=8.0` replay：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro_alpha8/20260609_154815_spmpc_B0_P2_alpha_B0_replay_same_path_alpha8_run1/20260609_154815_spmpc_B0_P2_alpha_B0_replay_same_path_alpha8_run1.bag`。planner log 确认 `robot/alpha_max=8.0` 且 backend 为 `continuous_mpcc_acados`；仍未到点，`progress_s 0.00426 -> 0.11467`，`B0_ACADOS_OK=1691`，`ACADOS_SOLVE_FAILED_4=66`，terminal 全程 `TRACKING`，t=5~30s 平均 `cmd_v=0.026 m/s`。结论：单纯提高 `alpha_max` 不能恢复 alpha-state B0，同一路径 gate 未通过；后续不应进入 fresh-sim 连续验证或实物试验，应先修/定位 alpha-state 代码侧问题。

## 实验数据目录整理与 alpha-state warm-start 修正

- 将 `/data/a` 根目录下 21 个 `spmpc*` 实验目录移动到 `/data/a/scout_spmpc_experiments/raw/` 并按日期/主题分类；同步重建 `/data/a/scout_spmpc_experiments/` 下的分类软链接与 `MANIFEST.md`，使 `/data/a` 根目录不再散落 `spmpc*` 目录。后续新仿真建议统一使用该索引下的 `OUT_ROOT`。
- 代码侧确认 alpha-state warm-start 真实问题不是单纯 `alpha_max` 上限：`DiffDriveFlatnessWarmStart` 仍按旧 direct-omega 语义只填 `control.omega`，但 `continuous_mpcc_acados` 注入 acados 时消费的是 `state.omega` 与 `control.alpha`，导致曲率 yaw-rate seed 没有进入 alpha-state OCP 初值。
- 修正 `diff_drive_flatness_warm_start.cpp`：flatness warm start 现在从实测 `robot.omega` 开始，按曲率目标生成 rate-bounded `state.omega` 序列和 `control.alpha` 序列；slosh rollout、finite check、`max_omega` 与横向加速度诊断改用 alpha-state 的 `state.omega` 口径，`control.omega` 仅保留为 legacy/debug mirror。
- 修正 `continuous_mpcc_solver_acados.cpp`：把 `params.alpha_max` 传入 `WarmStartBounds::omega_rate_max`，并避免把 alpha-state 的 `u_prev[1]` 继续当 `previous_omega` 传给 warm-start 输入；相关 warm-start 头文件注释同步澄清 `omega`/`alpha` 字段语义。
- 更新 `test_diff_drive_flatness_warm_start.cpp`：增加 alpha-state 轨迹一致性断言，检查 `states[k+1].omega ≈ states[k].omega + controls[k].alpha * dt` 且 `alpha` obeys `omega_rate_max`。
- 验证：`catkin_make --pkg spmpc_local_planner` 通过；`catkin_make run_tests_spmpc_local_planner_gtest_test_diff_drive_flatness_warm_start` 通过 4/4。项目红线 `catkin_make --pkg scout_local_planner` 仍失败，原因仍是 build 目录无 `scout_apps/control/scout_local_planner/all` 目标规则（`没有规则可制作目标`）；因此未继续运行 `run_s_curve_smoke_test.sh`。
- 用户手动补跑 warm-start 修正后的 alpha-state 同路径 replay：`/data/a/scout_spmpc_experiments/2026-06-09/01_after_warmstart_fix/20260609_165100_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_run1/20260609_165100_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_run1.bag`。backend=`continuous_mpcc_acados`、variant=`B0`、`alpha_max_override=8.0`；仍未到点，`progress_s 0.00516 -> 0.19246`，`GOAL_REACHED=0`，`B0_ACADOS_OK=1653`，`ACADOS_SOLVE_FAILED_4=104`，terminal 全程 `TRACKING`，t=5~30s 平均 `cmd_v=0.024 m/s`。相比 pre-fix alpha8 的 `0.11467` 有改善但远未通过 `>0.95` gate，且出现明显 path departure/progress freeze（cross-track sampled max 约 `5.21 m`）；因此仍不能进入 fresh-sim 连续验证或实物试验。

## alpha-state 主线实物前 No-Go 与 catkin 白名单问题记录

- 新增 `docs/Claude/遇到的问题与解决方案/2026-06-09_alpha-state主线实物前NoGo与catkin白名单问题.md`，整理当前正式实物试验 No-Go 原因：`continuous_mpcc_acados + B0` 修正后尚未通过同路径 replay / fresh-sim gate，RouteB direct-omega 成功不能替代 alpha-state 主线结论。
- 澄清 `catkin_make --pkg scout_local_planner` 当前失败不是源码已被本轮改坏，而是 `build/CMakeCache.txt` 中 `CATKIN_WHITELIST_PACKAGES=spmpc_local_planner` 导致 build graph 没有 `scout_local_planner/all` 目标；建议用 `catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES=scout_local_planner --pkg scout_local_planner` 重新验证红线。
- 文档同步列出实物前剩余 gate：当前代码同路径 replay、B0 fresh-sim、B_slosh/B_ours fresh-sim、slosh/RGB/rosbag 链路确认、无液体低速 dry-run、带液体正式实物。

## Subagent 仿真实验调用准则

- 新增 `docs/实物实验注意事项/Subagent.md`，固化后续调用子 agent 执行仿真实验的硬性准则：不动仿真环境、不删除代码、不覆盖数据；实验产物集中归档到 `/data/a/scout_spmpc_experiments/`；fresh-sim 启动后等待 30s、关闭后等待 30s，planner 启动后超过 70s 未到点判失败。
- 补充实验卡模板、仿真前检查清单、录包 topic 建议、成功/失败判据、安全异常处理、分析报告字段、对比公平性要求和子 agent 最终回复格式；明确 2026-06-09 当前 alpha-state 主线仍 NO-GO，RouteB 成功不能放行主线。
- 补充 fixed-path 生成口径与子 agent 启动口径：`stable_goal` 默认以小车当前位姿为起点、实验卡固定终点生成 fixed path JSON 并归档；子 agent 默认启动命令记录为 `claude-gpt-pro`。
- 补充当前仿真环境启动命令：source ROS/noetic 与工作区 setup 后，以 `SIM_ENV=open USE_RVIZ=true SPAWN_X=3.30 SPAWN_Y=0.15 SPAWN_Z=0.1 SPAWN_YAW=-3.08 rosrun scout_local_planner launch_sim_nav_stack.sh` 启动，并继续要求 30s/70s/30s 时间纪律。
- 补充子 agent 运行脚本前需先读 `src/scout_apps/control/spmpc_local_planner/scripts/README.md` 与 `src/scout_apps/control/spmpc_experiments/scripts/README.md`，避免误用实验脚本。

## 子 agent Gate 1 fresh-sim replay 复跑结果

- 子 agent 按 `Subagent.md` 安全规则执行 1 个 `continuous_mpcc_acados + B0` same-path replay case：使用用户指定 `launch_sim_nav_stack.sh` 仿真启动命令，启动后等待 30s，planner 跟踪 70s 超时判失败，结束后关闭本次仿真并等待 30s。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-09/02_gate1_replay_subagent/20260609_1case/20260609_231045_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_rearchive_run1`；路径文件使用归档后的 alpha-state same-path generated JSON。
- 结果仍为 FAIL：`progress_s 0.0028602513 -> 0.0028689418`，`GOAL_REACHED=0`，terminal 全程 `TRACKING`，`B0_ACADOS_OK=2043`，`ACADOS_SOLVE_FAILED_4=14`，`WAITING_FOR_ODOM=1`，`solver_time_ms mean/max=5.576/11.206`。
- 本次失败形态比 warm-start fix 后 `progress_s=0.19246` 的跑偏冻结更早：70s 内几乎无有效推进，`cmd_v` 在 t=5~30s 均值仅 `0.000794 m/s`，max `0.0690 m/s`；sampled path departure max/end 为 `0.1676/0.1512 m`。
- 已同步更新 `docs/Claude/遇到的问题与解决方案/2026-06-09_alpha-state主线实物前NoGo与catkin白名单问题.md`，正式实物继续 NO-GO。
