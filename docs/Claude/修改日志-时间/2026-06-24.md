# 2026-06-24 修改/运行日志

## 官方 LT-DWA 接入 Scout 隔离仿真环境

### 安全边界

```text
主仓库代码：未修改 /home/a/scout_ws/src
隔离仿真环境：未修改 /data/a/scout_sim_replacement 的 world/map/URDF/model/SOP
官方源码：未修改 /data/a/lt_dwa_official_repro_ws/src/LT_DWA
控制输出：shadow gate 未发布 /cmd_vel；用户明确授权后执行一次 bounded 60s /cmd_vel benchmark，结束后 zero stop 且 /cmd_vel Publishers=None
仿真进程：未停止/重启已有 Gazebo/RViz/Cartographer；只停止本次启动的 wrapper/path/local-map 子进程
```

### 完成项

```text
[PASS] #42 固化 map-frame odom bridge integration
[PASS] #46 隔离 official Robot TF side effect：worker argv 默认 remap /tf 和 /tf_static 到 sandbox topic
[PASS] #49 编写 Scout SOP 接入 launch overlay：默认 shadow-only，enable_actuated_output=false
[PASS] #50 安全 shadow integration gate：基于已运行的隔离 Scout SOP 环境验证 wrapper 接入，不做危险重启
[PASS] #51 准备 benchmark-safe actuated gate：配置和 60s monitor 已准备，默认 inert，不自动发布 /cmd_vel
[FAIL RESULT] #52 用户授权后执行 60s visible benchmark：60s 未到终点，min_distance=0.507 m，final_distance=0.658 m
```

### 关键改动

```text
/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/src/map_frame_odom_adapter_node.cpp
  新增 wrapper 侧 map-frame odom adapter，发布 /baseline/official_lt_dwa/odom_map，保持 Scout 仿真环境只读。

/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/src/worker_main.cpp
/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/src/scout_bridge_node.cpp
  worker official-core 模式改为从 argc/argv 初始化 ROS，并由 parent bridge 追加 /tf、/tf_static sandbox remap。

/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/launch/scout_sop_shadow_integration.launch
  新增 Scout SOP shadow 接入 overlay，默认不发布 /cmd_vel。

/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/launch/scout_sop_cmd_vel_benchmark.launch
/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper/scripts/lt_dwa_60s_goal_benchmark_monitor.py
  准备 benchmark-safe gate；仍需用户明确批准后才可发布 /cmd_vel。
```

### 验证

```text
catkin_make --directory /data/a/lt_dwa_wrapper_ws -DCMAKE_BUILD_TYPE=RelWithDebInfo -DLT_DWA_WRAPPER_ENABLE_OFFICIAL_CORE=ON  # PASS
catkin_make --directory /data/a/lt_dwa_wrapper_ws run_tests && catkin_test_results /data/a/lt_dwa_wrapper_ws/build/test_results
Summary: 74 tests, 0 errors, 0 failures, 0 skipped
```

Shadow integration gate（未重启已有仿真，只接入 wrapper overlay）：

```text
ROS_MASTER_URI=http://localhost:11328
GAZEBO_MASTER_URI=http://localhost:11362
MAP_FILE=/data/a/scout_sim_replacement/maps/proxy_world_manual_saved_20260611_154348.pbstream

/baseline/official_lt_dwa_rt06/status: OK
worker_result: LT_DWA_WORKER_RESULT status=OK reason=official_core_ok command_v=0.00972246 command_w=0.198013 core_return=0
/baseline/official_lt_dwa_rt06/odom_map.header.frame_id: map
command_publish_rate_hz=30
command_stale_timeout_sec=0.6
worker_latency_ms≈390 ms
enable_actuated_output=false
effective_cmd_vel=false
effective_benchmark_raw=false
/cmd_vel Publishers: None
/benchmark/cmd_vel_raw: Unknown topic
```

### 用户授权 /cmd_vel 后的 60s visible benchmark

```text
启动前修正：scout_sop_cmd_vel_benchmark.launch 增加 start_local_map_service 透传；本次用绝对路径启动 local_map_generation_node，避免修改官方/仿真环境。

START_POSE x=0.095775 y=0.016032 goal_x=5.000000 goal_y=-0.000000 distance=4.904252
PROGRESS t=5.0 x=2.829205 y=0.659287 distance=2.268702 min_distance=2.268702
PROGRESS t=15.0 x=3.822791 y=0.480408 distance=1.271461 min_distance=0.812466
PROGRESS t=50.0 x=4.118550 y=0.922426 distance=1.275863 min_distance=0.507000
PROGRESS t=55.0 x=4.404119 y=-0.211868 distance=0.632426 min_distance=0.507000
RESULT FAIL reason=timeout_60s_not_reached elapsed=60.000 reach_time=NA start_distance=4.904252 final_distance=0.657899 min_distance=0.507000 final_x=4.611976 final_y=-0.531289 goal_x=5.000000 goal_y=-0.000000

Final diagnostics: status=OK reason=official_core_ok command_publish_rate_hz=30 command_stale_timeout_sec=0.6 enable_actuated_output=true publish_cmd_vel=true command_fresh=true worker_latency_ms=315.609 command_v=0.513461 command_w=0.45924
Post-run safety: /cmd_vel Publishers=None; /benchmark/cmd_vel_raw Unknown topic; zero stop attempted after stopping wrapper bridge.
```

### 注意

```text
#52 已执行，按 60s 未到终点判 FAIL。虽然比前一次 benchmark 明显更接近终点（min_distance 0.507 m），仍未达到 0.30 m tolerance。
当前 one-shot worker 实测 latency 约 315-420 ms；shadow/benchmark overlay 使用 0.6s stale timeout 才能保持 fresh status。若要进一步提高 actuated 稳定性，应继续优化 worker latency 或改 persistent worker。
```

## 官方 LT-DWA wrapper 主仓库迁移试运行

### 安全边界

```text
隔离仿真环境：未修改 /data/a/scout_sim_replacement
官方复现源码：未修改 /data/a/lt_dwa_official_repro_ws/src/LT_DWA
Scout SOP：未修改 docs/实物实验注意事项/仿真环境/20260611_隔离Scout仿真环境使用SOP.md
命令输出：本轮迁移/构建/launch parse 未发布 /cmd_vel
实物默认：publish_cmd_vel=false，enable_actuated_output=false
```

### 迁移内容

```text
/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_wrapper

/data/a/lt_dwa_wrapper_ws/runtime/local_planner_runtime
  -> /home/a/scout_ws/tools/lt_dwa/local_planner_runtime

/home/a/scout_ws/third_party/LT_DWA/obstacle_msgs
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_vendor_deps/obstacle_msgs

/home/a/scout_ws/third_party/LT_DWA/local_map_generation
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_vendor_deps/local_map_generation
```

### 关键修正

```text
lt_dwa_official_wrapper/CMakeLists.txt
  默认 OFFICIAL_LT_DWA_ROOT 改为主仓库 third_party/LT_DWA
  移除 /data/a/lt_dwa_official_repro_ws/devel/include 依赖
  LT_DWA_WRAPPER_ENABLE_OFFICIAL_CORE=ON 时只编译 official local_planner core 源文件和必要 deps

lt_dwa_official_wrapper/package.xml
  增加 local_map_generation 依赖

lt_dwa_official_wrapper/include/.../scout_bridge.hpp
lt_dwa_official_wrapper/launch/scout_sop_*.launch
  real-machine 默认路径改为 /home/geist/scout_ws，可用 SCOUT_WS_ROOT 覆盖
  runtime_request_dir 默认 /tmp
  launch 默认仍为 shadow-only

lt_dwa_official_vendor_deps/local_map_generation/{CMakeLists.txt,package.xml}
  补齐 message_generation/message_runtime
  删除不存在的 local_map_generation library 导出
  为 local_map_generation_node 增加生成目标依赖
```

### 构建/测试

```text
命令：
catkin_make \
  -DCATKIN_WHITELIST_PACKAGES="obstacle_msgs;local_map_generation;lt_dwa_official_wrapper" \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DLT_DWA_WRAPPER_ENABLE_OFFICIAL_CORE=ON

结果：PASS（仅 official LT-DWA 上游代码 warning，无 build failure）

命令：
catkin_make run_tests \
  -DCATKIN_WHITELIST_PACKAGES="obstacle_msgs;local_map_generation;lt_dwa_official_wrapper" \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DLT_DWA_WRAPPER_ENABLE_OFFICIAL_CORE=ON
catkin_test_results /home/a/scout_ws/build/test_results

结果：Summary: 126 tests, 0 errors, 0 failures, 0 skipped
```

### package/launch parse

```text
rospack find lt_dwa_official_wrapper
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_wrapper
rospack find local_map_generation
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_vendor_deps/local_map_generation
rospack find obstacle_msgs
  -> /home/a/scout_ws/src/scout_apps/control/lt_dwa_official_vendor_deps/obstacle_msgs

roslaunch --nodes lt_dwa_official_wrapper scout_sop_shadow_integration.launch
  -> /local_map_generation_node
     /lt_dwa_sop_map_frame_odom_adapter
     /lt_dwa_sop_scout_bridge

roslaunch --nodes lt_dwa_official_wrapper scout_sop_cmd_vel_benchmark.launch
  -> /local_map_generation_node
     /lt_dwa_sop_map_frame_odom_adapter
     /lt_dwa_sop_scout_bridge
```

### 实物端迁移结论

```text
不建议把 /data/a/lt_dwa_official_repro_ws/src/LT_DWA 或 /data/a/lt_dwa_wrapper_ws 直接复制到实物机。
当前安全方案是：official LT-DWA 固定在 third_party/LT_DWA（source-only vendor），wrapper/runtime/deps 纳入 scout_ws 主仓库，由实物机 git pull 后按白名单构建。
注意：本次 catkin_make 为迁移检查显式设置了 CATKIN_WHITELIST_PACKAGES；后续普通主线构建前应按需要重置/覆盖白名单。
```
