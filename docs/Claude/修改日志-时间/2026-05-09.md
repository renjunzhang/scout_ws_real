# 2026-05-09 修改日志

## OSCRS 可插拔候选生成层方案再修订：明确两层隔离

修改文档：

```text
docs/Claude/修改方案-时间-简介/2026-05-08_OSCRS可插拔候选生成层设计方案.md
```

本次修订目的：

```text
把“OSCRS 可插拔”从泛泛的 generator-agnostic 说法，
进一步收紧为明确的两层代码职责：

Layer 1: Candidate Generator
  raw path -> CandidateSet

Layer 2: OSCRS Selector
  CandidateSet -> selected reference
```

关键补充：

```text
1. GeoRef 不再作为主方法本体，而是 CandidateGenerator 的默认实例；
2. OSCRS Selector 不应知道 candidate 是 smooth_path、B-spline 还是 elastic band 生成的；
3. 增加 Candidate.metadata，但 selector 第一版不依赖 metadata 做选择；
4. 明确禁止的隐式耦合：
   - selector 根据 "strong" 特判；
   - selector 假设候选顺序 original/mild/medium/mid/strong；
   - selector 读取 candidates/strong/gain 等 GeoRef-specific 参数；
   - fallback_code 依赖 GeoRef 档位名；
5. 增加 CandidateSet 不变量：
   - 至少一个 original；
   - candidate name 唯一；
   - points 至少 3 点；
   - 与 base path 同 frame；
6. 实施步骤改为 Step 0-4：
   - 先锁定当前 smoke 基线；
   - 再抽 Layer 1；
   - 再加 generator_type / passthrough；
   - 最后适配 debug/report/docs。
```

当前仍未改代码。

判断：

```text
下一步如果要实现，应先做低风险重构：
  candidate_generators.py
  Candidate
  CandidateGenerator
  GeoRefSmoothingGenerator
  PassthroughGenerator

不应先做 B-spline / elastic band / ROS 外部候选 topic。
```

## OSCRS 候选生成层抽取 — L2 最小重构落地（替代 2026-05-08 旧方案）

修改文档：

```text
新增：docs/Claude/修改方案-时间-简介/2026-05-09_OSCRS候选生成层抽取方案.md
旧方案：docs/Claude/修改方案-时间-简介/2026-05-08_OSCRS可插拔候选生成层设计方案.md
        （评分 5.5/10，过度设计；本次以 L2 最小方案替代）
```

回顾旧方案问题（用户与 Claude 共同评估）：

```text
1. 时机：实物 baseline 在即，引入 dataclass + ABC + 2 子类 + launch arg + yaml 字段
   = 同时打开 4 个调试面，违反 CLAUDE.md §3 外科手术；
2. PassthroughGenerator 三个用途立不住：fb=1 已验证 original_safe；
   RAW 已有 RAW_TUNED 通路；"未来调试对照" 是推测需求；
3. Candidate.level: int 把 GeoRef-specific 概念漏到通用接口；
4. 旧方案 §6.1 要 candidate_report bag 级 hash 等价 ——
   ROS 重跑有 timing/TF/costmap 时间戳非确定性，目标不可达；
5. 仅 1 个 generator 实例就抽 class 层级 = 过早抽象 (CLAUDE.md §2)。
```

L2 方案（本次落地）改成单函数提取：

```text
新增 1 文件：
  src/scout_apps/control/scout_local_planner/scripts/candidate_generators.py
    generate_georef_candidates(base, candidate_specs, min_segment_length, sanitize_fn)
      -> list[(name, points)]
    设计要点：
      - sanitize_fn 通过参数注入，避免反向 import post_processor (ROS 节点 module);
      - 返回顺序与 candidate_specs 严格一致，OSCRS 同分取最先依赖此性质;
      - 不引入 dataclass / 抽象基类 / 第二个 generator 实例。

修改 1 文件：
  src/scout_apps/control/scout_local_planner/scripts/anti_slosh_path_post_processor.py
    - import 段：去掉 smooth_path（refactor 后本文件不再直接调用），
                 加 from candidate_generators import generate_georef_candidates；
    - path_callback 候选生成段：6 行内联循环 -> 4 行函数调用 + 循环消费。
    - OSCRS rollout / score / hard gate / fallback 全部 0 行改动。

新增 1 测试：
  src/scout_apps/control/scout_local_planner/scripts/test_candidate_generators_equivalence.py
    锁死"代码搬家无 drift"——逐点 float 精确相等。
```

验收（按方案 §6 跑）：

```text
6.1 函数级 numeric exact equality：
    cd src/scout_apps/control/scout_local_planner/scripts
    python3 test_candidate_generators_equivalence.py
    输出：OK   ✓

6.3 静态：
    python3 -m py_compile candidate_generators.py
    python3 -m py_compile anti_slosh_path_post_processor.py
    python3 -m py_compile test_candidate_generators_equivalence.py
    全部 OK   ✓

6.2 仿真重跑序列 sanity：
    待用户在新 commit 上跑 SCENARIO=open_user_goal / open_custom_goal
    CONDITION=GEOREF_OSCRS_ACTIVE，比 selected.name 序列 / fb 直方图。
    属于行为不变性 sanity，不阻塞 merge（容许 ROS 时序差异 ±1 周期）。
```

论文叙事支持的内容：

```text
现在可以写：
  "OSCRS 在方法层定义为 G + S 两层结构。G 当前实例为 GeoRef smoothing。
   实现层 G 与 S 在代码上分离 (candidate_generators.generate_georef_candidates
   与 anti_slosh_path_post_processor.apply_oscrs_score / select_oscrs_candidate)。"

不写、也不暗示：
  × "支持任意 generator 替换"        ← 当前只 1 个实例
  × "已验证多种 generator"            ← 没做
  × "通过 launch arg 切换 generator"  ← 不引入此参数
```

实物时序：本次重构净 +2 行业务代码 + 1 新文件 (~30 行) + 0 新参数 + 0 新 mode，
单 commit revert 即可回退；不阻塞实物 baseline 时间表。

CLAUDE.md 对齐：

```text
§1 编码前先思考     – 旧方案打分 5.5/10 后才决定降级到 L2，未掩盖困惑；
§2 简洁优先         – 仅抽 1 个函数；不做 class/dataclass/Passthrough/launch arg；
§3 外科手术式修改   – 不动 yaml/launch；删掉因 refactor 产生的孤立 import smooth_path；
§4 目标驱动执行     – 验收 §6.1 numeric exact equality 通过 = "代码搬家无 drift" 成立。
```

## L2 重构验收 §6.2 仿真序列 sanity（用户回包结果）

回包集（refactor 后 commit 上跑）：

```text
/data/a/slosh_bags/sim/20260509/
  20260509_open_user_goal_GEOREF_TUNED_runrefactor_georef01_003706.bag
  20260509_open_user_goal_GEOREF_OSCRS_ACTIVE_runrefactor_oscrs01_003906.bag
  20260509_open_goal_c_GEOREF_OSCRS_ACTIVE_runrefactor_oscrs_c01_004055.bag
```

`check_oscrs_takeover.py` 输出：

```text
TUNED user_goal:
  reports=30 active=0 takeover=0 fallback=0
  fb={-1: 30}
  selected={strong:11, medium:14, mid:4, mild:1}

OSCRS_ACTIVE user_goal:
  reports=30 active=30 takeover=14 (47%) fallback=4
  fb={0:26, 2:4}
  selected={strong:27, medium:3}

OSCRS_ACTIVE goal_c:
  reports=30 active=30 takeover=5 (17%) fallback=6
  fb={0:24, 1:3, 3:3}
  selected={mild:6, strong:15, original:6, mid:2, medium:1}
```

行为层判定：

```text
1. GEOREF_TUNED 完全 fb=-1 (OSCRS disabled)
     -> G 层独立可用，S 层不介入；论文 §3.1/§3.2 分层在 runtime 被验证。
2. OSCRS_ACTIVE × 2 不同 goal 都有 takeover + fallback：
     - user_goal: fb={0,2} ——hard gate 命中 4 次 "geometry OK 但 slosh fail"；
     - goal_c:   fb={0,1,3} ——fb=1 与 fb=3 全部触发，OSCRS 全套 fallback
                  链路（B2 修过的 alarm semantics）在不同动力学下被走过。
3. selected 在 OSCRS_ACTIVE × goal_c 上覆盖全部 5 种 candidate name，
     生成 G 与选择 S 的解耦在 runtime 可见。
```

注意：5/8 smoke01 (19:29) 与 5/9 三个 bag 之间夹着 commit 64d7dab (5/8 20:18,
B1-B10 修复，含 B5 batch-norm 总是启用)。因此 §6.2 严格的 "refactor 前/后同
commit 对照" 已无法补做。§6.1 函数级 numeric exact equality 已经在函数层证明
refactor 零 drift，§6.2 的角色仅为行为不变性 sanity，本节三个 bag 行为健康
且与方法层 G+S 结构一致，sanity 通过。

后续 baseline：5/9 三个 bag 视为 "post-B1-B10 + post-refactor" 实物前最后一组
仿真基线，实物录包后离线对照用此组。

## 新增 GEOREF_FIXED_STRONG baseline

目的：

```text
用户提出需要一个“只有 GeoRef 候选、没有 geometry-only selector、没有 OSCRS selector”的 baseline，
用于判断 OSCRS 是否只是因为经常选择 strong smoothing 才有效。
```

代码改动：

```text
anti_slosh_path_post_processor.py:
  新增 fixed_candidate_name 参数。
  若 fixed_candidate_name 非空：
    - 仍生成并 evaluate 全部候选；
    - 若指定候选 accepted，则直接发布指定候选；
    - 若指定候选缺失或被 gate 拒绝，则回退 original；
    - 跳过 OSCRS active/select，避免混入 slosh selector。

anti_slosh_path_post_processor.launch:
  新增 arg/param fixed_candidate_name，默认空，不影响既有 GEOREF_TUNED / OSCRS。

run_sim_fixed_path_bag.sh:
  新增 CONDITION=GEOREF_FIXED_STRONG。
  该 condition 与 GEOREF_TUNED / OSCRS 使用同一 MPC、速度上限、candidate generator 参数；
  仅设置 fixed_candidate_name=strong。
```

文档改动：

```text
docs/重要文档/20260506有效性验证方案.md:
  主矩阵从 RAW / GEOREF_TUNED_STRONG / OSCRS_ACTIVE 三线扩展为四线，
  加入 GEOREF_FIXED_STRONG_REAL。

docs/重要文档/20260508_Online_GeoRef_OSCRS实物录包SOP.md:
  增加 GEOREF_FIXED_STRONG_REAL 启动命令。
```

语义：

```text
RAW_REAL:
  raw path -> MPC

GEOREF_FIXED_STRONG_REAL:
  GeoRef candidate generator -> fixed strong if accepted else original -> MPC

GEOREF_TUNED_STRONG_REAL:
  GeoRef candidate generator -> geometry-only selector -> MPC

GEOREF_OSCRS_ACTIVE_REAL:
  GeoRef candidate generator -> OSCRS hard gate + score selector -> MPC
```

验证：

```text
py_compile:
  anti_slosh_path_post_processor.py
  candidate_generators.py
  test_candidate_generators_equivalence.py
  通过。

bash -n:
  run_sim_fixed_path_bag.sh
  通过。

xmllint:
  anti_slosh_path_post_processor.launch
  通过。

candidate generator equivalence:
  test_candidate_generators_equivalence.py -> OK。
```

待跑：

```text
CONDITION=GEOREF_FIXED_STRONG 的仿真 smoke。
```

## GEOREF_FIXED_STRONG 仿真 smoke

输入 bag：

```text
/data/a/slosh_bags/sim/20260509/20260509_open_user_goal_GEOREF_FIXED_STRONG_runfixedstrong01_010731.bag
```

rosbag 话题检查：

```text
/anti_slosh_path/candidate_report: 30 msgs
/scout/global_path_anti_slosh: 30 msgs
/cmd_vel: 303 msgs
/slosh/height: 243 msgs
```

`check_oscrs_takeover.py` 输出：

```text
reports=30
active_reports=0
takeover_reports=0
fallback_reports=0
fb_counts={'-1': 30}
selected_counts={'strong': 29, 'original': 1}
last=selected=strong,geo=medium,oscrs=none,active=0,fallback=0,fb=-1,takeover=0
```

判定：

```text
GEOREF_FIXED_STRONG 模式通路正常：
  1. OSCRS 未启用，active=0, fb=-1；
  2. 不走 geometry-only selector，selected 与 geo 可不同；
  3. strong accepted 时发布 strong；
  4. 有 1 次回退 original，符合 fixed strong fallback 设计。
```

该 bag 可作为 fixed-smoothing baseline 的软件通路验证，不作为有效性结论。

## 实物 phase1 初步分析后的 gate 修正

输入目录：

```text
/data/a/slosh_bags/real/20260508_phase1
```

实物 smoke 发现：

```text
RAW_REAL / GEOREF_TUNED_STRONG_REAL / GEOREF_FIXED_STRONG_REAL /
GEOREF_OSCRS_ACTIVE_REAL(run01/run02) 都到达，solve_success_ratio=1.0。

但 post-processor 没有真正接管：
  GEOREF_TUNED_STRONG_REAL: selected=original, geo=original, active=0
  GEOREF_FIXED_STRONG_REAL: selected=original
  GEOREF_OSCRS_ACTIVE_REAL: active=1, fb=3, selected=original, takeover=0
```

核心原因：

```text
1. original 被 min_seg gate 判失败：
   original: reason=min_seg:0.009~0.019<0.020
   这是 resample endpoint 短尾段导致的语义问题；
   original 是安全 fallback，不应因 endpoint 短段被判 invalid。

2. 非 original 全部被 ay_ratio_limit=1.0 拒绝：
   mild/medium/mid/strong: reason=ay:1.2~2.9>1.0
   该阈值对实物 MBF 路径第一轮 smoke 过严，导致无法验证接管链路。
```

代码修正：

```text
src/scout_apps/control/scout_local_planner/scripts/anti_slosh_path_post_processor.py
  evaluate_candidate():
    min_seg gate 只作用于 generated candidates；
    name == "original" 时跳过 min_seg reject。
```

文档修正：

```text
docs/重要文档/20260506有效性验证方案.md
docs/重要文档/20260508_Online_GeoRef_OSCRS实物录包SOP.md
  增加 phase1 takeover smoke 规则：
    若所有非 original 都因 ay ratio 被拒绝，
    先临时设置 ay_ratio_limit=3.0 跑 1 包 smoke，
    只验证是否能发布非 original，不进入正式有效性统计；
    若 odom_ay_p95 / tracking / collision 可接受，再回收到 2.0 / 1.5。
```

当前结论：

```text
这批实物包不能判定方法有效/无效，只能说明录包链路通了、但 gate 过严导致方法未介入。
下一步先跑 ay_ratio_limit=3.0 的 takeover smoke。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/validate_georef_oscrs_bag.py
```

用途：

```text
单包行为验收，不替代效果分析。
自动检查：
  - /anti_slosh_path/candidate_report 是否存在；
  - /scout/global_path_anti_slosh 是否存在；
  - RAW / GEOREF / FIXED / OSCRS 是否符合预期 active 语义；
  - selected/fb/takeover/fallback 分布；
  - /anti_slosh_path/safety_alarm；
  - 基础 /slosh/height 与 eta_dot 摘要。

takeover smoke 可加：
  --require-non-original
  --require-takeover
```

文档同步：

```text
src/scout_apps/control/scout_local_planner/scripts/README.md
docs/重要文档/20260506有效性验证方案.md
```

追加修订：

```text
docs/重要文档/20260506有效性验证方案.md
  §6 标题由“三条线启动命令”修正为“四条线启动命令”；
  在 RAW / GEOREF_TUNED / GEOREF_FIXED_STRONG / GEOREF_OSCRS_ACTIVE
  每个启动命令后直接加入“录完立即检查”命令。
```

执行纪律：

```text
带 GeoRef/OSCRS 的包录完后必须立即跑 validate_georef_oscrs_bag.py。
若 selected=original / fb=3 / safety_alarm / require-non-original 失败，
该包只算 smoke 失败，不进入正式有效性统计，也不要继续机械式录下一包。
```

## validate_georef_oscrs_bag.py 可读性改进

用户反馈：

```text
现场脚本需要直接输出“正常/不正常”和“应该调哪个参数”，
不能只给 reports/fb/selected counters。
```

修改：

```text
src/scout_apps/control/scout_local_planner/scripts/validate_georef_oscrs_bag.py
  新增 print_human_verdict() / diagnose()：
    - 输出“行为验收结论”；
    - 正常性: 正常 / 不正常；
    - 原因；
    - 建议；
    - 仍保留原始 reports/selected/fb/height/eta_dot counters。
```

当前诊断规则：

```text
ay gate 拒绝非 original：
  建议 ay_ratio_limit 临时放宽到 3.0 做 takeover smoke；
  通过后回收到 2.0 -> 1.5。

fb=3：
  表示无可用几何候选；
  先修 ay_ratio_limit / costmap / geometry gate，不调 OSCRS score。

fb=2：
  表示几何可行但 slosh hard gate 失败；
  检查 eta_lim_mm / residual_ratio / height_coeff_mode。

safety_alarm：
  该包不进正式有效性统计。
```

用旧实物 OSCRS 失败包验证：

```text
python3 validate_georef_oscrs_bag.py \
  /data/a/slosh_bags/real/20260508_phase1/slosh_Q0_20260509_141055_GEOREF_OSCRS_ACTIVE_REAL_run01.bag \
  --mode oscrs --require-non-original

输出：
  正常性: 不正常
  建议:
    - 非 original 被 ay gate 拦住：先把 ay_ratio_limit 临时放宽到 3.0...
    - fb=3 表示无可用几何候选...
    - 有 safety_alarm：该包不要进正式有效性统计...
```

## 实物方案改为 SAFE mild variant

背景：

```text
phase2 实物 smoke 中，即使 vehicle_v_max:=1.5，较强 GeoRef/OSCRS 候选仍出现终点过冲回头。
RAW_REAL 默认参数不过冲，说明问题不在基础 MPC，而在 post-processor 改出的参考路径。
```

决策：

```text
实物正式验证不再使用 strong/medium candidate set。
统一改为 SAFE mild variant：

RAW_REAL
GEOREF_FIXED_MILD_REAL
GEOREF_TUNED_MILD_REAL
GEOREF_OSCRS_ACTIVE_MILD_REAL
```

文档修改：

```text
docs/重要文档/20260506有效性验证方案.md
  - 主矩阵改名为 *_MILD_REAL；
  - fixed baseline 改为 fixed_candidate_name:=mild；
  - GeoRef/OSCRS 均使用 max_candidate_level:=mild；
  - GeoRef/OSCRS 实物 smoke 保留 ay_ratio_limit:=3.0；
  - 删除 OSCRS 段中临时 vehicle_v_max:=1.5，避免把速度上限作为与 RAW 不一致的变量；
  - 检查命令改为对应的 *_MILD_BAG；
  - OSCRS 示例改为 selected=mild。
```

实验口径：

```text
RAW_REAL 仍是原始全局路径 + 普通 MPC，MPC 参数不改。
SAFE mild 三条线只改变参考路径来源，不改 MPC 防晃项。
```

## 实物 fixed mild 被 collision gate 拦截

用户回包：

```text
/home/geist/slosh_bags/real/20260508_phase2/slosh_Q0_20260509_172520_GEOREF_FIXED_MILD_REAL_run01.bag

validate_georef_oscrs_bag.py --mode fixed --require-non-original:
  selected=original
  reject_reason_counts={'collision': 4, 'level': 3}
  mild:accepted=0,reason=collision:idx=0:cost=68
```

判断：

```text
fixed mild 没接管的直接原因不是 ay_ratio_limit，也不是 OSCRS score，
而是 costmap collision gate 在起点附近把 mild 判为高代价。
```

文档修改：

```text
docs/重要文档/20260506有效性验证方案.md
  - 增加 phase2 collision smoke 注意；
  - fixed/tuned/oscrs mild 启动命令加入 collision_threshold:=90；
  - 若仍 collision:idx=0，则试 collision_threshold:=100；
  - enable_collision_check:=false 只允许开阔场地通路 smoke，不作为正式安全实验参数。
```
