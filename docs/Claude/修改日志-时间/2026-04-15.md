# 修改日志 2026-04-15

> 今日类型：代码实现（Day 2）

---

## 今日完成内容

### 新建文件

| 文件 | 内容 |
|---|---|
| `include/scout_local_planner/risk_scheduler.h` | RiskScheduler 接口（params struct、output struct、类声明） |
| `src/risk_scheduler.cpp` | RiskScheduler::update() 完整实现（fallback、r_k、u_k、sigmoid、rate-limit） |

### 修改文件

| 文件 | 改动 |
|---|---|
| `include/scout_local_planner/local_planner_ros.h` | 新增 `#include <std_msgs/Bool.h>` 和 `risk_scheduler.h`；添加 RiskScheduler/RiskSchedulerOutput 成员、5 个调试发布器、`E_slosh_prev_`、`rs_h_coeff_` |
| `src/local_planner_ros.cpp` | `loadParameters()` 末尾读 `risk_scheduler/*` yaml 参数并调用 `risk_scheduler_.init()`；`initialize()` 中在 slosh 初始化块末尾缓存 `rs_h_coeff_` 并 advertise 调试话题；`controlLoop()` TRACKING 段在 `getReferencePoints` 前插入 outer loop 调用、MPC 参数注入；solve 成功后保存 `E_slosh_prev_`、发布调试话题 |
| `config/mpc_params.yaml` | 末尾新增 `risk_scheduler:` 参数块（`enable: false`，所有参数带注释） |
| `CMakeLists.txt` | `add_library` 中加入 `src/risk_scheduler.cpp` |

### 关键实现细节

1. **fallback 三触发条件**：IMU timeout / bias not ready / u_k 持续高于阈值（计数 ≥ trigger）
2. **变化率限制**：Q_eta、eta_bar、v_ref_eff 均有 `rate_limit_per_step * 满量程` 的每步上限，防 QP 抖动
3. **参数换算**：`Q_slosh_eta = Q_eta_k * h_coeff²`；`slosh_eta_bar = eta_bar_k / (h_coeff * sqrt(2))`
4. **监控数据来源**：`h_pred_max_prev` = 上一周期 `last_predicted_height_max_`；`E_slosh_prev_` = 上一周期 solve 成功后从 `slosh_integration_.getSloshState()` 计算保存
5. **v_ref_eff 注入时机**：在 `getReferencePoints()` 之前完成，speed governor 的上限也跟随更新
6. **默认关闭**：yaml 中 `enable: false`，不影响现有控制行为

### 编译验证

```
[ 20%] Built target slosh_models
[ 86%] Built target scout_local_planner
[100%] Built target local_planner_node
```
编译无 warning/error。

### 分支状态

- 当前分支：`refactor/idea-improvement`
- 今日新增 commit：`9c70814 Day 2: 实现 ρ_k 风险自适应调度器 (RiskScheduler)`（代码已提交）

---

## Day 2 完成标准检查

- [x] `RiskScheduler` 编译通过
- [ ] bag 回放验证：`/risk_scheduler/rho_k` 在弯道段高、直道段低（需实机/bag 验证）
- [ ] `fallback_active` 在断开 `/imu/data` 话题时正确触发（需实机验证）
- [ ] Q_η 相邻周期变化 ≤ `dQ_max`（无抖动）（需实机验证）
- [ ] `mpc_params.yaml` 新增 `risk_scheduler` 参数块，带注释 ✓

---

## 明日计划（Day 3）

- [ ] 在 bag 回放中验证 `/risk_scheduler/rho_k`、`/risk_scheduler/Q_eta_k` 行为
- [ ] 将 `risk_scheduler/enable` 改为 `true`，端到端测试
- [ ] 实现 IMU lateral_accel 正式接入（A/B/C 对比实验，外部测量为主指标）
- [ ] 实现 T2 settling MPC 状态机：TRACKING → SETTLING → REACHED
  - [ ] 新增 `PlannerState::SETTLING` 枚举值
  - [ ] 计算 `N_settle = ceil(4π / ω₀ / dt) = 8` 步
  - [ ] SETTLING 期间 v_ref = 0，eta_bar 收紧到 fallback 值
