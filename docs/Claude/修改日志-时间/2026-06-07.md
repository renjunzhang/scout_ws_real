# 2026-06-07

## fixed-path common-limit baseline 对齐

- 按实物实验前仿真对比要求，先不继续调 SPMPC 权重，将 standalone TEB/DWA baseline 的普通运动学限制对齐到 SPMPC 当前限制：`max_vel_x=0.8`、`max_vel_theta=1.2`、`acc_lim_x=0.6`、`acc_lim_theta=1.2`。
- 保持 SPMPC 平台限制不动：`v_max=0.8`、`omega_max=1.2`、`a_max=0.6` 与 shared `linear_accel_max=0.6`。
- 为 fixed-path metrics 增加 cmd 线加速度 p95/max 与角速度变化率 p95/max 字段，便于 common-limit smoke 后检查实际运动强度是否接近。
- 按 fresh sim 规则完成 common-limit N=3 smoke，并记录到 `docs/实物实验注意事项/实物实验前的仿真/20260607_common_limit_fresh_smoke记录.md`；第一轮暴露出 fresh sim 起点与 `P2_s_curve` 路径起点不在同一区域，只作为流程/配置诊断。
- 按方案 B 使用 P2 起点附近 spawn 重跑 robust common-limit N=3：SPMPC 内部方法均 `success=1.0/stable_stop=1.0`，TEB/DWA 各 `success=0.67/stable_stop=0.67`；结果已追加到同一仿真记录文档。
- 暂停外部 TEB/DWA，不直接扩大 N=5；加回 `B_slosh` 做 P2-start fresh-sim `B_smooth / B_slosh / B_ours` 的 `w_slosh` N=1 小扫诊断。确认 slosh cost 已进入 `/spmpc/cost_breakdown` 且会显著改变 `cmd_vel` 行为，但权重/完整策略存在敏感区间，部分权重会导致停滞；结果记录到 `docs/实物实验注意事项/实物实验前的仿真/20260607_p2start_slosh_cost_sweep记录.md`。
- 继续暂停外部 TEB/DWA 与全量 N=5，按 fresh-sim 规则完成 `B_slosh/B_ours` 在 `w_slosh=2.0/2.2/2.4` 的 P2-start 窄扫 N=3。结果显示 `B_slosh w=2.4` 是本轮唯一 `3/3 success/stable` 组合，但降晃优势不明显；`B_slosh w=2.0/2.2` 有更低成功 run 峰值潜力但仍有停滞；`B_ours` 三个权重均为 `2/3`，暂不能直接扩大 N=5。结果记录到 `docs/实物实验注意事项/实物实验前的仿真/20260607_p2start_slosh_narrow_sweep记录.md`。
