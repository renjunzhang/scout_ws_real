# 2026-05-07 修改日志

## Step 0a 扩样准备

当前状态：

```text
Step 0a 已用 open_user_goal GEOREF_TUNED 三包完成初版诊断：
  run01: STOP_NO_GAP
  run02: STOP_NO_GAP
  run03: MULTI_CHANNEL_BRANCH (jerk, omega)
  overall: EDGE_INCONCLUSIVE
```

结论：

```text
证据不足以直接进入 MPC reference constraints 代码实现。
需要补采 GEOREF_TUNED open_user_goal 至少 2 包，
目标是判断 jerk/omega gap 是否能达到 2/5 或更强一致触发。
```

本轮只准备补采命令，不改 MPC 代码。

## Step 0a 五包扩样结果

新增输入：

```text
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run04_120200.bag
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run05_120405.bag
```

重跑命令：

```bash
src/scout_apps/control/scout_local_planner/scripts/diagnose_georef_budget_gap.py \
  --csv /data/a/slosh_bags/analysis/georef_budget_gap_step0a_5bags_20260507.csv \
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run01_190153.bag \
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run02_190940.bag \
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run03_191424.bag \
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run04_120200.bag \
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run05_120405.bag
```

输出：

```text
csv: /data/a/slosh_bags/analysis/georef_budget_gap_step0a_5bags_20260507.csv

run01: STOP_NO_GAP
  gap_jerk=0.564, gap_omega=0.230
  但 top_bin_fraction 未过 0.25

run02: STOP_NO_GAP
  gap_jerk=0.356, gap_omega=0.380
  但 top_bin_fraction 未过 0.25

run03: MULTI_CHANNEL_BRANCH
  triggers=jerk,omega
  gap_jerk=0.317, gap_omega=0.389
  top_s≈6.75m

run04: MULTI_CHANNEL_BRANCH
  triggers=jerk,omega
  gap_jerk=0.388, gap_omega=0.480
  top_s≈6.75m

run05: A_JERK_BRANCH
  triggers=jerk
  gap_jerk=0.519
  top_s≈6.75m

trigger_counts:
  a=0
  jerk=3
  omega=2
  alpha=0
  min_consistent=2

overall:
  MULTI_CHANNEL_BRANCH
```

结论：

```text
Step 0a 五包扩样后，normal MPC 确实存在突破 GeoRef budget 的一致证据。
主要问题是 jerk 通道 3/5 包触发，同时 omega 通道 2/5 包触发；
触发位置集中在 path s≈6.75m。
```

按方案分支：

```text
进入 Step 0b / Step 1 是合理的。
但这是 MULTI_CHANNEL_BRANCH，不是纯 a/jerk 问题。
第一波建议仍先做 v_upper + a_upper + a_lower + jerk_upper 的 vectorized hard bounds smoke，
然后观察 ay/omega 是否随 v_upper 间接下降；
若 omega 仍高，再单独设计 ay/omega 约束。
```

## Step 0b 代码结构阅读

阅读文件：

```text
src/scout_apps/control/scout_local_planner/include/scout_local_planner/types.h
src/scout_apps/control/scout_local_planner/include/scout_local_planner/mpc_solver.h
src/scout_apps/control/scout_local_planner/include/scout_local_planner/constraint_manager.h
src/scout_apps/control/scout_local_planner/src/mpc_solver.cpp
src/scout_apps/control/scout_local_planner/src/constraint_manager.cpp
src/scout_apps/control/scout_local_planner/src/path_handler.cpp
src/scout_apps/control/scout_local_planner/src/local_planner_ros.cpp
src/scout_apps/control/scout_local_planner/scripts/anti_slosh_path_post_processor.py
```

当前求解链路：

```text
LocalPlannerROS::controlLoop
  -> PathHandler::getReferencePoints(...)
  -> std::vector<ReferencePoint> ref_points
  -> MPCSolver::solve(current_state, ref_points)
  -> MPCSolver::buildQP(...)
  -> ConstraintManager::buildQPConstraints(...)
  -> OSQP
```

确认结果：

```text
1. ReferencePoint 当前只有 x/y/theta_path/kappa/s/v_path/v_ref。
   v_ref 只进入 cost_function，是软速度目标，不是硬边界。

2. ConstraintManager 已有 v、a、omega、Δa、Δomega 约束行。
   v/a/jerk 约束可以优先做成已有 bound 的时变 l/u，不需要新增约束行。

3. MPCSolver::updateOSQP 每周期更新 P/A/q/l/u。
   若 Step 2a 只改 l/u，不改稀疏结构，可以保持 OSQP update_bounds 路径。

4. post-processor 当前只发布 /scout/global_path_anti_slosh、metrics、candidate_report 和 debug paths。
   尚未发布 path 点级别 v/a/jerk budget sibling topic。
```

最小实现建议：

```text
Step 1:
  post-processor 为 selected candidate 发布与 path 点对齐的 budget topic。
  正式 topic 建议 trajectory_msgs/JointTrajectory:
    point.positions = [v_upper, a_upper, a_lower, jerk_upper]

Step 2a:
  ReferencePoint 增加可选 budget 字段。
  LocalPlannerROS/PathHandler 将 path 点级别 budget 按 s 插值到 horizon ref_points。
  MPCSolver 把 horizon budget 传给 ConstraintManager。
  ConstraintManager 仅在已有 v/a/Δa 约束行上修改 l/u。
```

边界：

```text
这只覆盖 v/a/jerk 执行一致性。
Step 0a 已显示 omega 也有 2/5 包触发，因此 Step 2a 后必须重点检查 omega/ay 是否被间接压住。
如果 omega 仍反弹，不能把 Step 2a 写成完成版，需要进入 ay/omega 约束设计或停止本方向。
```

## Step 1 / Step 2a 初版实现

目标：

```text
按 GeoRef 约束一致性 MPC 方案推进最小闭环。
实现默认关闭的 budget topic + v/a/jerk hard-bound smoke。
不做 slack，不做 ay/omega 线性化，不改 slosh cost 主线。
```

代码改动：

```text
package.xml / CMakeLists.txt
  增加 trajectory_msgs 依赖。

anti_slosh_path_post_processor.py
  新增 /scout/global_path_anti_slosh_budget sibling topic。
  类型: trajectory_msgs/JointTrajectory。
  point.positions = [s, v_upper, a_upper, a_lower, jerk_upper]。

anti_slosh_path_post_processor.launch
  暴露 budget_topic、prediction_jerk_min、prediction_jerk_max。

types.h
  ReferencePoint 增加 has_ref_budget / s_global / ref_* budget 字段。
  MPCParams 增加 reference_constraints_enable，默认 false。

PathHandler
  新增 updateReferenceBudget()。
  按 path s 插值 budget 到 horizon ReferencePoint。

LocalPlannerROS
  新增 referenceBudgetCallback()。
  仅在 mpc/reference_constraints_enable=true 时订阅 budget topic。

MPCSolver / ConstraintManager
  仅在 horizon ref_points 实际带 budget 时启用 reference bounds。
  复用已有 v、a、Δa 约束行，只改 l/u。

slosh_experiment.launch / slosh_experiment_sim.launch
  新增 reference_constraints_enable / reference_budget_topic 参数，默认关闭。

run_sim_fixed_path_bag.sh
  录包 topics 增加 /scout/global_path_anti_slosh_budget。
  新增 GEOREF_CONSTRAINED condition。
  roslaunch 参数透传 reference_constraints_enable / reference_budget_topic。
```

实现取舍：

```text
budget topic 使用 [s, v_upper, a_upper, a_lower, jerk_upper]，
而不是原方案中的 [v_upper, a_upper, a_lower, jerk_upper]。

原因：
当前 PathHandler 会 sanitize / resample / smooth path，
只靠 budget 与 path 点索引对齐不安全；
显式 s 允许 MPC 端按弧长插值，避免索引错位。
```

验证：

```text
python3 -m py_compile:
  anti_slosh_path_post_processor.py
  diagnose_georef_budget_gap.py
  PASS

git diff --check:
  PASS

catkin_make --pkg scout_local_planner:
  PASS

bash -n run_sim_fixed_path_bag.sh:
  PASS
```

边界：

```text
当前只是默认关闭的 Step 2a hard-bound smoke。
尚未跑闭环 bag。
即使 Step 2a 通过，也只证明 v/a/jerk 执行一致性改善；
omega/ay 是否被间接压住必须由后续 x3 bag 验证。
```

## Step 2a smoke 首次运行无效

命令：

```bash
source /home/a/scout_ws/devel/setup.bash && \
PATH_MODE=global_goal \
PATH_ID=open_user_goal \
CONDITION=GEOREF_CONSTRAINED \
RUN_ID=smoke01 \
START_DELAY=5 \
RECORD_DURATION=80 \
BAG_DATE=20260507 \
TEMPLATE_GOAL_X=-3.1570560932159424 \
TEMPLATE_GOAL_Y=-2.897411346435547 \
TEMPLATE_GOAL_QZ=-0.978164583074326 \
TEMPLATE_GOAL_QW=0.2078317791364693 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

输出 bag：

```text
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke01_122531.bag
```

判定：

```text
无效包。
```

原因：

```text
运行时没有完整 open sim / nav stack。
脚本启动 rosbag 时 ROS master 不存在；
anti_slosh_path_post_processor.launch 随后 auto-start 了一个空 master。
bag 中没有 /odom、/scout/global_path_anti_slosh、/scout/global_path_anti_slosh_budget。
extract_slosh_metrics 输出 tracking=0.0s，height 指标为 nan。
```

结论：

```text
这包不能用于 Step 2a 判断。
下一次必须先启动完整仿真环境：
  SIM_ENV=open USE_RVIZ=true SPAWN_X=-4.0 SPAWN_Y=0.0 SPAWN_Z=0.1 SPAWN_YAW=0.0 \
    rosrun scout_local_planner launch_sim_nav_stack.sh
确认 /clock、/odom、/scout/global_path 有数据后，再跑 GEOREF_CONSTRAINED。
```

## Step 2a smoke02 有效包分析

输入：

```text
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke02_122946.bag
```

有效性：

```text
bag 有 /odom、/scout/global_path_anti_slosh、/scout/global_path_anti_slosh_budget。
/scout/global_path_anti_slosh_budget:
  type = trajectory_msgs/JointTrajectory
  points = 247
  joint_names = [s, v_upper, a_upper, a_lower, jerk_upper]

config:
  REFERENCE_CONSTRAINTS_ENABLE=true
  reference_budget_topic=/scout/global_path_anti_slosh_budget

status:
  TRACKING 有效
  SETTLING/REACHED 存在
  solve_success=1.0
```

基础指标：

```text
tracking=7.4s
h_rms=0.000789m
h_p95=0.001695m
h_max=0.001886m
eta_dot_rms=0.006116m/s
energy_norm_rms=0.01487
solve_mean=6.379ms
track_p95=0.480m
odom_ay_p95=0.184
```

与 GEOREF_TUNED 五包粗对比：

```text
GEOREF_TUNED tracking 约 6.25~6.70s；
GEOREF_CONSTRAINED tracking=7.4s，约 +16%，已经略超 +15% 工程门槛。

GEOREF_TUNED eta_dot_rms 约 0.00323~0.00457m/s；
GEOREF_CONSTRAINED eta_dot_rms=0.006116m/s，明显上升。

h_p95=0.001695m，低于 GEOREF_TUNED 五包均值附近，
但不是关键正信号，因为 eta_dot 与 tracking 已经变差。
```

budget gap 诊断：

```text
csv:
  /data/a/slosh_bags/analysis/georef_budget_gap_constrained_smoke02_20260507.csv

verdict:
  MULTI_CHANNEL_BRANCH

triggers:
  jerk, omega

gap:
  gap_a=-0.045
  gap_jerk=0.361
  gap_omega=0.265
  gap_alpha=-2.659
  top_s(j/w)=(6.75,6.75)
```

解释：

```text
Step 2a hard-bound smoke 能跑通，且 a 通道确实被压住。
但 jerk 与 omega 仍在同一 s≈6.75m 段触发。
这说明当前 v/a/jerk 第一版没有解决 Step 0a 暴露的多通道问题。
```

当前问题：

```text
1. jerk budget 生成口径偏弱。
   当前 post-processor 的 jerk_upper 来自自身 rollout 的 |jerk| clamp，
   多数点可能等于 vehicle_j_max=4.0，不能稳定收紧 Δa。

2. omega 本来就不在 Step 2a 约束范围内。
   Step 0a 已经显示 omega 2/5 包触发，smoke02 仍触发，符合预期。

3. eta_dot_rms 上升说明 hard-bound 可能把 h_p95 降低换成了模态速度增加。
   这和旧 PMG 的失败模式相似，不能只看 h_p95。
```

结论：

```text
不建议直接进入 GEOREF_CONSTRAINED x3 正式录包。
先修 Step 2a 的 jerk budget 口径，或者按方案进入 ay/omega 约束设计。
最小下一步：
  A. 将 jerk_upper 改成固定预算 min(vehicle_j_max, prediction_jerk_budget)，例如 2.0；
  B. smoke 再跑 1 包；
  C. 若 jerk 下降但 omega 仍触发，再设计 ay/omega 约束。
```

## Step 2a jerk budget 口径修正

问题：

```text
smoke02 中 jerk gap 仍触发。
原因之一是 post-processor 的 jerk_upper 来自 rollout jerk 的 clamp 结果，
大量点接近 vehicle_j_max=4.0，实际没有比车辆默认 Δa 约束更紧。
```

修改：

```text
anti_slosh_path_post_processor.py:
  prediction/jerk_budget 作为固定 jerk_upper。
  budget point.positions = [s, v_upper, a_upper, a_lower, jerk_upper]。

anti_slosh_path_post_processor.launch:
  prediction_jerk_budget 默认 4.0。

run_sim_fixed_path_bag.sh:
  GEOREF_CONSTRAINED 默认 POST_PROCESSOR_PREDICT_JERK_BUDGET=2.0。
```

验证：

```text
python3 -m py_compile:
  PASS

bash -n run_sim_fixed_path_bag.sh:
  PASS

git diff --check:
  PASS

catkin_make --pkg scout_local_planner:
  PASS
```

下一步：

```text
重新跑一包 GEOREF_CONSTRAINED smoke，确认：
  1. budget topic 中 jerk_upper 是否为 2.0；
  2. gap_jerk 是否下降；
  3. omega 是否仍触发；
  4. eta_dot_rms 与 tracking_time 是否不再恶化。
```

## smoke03 未应用 jerk_budget=2.0

输入：

```text
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke03_123858.bag
```

检查结果：

```text
有效包，有 /odom、/scout/global_path_anti_slosh、/scout/global_path_anti_slosh_budget。
但 /scout/global_path_anti_slosh_budget 中:
  jerk_upper min/max = 4.0 / 4.0
```

指标：

```text
tracking=7.4s
h_p95=0.001752m
eta_dot_rms=0.003269m/s
solve_success=1.0
odom_ay_p95=0.294
```

判定：

```text
这包不能作为 jerk_budget=2.0 的验证。
虽然 eta_dot_rms 比 smoke02 回落，但 budget topic 仍是 4.0，
因此不能把该变化归因于 jerk 约束修正。
```

补充修正：

```text
run_sim_fixed_path_bag.sh 的 config summary 增加:
  POST_PROCESSOR_PREDICT_JERK_BUDGET
终端 echo 增加:
  post_jerk_budget

目的:
下一包能直接从 .txt 判断 jerk budget 是否传入，而不只靠事后读 bag。
```

## smoke04 jerk_budget=2.0 有效验证

输入：

```text
/data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke04_124232.bag
```

配置确认：

```text
POST_PROCESSOR_PREDICT_JERK_BUDGET=2.0
REFERENCE_CONSTRAINTS_ENABLE=true
```

budget topic：

```text
/scout/global_path_anti_slosh_budget:
  points=245
  joint_names=[s, v_upper, a_upper, a_lower, jerk_upper]
  jerk_upper min/max = 2.0 / 2.0
```

有效性：

```text
TRACKING 有效；
SETTLING/REACHED 存在；
solve_success=1.0。
```

指标：

```text
tracking=6.3s
h_rms=0.001029m
h_p95=0.002123m
h_max=0.003344m
eta_dot_rms=0.006053m/s
energy_norm_rms=0.018688
solve_mean=3.704ms
track_p95=0.481m
odom_ay_p95=0.204
cmd_dwz_rms=1.495
```

budget gap 诊断：

```text
csv:
  /data/a/slosh_bags/analysis/georef_budget_gap_constrained_smoke04_20260507.csv

verdict:
  STOP_NO_GAP

triggers:
  none

gap:
  gap_a=0.156
  gap_jerk=0.631
  gap_omega=0.172
  gap_alpha=-2.032
  但 gap_dist 未满足触发判据，因此 overall STOP_NO_GAP。
```

解释：

```text
Step 2a 的执行一致性目标基本达到：
  从 smoke02 的 MULTI_CHANNEL_BRANCH 变成 smoke04 的 STOP_NO_GAP。

但 slosh outcome 变差：
  h_p95 高于 GEOREF_TUNED 五包多数结果；
  eta_dot_rms 接近 smoke02 的高值；
  energy_norm_rms 也偏高。
```

结论：

```text
v/a/jerk hard-bound smoke 可以改善 execution budget consistency，
但当前没有改善 /slosh/height 或 eta_dot，反而有能量/eta_dot 变差风险。

不建议直接进入 GEOREF_CONSTRAINED x3 正式表。
按方案分支，应停止 v/a/jerk-only 方向，转入 ay/omega 约束设计评估；
或者把 GeoRef-constrained MPC 保留为负结果/工程边界，不作为主方法增强。
```

## GEOREF_CONSTRAINED x3 统计

输入：

```text
B1 GEOREF_TUNED x5:
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run01_190153.bag
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run02_190940.bag
  /data/a/slosh_bags/sim/20260506/20260506_open_user_goal_GEOREF_TUNED_run03_191424.bag
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run04_120200.bag
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_TUNED_run05_120405.bag

Ours GEOREF_CONSTRAINED x3:
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke04_124232.bag
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke05_133505.bag
  /data/a/slosh_bags/sim/20260507/20260507_open_user_goal_GEOREF_CONSTRAINED_runsmoke06_133640.bag
```

budget 有效性：

```text
smoke04: jerk_upper min/max = 2.0 / 2.0
smoke05: jerk_upper min/max = 2.0 / 2.0
smoke06: jerk_upper min/max = 2.0 / 2.0
```

budget gap 诊断：

```text
csv:
  /data/a/slosh_bags/analysis/georef_budget_gap_constrained_x3_20260507.csv

smoke04:
  STOP_NO_GAP

smoke05:
  OMEGA_ALPHA_BRANCH
  triggers=omega

smoke06:
  A_JERK_BRANCH
  triggers=jerk

overall:
  EDGE_INCONCLUSIVE
```

指标均值对比：

```text
tracking:
  B1 mean=6.38s, std=0.186
  Ours mean=6.917s
  delta=+8.41% (+2.89 sigma)

h_rms:
  B1 mean=0.000852m, std=0.000081
  Ours mean=0.000969m
  delta=+13.73% (+1.44 sigma)

h_p95:
  B1 mean=0.001887m, std=0.000267
  Ours mean=0.002006m
  delta=+6.29% (+0.44 sigma)

h_max:
  B1 mean=0.002399m, std=0.000457
  Ours mean=0.002646m
  delta=+10.30% (+0.54 sigma)

eta_dot_rms:
  B1 mean=0.004070m/s, std=0.000514
  Ours mean=0.005245m/s
  delta=+28.85% (+2.29 sigma)

energy_norm_rms:
  B1 mean=0.015204, std=0.001385
  Ours mean=0.017469
  delta=+14.90% (+1.64 sigma)

odom_ay_p95:
  B1 mean=0.1958
  Ours mean=0.1780
  delta=-9.09%
```

结论：

```text
Ours x3 没有超过 B1。
虽然 odom_ay_p95 与 cmd_dwz_rms 有下降，但 /slosh/height 与 eta_dot/energy 没改善，
且 eta_dot_rms 超过 B1 noise floor 2 sigma 以上。

按方案 §11/§12：
  Step 2a 判定为 FAIL / negative result。
  不进入 Step 2b slack。
  不把 GEOREF_CONSTRAINED 作为论文主方法增强。
```

下一步建议：

```text
停止 v/a/jerk hard-bound 路线。
若继续研究 MPC 内部增强，只能另立 ay/omega 约束设计，
但这已经是新分支，不应阻塞当前 Online GeoRef 主线与实物 open 验证。

当前论文主线仍应保持:
  Online GeoRef + normal MPC tracking
GEOREF_CONSTRAINED 放入 failure analysis:
  "reference budget consistency can be improved, but hardening v/a/jerk bounds
   transfers excitation to modal velocity/energy and does not improve slosh."
```

## 外科撤回 GEOREF_CONSTRAINED 实现

目的：

```text
保留 GeoRef 约束一致性 MPC 的诊断脚本、方案文档与负结果；
移除已验证失败的 reference-constrained MPC 工程入口，避免后续误用。
```

已撤回代码：

```text
src/scout_apps/control/scout_local_planner/include/scout_local_planner/types.h
src/scout_apps/control/scout_local_planner/include/scout_local_planner/constraint_manager.h
src/scout_apps/control/scout_local_planner/include/scout_local_planner/path_handler.h
src/scout_apps/control/scout_local_planner/include/scout_local_planner/local_planner_ros.h
src/scout_apps/control/scout_local_planner/src/constraint_manager.cpp
src/scout_apps/control/scout_local_planner/src/mpc_solver.cpp
src/scout_apps/control/scout_local_planner/src/path_handler.cpp
src/scout_apps/control/scout_local_planner/src/local_planner_ros.cpp
src/scout_apps/control/scout_local_planner/scripts/anti_slosh_path_post_processor.py
src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
src/scout_apps/control/scout_local_planner/launch/*.launch
src/scout_apps/control/scout_local_planner/CMakeLists.txt
src/scout_apps/control/scout_local_planner/package.xml
```

撤回内容：

```text
/scout/global_path_anti_slosh_budget topic
trajectory_msgs 依赖
LocalPlannerROS budget subscriber
PathHandler reference budget cache/interpolation
ReferencePoint ref_* fields
MPCParams reference_constraints_enable
ConstraintManager v/a/jerk 时变 bound tightening
run_sim_fixed_path_bag.sh CONDITION=GEOREF_CONSTRAINED
post-processor budget publisher
```

保留内容：

```text
src/scout_apps/control/scout_local_planner/scripts/diagnose_georef_budget_gap.py
docs/Claude/修改方案-时间-简介/2026-05-06_GeoRef约束一致性MPC方案.md
本日志中的 Step0a / smoke / x3 负结果
Online GeoRef + normal MPC tracking 主线
```

当前代码状态：

```text
src/scout_apps/control/scout_local_planner 工作区 diff 中不再包含 reference-constrained MPC 实现残留；
只保留新增诊断脚本 diagnose_georef_budget_gap.py。
```
