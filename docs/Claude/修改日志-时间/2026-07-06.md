# 2026-07-06 修改日志

## 20260705 fixed-path N=3 离线分析

- 新增综合分析记录：
  - `docs/实物实验注意事项/对比试验/实物对比试验分析/20260705_fixed_path_N3_RGB_模型综合分析.md`
- 使用离线分析脚本汇总 9 个 formal bag 的：
  - `GOAL_REACHED` / effective_config / delay compensation；
  - `/spmpc/slosh_height` internal model p95/max/RMS；
  - `/spmpc/debug/projector`、`/spmpc/debug/stage0_reference` tracking 指标；
  - `/spmpc/debug/command_intervention` command safety/limiter 指标；
  - `/spmpc/cost_breakdown` 与 `/spmpc/debug/slosh_cost_monitor` cost share；
  - RGB `h_mm_max_lcr_smooth_corr` 视觉真值。
- 分析脚本位于：
  - `docs/Claude/分析数据/20260705_fixed_path_compare/analyze_20260705_fixed_path_n3.py`
- 输出 CSV/JSON 位于：
  - `docs/Claude/分析数据/20260705_fixed_path_compare/`
- 因只做离线数据分析，未修改控制器生产代码，未运行 `catkin_make` 或 smoke test。

## 0706 实物补充矩阵启动方式

- 新增 0706 补充矩阵计划：
  - `docs/实物实验注意事项/对比试验/实物对比实验/0706_SPMPC实物补充实验矩阵计划.md`
- 在一键脚本 `src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_real_fixed_path_trial.sh` 中新增 `MATRIX_PRESET=0706_bsmooth_bours`：
  - 默认先跑 `B0_fixed_150_220_0706_gate01`；
  - 再按交错顺序跑 `B_smooth / B_ours` 同日 bridge；
  - 子 run 统一显式使用 `fixed_closed_loop 0.15 / 0.22`、`V_REF=0.20`、共享约束 `0.6 / 1.2 / 1.2`；
  - B0 gate 默认 `RECORD_RGB=false`，正式 `B_smooth / B_ours` 默认 `RECORD_RGB=true`；
  - 每个 run 前交互暂停，提示回起点、对齐 heading、液体静稳并检查 RGB/TF/odom。
- 本次只增加矩阵调度逻辑，不改变单次 trial 的控制参数默认值和单 run 执行链路。

## 20260705 fixed-path N=3 Ferrari-style 保真度

- 新增 Ferrari-style 绝对保真度离线脚本：
  - `docs/Claude/分析数据/20260705_fixed_path_compare/analyze_20260705_fidelity.py`
- 输出：
  - `20260705_fixed_path_n3_ferrari_fidelity_per_run.csv`
  - `20260705_fixed_path_n3_ferrari_fidelity_aggregate.csv`
  - `20260705_fixed_path_n3_ferrari_gate_threshold_sweep.csv`
  - `20260705_fixed_path_n3_ferrari_gate_threshold_sweep_aggregate.csv`
  - `20260705_fixed_path_n3_ferrari_fidelity_summary.json`
- Obsidian 记录：
  - `/data/a/Obsidian/vaults/StudyVault/30-Projects/MPC/规控一体的实验记录/实物实验/20260705_SPMPC_fixed_path_N3_Ferrari绝对保真度.md`
- 结论：`/spmpc/slosh_height` 可作为控制侧辅助指标；B_slosh 模型侧对 RGB 包络存在系统低估，不能单独作为真实液面 ground truth 或 hard gate。

## 0706 外部 baseline 实物启动计划

- 新增 LT-DWA / TEB / mpc_local_planner 实物 fixed-path 启动计划：
  - `docs/实物实验注意事项/对比试验/实物对比实验/0706_外部baseline_LT-DWA_TEB_MPC实物启动计划.md`
- 计划口径：
  - 三个 baseline 都先 shadow / dry-run，再 actuated N=1 smoke；
  - N=1 通过后再进入 interleaved formal N=3；
  - 任一时刻只允许一个 planner 发布 `/cmd_vel`；
  - 外部 baseline 不订阅 `/slosh/*`、`/liquid/*` 或 RGB 结果作为在线控制输入；
  - mpc_local_planner 单独记录 isolated MPC overlay source 要求。
- 新增外部 baseline 单 run 一键脚本：
  - `src/scout_apps/control/spmpc_local_planner/scripts/run_external_baseline_real_fixed_path_trial.sh`
  - 支持 `METHOD=lt_dwa_official|teb|mpc_local_planner` 与 `STAGE=shadow|actuated`；
  - 默认只跑单个 baseline 单个 stage，不自动执行 formal N=3；
  - `STAGE=actuated` 前硬检查 `/cmd_vel` 旧 publisher，退出时发布 zero `/cmd_vel`；
  - recorder 仍先于 planner 启动，默认 `RECORD_ALL_EXISTING_TOPICS=true` 便于第一次 smoke 收齐 baseline topics。
- 扩展 `record_spmpc_full_rgb_bag.sh` whitelist，加入 LT-DWA / TEB / mpc_local_planner baseline 诊断 topics。

## 20260706 fixed-path B_smooth / B_ours / B_slosh 离线分析

- 新增 0706 实物补充矩阵综合分析记录：
  - `docs/实物实验注意事项/对比试验/实物对比试验分析/20260706_Bsmooth_Bours_RGB_模型对比分析.md`
- 新增/使用离线分析脚本：
  - `docs/Claude/分析数据/20260706_fixed_path_compare/analyze_20260706_fixed_path_compare.py`
  - `docs/Claude/分析数据/20260706_fixed_path_compare/analyze_20260706_fidelity.py`
- 输出 CSV/JSON 位于：
  - `docs/Claude/分析数据/20260706_fixed_path_compare/`
- Obsidian 记录：
  - `/data/a/Obsidian/vaults/StudyVault/30-Projects/MPC/规控一体的实验记录/实物实验/20260706_SPMPC_Bsmooth_Bours_Bslosh_RGB与模型综合对比.md`
- 结论：0706 10 个 SPMPC bags 均 `GOAL_REACHED`、配置 10/10 OK、`delay_compensation_applied_frac=1.0`、`zero_due_to_tracking_safety=0`；`B_ours` 相对 `B_smooth` 在 internal model 侧有额外降幅，但 RGB `H_vis` 与 `B_smooth` 基本持平，不能强 claim RGB 显著优于 smooth-only；`B_slosh` 模型侧最低但 RGB 更差且 Ferrari-style 低估最严重，仍建议作为 ablation 而非最终主方法。
