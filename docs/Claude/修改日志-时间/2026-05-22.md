# 2026-05-22 修改日志

## 更新固定 P2 路径 terminal 专项验证方案

背景：

```text
用户完成两包普通 goal terminal smoke：
  slosh_Q0_20260522_151223_terminal_smoke_twostage_d150_run01.bag
  slosh_Q0_20260522_151317_terminal_smoke_twostage_d150_run02.bag

结果：
  两包均 TERMINAL_MPC_STOP -> REACHED；
  GOAL_PASSED=0；
  turnback=0；
  min_dist=0.178 / 0.194 m；
  v_des_eff_terminal 均值约 0.201 m/s；
  odom_v_terminal 均值约 0.518 m/s；
  odom_ax_max / odom_jerk_max 仍偏高。
```

判读：

```text
两段式 terminal envelope 已解决安全收敛问题；
但 terminal smoothness 仍未完全通过。

普通 goal smoke 只能证明“能停住”；
下一步应使用固定 P2_s_curve 路径验证正式路径形状下的 terminal approach。
```

修改：

```text
docs/重要文档/20260518_MPC终点收敛与固定路径验证方案.md
```

新增：

```text
## 2B. 固定 P2 路径 terminal 专项验证
```

内容：

```text
1. 明确该阶段不是 C/D/F 主效果实验：
   - 固定 P2_s_curve；
   - 固定 Q_slosh=0 的 C/base cost；
   - 只扫 terminal_slowdown_distance；
   - 只分析 terminal_approach_1s；
   - 不进入论文主表。

2. 推荐录包顺序：
   - P2_terminal_d150_run01
   - P2_terminal_d180_run01
   - 必要时 P2_terminal_d200_run01

3. 写入固定 P2 路径生成命令：
   template_fixed_path_generator.py + send_fixed_goal.py。

4. 写入 d150 / d180 / d200 的 MPC 启动与录包命令。

5. 写入专项分析命令：
   analyze_terminal_approach_1s.py --ref-v-high 0.25
   analyze_terminal_transition.py

6. 写入 PASS 标准：
   REACHED=1；
   GOAL_PASSED=0；
   recovery=0；
   odom_v_terminal <= 0.30-0.40；
   odom_ax_max < 0.8；
  odom_jerk_max 明显低于普通 goal d150 的 17-25。
```

## 更新第二阶段固定路径 MPC cost 验证方案

背景：

```text
用户询问是否需要用新版 terminal 重新跑 C/D/F。

已分析 20260520_fixed_path_cost：
  该批 bag 话题完整，能定位 v_ref -> cmd -> odom -> slosh_peak 链路；
  但它使用旧 terminal 结构；
  terminal 前 1s 仍存在 v_des_eff≈0.773m/s、cmd/odom 速度偏高和 ax/jerk 脉冲。

已分析 20260522 P2 terminal d150/d180/d200：
  d150 能停但速度太高；
  d180 有改善但 ax/jerk 仍高；
  d200 no overshoot / no turnback / REACHED，near-goal 速度最低。
```

修改：

```text
docs/重要文档/20260518_MPC终点收敛与固定路径验证方案.md
```

更新内容：

```text
1. 当前状态改为 2026-05-22 固定 P2 terminal d150/d180/d200 后的结论。
2. 第二阶段改为：
   - 用新版 d200 terminal 重跑固定 P2_s_curve；
   - 先 C/D/F 各 1 包 smoke；
   - 生成 4 窗口诊断；
   - 通过后再扩展每组 3 包。
3. 第二阶段所有 C/D/F/E/G 启动命令的 terminal_slowdown_distance 改为 2.00。
4. 第二阶段默认输出目录改为：
   /home/geist/slosh_bags/real/20260522_fixed_path_cost_d200
5. E/G 改为可选补充组，不作为第一轮必跑。
6. 明确 20260520 结果只作为旧 terminal 问题证据，不作为新版 terminal 下主效果结论。
```
