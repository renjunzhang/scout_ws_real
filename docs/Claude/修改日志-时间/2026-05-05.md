# 2026-05-05 修改日志

## 1. Step 0 路径几何审查脚本

依据：

```text
docs/Claude/修改方案-时间-简介/2026-05-01_Anti-Slosh_轨迹几何层重设计方案.md
```

目标：

```text
不改控制器；
不录新 bag；
先离线判断 P2/P3 slosh 峰值由哪些几何/执行事件触发。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/analyze_path_geometry_slosh_triggers.py
```

输入：

```text
已有 P2/P3 NOM bag
/data/a/fixed_paths/sim/{PATH_ID}.json
```

输出指标：

```text
kappa / dkappa
ay_ref proxy = v_odom^2 * kappa
alpha_ref proxy = ax_odom * kappa + v_odom^2 * dkappa
odom_ax / odom_ay / odom_jerk
track_dist / heading_err
height / eta_dot / energy 峰值前 1.0s 窗口
```

验证：

```text
python3 -m py_compile analyze_path_geometry_slosh_triggers.py  PASS
git diff --check                                                PASS
P2/P3 各 1 包 smoke run                                         PASS
```

## 2. Step 0 首轮诊断

运行数据：

```text
P2_s_curve NOM:
  /data/a/slosh_bags/sim/20260427/20260427_P2_s_curve_NOM_run16_221711.bag
  /data/a/slosh_bags/sim/20260427/20260427_P2_s_curve_NOM_run17_221911.bag
  /data/a/slosh_bags/sim/20260427/20260427_P2_s_curve_NOM_run18_222058.bag

P3_mixed NOM:
  /data/a/slosh_bags/sim/20260427/20260427_P3_mixed_NOM_run01_235027.bag
  /data/a/slosh_bags/sim/20260427/20260427_P3_mixed_NOM_run02_235223.bag
  /data/a/slosh_bags/sim/20260427/20260427_P3_mixed_NOM_run03_235358.bag
```

CSV：

```text
docs/Claude/分析数据/2026-05-05_step0_path_geometry_triggers_p2_p3_nom.csv
docs/Claude/分析数据/2026-05-05_step0_path_geometry_triggers_p2_p3_nom_eta_dot.csv
```

height 峰值口径 group summary：

```text
P2_s_curve NOM:
  k_p95=2.365
  dk_p95=20.640
  ax_p95=9.127
  ay_p95=3.383
  track_p95=1.369
  hints:
    high_kappa + sharp_dkappa + longitudinal_timing
    longitudinal_timing
    longitudinal_timing + lateral_excitation

P3_mixed NOM:
  k_p95=4.199
  dk_p95=45.917
  ax_p95=10.683
  ay_p95=1.705
  track_p95=0.922
  hints:
    longitudinal_timing
    high_kappa + sharp_dkappa + longitudinal_timing + lateral_excitation
```

eta_dot 峰值口径 group summary：

```text
P2_s_curve NOM:
  k_p95=2.314
  dk_p95=21.879
  ax_p95=8.271
  ay_p95=3.380
  track_p95=1.361

P3_mixed NOM:
  k_p95=4.725
  dk_p95=48.515
  ax_p95=9.616
  ay_p95=1.532
  track_p95=0.913
```

## 3. 初步判断

P2：

```text
早期峰：存在 high_kappa / sharp_dkappa 和纵向 ax；
后段峰：longitudinal_timing + lateral_excitation，更接近速度/转向执行与 tracking 共同触发。
```

P3：

```text
第一峰：几何 kappa/dkappa 很低，但 ax_p95 很高，属于纵向 timing/加速度脉冲；
后续峰：高 kappa + 高 dkappa + longitudinal_timing + lateral_excitation 同时出现。
```

对后续 candidate 的含义：

```text
只做 timing retiming 不够；
只做 waypoint smoothing 也不够；
需要分段策略：
  1. 起步/早期段：降低 ax / jerk，加入 rest-to-rest 或低 jerk timing；
  2. 转弯/混合段：降低 kappa 和 dkappa，优先 turn radius inflation + waypoint smoothing；
  3. P2 后段：必须检查 tracking 放大，不能只看 fixed path 几何。
```

下一步：

```text
进入 Step 1 前，先把候选生成限制为 C1+C2+C3 三类低复杂度 candidate；
不写在线控制器；
不录新 bag。
```

## 4. Step 1 第一小步：离线路径几何候选生成

目标：

```text
只生成离线 candidate path；
只做几何指标对比；
暂不做 timing retiming；
暂不做 slosh rollout；
不接 ROS，不改 MPC，不录 bag。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/generate_anti_slosh_path_candidates.py
```

输入：

```text
/data/a/fixed_paths/sim/P2_s_curve.json
/data/a/fixed_paths/sim/P3_mixed.json
```

输出：

```text
/data/a/fixed_paths/candidates/P2_s_curve_smooth.json
/data/a/fixed_paths/candidates/P2_s_curve_radius.json
/data/a/fixed_paths/candidates/P3_mixed_smooth.json
/data/a/fixed_paths/candidates/P3_mixed_radius.json
```

几何对比 CSV：

```text
docs/Claude/分析数据/2026-05-05_step1_candidate_geometry_summary.csv
```

候选定义：

```text
C1 smooth:
  轻量 waypoint smoothing；
  目标是压 dkappa；
  最大漂移约 2-3 cm。

C2 radius:
  更强 smoothing / turn radius inflation proxy；
  目标是进一步压 kappa / dkappa；
  最大漂移约 5-6 cm。
```

暂缓 C3：

```text
C3 low-jerk timing / terminal rest-to-rest 需要生成 v(s) 或 p(t)；
应在几何候选通过基础指标后再做；
否则会混淆“路径几何是否变好”和“timing 是否变好”。
```

结果：

```text
P2 original_resampled:
  length=9.180
  kappa_p95=1.565
  kappa_max=2.614
  dkappa_p95=4.422
  dkappa_max=13.075

P2 smooth:
  length=9.147
  kappa_p95=1.231
  kappa_max=1.387
  dkappa_p95=2.615
  dkappa_max=4.284
  max_drift=0.023

P2 radius:
  length=9.098
  kappa_p95=1.109
  kappa_max=1.293
  dkappa_p95=2.102
  dkappa_max=2.938
  max_drift=0.052

P3 original_resampled:
  length=9.271
  kappa_p95=1.742
  kappa_max=2.443
  dkappa_p95=4.713
  dkappa_max=19.555

P3 smooth:
  length=9.208
  kappa_p95=1.469
  kappa_max=1.654
  dkappa_p95=2.881
  dkappa_max=6.910
  max_drift=0.025

P3 radius:
  length=9.116
  kappa_p95=1.271
  kappa_max=1.451
  dkappa_p95=3.084
  dkappa_max=4.405
  max_drift=0.061
```

验证：

```text
python3 -m py_compile generate_anti_slosh_path_candidates.py  PASS
git diff --check                                              PASS
dry-run P2/P3                                                 PASS
写入 /data/a/fixed_paths/candidates                           PASS
起终点 position / orientation 保持原始 fixed path              PASS
```

判断：

```text
C1/C2 均能显著降低 kappa 和 dkappa；
candidate 没有大幅偏离原路径；
可以进入 Step 2 的离线 rollout / tracking proxy 验证。
```

## 5. Step 2 第一版：离线 signed slosh rollout + 几何 proxy

目标：

```text
对 original / smooth / radius 做统一速度下的 signed slosh rollout；
只验证路径几何对 ay / alpha / modal rollout 的影响；
暂不混入 C3 timing retiming。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/evaluate_anti_slosh_path_candidates.py
```

口径：

```text
v_ref = 1.2 m/s constant
linear modal slosh rollout
input:
  eta_x: ux = 0
  eta_y: uy = v_ref^2 * signed kappa(s)
alpha proxy = v_ref^2 * dkappa(s)
```

输出：

```text
docs/Claude/分析数据/2026-05-05_step2_candidate_rollout_proxy.csv
```

结果：

```text
P2 original:
  h_p95=0.009917
  energy_p95=0.05346
  eta_dot_p95=0.1691
  ay_p95=2.833
  alpha_p95=39.82

P2 smooth:
  h_p95 -65.5%
  energy_p95 -92.9%
  eta_dot_p95 0.01744
  ay_p95 -34.9%
  alpha_p95 4.55

P2 radius:
  h_p95 -70.0%
  energy_p95 -94.4%
  eta_dot_p95 0.02035
  ay_p95 -41.1%
  alpha_p95 4.19

P3 original:
  h_p95=0.010737
  energy_p95=0.05721
  eta_dot_p95=0.1980
  ay_p95=3.764
  alpha_p95=47.39

P3 smooth:
  h_p95 -62.4%
  energy_p95 -90.3%
  eta_dot_p95 0.02816
  ay_p95 -34.5%
  alpha_p95 4.66

P3 radius:
  h_p95 -68.2%
  energy_p95 -93.4%
  eta_dot_p95 0.02443
  ay_p95 -38.0%
  alpha_p95 5.15
```

判断：

```text
C1/C2 在几何诱导 lateral slosh rollout 上均为强正信号；
radius 对 h_p95 / energy / ay 降幅最大；
smooth 的 dkappa/alpha 也明显改善；
二者都值得进入下一步。
```

限制：

```text
该 proxy 没有包含 Step 0 发现的起步/纵向 ax 脉冲；
因此不能说已经解决 P2/P3；
只能说明“改变几何路径”比继续固定路径 retiming 更有希望。
```

下一步：

```text
补 C3 low-jerk timing / terminal rest-to-rest proxy；
把 C1/C2 几何候选和 C3 timing 组合后，再做完整 signed ax/ay rollout。
```

## 6. Step 2 第二版：补 signed ax / low-jerk timing proxy

目标：

```text
把 rollout 从“只看几何 lateral ay”扩展到 signed ax / ay 双通道；
同时加入一个最简单 C3 low-jerk start/end timing proxy；
检查 C1/C2 几何候选在加入纵向加速度后是否仍有正信号。
```

修改脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/evaluate_anti_slosh_path_candidates.py
```

新增口径：

```text
--timing-mode constant | low_jerk | both
low_jerk:
  起点和终点各 1.0m smootherstep ramp
  rollout dt 使用 v_floor=0.15 防止零速发散

rollout input:
  eta_x: ax = (v_i^2 - v_{i-1}^2) / (2 ds)
  eta_y: ay = v_i^2 * signed kappa_i
  alpha = ax * kappa + v_i^2 * dkappa
```

输出：

```text
docs/Claude/分析数据/2026-05-05_step2_candidate_rollout_proxy_with_timing.csv
```

结果摘要：

```text
P2 original constant:
  time=7.65s h_p95=0.009917 energy_p95=0.05346 ay_p95=2.835

P2 original low_jerk:
  time=12.10s h_p95 -8.8% energy_p95 -8.8% ay_p95 -29.1%
  time +58% -> FAIL

P2 smooth constant:
  h_p95 -65.5% energy_p95 -92.9% ay_p95 -34.8% alpha_p95 4.61

P2 smooth low_jerk:
  time=12.08s h_p95 -65.2% energy_p95 -93.2% ay_p95 -41.0%
  time +58% -> FAIL

P2 radius constant:
  h_p95 -70.0% energy_p95 -94.4% ay_p95 -41.2% alpha_p95 4.22

P2 radius low_jerk:
  time=12.04s h_p95 -66.9% energy_p95 -93.9% ay_p95 -59.4%
  time +59% -> FAIL

P3 original low_jerk:
  time=12.18s h_p95 ~0% energy_p95 ~0%
  time +58% -> FAIL

P3 smooth constant:
  h_p95 -62.4% energy_p95 -90.3% ay_p95 -34.0% alpha_p95 4.67

P3 radius constant:
  h_p95 -68.2% energy_p95 -93.4% ay_p95 -37.9% alpha_p95 5.16
```

判断：

```text
C1/C2 几何候选在 full signed ax/ay rollout 下仍然是强正信号；
但当前 naive low_jerk ramp 不是可用方案，因为 time_proxy 约 +58%，远超 +15% 门槛；
不能把 C3 当前实现拿去闭环仿真。
```

下一步：

```text
不再用“起终点 1m 硬 ramp”作为 C3；
下一步应做 matched-time timing 生成：
  约束 time_proxy <= original * 1.15
  保留 C1/C2 的 kappa/dkappa 降幅
  只在起步、终端和 Step0 标出的 peak lookback 窗口内做局部低 jerk 修正

闭环仿真进入条件仍未满足；
当前只允许继续离线 candidate/timing 设计。
```

## 7. Step 2C：matched-time timing 初筛

继续扫描 shorter ramp：

```text
ramp=0.10m:
  time 增量小，但 energy / eta_dot 经常反向变差；
  说明过短 ramp 会制造纵向模态激励。

ramp=0.25m:
  time 约 +14%，满足 +15%；
  C1/C2 对 h / energy / eta_dot / ay 仍有明显正收益；
  但 ax_max≈6.1~6.5，jerk_max≈104，不能称为 low-jerk。

ramp=0.50m / 0.75m:
  ax_max / jerk_max 下降；
  但 v_ref=1.2 时 time 约 +29% / +43%，不满足 +15%。
```

因此做了一次 matched-time proxy：

```text
候选：
  C1/C2 smooth/radius
  v_ref = 1.4 m/s
  ramp = 0.50m

目的：
  利用 C1/C2 降低 kappa 后释放的 ay 余量；
  在 ay_p95 不超过 original 的前提下，把 time 拉回 +15% 内。
```

输出：

```text
docs/Claude/分析数据/2026-05-05_step2_candidate_rollout_proxy_v140_ramp050.csv
```

结果：

```text
P2 smooth:
  time +13.9%
  h_p95 -46.2%
  energy_p95 -83.4%
  eta_dot_p95 -74.0%
  ay_p95 -13.7%
  ax_max=4.61 jerk_max=48.6

P2 radius:
  time +13.5%
  h_p95 -50.6%
  energy_p95 -83.8%
  eta_dot_p95 -75.3%
  ay_p95 -20.7%
  ax_max=4.61 jerk_max=48.6

P3 smooth:
  time +13.0%
  h_p95 -44.0%
  energy_p95 -79.5%
  eta_dot_p95 -77.2%
  ay_p95 -10.1%
  ax_max=4.60 jerk_max=48.6

P3 radius:
  time +12.0%
  h_p95 -51.3%
  energy_p95 -85.7%
  eta_dot_p95 -80.0%
  ay_p95 -15.4%
  ax_max=4.60 jerk_max=48.6
```

判断：

```text
这是本轮第一个满足 time_proxy <= +15%、ay_p95 不升、predicted slosh 全指标下降的离线组合；
但 ax_max / jerk_max 仍高，说明它只能作为 Step 2 正信号，不能直接进入闭环录 bag。
```

下一步：

```text
在 v_ref≈1.35~1.45、ramp≈0.45~0.65m 范围内做小网格；
筛选条件：
  time <= +15%
  ay_p95 <= original
  h / energy / eta_dot 全下降
  ax_max 和 jerk_max 相对 v=1.4/ramp=0.5 继续下降

若找不到更低 ax/jerk 的组合，再进入路径几何 C1/C2 的闭环仿真前，应把 ax/jerk 风险明确标为 residual risk。
```

## 8. Step 2D：timing 小网格筛选

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/sweep_anti_slosh_timing_candidates.py
```

作用：

```text
读取 original P2/P3 baseline；
对 smooth/radius candidate 扫 v_ref 和 ramp；
输出 time / h / energy / eta_dot / ay pass/fail；
按 ax_max / jerk_max 排序，筛出能进入下一步的离线组合。
```

输出：

```text
docs/Claude/分析数据/2026-05-05_step2c_timing_candidate_sweep.csv
```

扫描范围：

```text
v_ref = 1.35 / 1.40 / 1.45
ramp = 0.45 / 0.50 / 0.60 / 0.65
```

pass 条件：

```text
time_delta <= +15%
h_p95 / energy_p95 / eta_dot_p95 全下降
ay_p95 不升
```

每组最小 jerk pass：

```text
P2 smooth:
  v=1.40 ramp=0.50
  time +13.9%
  h -46.2% E -83.4% eta_dot -74.0% ay -13.8%
  ax_max=4.61 jerk_max=48.6

P2 radius:
  v=1.40 ramp=0.50
  time +13.5%
  h -50.6% E -83.8% eta_dot -75.3% ay -20.7%
  ax_max=4.61 jerk_max=48.6

P3 smooth:
  v=1.40 ramp=0.50
  time +13.0%
  h -44.0% E -79.5% eta_dot -77.2% ay -10.1%
  ax_max=4.60 jerk_max=48.6

P3 radius:
  v=1.45 ramp=0.60
  time +14.4%
  h -48.1% E -83.8% eta_dot -83.0% ay -9.3%
  ax_max=4.15 jerk_max=40.0
```

统一参数可用组合：

```text
v=1.40 ramp=0.50 同时通过 P2/P3 smooth/radius；
如果先追求最小复杂度，推荐用 radius + v=1.40 + ramp=0.50 作为第一闭环候选。
```

当前判断：

```text
Step 2 已经出现完整离线 pass 组合；
但 ax_max≈4.6、jerk_max≈48.6 仍是闭环风险；
进入 ROS 仿真前，需要确认 fixed_path runner 是否支持候选路径文件和 v_ref/ramp 参数注入。
```

## 9. 闭环入口最小补丁

检查结果：

```text
run_sim_fixed_path_bag.sh 已支持 PATH_FILE；
因此可以直接加载 /data/a/fixed_paths/candidates/*_radius.json。
```

缺口：

```text
离线候选 v_ref=1.40；
local_planner_ros 内部使用 v_nominal = vehicle/v_max * 0.8；
原 launch 未暴露 vehicle/v_max，无法从命令行对齐 v_nominal。
```

最小修改：

```text
launch/slosh_experiment_sim.launch:
  新增 arg vehicle_v_max，默认 3.0；
  覆盖 param vehicle/v_max。

scripts/run_sim_fixed_path_bag.sh:
  新增 VEHICLE_V_MAX 环境变量，默认 3.0；
  透传到 roslaunch；
  写入 config summary 和 /experiment/config_summary。
```

推荐第一闭环 smoke 命令：

```bash
PATH_ID=P2_s_curve_radius CONDITION=CUSTOM RUN_ID=geom01 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P2_s_curve_radius.json VEHICLE_V_MAX=1.75 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

P3：

```bash
PATH_ID=P3_mixed_radius CONDITION=CUSTOM RUN_ID=geom01 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P3_mixed_radius.json VEHICLE_V_MAX=1.75 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

注意：

```text
CONDITION=CUSTOM 只用于关闭旧 anti-slosh controller；
真正变化来自候选 PATH_FILE + VEHICLE_V_MAX；
不是重新启用 PROFILE_ENERGY / PMG / MPC slosh cost。
```

验证：

```text
bash -n run_sim_fixed_path_bag.sh                    PASS
xmllint --noout slosh_experiment_sim.launch          PASS
py_compile offline scripts                           PASS
git diff --check                                     PASS
```

## 10. radius path 闭环 smoke 结果

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P2_s_curve_radius_CUSTOM_rungeom01_133337.bag
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_CUSTOM_rungeom01_133631.bag
```

分析输出：

```text
docs/Claude/分析数据/2026-05-05_geom_smoke_metrics.csv
docs/Claude/分析数据/2026-05-05_geom_smoke_peak_windows.csv
docs/Claude/分析数据/2026-05-05_step2d_timing_sweep_true_nom_speed.csv
```

对比基线：

```text
P2 NOM:
  20260427_P2_s_curve_NOM_run16/17/18

P3 NOM:
  20260427_P3_mixed_NOM_run01/02/03
```

P2 radius CUSTOM vs NOM 均值：

```text
tracking_time:       5.90s -> 7.95s   (+34.7%)  FAIL
h_rms:               -47.2%
h_p95:               -49.4%
h_max:               -28.2%
eta_dot_rms:         -38.3%
modal_energy_rms:    -46.6%
odom_ay_p95:         -46.9%
track_dist_p95:      +72.9%           FAIL
solve_success_ratio: 0.994 -> 0.987
v_des_eff_mean:      2.37 -> 1.40     (-41.1%)
```

P3 radius CUSTOM vs NOM 均值：

```text
tracking_time:       6.02s -> 6.30s   (+4.7%)   PASS
h_rms:               -16.6%
h_p95:               -11.9%
h_max:               -23.7%
eta_dot_rms:         -18.2%
modal_energy_rms:    -16.8%
odom_ay_p95:         -6.1%
track_dist_p95:      -31.2%
solve_success_ratio: 1.000 -> 1.000
v_des_eff_mean:      2.37 -> 1.40     (-40.9%)
```

结论：

```text
P3 是正信号：在不启用旧 anti-slosh controller 的情况下，
radius path + v_nominal≈1.40 同时降低 h / eta_dot / energy / ay / track_dist，并且时间 +4.7%。

P2 不通过：虽然 slosh 全指标显著下降，但 tracking_time +34.7%，track_dist_p95 +72.9%。
不能声称 P2 成功。
```

关键修正：

```text
Step 2D 离线 sweep 的 baseline 速度口径有误：
  离线 baseline 使用 v_ref=1.2；
  实际 NOM bag 的 v_des_eff_mean≈2.37。

因此之前 “v=1.40 ramp=0.50 满足 time<=+15%” 只相对错误低速 baseline 成立；
不能作为真实闭环验收依据。
```

用真实 NOM 速度口径重跑离线 sweep：

```text
baseline_v_ref=2.4
candidate v_ref=1.6/1.8/2.0/2.2/2.4
ramp=0.20/0.30/0.40/0.50

结果：
  pass_all = 0 / 40
```

判断：

```text
当前 radius path 在低速下能降晃；
但尚未证明能在真实 NOM 时间 +15% 内降晃。
下一步不能继续用 VEHICLE_V_MAX=1.75 录包。
```

建议下一步：

```text
只做一轮速度口径校准，不改 MPC：
  P2 radius VEHICLE_V_MAX=2.05 或 2.10

目标：
  tracking_time <= 6.8s
  h / eta_dot / energy 仍下降
  track_dist 不继续恶化

若 P2 仍不通过：
  radius path 只能作为“慢速低激励路径”负结果；
  不能进入论文主方法。
```

## 11. P2 radius 速度校准 v205

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P2_s_curve_radius_CUSTOM_rungeom_v205_134622.bag
```

分析输出：

```text
docs/Claude/分析数据/2026-05-05_p2_radius_speed_calibration_metrics.csv
docs/Claude/分析数据/2026-05-05_p2_radius_v205_peak_windows.csv
```

P2 radius v205 vs P2 NOM 均值：

```text
tracking_time:       5.90s -> 6.70s   (+13.6%)  PASS
h_rms:               -27.4%           PASS
h_p95:               -38.0%           PASS
h_max:               +16.9%           FAIL
eta_dot_rms:         +3.8%            FAIL
modal_energy_rms:    -24.9%           PASS
odom_ay_p95:         -41.4%           PASS
track_dist_p95:      +56.7%           FAIL
solve_success_ratio: 0.994 -> 0.993   OK
v_des_eff_mean:      2.37 -> 1.64     (-31.1%)
```

对比 v175：

```text
v175:
  tracking_time +34.7%
  h_p95 -49.4%
  eta_dot -38.3%

v205:
  tracking_time +13.6%
  h_p95 -38.0%
  eta_dot +3.8%
```

peak-window 诊断：

```text
主峰 height@11.53s = 0.008831
hint = longitudinal_timing + tracking_amplification
1s窗口:
  ax_p95=6.955
  ay_p95=1.860
  jerk_p95=946.876
  track=1.900
  eta_dot=0.06982
```

结论：

```text
v205 是清晰边界点：
  时间约束通过；
  h_p95 / energy / ay 通过；
  但 h_max、eta_dot、track_dist 失败。

这说明 radius path 不是简单“速度调高一点就能通过”；
P2 失败机理变成了纵向 ax 脉冲与 tracking amplification。
```

执行决策：

```text
停止继续录同类 P2 radius speed calibration；
不再扫 VEHICLE_V_MAX；
不把 radius path 作为论文主方法成功结果。
```

当前可写结论：

```text
几何降激励路径在 P3 上给出正信号；
在 P2 上存在速度-晃动-跟踪三者 trade-off：
  低速能降晃但时间失败；
  提速能过时间但 eta_dot / h_max / tracking 失败。

因此“只做几何 smoothing / radius inflation”仍不足以形成通用成功方案。
```

## 12. 升级 05-01 轨迹几何层方案

已更新：

```text
docs/Claude/修改方案-时间-简介/2026-05-01_Anti-Slosh_轨迹几何层重设计方案.md
```

核心修正：

```text
低复杂度 C1/C2/C3 版本失败；
轨迹/参考生成层方向未失败。
```

新增 V2：

```text
Constrained anti-slosh reference optimization
```

V2 不再做：

```text
手工 smoothing；
手工 turn radius inflation；
继续扫 VEHICLE_V_MAX；
继续调 PROFILE_ENERGY；
继续录同类 P2 radius bag。
```

V2 要求在离线层同时优化：

```text
p(s)
v(s) / t(s)
```

并显式约束：

```text
time
trackability
ax / jerk
kappa / dkappa
terminal residual
modal slosh response
```

下一步如果继续实现，应先做：

```text
Step V2-0:
  从真实 NOM bag 提取 baseline_time / v_des_eff / h / eta_dot / energy / ay / track_dist；
  修正所有离线 pass/fail 口径，禁止再用 v_ref=1.2 作为假 baseline。

Step V2-1:
  写 optimize_anti_slosh_reference.py；
  先离线搜索 constrained candidate；
  不写控制器代码。
```

## 13. V2-0 / V2-1 第一版执行

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/optimize_anti_slosh_reference.py
```

作用：

```text
从真实 NOM bag metrics 提取 baseline；
对 original / smooth / radius 路径做 constrained reference search；
搜索 cruise_speed × ay_ratio × accel_limit；
输出 time / h / h_max / eta_dot / ay hard gates。
```

输入：

```text
docs/Claude/分析数据/2026-05-05_geom_smoke_metrics.csv
/data/a/fixed_paths/sim/P2_s_curve.json
/data/a/fixed_paths/candidates/P2_s_curve_smooth.json
/data/a/fixed_paths/candidates/P2_s_curve_radius.json
/data/a/fixed_paths/sim/P3_mixed.json
/data/a/fixed_paths/candidates/P3_mixed_smooth.json
/data/a/fixed_paths/candidates/P3_mixed_radius.json
```

输出：

```text
docs/Claude/分析数据/2026-05-05_v2_nom_baseline.csv
docs/Claude/分析数据/2026-05-05_v2_reference_optimization_sweep_tracking_window.csv
```

真实 NOM baseline：

```text
P2_s_curve:
  n=3
  tracking_time=5.90s
  h_p95=0.006105
  h_max=0.007554
  eta_dot_rms=0.014333
  ay_p95=2.516
  track_dist_p95=1.245
  v_des_eff_mean=2.374

P3_mixed:
  n=3
  tracking_time=6.02s
  h_p95=0.005335
  h_max=0.008507
  eta_dot_rms=0.014881
  ay_p95=1.463
  track_dist_p95=0.736
  v_des_eff_mean=2.366
```

第一版搜索结果：

```text
pass_all = 0 / 270
```

分解：

```text
P2:
  pass_time = 124 / 135
  pass_h = 57 / 135
  pass_eta_dot = 0 / 135
  pass_ay = 111 / 135
  time+h+ay = 50 / 135

P3:
  pass_time = 24 / 135
  pass_h = 58 / 135
  pass_eta_dot = 0 / 135
  pass_ay = 90 / 135
  time+h+ay = 0 / 135
```

代表性边界：

```text
P2 radius v=1.6 ay_ratio=0.6 accel=1.0:
  time +7.4%
  h_p95 -49.4%
  h_max -38.1%
  ay -41.9%
  eta_dot +8.1%   -> FAIL

P2 radius v=1.8 ay_ratio=0.6 accel=1.0:
  time +0.2%
  h_p95 -49.4%
  h_max -38.1%
  ay -41.9%
  eta_dot +9.5%   -> FAIL

P3 radius v=1.6 ay_ratio=1.0 accel=1.0:
  time +14.8%
  h_p95 -32.8%
  h_max -42.3%
  ay +4.6%        -> FAIL
  eta_dot +63.9%  -> FAIL
```

重要修正：

```text
初版 optimizer 曾把起点/终点速度强制为 0；
这与当前验收口径 TRACKING + terminal NONE 不一致，会高估 time_proxy。
已改为默认不强制端点零速；
terminal residual 后续应作为独立指标，不混入主段 time gate。
```

当前判断：

```text
V2 第一版没有找到可行 candidate；
主要卡在 eta_dot hard gate。
但该结论不能直接等价为“轨迹优化方向失败”，因为 P3 闭环 smoke 实际 eta_dot 下降，而离线 rollout 对 P3 eta_dot 预测为上升。
```

因此新的 blocker 是：

```text
当前 linear rollout 对 eta_dot 的离线 fidelity 不足；
它可以作为 h / ay / geometry 风险筛选工具；
不能单独作为 eta_dot hard gate 决定是否进闭环。
```

下一步需要商讨：

```text
选项 A：
  修正 eta_dot rollout fidelity，再继续 V2 offline optimizer；

选项 B：
  把 eta_dot gate 从离线 hard gate 降级为 closed-loop gate；
  允许 time+h+h_max+ay 通过的候选进入极少量闭环；

选项 C：
  停止当前模型优化路线，转向真液面视觉/更高保真 slosh model。
```

## V2-2：按选项 A 修正 eta_dot rollout fidelity

目标：

```text
解释 V2 第一版为什么 eta_dot hard gate 全部失败；
避免把时间戳/口径错误误判为“轨迹优化方向失败”。
```

诊断：

```text
offline_pmg_replay.py --profile-energy-rollout
bags:
  P2 NOM ×3
  P3 NOM ×3
  P2 radius CUSTOM v205 ×1
  P3 radius CUSTOM ×1
```

输入 shift 扫描：

```text
easp_input_shift=-0.05:
  PASS 0/8
  eta_dot_corr_mean=0.511
  eta_dot_best_corr_mean=0.954 @ +0.10s

easp_input_shift=0.00:
  PASS 0/8
  eta_dot_corr_mean=0.117
  eta_dot_best_corr_mean=0.951 @ +0.05s

easp_input_shift=+0.05:
  PASS 8/8
  h_p95_err_mean=0.022
  peak_dt_mean=0.000s
  eta_dot_corr_mean=0.971
  risk_recall_mean=1.000
```

结论：

```text
eta_dot 离线 replay 失败的主因是 /slosh/ax_est、/slosh/ay_est 与 /slosh/state 的时间戳对齐偏差；
不是 slosh ODE 本身不能重建 eta_dot。
```

代码修正：

```text
src/scout_apps/control/scout_local_planner/scripts/offline_pmg_replay.py
  --easp-input-shift default: 0.00 -> +0.05

src/scout_apps/control/scout_local_planner/scripts/optimize_anti_slosh_reference.py
  slosh hard gate 改为模型内相对比较：
    candidate rollout vs original-path rollout
  保留真实 NOM baseline 用于 time / ay gate。
```

验证：

```text
python3 -m py_compile offline_pmg_replay.py optimize_anti_slosh_reference.py
offline_pmg_replay.py 默认 shift 复跑 8 包：PASS 8/8
```

修正后 V2 optimizer：

```text
output:
  docs/Claude/分析数据/2026-05-05_v2_nom_baseline_model_gate.csv
  docs/Claude/分析数据/2026-05-05_v2_reference_optimization_model_gate.csv

pass_all = 93 / 270

P2_s_curve:
  pass_all = 91 / 135
  original=11, smooth=35, radius=45

P3_mixed:
  pass_all = 2 / 135
  original=0, smooth=0, radius=2
```

P3 top candidates：

```text
P3 radius v=2.4 ay_ratio=0.8 accel=2.0:
  time +13.8%
  h_p95 -69.0%
  eta_dot -61.6%
  ay -10.5%

P3 radius v=2.2 ay_ratio=0.8 accel=2.0:
  time +14.3%
  h_p95 -69.8%
  eta_dot -61.4%
  ay -10.5%
```

当前判断：

```text
V2 不是已失败；已从“eta_dot gate 口径错误”恢复到“有少量离线候选待闭环 smoke”。
下一步不要大规模录包，只选 1 个 P3 radius top candidate + 1 个 P2 radius sanity candidate 做闭环 smoke。
```

## V2-3：闭环 smoke 运行入口

目标：

```text
把 V2 离线候选映射到当前仿真运行脚本；
只做 reference/profile 层验证，不重新引入 MPC slosh cost 或 output cap。
```

代码修改：

```text
src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
  新增 CONDITION=PROFILE_REF_V2
  Q_slosh=0
  Q_slosh_eta_dot=0
  risk_scheduler=false
  input_shaping=false
  energy_profile_enable=true
  energy_profile_omega_max=999
  energy_profile_alpha_max=999
  energy_profile_min_v=0

src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
  KNOWN_CONDITIONS 增加 PROFILE_REF_V2
```

V2 candidate 到运行参数的映射：

```text
cruise_speed = vehicle_v_max * 0.8
ay_limit     = ENERGY_PROFILE_LAT_ACCEL
accel_limit  = ENERGY_PROFILE_AX_MAX
decel_limit  = ENERGY_PROFILE_DECEL_MAX * 0.8  # C++ PathHandler 内有 decel_safety_factor=0.8
path         = /data/a/fixed_paths/candidates/*_radius.json
```

P3 top smoke 命令：

```bash
PATH_ID=P3_mixed_radius CONDITION=PROFILE_REF_V2 RUN_ID=smoke01 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P3_mixed_radius.json \
VEHICLE_V_MAX=3.0 ENERGY_PROFILE_LAT_ACCEL=1.1701333333333332 ENERGY_PROFILE_AX_MAX=2.0 ENERGY_PROFILE_DECEL_MAX=2.5 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

对应离线候选：

```text
P3 radius v=2.4 ay_ratio=0.8 accel=2.0:
  time +13.8%
  h_p95 -69.0%
  eta_dot -61.6%
  ay -10.5%
```

P2 sanity smoke 命令：

```bash
PATH_ID=P2_s_curve_radius CONDITION=PROFILE_REF_V2 RUN_ID=smoke01 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P2_s_curve_radius.json \
VEHICLE_V_MAX=3.0 ENERGY_PROFILE_LAT_ACCEL=2.5156666666666667 ENERGY_PROFILE_AX_MAX=1.0 ENERGY_PROFILE_DECEL_MAX=1.25 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

对应离线候选：

```text
P2 radius v=2.4 ay_ratio=1.0 accel=1.0:
  time -20.3%
  h_p95 -83.9%
  eta_dot -88.1%
  ay -11.5%
```

验证：

```text
bash -n run_sim_fixed_path_bag.sh
python3 -m py_compile extract_slosh_metrics.py optimize_anti_slosh_reference.py
```

## V2-4：PROFILE_REF_V2 闭环 smoke 结果

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runsmoke01_143137.bag
/data/a/slosh_bags/sim/20260505/20260505_P2_s_curve_radius_PROFILE_REF_V2_runsmoke01_143352.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_profile_ref_v2_smoke_metrics.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_peak_windows.csv
```

NOM baseline 均值：

```text
P2_s_curve:
  tracking_time=5.90s
  h_rms=0.003092
  h_p95=0.006105
  h_max=0.007554
  eta_dot_rms=0.014333
  energy_norm_rms=0.054979
  ay_p95=2.516
  track_p95=1.245
  solve_success=0.994

P3_mixed:
  tracking_time=6.02s
  h_rms=0.002851
  h_p95=0.005335
  h_max=0.008507
  eta_dot_rms=0.014881
  energy_norm_rms=0.051178
  ay_p95=1.463
  track_p95=0.736
  solve_success=1.000
```

P2 PROFILE_REF_V2 smoke：

```text
tracking_time: 5.60s  (-5.1%)   PASS
h_rms:        +10.5%            FAIL
h_p95:        +3.9%             FAIL
h_max:        +19.7%            FAIL
eta_dot_rms:  -2.8%             weak positive
energy_rms:   +9.6%             FAIL
ay_p95:       +1.9%             FAIL
track_p95:    +38.6%            FAIL
solve_success=0.982             below previous 0.994 baseline
```

P3 PROFILE_REF_V2 smoke：

```text
tracking_time: 5.35s  (-11.1%)  PASS
h_rms:        +4.3%             FAIL
h_p95:        +12.0%            FAIL
h_max:        -8.5%             weak positive
eta_dot_rms:  +22.5%            FAIL
energy_rms:   +6.0%             FAIL
ay_p95:       +18.3%            FAIL
track_p95:    -24.2%            PASS
solve_success=1.000             PASS
```

峰值窗口诊断：

```text
P2 peak2 height@11.390s:
  hint=longitudinal_timing+lateral_excitation+tracking_amplification
  ax_p95=7.896
  ay_p95=2.556
  jerk_p95=930.461
  track=1.776
  eta_dot=0.04006

P3 peak1 height@7.790s:
  hint=longitudinal_timing
  ax_p95=11.086
  ay_p95=0.094
  jerk_p95=1125.685
  track=0.489

P3 peak2 height@9.990s:
  hint=longitudinal_timing
  ax_p95=5.294
  ay_p95=1.652
  jerk_p95=571.856
  eta_dot=0.06180
```

结论：

```text
PROFILE_REF_V2 闭环 smoke 未通过。
失败不是参数未注入：txt 配置确认 ENERGY_PROFILE_ENABLE=true 且 V2 参数正确。
失败主因是 execution fidelity：
  离线 candidate 假设 ax_limit=2.0 / ay_limit=1.17(P3)
  闭环峰值窗口出现 ax_p95=5~11、jerk_p95=570~1125、P3 ay_p95=1.73
```

当前状态：

```text
V2 reference generation 离线有候选；
但当前 MPC 执行层不能按该 reference 限制真实 ax / ay / jerk。
继续只调 PathHandler profile 参数意义不大。
下一步若继续该路线，必须先做 execution-fidelity 修正：
  1. 记录/发布参考序列中的 v_ref、v_path、kappa；
  2. 比较 ref_ax/ref_ay 与 odom_ax/odom_ay；
  3. 明确 MPC 为什么在 v_ref profile 下仍打出 5~11 m/s^2 ax 脉冲。
```

## V2-5：reference execution fidelity instrumentation

目标：

```text
不改控制律；
只记录 MPC 输入 reference 的低激励假设是否真的进入执行层；
下一包用于区分：
  A. PathHandler 生成的 ref 本身已高激励；
  B. ref 低激励，但 MPC/底盘执行成高 ax / ay / jerk。
```

代码修改：

```text
src/scout_apps/control/scout_local_planner/include/scout_local_planner/local_planner_ros.h
src/scout_apps/control/scout_local_planner/src/local_planner_ros.cpp
  新增 publishReferenceExecutionDebug(ref_points)

src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
  rosbag topics 增加 /reference/*

src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py
  读取 /reference/*，输出 ref/odom execution fidelity 字段
```

新增 topic：

```text
/reference/v_ref
/reference/v_path
/reference/kappa
/reference/s
/reference/implied_ax
/reference/implied_ay
/reference/implied_jerk
/reference/implied_ax_abs_p95
/reference/implied_ay_abs_p95
/reference/implied_jerk_abs_p95
```

`extract_slosh_metrics.py` 新增字段：

```text
ref_v_ref_mean
ref_v_path_mean
ref_kappa_abs_p95
ref_implied_ax_abs_p95
ref_implied_ay_abs_p95
ref_implied_jerk_abs_p95
odom_ax_abs_p95
odom_jerk_abs_p95
exec_ax_to_ref_p95_ratio
exec_ay_to_ref_p95_ratio
exec_jerk_to_ref_p95_ratio
```

验证：

```text
python3 -m py_compile extract_slosh_metrics.py
bash -n run_sim_fixed_path_bag.sh
git diff --check
catkin_make -DCATKIN_WHITELIST_PACKAGES=scout_local_planner
```

结果：

```text
编译通过。
```

下一包命令：

```bash
PATH_ID=P3_mixed_radius CONDITION=PROFILE_REF_V2 RUN_ID=fidelity01 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P3_mixed_radius.json \
VEHICLE_V_MAX=3.0 ENERGY_PROFILE_LAT_ACCEL=1.1701333333333332 ENERGY_PROFILE_AX_MAX=2.0 ENERGY_PROFILE_DECEL_MAX=2.5 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

只需先跑 P3 一包。

## V2-6：fidelity 包断点定位与 C++ reference 对齐修正

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runfidelity01_144753.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_profile_ref_v2_fidelity_metrics.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_fidelity_peak_windows.csv
```

核心结果：

```text
P3 PROFILE_REF_V2 fidelity01:
  tracking_time -11.1%
  h_rms +12.1%
  h_p95 +32.8%
  h_max -4.4%
  eta_dot -0.8%
  energy +11.1%
  ay_p95 +13.1%
  track_p95 -15.7%

reference diagnostics:
  ref_implied_ax_abs_p95=11.319
  ref_implied_ay_abs_p95=2.000
  ref_implied_jerk_abs_p95=284.777

execution diagnostics:
  odom_ax_abs_p95=7.031
  odom_ay_abs_p95=1.654
  odom_jerk_abs_p95=930.143
  exec_ax_to_ref_p95_ratio=0.621
  exec_ay_to_ref_p95_ratio=0.827
  exec_jerk_to_ref_p95_ratio=3.266
```

断点判断：

```text
不是 MPC 把低激励 reference 放大；
进入 MPC 的 reference 本身已经不满足 V2 低激励假设。
```

原因：

```text
PathHandler::getReferencePoints() 中：
  geometry reference 用 s_progress + lookahead_distance；
  speed profile 却用 s_progress。

结果：
  低曲率段速度被配到 lookahead 高曲率几何上；
  C++ reference 重新生成 ay≈2.0，而 V2 离线 candidate 假设 ay_limit=1.17。

此外：
  v_curve_cap 使用 path_handler/max_lat_accel=2.0；
  PROFILE_REF_V2 下应使用 energy_profile_lat_accel=1.1701。

终点捕获区最低速度也在曲率 cap 之后执行，可能抬高已限速的 v_ref。
```

代码修正：

```text
src/scout_apps/control/scout_local_planner/src/path_handler.cpp
  energy_profile_enable=true 时：
    speed profile 采样弧长改为 s_geom_global，与 reference geometry 对齐；
    v_curve_cap 使用 energy_profile_lat_accel；
    goal_capture_min_speed 后再次应用 curve/v_plan/v_exec cap。
```

验证：

```text
git diff --check
catkin_make -DCATKIN_WHITELIST_PACKAGES=scout_local_planner
```

下一包：

```bash
PATH_ID=P3_mixed_radius CONDITION=PROFILE_REF_V2 RUN_ID=fidelity02 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P3_mixed_radius.json \
VEHICLE_V_MAX=3.0 ENERGY_PROFILE_LAT_ACCEL=1.1701333333333332 ENERGY_PROFILE_AX_MAX=2.0 ENERGY_PROFILE_DECEL_MAX=2.5 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

fidelity02 首要判据：

```text
ref_implied_ay_abs_p95 应接近 <= 1.17；
ref_implied_ax_abs_p95 应显著低于 11.3，理想接近 2.0；
若 reference 已低激励但 odom 仍高，则再转 MPC execution 层。
```

## V2-7：fidelity02 结果与 horizon 加减速一致性修正

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runfidelity02_145656.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_profile_ref_v2_fidelity02_metrics.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_fidelity02_peak_windows.csv
```

相对 P3 NOM：

```text
tracking_time -7.8%
h_rms +18.7%
h_p95 +24.6%
h_max -11.4%
eta_dot +17.8%
energy +18.6%
ay_p95 +32.8%
track_p95 +46.7%
solve_success 1.000
```

reference fidelity：

```text
ref_implied_ay_abs_p95:
  fidelity01 2.000 -> fidelity02 1.170
  lateral cap 对齐成功。

ref_implied_ax_abs_p95:
  fidelity01 11.319 -> fidelity02 10.617
  仍远高于 V2 ax_limit=2.0。
```

断点更新：

```text
速度/曲率错位已修正；
但 reference horizon 内 v_ref 序列仍存在跳变。
```

代码修正：

```text
src/scout_apps/control/scout_local_planner/src/path_handler.cpp
  energy_profile_enable=true 且 time_parameterize=true 时：
    在 ref_points 生成完成后，对 horizon v_ref 做 forward/backward time-domain pass：
      v[i] <= v[i-1] + energy_profile_ax_max * dt
      v[i] <= v[i+1] + 0.8 * energy_profile_decel_max * dt
    同步更新 v_path。
```

验证：

```text
git diff --check
catkin_make -DCATKIN_WHITELIST_PACKAGES=scout_local_planner
```

下一包：

```bash
PATH_ID=P3_mixed_radius CONDITION=PROFILE_REF_V2 RUN_ID=fidelity03 START_DELAY=30 APPROACH_START_ENABLE=false \
PATH_FILE=/data/a/fixed_paths/candidates/P3_mixed_radius.json \
VEHICLE_V_MAX=3.0 ENERGY_PROFILE_LAT_ACCEL=1.1701333333333332 ENERGY_PROFILE_AX_MAX=2.0 ENERGY_PROFILE_DECEL_MAX=2.5 \
rosrun scout_local_planner run_sim_fixed_path_bag.sh
```

fidelity03 首要判据：

```text
ref_implied_ay_abs_p95 维持约 1.17；
ref_implied_ax_abs_p95 从 10.6 降到接近 2.0。
```

## PROFILE_REF_V2 fidelity03 复核

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runfidelity03_150325.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_profile_ref_v2_fidelity03_metrics.csv
docs/Claude/分析数据/2026-05-05_p3_radius_custom_baseline_metrics.csv
```

reference 约束结果：

```text
ref_implied_ax_abs_p95   = 2.000
ref_implied_ay_abs_p95   = 1.170
ref_implied_jerk_abs_p95 = 46.253
```

说明 V2-7 的 horizon time-domain 加减速约束已生效，reference 层已不再是 10 m/s^2 级别的高 ax 参考。

但同路径 `P3_mixed_radius_CUSTOM_rungeom01` 基线对比失败：

```text
tracking_time -5.4%
h_rms        +28.9%
h_p95        +30.2%
h_max        +22.6%
eta_dot      +25.3%
energy       +28.7%
ay_p95       +11.0%
track_p95    +43.9%
odom_ax_p95  +5.8%
odom_jerk    -0.2%
solve_success_ratio = 1.0
```

结论：

```text
PROFILE_REF_V2 已经把 reference 侧 ax/ay 限住；
闭环仍然比 CUSTOM 基线更晃，且 tracking 变差。

失败点不再是 reference 生成没有限幅，而是 MPC/底盘执行层没有把低激励 reference 转化为低激励 odom。
特别是 odom_ax_p95 仍为 6.836 m/s^2，远高于 ref_ax_p95=2.0。
```

## reference 执行链诊断

新增离线脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/diagnose_reference_execution_chain.py
```

目的：

```text
不改控制器，只从 bag 中对齐：
  /reference/v_ref, /reference/implied_ax
  /cmd_vel
  /odom

输出 v_ref -> cmd_v -> odom_v 和 ref_ax -> cmd_ax -> odom_ax 的 RMSE、相关性、最佳滞后、p95 和峰值时刻。
```

运行：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/diagnose_reference_execution_chain.py \
  --csv docs/Claude/分析数据/2026-05-05_reference_execution_chain_fidelity03.csv \
  /data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runfidelity03_150325.bag
```

结果：

```text
ref_ax_p95  = 2.000
cmd_ax_p95  = 4.014
odom_ax_p95 = 4.132

cmd_ax/ref_ax  = 2.007
odom_ax/ref_ax = 2.066
odom_ax/cmd_ax = 1.029

v_corr(ref,cmd)  = -0.303
v_corr(ref,odom) = -0.289
v_corr(cmd,odom) = 0.986

ax_corr(ref,cmd)  = -0.355
ax_corr(ref,odom) = -0.300
ax_corr(cmd,odom) = 0.898
```

代码检查：

```text
src/scout_apps/control/scout_local_planner/src/cost_function.cpp
  v_ref 只以 Q_v * (v - v_ref)^2 软代价进入 QP。

src/scout_apps/control/scout_local_planner/src/constraint_manager.cpp
  硬约束只有 vehicle/a_max、omega_max、j_max、alpha_max，不包含 ref 侧的 v_upper(s) 或 a_profile(s) 上界。

src/scout_apps/control/scout_local_planner/src/mpc_solver.cpp
  v_cmd = v0 + a0 * cmd_vel_lead_time，仿真默认 lead_time=0.15。
```

结论：

```text
低激励 reference 已生成，但 MPC 把它当软速度目标，而不是硬执行边界。
cmd 与 odom 高度一致，说明失败主要发生在 MPC 输出层之前/内部，不是底盘低层单独吞命令。

继续调 PROFILE_REF_V2 的 v(s) 参数意义不大；
下一步应做 reference-constrained MPC 消融：
  A. 先做最小诊断消融：cmd_vel_lead_time=0、提高 Q_v/R_a，验证 cmd_ax 是否能跟 ref_ax 接近；
  B. 若 A 有效，再设计 v_profile/a_profile 作为硬约束或逐步上界，而不是只作为 v_ref 软代价。
```

为执行 A，新增默认不改变行为的实验参数覆盖：

```text
src/scout_apps/control/scout_local_planner/launch/slosh_experiment_sim.launch
  mpc_Q_v
  mpc_R_a
  mpc_R_da
  mpc_cmd_vel_lead_time

src/scout_apps/control/scout_local_planner/scripts/run_sim_fixed_path_bag.sh
  MPC_Q_V
  MPC_R_A
  MPC_R_DA
  MPC_CMD_VEL_LEAD_TIME
```

默认值保持仿真原配置：

```text
Q_v=8.0
R_a=0.4
R_da=0.5
cmd_vel_lead_time=0.15
```

验证：

```text
python3 -m py_compile diagnose_reference_execution_chain.py
bash -n run_sim_fixed_path_bag.sh
git diff --check
catkin_make -DCATKIN_WHITELIST_PACKAGES=scout_local_planner
```

## PROFILE_REF_V2 lead0 消融

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead0_01_151821.bag
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead0_02_151948.bag
```

运行：

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/diagnose_reference_execution_chain.py \
  --csv docs/Claude/分析数据/2026-05-05_reference_execution_chain_lead0.csv \
  <lead0 bags>

python3 src/scout_apps/control/scout_local_planner/scripts/extract_slosh_metrics.py \
  --csv docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead0_metrics.csv \
  <lead0 bags>
```

结果：

```text
lead0_01:
  ref_ax_p95=2.000 cmd_ax_p95=0.024 odom_ax_p95=0.084
  ref_v_mean=2.296 cmd_v_mean=0.001 odom_v_mean=0.001

lead0_02:
  ref_ax_p95=2.000 cmd_ax_p95=0.171 odom_ax_p95=0.280
  ref_v_mean=2.005 cmd_v_mean=0.025 odom_v_mean=0.025
```

同路径 CUSTOM 均值对比：

```text
tracking_time +218.7%
h_p95        -96.5%
eta_dot      -89.5%
energy       -95.4%
ay_p95       -100.0%
track_p95    -5.8%
odom_ax      -91.7%
odom_jerk    -91.9%
```

解释：

```text
lead0 的低晃动不是有效 anti-slosh 成功，而是车辆几乎没执行速度。
mpc_solver.cpp 中 v_cmd = v0 + a0 * cmd_vel_lead_time；
当 lead_time=0 时，MPC 的加速度决策 a0 不会转化成速度命令，cmd_v 退化为当前速度 v0。

因此 lead0 只能证明 cmd_vel_lead_time 是 cmd_ax 放大的关键通道，
不能作为可用配置。
```

下一步：

```text
跑 lead_time=dt=0.05 的中间消融。
目标不是最小晃动，而是判断：
  cmd_ax 是否从 4.0 降到接近 2.0；
  tracking_time 是否仍在 CUSTOM +15% 内；
  h_p95/eta_dot 是否不再恶化。
```

## PROFILE_REF_V2 lead005 消融

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead005_01_154150.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_reference_execution_chain_lead005.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead005_metrics.csv
```

执行链：

```text
ref_ax_p95  = 2.000
cmd_ax_p95  = 2.191
odom_ax_p95 = 2.542

cmd/ref  = 1.096
odom/ref = 1.271

ref_v_mean  = 1.553
cmd_v_mean  = 1.329
odom_v_mean = 1.308
cmd/odom velocity corr = 0.995
```

相对同路径 CUSTOM：

```text
tracking_time -0.8%
h_rms         -5.3%
h_p95         -23.1%
h_max         -6.7%
eta_dot       -4.2%
energy        -5.3%
ay_p95        +13.3%
track_p95     +28.7%
odom_ax       -24.1%
odom_jerk     -30.0%
solve_success 1.0
```

结论：

```text
lead_time=0.05 是当前第一组有效正信号：
  1. MPC 输出端 ax 放大基本被压住；
  2. tracking_time 不增加；
  3. h_p95 明显下降。

但尚未通过总验收：
  ay_p95 +13.3%，违反 ay 不升；
  track_p95 +28.7%，违反 tracking +15%。

因此不是最终成功，而是说明失败根因已定位到 cmd_vel_lead_time 与 reference 执行一致性。
下一步应做小规模复现：
  P3 lead005 再录 1 包；
  若重复正信号，再调横向执行一致性（ay/track），不要再回到 slosh cost。
```

## PROFILE_REF_V2 lead005 复现包

新增输入：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead005_02_191937.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_reference_execution_chain_lead005_rerun.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead005_rerun_metrics.csv
```

执行链复现：

```text
lead005_01: ref_ax=2.000 cmd_ax=2.191 odom_ax=2.542
lead005_02: ref_ax=2.000 cmd_ax=2.278 odom_ax=2.786
```

说明 `lead_time=0.05` 对 MPC 输出 ax 放大的修复是可复现的。

但 slosh 指标没有稳定复现：

```text
lead005_01 vs CUSTOM:
  h_p95   -23.1%
  eta_dot -4.2%
  energy  -5.3%
  ay_p95  +13.3%
  track   +28.7%

lead005_02 vs CUSTOM:
  h_p95   +2.9%
  eta_dot +22.9%
  energy  +9.2%
  ay_p95  +14.7%
  track   +61.9%
```

两包均值：

```text
tracking_time -2.0%
h_rms         +1.2%
h_p95         -10.1%
h_max         -10.2%
eta_dot       +9.4%
energy        +2.0%
ay_p95        +14.0%
track_p95     +45.3%
odom_ax       -20.5%
odom_jerk     -25.5%
solve_success 1.0
```

结论更新：

```text
reference 执行一致性问题已部分解决；
但 slosh 抑制不稳定，且横向激励/跟踪显著恶化。

这说明 P3_mixed_radius + PROFILE_REF_V2 只压纵向 ax/jerk 不够；
横向路径跟踪误差和 ay 增长会重新激发晃动。

下一步不应继续单独调 lead_time；
应回到方案的几何层，做横向约束：
  1. 降低 P3 radius candidate 的 kappa/ay；
  2. 或在 MPC 中把 reference lateral acceleration / curvature tracking 作为硬边界；
  3. 至少先做一组 ay_limit 更低的 PROFILE_REF_V2 消融，验证 ay_p95 是否能不升。
```

## PROFILE_REF_V2 lead005 + ay09 横向消融

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead005_ay09_01_192850.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_reference_execution_chain_lead005_ay09.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead005_ay09_metrics.csv
```

执行链：

```text
ref_ax_p95  = 2.000
cmd_ax_p95  = 2.252
odom_ax_p95 = 2.370
cmd/ref     = 1.126
odom/ref    = 1.185
```

相对 CUSTOM：

```text
tracking_time +8.7%
h_rms         -11.7%
h_p95         -16.9%
h_max         -23.8%
eta_dot       -14.8%
energy        -11.9%
ay_p95        +6.7%
track_p95     +29.1%
odom_ax       -17.0%
odom_jerk     -17.6%
solve_success 1.0
```

相对 lead005 两包均值：

```text
h_p95     -7.6%
eta_dot   -22.1%
energy    -13.6%
ay_p95    -6.4%
track_p95 -11.2%
```

结论：

```text
降低 ENERGY_PROFILE_LAT_ACCEL 到 0.9 后，横向问题有所缓解，并且 h/eta_dot/energy 全部改善；
但 ay_p95 仍比 CUSTOM 高 6.7%，track_p95 仍高 29.1%。

该方向不是失败，但还没有达到总验收。
下一步可以再做一档 ay_limit=0.75 的单包消融；
若 ay 仍不低于 CUSTOM 或 tracking 继续超限，则固定 P3_mixed_radius 几何需要重做，而不是继续压速度。
```

## PROFILE_REF_V2 lead005 + ay075 横向消融

输入：

```text
未到达包（不纳入主均值）：
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead005_ay075_01_193853.bag

有效到达包：
/data/a/slosh_bags/sim/20260505/20260505_P3_mixed_radius_PROFILE_REF_V2_runlead005_ay075_01_194051.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_reference_execution_chain_lead005_ay075.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead005_ay075_metrics.csv
docs/Claude/分析数据/2026-05-05_profile_ref_v2_lead005_ay075_failed193853_metrics.csv
```

有效包执行链：

```text
ref_ax_p95  = 2.000
cmd_ax_p95  = 2.052
odom_ax_p95 = 2.346
cmd/ref     = 1.026
odom/ref    = 1.173
```

有效包相对 CUSTOM：

```text
tracking_time +19.0%
h_rms         -24.2%
h_p95         -30.2%
h_max         -26.9%
eta_dot       -34.2%
energy        -25.0%
ay_p95        +4.7%
track_p95     +10.5%
odom_ax       -17.9%
odom_jerk     -17.7%
solve_success 1.0
```

未到达包作为参考：

```text
tracking_time +15.1%
h_p95         -31.3%
eta_dot       -29.0%
energy        -22.2%
ay_p95        +4.4%
track_p95     +18.8%
```

结论：

```text
ay075 进一步降低了 h/eta_dot/energy，并把 track_p95 拉回到 +10.5%；
但代价是 tracking_time 超过 +15%，且 ay_p95 仍比 CUSTOM 高约 4~5%。

这已经接近固定 P3_mixed_radius 几何 + 速度剖面可做到的边界：
  继续降低 ay_limit 预计会继续拉长时间/增加无法到达风险；
  但 ay_p95 仍未低于 CUSTOM，说明速度剖面不能完全补偿当前几何/跟踪横向激励。

下一步不建议继续单调压 ay_limit。
应转向新 P3 几何候选：更大转弯半径/更低 kappa 和更平滑 dkappa，
再使用 lead_time=0.05 + ax=2.0 + ay=0.75~0.9 作为执行配置。
```

## 新 P3 几何候选生成

目的：

```text
P3_mixed_radius + 速度剖面已接近边界；
重新生成更低 kappa / dkappa 的 P3 几何候选，再进入闭环。
```

新增脚本：

```text
src/scout_apps/control/scout_local_planner/scripts/sweep_p3_geometry_candidates.py
```

输入：

```text
/data/a/fixed_paths/sim/P3_mixed.json
```

输出：

```text
候选路径：
docs/Claude/分析数据/generated_paths/

几何 sweep：
docs/Claude/分析数据/2026-05-05_p3_geometry_sweep_summary.csv

reference 离线筛选：
docs/Claude/分析数据/2026-05-05_p3_geo_v2_reference_optimization.csv
docs/Claude/分析数据/2026-05-05_p3_geo_v2_nom_baseline.csv
```

当前 P3_mixed_radius 几何：

```text
length=9.116
kappa_p95=1.271
kappa_max=1.451
dkappa_p95=3.084
drift=0.061
```

新候选 1（优先仿真，激进低曲率）：

```text
P3_mixed_geo_i56_g55_d18
path=/home/a/scout_ws/docs/Claude/分析数据/generated_paths/P3_mixed_geo_i56_g55_d18.json
length=8.874
kappa_p95=0.824
kappa_max=0.916
dkappa_p95=1.804
drift=0.161
```

新候选 2（备选，漂移更小）：

```text
P3_mixed_geo_i40_g55_d12
path=/home/a/scout_ws/docs/Claude/分析数据/generated_paths/P3_mixed_geo_i40_g55_d12.json
length=8.952
kappa_p95=0.933
kappa_max=1.685
dkappa_p95=2.625
drift=0.120
```

离线筛选结果：

```text
pass_all=629/975
```

优先候选 `geo_i56_g55_d18` 推荐参数：

```text
cruise=2.4 或 2.6
ay_limit=1.3164  (ay_ratio=0.9)
accel_limit=2.0

离线预估：
time -5.4%
h_p95 -59.3%
eta_dot -55.6%
ay -3.1%
```

为了和已验证执行链保持一致，闭环首包使用：

```text
MPC_CMD_VEL_LEAD_TIME=0.05
ENERGY_PROFILE_AX_MAX=2.0
ENERGY_PROFILE_DECEL_MAX=2.5
ENERGY_PROFILE_LAT_ACCEL=1.3164
VEHICLE_V_MAX=3.0
```

下一步最小录包：

```text
1. 先跑 PROFILE_REF_V2 x1，确认能到达且 ay/h/track 没明显爆。
2. 若通过，再跑同路径 CUSTOM x1 做公平基线。
3. 若 PROFILE_REF_V2 第一包失败，改用备选 geo_i40_g55_d12，不继续调速度参数。
```

## P3 geo56 PROFILE_REF_V2 首包

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_geo56_PROFILE_REF_V2_rungeo56_profile01_195656.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_reference_execution_chain_geo56_profile01.csv
docs/Claude/分析数据/2026-05-05_p3_geo56_profile01_metrics.csv
```

执行链：

```text
ref_ax_p95  = 2.000
cmd_ax_p95  = 2.037
odom_ax_p95 = 2.301
cmd/ref     = 1.018
odom/ref    = 1.150
```

指标：

```text
tracking=5.55s
h_rms=0.002284
h_p95=0.003640
h_max=0.006708
eta_dot=0.013646
energy=0.041526
ay_p95=1.630
track_p95=0.562
solve_success=1.0
```

临时对比旧 `P3_mixed_radius CUSTOM`：

```text
tracking_time -11.9%
h_rms         -4.0%
h_p95         -22.6%
h_max         +3.4%
eta_dot       +12.1%
energy        -2.5%
ay_p95        +18.6%
track_p95     +11.1%
odom_ax       -20.8%
odom_jerk     -15.1%
```

结论：

```text
geo56 能到达，且 reference execution 正常；
但 ay_p95 偏高，eta_dot/h_max 也未稳定优于旧 radius CUSTOM。

不能用旧 radius CUSTOM 做最终判断。
下一步必须补同路径 geo56 CUSTOM x1：
  如果 geo56 CUSTOM 本身已经低晃，说明主要贡献来自几何；
  如果 PROFILE_REF_V2 相对 geo56 CUSTOM 仍有增益，才说明几何+速度剖面组合有效。

暂时不要补第二个 PROFILE_REF_V2 包。
```

## P3 geo56 CUSTOM 同路径基线

输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_geo56_CUSTOM_rungeo56_custom01_215341.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_p3_geo56_custom01_metrics.csv
docs/Claude/分析数据/2026-05-05_reference_execution_chain_geo56_custom01.csv
```

geo56 CUSTOM 指标：

```text
tracking=5.30s
h_rms=0.002111
h_p95=0.003325
h_max=0.006052
eta_dot=0.010900
energy=0.037847
ay_p95=1.487
track_p95=0.583
solve_success=1.0
```

同路径 `PROFILE_REF_V2` vs `CUSTOM`：

```text
tracking_time +4.7%
h_rms         +8.2%
h_p95         +9.5%
h_max         +10.8%
eta_dot       +25.2%
energy        +9.7%
ay_p95        +9.6%
track_p95     -3.6%
odom_ax       +15.3%
odom_jerk     +2.3%
```

`geo56 CUSTOM` vs 旧 `P3_mixed_radius CUSTOM`：

```text
tracking_time -15.9%
h_rms         -11.2%
h_p95         -29.3%
h_max         -6.7%
eta_dot       -10.5%
energy        -11.1%
ay_p95        +8.2%
track_p95     +15.2%
odom_ax       -31.3%
odom_jerk     -17.0%
```

结论：

```text
geo56 的主要收益来自几何本身；
在 geo56 上再叠 PROFILE_REF_V2 反而变差。

因此新主线应收敛为：
  low-excitation geometry reference generation
而不是：
  geometry + PROFILE_REF_V2 必须组合。

下一步：
  不继续跑 geo56 PROFILE_REF_V2；
  需要复现 geo56 CUSTOM x1；
  若复现稳定，再补 matched-time slow / 原始 P3 baseline 表，用于论文主结论。
```

## P3 geo56 CUSTOM 复现

新增输入 bag：

```text
/data/a/slosh_bags/sim/20260505/20260505_P3_geo56_CUSTOM_rungeo56_custom02_215851.bag
```

输出：

```text
docs/Claude/分析数据/2026-05-05_p3_geo56_custom_rerun_metrics.csv
docs/Claude/分析数据/2026-05-05_reference_execution_chain_geo56_custom_rerun.csv
```

两包相对旧 `P3_mixed_radius CUSTOM`：

```text
geo56_custom01:
  tracking -15.9%
  h_rms    -11.2%
  h_p95    -29.3%
  h_max    -6.7%
  eta_dot  -10.5%
  energy   -11.1%
  ay_p95   +8.2%
  track    +15.2%

geo56_custom02:
  tracking -18.3%
  h_rms    -5.7%
  h_p95    -27.0%
  h_max    -8.8%
  eta_dot  -5.8%
  energy   -5.7%
  ay_p95   +15.7%
  track    +26.3%
```

两包均值：

```text
tracking_time -17.1%
h_rms         -8.5%
h_p95         -28.1%
h_max         -7.7%
eta_dot       -8.2%
energy        -8.4%
ay_p95        +12.0%
track_p95     +20.8%
odom_ax       -29.6%
odom_jerk     -15.9%
solve_success 1.0
```

结论：

```text
geo56 CUSTOM 的低晃收益已复现；
主贡献来自低曲率几何参考，而不是 MPC 内 slosh cost 或 PROFILE_REF_V2。

但仍未满足原始总验收：
  ay_p95 不升：失败
  track_p95 <= +15%：均值失败，custom01 临界

为了尽快完成，论文主线可收敛为：
  低激励几何参考生成能显著降低 h_p95/eta_dot/energy 与纵向 ax/jerk，
  但当前几何候选仍存在横向 ay/tracking trade-off。

下一步最小工作：
  1. 不再跑 PROFILE_REF_V2；
  2. 补一个 matched-time slow 或旧 radius CUSTOM 表，明确不是单纯减速；
  3. 或生成保守候选 geo_i40_g55_d12，尝试降低 track/ay trade-off。
```
