# 2026-06-10

## alpha-state Gate 1 replay 离线诊断

- 子 agent 对 `/data/a/scout_spmpc_experiments/raw/2026-06-09/02_gate1_replay_subagent/20260609_1case/20260609_231045_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_rearchive_run1` 的失败 bag / meta / planner log 做离线诊断；未启动仿真、未改代码、未覆盖旧文件。
- 诊断确认运行口径可核对为 `continuous_mpcc_acados + B0 + fixed_path/replay`，`/spmpc/solver_backend` 同为 `continuous_mpcc_acados`；`alpha_max_override=8.0` 已进入 wrapper/runtime 记录口径，但仍不能仅凭 bag/log 证明其进入 acados OCP `lbu/ubu`。
- 关键事实：`progress_s` 在起点后约 0.05s 到最大 `0.0028689418` 后 5~70s 不再增长；`cmd_v` 近零但 `cmd_omega` 可到 `1.1169 rad/s`；`warm_start_status=OK` 全程，`used_flatness=1`、`used_fallback=0`；terminal 全程 `TRACKING`，`envelope_active=0`。
- 关键推断：`cmd_v` 近零的主因不是 terminal clamp、WAITING、wrapper 后级压低，而更像是 `continuous_mpcc_acados + B0` 正常求解周期本身给出了低推进、高转向的输出；当前失败形态是起始附近 progress/projection 区域几乎不推进，不是先跑偏几米再 freeze。
- 离线报告已生成：`/data/a/scout_spmpc_experiments/raw/2026-06-09/02_gate1_replay_subagent/20260609_1case/20260609_231045_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_rearchive_run1/20260609_231045_spmpc_B0_P2_alpha_B0_replay_warmstart_fix_alpha8_rearchive_run1_offline_diagnostics.md`。
- 已同步更新 `docs/Claude/遇到的问题与解决方案/2026-06-09_alpha-state主线实物前NoGo与catkin白名单问题.md` 的子 agent 离线诊断结论；正式实物继续 NO-GO。

## alpha-state acados 运行时约束与首拍诊断

- 在 `continuous_mpcc_acados` wrapper 中显式下发运行时 acados 约束：所有控制 stage 的 `lbu/ubu` 现在按运行参数设置 `u=[a, alpha, v_s]`，非初始状态 stage 的 `lbx/ubx` 现在按运行参数设置 `[v, omega]`；stage-0 初始状态等式约束保持不变。
- 新增求解器内部摘要：`runtime_bounds`、`generated_bounds`、`first_shot_debug`，用于下一次 Gate 1 replay 直接核对运行时约束与首拍 OCP 输出。
- 新增诊断 topic，未改已有 topic 名或已有 layout：
  - `/spmpc/debug/runtime_bounds`：`a_min,a_max,alpha_min,alpha_max,vs_min,vs_max,v_min,v_max,omega_min,omega_max`
  - `/spmpc/debug/generated_bounds`：同上，用于记录当前生成器默认/烘焙参考约束
  - `/spmpc/debug/first_shot_summary`：记录 `x0`、`u0=[a,alpha,v_s]`、cmd clamp 前后值、以及 `x1/x2/x3` 的 `v/omega/s`
- strengthened `DiffDriveFlatnessWarmStart` gtest：确认非零初始 `omega` 被保留、`v_s` 遵守 `v_s_max`，并要求曲率轨迹产生非零且有界的 `alpha`。
- 已验证：`git diff --check` 通过；`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner" --pkg spmpc_local_planner` 通过；`run_tests_spmpc_local_planner_gtest_test_diff_drive_flatness_warm_start` 5/5 通过；`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="scout_local_planner" --pkg scout_local_planner` 通过；最后已恢复 `CATKIN_WHITELIST_PACKAGES=spmpc_local_planner` 并重建通过。
- `bash scripts/run_s_curve_smoke_test.sh` 未执行：工作区根目录没有该脚本；实际找到的脚本为 `src/scout_apps/control/scout_local_planner/scripts/run_s_curve_smoke_test.sh`，但其 cleanup 使用较宽的 `pkill -KILL -f gzserver/roslaunch/rosmaster/...`，且不满足当前 Subagent 仿真规则中的“启动后等待 30s、关闭后等待 30s、文件归档尽量放 `/data/a`、不做宽泛 kill”安全口径。因此本轮只记录未执行原因，未为了通过 smoke 而冒险运行或改仿真环境。
- 该修改只解决“运行时约束是否进入 acados + 首拍输出是否可观测”的 P0 诊断/一致性问题，不构成实物放行；`continuous_mpcc_acados + B0` 仍需重新通过同路径 Gate 1 replay 和后续 fresh-sim gates 后，才能进入实物 dry-run。

## runtime-bounds 诊断版同路径 Gate 1 replay

- 子 agent 按 `docs/实物实验注意事项/Subagent.md` 规则跑了 1 次同路径 Gate 1 replay：fresh 启动仿真后等待 30s，只跑 1 个 case，70s 未到点判失败，结束后安全停止本次启动进程并等待 30s；未使用 `rm -rf`、`git reset/clean/checkout/push` 或宽泛 `killall/pkill`。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/03_gate1_replay_runtime_bounds/20260610_010202_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_run1`
- 使用同路径 JSON：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1_generated_path.json`
- 结果：`FAIL`；`GOAL_REACHED=0`；terminal `REACHED=0`；`progress_s=0.002255 -> 0.008541`，max `0.008541`，仍卡在起点附近。
- 新增 debug topics 均成功录包，各 `2724` 条：`/spmpc/debug/runtime_bounds`、`/spmpc/debug/generated_bounds`、`/spmpc/debug/first_shot_summary`。
- bounds 诊断：`runtime_bounds.alpha_max=1.2`，`generated_bounds.alpha_max=1.2`；本次 replay 是默认 `alpha_max=1.2` 口径，不是 `alpha_max=8.0` 口径。
- 首拍诊断：`u0_a/u0_alpha/u0_vs=-0.008475/1.2/0.0`，`cmd_v_pre/post=0.0/0.0`，`cmd_omega_pre/post=0.04/0.04`，`x1/x2/x3` 的 `v` 近零、`omega` 递增到约 `0.04/0.08/0.12`、`s` 停在约 `0.0193`。
- 状态统计：`B0_ACADOS_OK=2719`，`ACADOS_SOLVE_FAILED_4=5`；`warm_start_status=OK=2724`；terminal mode 全程 `TRACKING=2724`；solver time mean/max `5.207/10.879 ms`。
- cmd 统计：early 5s `cmd_v` mean/max `0.000199/0.024 m/s`，全程 `0.0008/0.144369 m/s`；early 5s `cmd_omega` mean/max `0.079312/0.689282 rad/s`，全程 `0.017552/1.175397 rad/s`。
- 解释：新增诊断链路正常，但主线仍表现为“可转向、低推进、起点附近几乎不走”；同路径 Gate 1 未通过，正式实物继续 NO-GO。
- 分析报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/03_gate1_replay_runtime_bounds/20260610_010202_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_run1/20260610_010202_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_run1_analysis.md`

## alpha8 运行时约束同路径 Gate 1 replay

- 子 agent 按同一安全 SOP 跑了 1 次 `continuous_mpcc_acados + B0 + alpha_max:=8.0` 同路径 Gate 1 replay：fresh 启动仿真后等待 30s，只跑 1 个 case，结束后安全停止本次启动进程并等待 30s；未使用 `rm -rf`、`git reset/clean/checkout/push` 或宽泛 `killall/pkill`。
- 参数核对：`spmpc_fixed_path.launch` 的真实参数名是 `alpha_max`；实验 suite 中对应环境变量为 `SPMPC_ALPHA_MAX`，meta 记录为 `alpha_max_override`。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/04_gate1_replay_runtime_bounds_alpha8/20260610_012718_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run1`
- 使用同路径 JSON：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1_generated_path.json`
- 结果：`PASS`；首次 `GOAL_REACHED≈50.11s`，首次 terminal `REACHED≈50.11s`；`progress_s=0.002821 -> 0.977385`，max `0.977385`。
- bounds 诊断：`runtime_bounds.alpha_max≈8.0`，`generated_bounds.alpha_max≈1.2`，说明 alpha8 运行时约束已进入诊断链；bag 尾段有收尾全零样本，报告使用最后一个有效非零 bounds 样本。
- 首拍诊断：`u0_a/u0_alpha/u0_vs=-0.007414/8.0/0.023924`，`cmd_v_pre/post≈7.88e-10/7.88e-10`，`cmd_omega_pre/post≈0.266983/0.266983`；第 11 条早期样本已出现 `x0_v≈0.1007`、`cmd_v_post≈0.0807`、`cmd_omega_post≈0.7992`。
- 状态统计：`WAITING_FOR_ODOM=1`，`B0_ACADOS_OK=1344`，`ACADOS_SOLVE_FAILED_4=160`，`GOAL_REACHED=811`；`warm_start_status=OK=2315`；terminal mode 为 `TRACKING=1308`、`TERMINAL_SLOWDOWN=107`、`TERMINAL_CAPTURE_STOP=89`、`REACHED=811`。
- cmd 统计：early 5s `cmd_v` mean/max `0.190268/0.596466 m/s`，到首次 goal 前 `cmd_v` mean/max `0.396202/0.8 m/s`；early 5s `|cmd_omega|` mean/max `0.298169/1.2 rad/s`，到首次 goal 前 `0.588127/1.2 rad/s`。
- 解释：alpha8 口径下同路径 Gate 1 单次通过，且运行时 bounds 可观测；但这只是 1 次 replay，不等于 2~3 次独立 fresh-sim gate，更不等于实物放行。正式实物仍需继续 NO-GO，直到 alpha8 独立复现、path-departure 安全门、hard-zero 安全输出和 dry-run 链路满足。
- 分析报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/04_gate1_replay_runtime_bounds_alpha8/20260610_012718_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run1/20260610_012718_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run1_analysis.md`

## alpha8 独立 fresh-sim 复现批次

- 子 agent 串行补跑 2 次 `continuous_mpcc_acados + B0 + alpha_max:=8.0` 同路径 Gate 1 fresh-sim 复现；每次 fresh 启动后等待 30s，只跑 1 个 case，70s gate，结束后安全停止本次启动进程并等待 30s；未使用 `rm -rf`、`git reset/clean/checkout/push` 或宽泛 `killall/pkill`。
- batch 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/05_gate1_alpha8_independent_repro`
- 使用同路径 JSON：`/data/a/scout_spmpc_experiments/raw/2026-06-09/01_alpha_state_mainline_failure/spmpc_alpha_repro/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1/20260609_124323_spmpc_B0_P2_alpha_B0_repro_run1_generated_path.json`
- 本批新增统计：`PASS=1, FAIL=1, INVALID_ALPHA8=0`；两次均核对到 `runtime_bounds.alpha_max=8.0`，`generated_bounds.alpha_max≈1.2`。
- run2：`FAIL`；`progress_s=0.000715 -> 0.004223`，max `0.004223`，无 first goal；`B0_ACADOS_OK=2290`，`ACADOS_SOLVE_FAILED_4=24`，`warm_start_status=OK=2313`，terminal 全程 `TRACKING=2313`；报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/05_gate1_alpha8_independent_repro/20260610_110418_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run2/20260610_110418_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run2_analysis.md`
- run3：`PASS`；first goal `45.06s`；`progress_s=0.000713 -> 0.980470`，max `0.980470`；`B0_ACADOS_OK=1124`，`ACADOS_SOLVE_FAILED_4=94`，`GOAL_REACHED=1096`，`warm_start_status=OK=2313`，terminal `REACHED=1096`；报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/05_gate1_alpha8_independent_repro/20260610_110727_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run3/20260610_110727_spmpc_B0_P2_alpha_B0_gate1_runtime_bounds_alpha8_run3_analysis.md`
- 合并上一轮 run1 后，alpha8 同路径 Gate 1 共 `3` 次独立样本：`PASS=2`、`FAIL=1`、`INVALID_ALPHA8=0`；run1/run3 两个成功样本 first goal 分别为 `50.11s`、`45.06s`，run2 是起点附近几乎不推进的失败离群样本。
- 总判断：alpha8 不是单次偶然唯一成功，但稳定性不足，不能称为稳定通过；正式实物仍 `NO-GO`。下一步应先做失败机理诊断或继续增加独立 fresh-sim 样本，不应直接进入实物。
- batch summary：`/data/a/scout_spmpc_experiments/raw/2026-06-10/05_gate1_alpha8_independent_repro/20260610_gate1_alpha8_independent_repro_batch_summary.md`

## alpha8 run2 失败离线诊断

- 子 agent 只读对比 `alpha8` 的 run2 FAIL 与 run1/run3 PASS；未启动仿真、未发 `/cmd_vel`、未改代码、未改 launch、未删除数据。
- 诊断目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/06_alpha8_run2_failure_diagnostics/20260610_121338_alpha8_run2_offline_diagnostics`
- 诊断报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/06_alpha8_run2_failure_diagnostics/20260610_121338_alpha8_run2_offline_diagnostics/alpha8_run2_offline_diagnostics.md`
- 中间摘要：`summary.json`；对比表：`key_metrics.csv`。
- 配置核对：run1/run2/run3 的 backend、variant、path JSON、spawn 口径、planner 参数、record topics 一致；三次均为 `runtime_bounds.alpha_max=8.0`、`generated_bounds.alpha_max≈1.2`，run2 不是 `INVALID_ALPHA8`。
- progress 分叉：run2 `progress_s=0.000715 -> 0.004223`，从未超过 `0.01`，约 `4.97s` 后冻结；run1/run3 在约 `5s` 已分别到 `0.106/0.105`，最终到点。
- first-shot/早期分叉：run2 首拍与 run3 的 `x0_s/progress_s` 几乎相同，但 run2 在约 `0.35s` 已有更激进的 `x0_omega≈0.914`、`cmd_omega_post≈1.111`、`cmd_v_post≈0.119`、`u0_vs≈0.148`；到 `5s` 末仍只到 `x0_s≈0.0362`，随后 `cmd_v_post≈0`。
- `/cmd_vel` 统计：run2 `0~5s` 还有 `cmd_v mean≈0.0395`，但 `5~15s` 降到 `7.1e-05` 且 near-zero fraction `0.993`，`15~30s` 基本全零；不是一开始就零命令，而是短暂尝试后命令塌零。
- `/odom` 统计：run2 `0~30s` 位移约 `0.268m`、累积约 `0.475m`，不像大幅跑偏后 progress 不跟；更像起点附近小幅转动/位移后进入低推进局部解。
- 状态排除：run2 全程 `warm_start_status=OK`、terminal `TRACKING`，solver time 正常；run2 的 solve fail 次数不比 PASS 样本更坏，因此不优先归因于 warm-start、terminal 或实时性。
- 高置信结论：run2 最可能是前 `5s` 内进入近起点 progress/projection 与 alpha-state OCP 耦合的低推进局部锁死解；主嫌疑不是 alpha8 配置失效、path 缺失或 ROS 启动异常。
- 下一步最小诊断建议：优先补导出/发布 `nearest-path distance`、`projection s`、`contour/lag error`、`reference kappa`、warm-start `state.omega/control.alpha/v_s`、local trajectory 前几拍，只看 run2 `0~6s` 即可；正式实物仍 `NO-GO`。

## alpha8 run2 0~6s projection / OCP 聚焦诊断

- 只读离线分析 run2 FAIL 的 `0~6s`，并与 run1/run3 PASS 做对比；未启动仿真、未启动 planner、未发 `/cmd_vel`、未改代码。
- 诊断目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/07_alpha8_run2_0_6s_projection_diagnostics`
- 诊断脚本：`analyze_run2_0_6s.py`；输出：`run2_0_6s_summary.json`、`run2_0_6s_samples.csv`、`run2_0_6s_projection_diagnostics.md`。
- 0~6s 关键分叉：run2 `progress_s=0.000715 -> 0.004223`，从未超过 `0.005`；run1/run3 到 `6s` 已分别到 `0.177138/0.187167`，并在约 `5s` 前超过 `0.1`。
- 独立 path 投影显示：run2 到 `5~6s` 仍投影在 path front，`proj_s_abs=0`，nearest distance 约 `0.33m`，yaw error 约 `0.81~0.96rad`；run1/run3 到 `5s` 已投影到约 `0.968m` 沿路径位置。
- first-shot / OCP 诊断：run2 在 `0.35s` 有短暂推进与高角速度，`u0_vs≈0.148`、`cmd_v_post≈0.119`、`cmd_omega_post≈1.111`；但到 `1s` 进度已冻结，`x0_s≈0.036`，到 `5~6s` `cmd_v` 基本塌零。
- local trajectory 诊断：run2 的局部轨迹前几拍在 `0.35s` 还略向前，但 `3~6s` 已全部投影回 path front，且 yaw error 大，说明预测轨迹自身也回到前端低推进 basin。
- 成本项现象：run2 `3~6s` 的 `J_contour/J_lag` 并未爆炸，尽管独立投影距离约 `0.33m`；这支持“projection/OCP 局部 basin 下低推进解仍被视为可接受”的假设。
- 聚焦结论：run2 不是高曲率起点导致，也不是 alpha8 失效；更像 projection、heading/yaw-rate 对齐与 OCP 目标耦合形成的 near-start low-progress basin。下一步应增加 planner 自身 projector 原始结果、monotonic/min-progress clipping、stage0 contour/lag/yaw error、local trajectory 前几拍投影、warm-start 前几拍 `omega/alpha/v_s` 等诊断，再决定是否改控制逻辑或权重。
- 报告：`/data/a/scout_spmpc_experiments/raw/2026-06-10/07_alpha8_run2_0_6s_projection_diagnostics/run2_0_6s_projection_diagnostics.md`

## near-start lock debug-only 话题接入

- 按 run2 `0~6s` 诊断结论，给 `continuous_mpcc_acados` 增加只读内部诊断摘要；本轮不改控制逻辑、不改 objective/weight、不改 acados codegen、不改已有 topic layout。
- 新增 `SolverOutput` debug summary：`projector_debug`、`stage0_reference_debug`、`local_traj_head_debug`、`warm_start_head_debug`。
- 新增 bag-visible topic：
  - `/spmpc/debug/projector`：raw/guarded projection、signed distance、`min_progress_s`、`monotonic_clip_applied`。
  - `/spmpc/debug/stage0_reference`：`s0`、参考 `x/y/yaw/kappa`、robot `x/y/yaw`、stage0 `yaw_error/contour_error/lag_error`。
  - `/spmpc/debug/local_traj_head`：求解后局部轨迹前 3 拍的 `x/y/yaw/v/omega/s`、raw projection、contour/lag/yaw error。
  - `/spmpc/debug/warm_start_head`：实际选中的 warm-start 前 3 拍 `state_s/state_omega/control_alpha/control_vs`。
- 已同步更新 `docs/Claude/遇到的问题与解决方案/2026-06-09_alpha-state主线实物前NoGo与catkin白名单问题.md`，加入“给别人请教时重点看什么”：用新 topic 判断 run2 near-start lock 更像 projector/front-clamp、stage0 局部 basin、warm-start seed、progress 激励不足，还是几者耦合。
- 非仿真验证：
  - `catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner" --pkg spmpc_local_planner` 通过；
  - `catkin_make run_tests_spmpc_local_planner_gtest_test_diff_drive_flatness_warm_start --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner"` 5/5 通过；
  - `catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="scout_local_planner" --pkg scout_local_planner` 通过；
  - 已恢复 `CATKIN_WHITELIST_PACKAGES=spmpc_local_planner` 并重建通过。
- 本轮没有启动仿真；新增 topic 尚未有 fresh-sim bag 证据。正式实物仍 `NO-GO`。

## alpha8 debug topic replay invalid 样本

- 按 `docs/实物实验注意事项/Subagent.md` 安全规则尝试跑 1 轮带新增 4 个 debug topic 的 `continuous_mpcc_acados + B0 + alpha_max:=8.0` 同路径 fresh-sim replay；启动后等待 30s，只跑 1 个 case，结束后只停止本次启动进程并等待 30s。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/08_gate1_alpha8_debug_topics_replay/20260610_133051_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_topics_run1`
- bag：`/data/a/scout_spmpc_experiments/raw/2026-06-10/08_gate1_alpha8_debug_topics_replay/20260610_133051_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_topics_run1/20260610_133051_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_topics_run1.bag`
- analysis：`/data/a/scout_spmpc_experiments/raw/2026-06-10/08_gate1_alpha8_debug_topics_replay/20260610_133051_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_topics_run1/20260610_133051_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_topics_run1_analysis.md`
- 结果判定：`INVALID_ALPHA8`，不作为有效 Gate 1 PASS/FAIL 样本。
- 原因：planner log 参数里 `robot/alpha_max=8.0`，但 bag 内 `/spmpc/debug/runtime_bounds` 与 `/spmpc/debug/generated_bounds` 全程为全零数组，不能核对 `runtime_bounds.alpha_max≈8.0`；同时 first odom 已在路径终点附近，距路径起点约 `7.1786m`、距路径终点约 `0.2409m`，所以 `GOAL_REACHED` 在 INITIALIZED 后约 `0.15s` 即触发。
- progress 摘要：`progress_s start/end/max = 0.984452 / 0.995755 / 0.995755`。
- 新 4 个 topic 已进入 bag：`/spmpc/debug/projector=2061`、`/spmpc/debug/stage0_reference=2061`、`/spmpc/debug/local_traj_head=2061`、`/spmpc/debug/warm_start_head=2061`；但本次 payload 全程为 0，不能用于 near-start lock 机理分析。
- 已同步更新 No-Go 文档增加 `3.11 debug topic 首次 replay 结果：INVALID_ALPHA8`。正式实物仍 `NO-GO`；若继续跑，应先保证 fresh sim 起始 odom/路径起点一致，并确认 runtime_bounds 有非零有效样本。

## alpha8 debug topic 双 precheck replay

- 按用户要求又跑 1 轮 alpha8 同路径 fresh-sim replay；本轮在正式 replay 前增加硬性 precheck：先确认 `/odom` 靠近固定 path 起点，再确认 `/spmpc/debug/runtime_bounds` 非全零且 `alpha_max≈8.0`。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/09_gate1_alpha8_debug_topics_checked_replay/20260610_134504_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_checked_run1`
- bag：`/data/a/scout_spmpc_experiments/raw/2026-06-10/09_gate1_alpha8_debug_topics_checked_replay/20260610_134504_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_checked_run1/20260610_134504_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_checked_run1.bag`
- analysis：`/data/a/scout_spmpc_experiments/raw/2026-06-10/09_gate1_alpha8_debug_topics_checked_replay/20260610_134504_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_checked_run1/20260610_134504_spmpc_B0_P2_alpha_B0_gate1_alpha8_debug_checked_run1_analysis.md`
- precheck_start：`PASS`，odom 到 path 起点 `0.0320m`，到 path 终点 `7.4113m`。
- precheck_bounds：`PASS`，`runtime_bounds.alpha_max=8.0`，`generated_bounds.alpha_max=1.2000000476837158`，runtime bounds 非全零；runtime sample 为 `[-0.6,0.6,-8.0,8.0,0.0,0.8,0.0,0.8,-1.2,1.2]`。
- bag 内推进结果：`progress_s start/end/max = 0.000367 / 0.981806 / 0.981806`；first `GOAL_REACHED≈38.10s` after first progress sample；status counts 为 `WAITING_FOR_ODOM=1, B0_ACADOS_OK=1017, ACADOS_SOLVE_FAILED_4=129, GOAL_REACHED=903`。
- 新 4 个 topic 均有效录包且 payload 非零：`/spmpc/debug/projector=2046`、`/spmpc/debug/stage0_reference=2046`、`/spmpc/debug/local_traj_head=2046`、`/spmpc/debug/warm_start_head=2046`。
- 本次未复现 run2 near-start lock；约 `5s` 时 `progress_s≈0.03497`，随后继续推进并到点。
- 注意：subagent 开始 formal 计时时，`/spmpc/terminal/mode` 已是 `REACHED`、`progress_s≈0.9810`，所以该样本不包装成严格有效 Gate 1 PASS；它更适合作为“precheck 通过 + alpha8 能到点 + 新 debug payload 有效”的证据。正式实物仍 `NO-GO`。
- 已同步更新 No-Go 文档增加 `3.12 起点/runtime_bounds 双 precheck replay`。

## warm-start `v_s = Δs/dt` 一致性外科修正

- 根据 near-start low-progress basin 的外部复盘建议，先修一个明确代码级问题：alpha-state OCP 中 `s_dot = v_s`，但 flatness warm-start / conservative fallback 之前从物理速度 `state.v` 派生 `control.v_s`，起步 `robot.v=0` 时可能与已推进的 `state.s` 序列不一致。
- 修改 `src/scout_apps/control/spmpc_local_planner/src/warm_start/diff_drive_flatness_warm_start.cpp`：`DiffDriveFlatnessWarmStart::generate` 中 `control.v_s` 改为由相邻 warm-start state 的 `Δs / dt` 生成，再走原有 `v_s_max` / `v_max` clamp 与 `bound_violation_count`。
- 修改 `src/scout_apps/control/spmpc_local_planner/src/solvers/continuous_mpcc_solver_acados.cpp`：`makeConservativeWarmStart` 先生成全部 state，再用相邻 `state.s` 的 `Δs / dt` 回填 `controls[k].v_s`，保持 `[0, params.v_max]` clamp；不改 objective、projector、terminal、cmd safety 或 acados codegen。
- 扩展 `src/scout_apps/control/spmpc_local_planner/test/test_diff_drive_flatness_warm_start.cpp`：`PreservesInitialOmegaAndClampsPathSpeed` 现在逐拍断言 `control.v_s == clamp((states[k+1].s - states[k].s) / dt, 0, v_s_max)`，覆盖 `robot.v=0` 但 path seed 前进的旧 bug。
- 已完成非仿真验证：`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner" --pkg spmpc_local_planner` 通过；`catkin_make run_tests_spmpc_local_planner_gtest_test_diff_drive_flatness_warm_start --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner"` 5/5 通过；`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="scout_local_planner" --pkg scout_local_planner` 通过；最后已恢复 `CATKIN_WHITELIST_PACKAGES=spmpc_local_planner` 并重建通过；`git diff --check` 通过。
- 本修正只解决 warm-start path-progress channel 的动态一致性；尚未补跑 fresh-sim replay，不能改变正式物理结论。`continuous_mpcc_acados` alpha-state 主线仍为实物前 `NO-GO`。

## `v_s=Δs/dt` 后严格 alpha8 fresh-sim Gate 1 replay

- 按安全 SOP 调用 subagent 跑了 1 轮 `continuous_mpcc_acados + B0 + alpha_max=8.0` 严格 fresh-sim Gate 1 replay：fresh sim 启动后等待 30s、formal 计时前检查起点/runtime bounds/timer boundary、70s 未到点判失败、结束后只停止本轮启动进程并等待 30s。
- run 目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/10_gate1_alpha8_after_vs_dt_fix_strict_replay/20260610_145721_spmpc_B0_alpha8_gate1_strict_replay`
- bag：`/data/a/scout_spmpc_experiments/raw/2026-06-10/10_gate1_alpha8_after_vs_dt_fix_strict_replay/20260610_145721_spmpc_B0_alpha8_gate1_strict_replay/20260610_145721_spmpc_B0_alpha8_gate1_strict_replay.bag`
- analysis：`/data/a/scout_spmpc_experiments/raw/2026-06-10/10_gate1_alpha8_after_vs_dt_fix_strict_replay/20260610_145721_spmpc_B0_alpha8_gate1_strict_replay/analysis/analysis.md`；summary：`analysis/run_summary.md`、`analysis/run_summary.json`。
- precheck_start：PASS，odom 到 path 起点 `0.091m`，到 path 终点 `7.389m`。
- precheck_bounds：PASS，`runtime_bounds.alpha_max=8.0`，`generated_bounds.alpha_max=1.2`。
- precheck_timer_boundary：PASS，formal 前 `terminal=TRACKING`、`progress_s=0.006390`，不是已 REACHED 样本。
- 结果：`FAIL`；strict timer 从 precheck 后第一条 `terminal != REACHED` 且 `progress_s <= 0.1` 的有效样本开始，70.012s 内没有 `GOAL_REACHED/REACHED`。
- progress：`progress_s start/end/max = 0.006002 / 0.006390 / 0.006477`，near-start lock 在严格口径下复现。
- 状态统计：`WAITING_FOR_ODOM=1`、`B0_ACADOS_OK=7097`、`ACADOS_SOLVE_FAILED_4=38`；terminal mode 全程 `TRACKING=7134`。
- 新 4 个 debug topic 均有效录包且 payload 非零：`/spmpc/debug/projector=7135/7135`、`/spmpc/debug/stage0_reference=7135/7135`、`/spmpc/debug/local_traj_head=7135/7097`、`/spmpc/debug/warm_start_head=7135/7135`（格式为 msgs/nonzero）。
- 结论：`v_s=Δs/dt` 是必要一致性修正，但不是 near-start basin 的充分修复；该有效 alpha8 strict replay 失败进一步确认正式实物仍 `NO-GO`。下一步应基于本轮有效 debug payload 做离线机理诊断。

## strict replay near-start lock 离线机理诊断

- 对上节有效 FAIL bag 做了 offline-only 诊断；未启动仿真、未发 `/cmd_vel`、未改代码。
- 诊断目录：`/data/a/scout_spmpc_experiments/raw/2026-06-10/10_gate1_alpha8_after_vs_dt_fix_strict_replay/20260610_145721_spmpc_B0_alpha8_gate1_strict_replay/analysis/near_start_lock_diagnostics`
- 生成文件：`parse_near_start_lock.py`、`key_time_samples.csv`、`near_start_lock_summary.json`、`near_start_lock_diagnostics.md`。
- 关键结果：`progress_s` 在 strict 70s 窗口内完全锁住，`start/max/end = 0.006390 / 0.006390 / 0.006390`。
- warm-start 证据：`warm_start_head` 前 3 拍 `control_vs=0.56/0.56/0.56`、`state_s=0.054748/0.073414/0.092081`，说明 `v_s=Δs/dt` 修正已让 seed 带非零 path-progress；旧 `v_s=state.v` bug 不是唯一根因。
- OCP/first-shot 证据：`first_shot u0_vs` 仍为 `0.0`，说明非零 warm-start 没有转化为非零求解输出。
- projector 证据：`raw_s` 全程 `0.0`，`guarded_s` 全程 `0.05474757`，`monotonic_clip_applied=2100/2100`，表示投影与 guarded progress 都困在 path front 小邻域。
- local trajectory 证据：前 3 个 solved local trajectory head 的 solver `s` 全程停在 `0.05474757`，几何 `proj_s` 全程为 `0.0`；不是“solver s 前进但投影回前端”，而是 solved s 和 projected s 都被困住。
- command 证据：各时间窗 `cmd_v` 基本为 `1e-10` 量级，`cmd_omega` 小幅非零（约 `0.0025~0.0048 rad/s`），符合轻微原地 heading 活动但无平移。
- stage0 证据：`yaw_error≈0.984~0.990rad`、`contour_error≈-0.080~-0.074m`、`lag_error≈0.152~0.158m`，说明起点局部 basin 明显。
- 当前排序假设：1) projector/path-front geometry lock；2) stage0 yaw/contour/lag basin；3) progress objective 激励或耦合不足；4) alpha-state omega/heading 动力学导致可转向但不推进；5) 缺少 start-lock recovery/safety。
- 已同步更新 No-Go 文档增加 `3.15 strict replay 离线机理诊断`；正式实物继续 `NO-GO`。

## StartLockRecovery detector-only 观测层

- 新增纯诊断 `StartLockRecovery`：默认 `start_lock_recovery.enable=false`，`detect_only=true`，只输出 active/mode/debug，不覆盖 `/cmd_vel`，不改 terminal、projector、objective、acados codegen 或现有 topic layout。
- 检测口径使用绝对 `progress_abs_s`：近起点窗口内 progress 长期不增长、`cmd_v` 近零、warm-start 首拍 `control_v_s` 要求前进、first-shot `u0_v_s` 近零、且 monotonic clip 按配置触发时，才进入 `ACTIVE_START_LOCK`；投影距离过大只报 `UNSAFE_PROJECTION_DISTANCE`。
- 新增 additive topic：`/spmpc/start_lock/active`、`/spmpc/start_lock/mode`、`/spmpc/start_lock/debug`；未修改 `/mpc/cost_breakdown`、`/slosh/*`、`/terminal/*`、`/reference/*`、`/profile_cap/*` 或既有 `/spmpc/debug/*` layout。
- 新增 `test_start_lock_recovery` gtest，覆盖默认关闭、dwell 后触发、progress 恢复清除、离开 start window、无 warm-start/solver mismatch、无 monotonic clip、projection distance unsafe、goal reached 不触发等场景。
- 已完成非仿真验证：`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner" --pkg spmpc_local_planner` 通过；`run_tests_spmpc_local_planner_gtest_test_start_lock_recovery` 8/8 通过；`test_terminal_controller` 6/6 通过；`test_diff_drive_flatness_warm_start` 5/5 通过；`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="scout_local_planner" --pkg scout_local_planner` 通过；最后已恢复 `CATKIN_WHITELIST_PACKAGES=spmpc_local_planner` 并重建通过；`git diff --check` 通过；新增 start-lock 文件 trailing-whitespace 脚本检查通过。
- 该补丁只让 near-start lock 可显式观测，仍未做 recovery/override/hard-zero；正式实物继续 `NO-GO`。

## current-pose strict Gate false-PASS 与 tracking_safety 门控

- 用户现场观察明确指出前一轮脚本 PASS 不成立：机器人“根本没有跟上轨迹”“启动的那段时间一直在转圈”。因此本轮把视觉行为和 bag 行为复核作为 Gate 结论的一部分，不能只看 `/spmpc/terminal/mode=REACHED` 或 `/spmpc/status=GOAL_REACHED`。
- 修正 terminal false-PASS 根因：`SpmpcProblem::solve()` 中 `TerminalGoalInfo.distance_to_goal` 改为机器人到 reference path 末端的欧氏距离，`remaining_s` 仅保留为投影/剩余弧长诊断，不再作为 `position_reached` 的距离依据。
- 新增 terminal 角速度出口限制：`terminal.command_clamp.omega_enable/omega_max/omega_near_goal_max/omega_near_goal_distance`，terminal phase 内先把 `cmd_omega` clamp 到 `0.25rad/s`，近终点进一步限制到 `0.10rad/s`。
- 新增 ROS wrapper 层 terminal spin-fail：terminal phase 内持续高角速度会置 `output.status=TERMINAL_SPIN_FAIL`、`output.success=false` 并输出零命令，避免 near-goal 转圈被误报为成功。
- 新增 ROS wrapper 层 `tracking_safety`：默认开启 projection 和 tracking-phase spin 检测；`TRACKING_UNSAFE_PROJECTION` 或 `TRACKING_SPIN_FAIL` latch 后强制 hard-zero `/cmd_vel`。`publishZeroCommand()` 现在在失败路径直接发布全零 Twist，不再通过共享加速度限幅慢慢回零。
- 修正 repeated fixed-path 发布导致 safety latch 被清零的问题：`pathCallback()` 现在用 path frame、size、首点和尾点生成 reference signature，只有实际路径签名变化时才 reset terminal/tracking gates；重复发布同一路径不会解除 latch。
- strict fresh-sim/current-pose generated fixed path 终端修复轮次已改判失败：`/data/a/scout_spmpc_experiments/raw/2026-06-10/14_gate1_alpha8_strict_fresh_current_pose_terminal_spin_fix/20260610_183000_gate1_b0_alpha8_strict_fresh_current_pose_spinfix_run1/run_summary.json`。行为复核显示虽然后来 terminal 欧氏距离约 `0.166m < 0.20m`，但最大离路径约 `1.156m`、`|omega|>0.5` 持续约 `11.15s`、StartLockRecovery unsafe projection 约 `7.56s`，所以不是可接受 Gate PASS。
- tracking_safety 最终 strict fresh-sim 有效样本：`/data/a/scout_spmpc_experiments/raw/2026-06-10/15_gate1_alpha8_strict_fresh_current_pose_tracking_safety/20260610_194000_gate1_b0_alpha8_strict_fresh_current_pose_trackingsafety_run4/run_summary.json`。结果 `FAIL`，`fresh_sim_gate_valid=true`，formal `14s` 时 `TRACKING_UNSAFE_PROJECTION` 触发并 latch，`terminal_mode_counts={TRACKING:377}`，没有 terminal reach。
- run4 行为复核：`behavior_check.json` 判定 `FAIL_SAFETY_GATE`；首次 guarded projection distance 超过 `0.5m` 在约 `13.83s`，first safety status 约 `14.0s`，`TRACKING_UNSAFE_PROJECTION=32`，latch 后 `/cmd_vel` 全部 hard-zero。说明新门控能阻止 terminal-only false PASS，但主线 tracking/path-feasibility 问题仍未解决。
- 最终非仿真验证已补跑：`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="spmpc_local_planner" --pkg spmpc_local_planner` 通过；`run_tests_spmpc_local_planner_gtest_test_terminal_controller` 9/9 通过；`run_tests_spmpc_local_planner_gtest_test_start_lock_recovery` 8/8 通过；`catkin_make --force-cmake -DCATKIN_WHITELIST_PACKAGES="scout_local_planner" --pkg scout_local_planner` 通过；`git diff --check` 通过。
- 结论：当前代码已经堵住“投影/terminal 口径误报 PASS”和“tracking 已失控仍继续发非零命令”的门控缺口；但 `continuous_mpcc_acados + B0 + alpha_max=8.0` 在 current-pose strict fresh-sim Gate 仍失败，正式实物继续 `NO-GO`。下一阶段应修 planner tracking/path-feasibility 根因，不能通过修改 world/map/URDF/model/spawn/Cartographer/TF 或移动机器人来掩盖问题。
- 针对“是否可能是仿真不准、想先上实物跑”的现场问题，新增实物端 shadow/dry-run 安全文档：`docs/实物实验注意事项/代码移植/20260610_SPMPC主线实物Shadow测试与NO-GO安全流程.md`。文档明确：可以用真实传感器栈做 SPMPC shadow/no-publish 诊断，但当前不能把 SPMPC 主线直接接 `/cmd_vel` 地面闭环跑。
- 按用户确认“现场安全措施已到位、直接走地面闭环”的 operator override 口径，补充 `docs/实物实验注意事项/代码移植/20260602_实物端代码拉取与子模块注意事项.md` 第 9 节：记录直接地面闭环启动 SPMPC 主线的人工安全条件、固定目标点 `x=7.164488315582275, y=9.307367324829102, yaw=1.0808`、`alpha_max:=8.0` 手动 roslaunch、短窗口录包、立即停机条件和结果记录口径；同时更新 shadow 文档中的同一固定目标点命令。
- 针对本次“实地落实 SPMPC 主线，不录全量”的需求，新增轻量录包脚本 `src/scout_apps/control/spmpc_local_planner/scripts/record_spmpc_mainline_ground_smoke.sh`。默认面向 `B_ours + continuous_mpcc_acados`，即 slosh 融入 state 和 cost、由 acados 求局部最优解；默认只录 `/cmd_vel`、`/odom`、`/tf`、路径、`/spmpc/*` 关键诊断、SPMPC slosh state/cost 相关量和少量 rosout/diagnostics，不录全量相机图像。同步把 operator override 与 shadow 文档的默认 planner 从 `B0` 改为 `B_ours`，并说明消融可切到 `B_slosh`。
- 开测前最终整理 `docs/实物实验注意事项/代码移植/20260610_SPMPC主线实物Shadow测试与NO-GO安全流程.md`：改成现场可直接照做的简洁 runbook，明确测试顺序 `B0 -> B_slosh -> B_ours`，包含传感器检查、固定目标点、路径生成、轻量录包脚本、shadow/闭环启动命令、立即停机条件和测试结论记录口径。
