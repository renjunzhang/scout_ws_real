# 2026-06-11

## SPMPC 实物 Smoke：P2P 到固定路径 SOP

- 新增实物试验步骤文档：`docs/实物实验注意事项/代码移植/20260611_SPMPC实物Smoke_P2P到固定路径步骤.md`。
- 文档明确本轮不把现有仿真作为实物放行门槛，实物 smoke 以真实 `/odom`、TF、`/map`、`/scan_front`、bag 和现场人工安全观察为准。
- 试验顺序固定为：实物传感器栈与 `/odom`/TF/`/map`/`/scan_front` 检查 -> P0 straight 点到点 B0 smoke -> P2 s_curve 固定路径 B0/B_slosh/B_ours -> 默认速度全部安全后才允许 `fast_diagnostic` 提速诊断。
- 点到点 smoke 采用与 20260610 实物流程一致的传感器栈启动方式、同一固定终点 `x=7.164488315582275, y=9.307367324829102, yaw=1.0808`，并建议用 `template_fixed_path_generator.py --template straight` + `spmpc_fixed_path.launch`，避免一开始引入复杂 P2P pipeline。
- 录包步骤默认强调 `RECORD_SCAN=true`，保留 `/scan_front` 作为实物安全与障碍证据；每个 variant/run 单独命名 bag，保留失败数据。
- 文档保留 shadow-first、operator override 闭环、立即停机条件、zero `/cmd_vel`、现场记录模板和测后窗口化指标分析流程。
- 本轮只新增/更新文档，没有修改控制代码、launch 参数语义、topic layout、仿真环境或实验数据；提交前补跑 `git diff --check`、metrics 脚本 `py_compile`、fixed-path shell `bash -n`、launch XML parse、`spmpc_local_planner` build、`test_terminal_controller` gtest、`scout_local_planner` build，均通过。未启动仿真、未启动实物、未发布 `/cmd_vel`。

## TEB/DWA 新隔离 Scout 仿真脚本与对比矩阵

- 在 `/data/a/scout_sim_replacement` 下补充 TEB/DWA proxy-local launch、attach、smoke 与 observer 入口，复用 read-only `/home/a/scout_ws` 中的 `baseline_local_planner_runner`、TEB/DWA standalone config 与 `slosh_models`。
- 新增 proxy baseline costmap：`/data/a/scout_sim_replacement/classic_ws/src/scout_mini_proxy_nav_adapter/config/local_costmap_proxy_baseline.yaml`，避免为了隔离 proxy baseline 修改旧 workspace costmap。
- 新增对比矩阵 SOP：`docs/实物实验注意事项/实物实验前的仿真/20260611_TEB_DWA隔离仿真对比矩阵SOP.md`，明确 SPMPC/TEB/DWA 的 path/topic/frame/cmd/status/slosh evaluation-only 口径。
- 安全边界：未修改 world/map/URDF/robot model/Gazebo model/spawn/Cartographer/TF；TEB/DWA 不使用 `/slosh/*` 做控制；脚本只设计 tracked-PID cleanup，不使用 broad `killall`/`pkill`。
- 验证：静态检查通过（shell `bash -n`、observer `py_compile`、TEB/DWA launch XML parse、ROS package/launch resolution）。随后各跑一次隔离 closed-loop smoke：TEB 与 DWA 均有 `/baseline/<planner>/status`、路径、`/cmd_vel`、`/cmd_vel_drive`、`/slosh/height` 记录，但 70s 内均未发布 `GOAL_REACHED`，observer 标记 `GOAL_NOT_REACHED`；结果已写入仿真对比矩阵 SOP。smoke 后 ROS master 不可通信，Gazebo 输出 `An instance of Gazebo is not running.`。
- 追加修正 TEB/DWA fixed path 默认 one-shot 发布：`PATH_PUBLISH_COUNT=1`、`PATH_KEEP_ALIVE_AFTER_PUBLISH_COUNT=true`，避免 2Hz 重复 path/goal 触发反复 `setPlan()` 与 goal latch reset；复测确认 one-shot 生效，`status_counts` 只剩 `TRACKING: 1`，但 TEB/DWA 仍为 `GOAL_NOT_REACHED`。新的主要线索是 `/baseline/<planner>/global_plan` 终点在 `odom` 下约为 `x=5.0`，而 70s 结束时机器人 `/odom` 仅到 TEB `x≈1.017`、DWA `x≈0.864`，下一步应诊断 `path_frame=map -> plan_target_frame=odom` 与 wrapper goal check 口径，不通过修改仿真环境掩盖问题。
- 完成 frame/goal 口径诊断：带 `/tf` bag 的开发 smoke 显示 baseline/local planner 实际使用 TF tree 中的 `odom -> base_footprint`，而 observer 的 `/odom` topic pose 带 spawn/world 偏移，不能直接和 `/baseline/<planner>/global_plan` 比较；真正导致 `GOAL_NOT_REACHED` 的是 `/scout/goal` 的 `GOAL_YAW=0.0` 覆盖了 path 末点 yaw，而 S-curve/global_plan 末端 yaw 约 `0.624~0.625 rad`。仅把诊断输入改为 `GOAL_YAW=0.624` 后，TEB 与 DWA 均发布 `GOAL_REACHED`。结果和建议已追加到仿真对比矩阵 SOP；未修改仿真环境。
- 落实 TEB/DWA fixed-path goal yaw 修正：`proxy_fixed_path_publisher.py` 新增 `goal_yaw_mode`，默认仍为旧行为 `explicit`，只在 `proxy_teb_localized.launch` 与 `proxy_dwa_localized.launch` 默认启用 `path_end`；baseline attach/smoke 脚本透传并记录 `GOAL_YAW_MODE`。修正后用默认 `GOAL_YAW=0.0` + `GOAL_YAW_MODE=path_end` 复测，TEB 和 DWA 均 `GOAL_REACHED`，slosh evaluation-only 正常记录；复测后 ROS master/Gazebo 均退出，未使用 broad kill。
- 为公平 peak 对比补齐 SPMPC 外部 `/slosh/*` 评估支持：`run_proxy_spmpc_mainline_smoke.sh` 新增 `SLOSH_MONITOR_ENABLE`，会启动 `slosh_models/slosh_monitor.launch` 并在 bag 中记录 `/slosh/height`、`/slosh/state`、`/slosh/debug`；只处理脚本记录的 PID，不改 world/map/URDF/spawn/Cartographer/TF。
- 补跑单次 bag-backed 公平矩阵 `fair_peak_20260611_190337`：SPMPC `B_ours` 显式 `ALPHA_MAX=1.2`、`MAX_ANGULAR_ACCEL=1.2`，TEB/DWA 使用 common-limit `0.8/1.2/0.6/1.2` 与 `GOAL_YAW_MODE=path_end`，三者均 `GOAL_REACHED`。
- 已把 ALL 与去起终点段（progress 10%--90%）peak/p95 表和参数公平表追加到 `docs/实物实验注意事项/实物实验前的仿真/20260611_TEB_DWA隔离仿真对比矩阵SOP.md`；当前单次结果：ALL peak 为 SPMPC 1.266mm、TEB 2.350mm、DWA 2.938mm，去起终点段 peak 为 SPMPC 0.939mm、TEB 2.346mm、DWA 2.361mm。该表标注为公平单次开发对比，不冒充 strict fresh-sim Gate。
- 补齐 plain `mpc_local_planner` 隔离 proxy baseline 入口与 strict fresh-sim tooling：新增 `/data/a/scout_sim_replacement/classic_ws/src/scout_mini_proxy_nav_adapter/launch/proxy_mpc_local_planner_localized.launch`、`run_strict_fresh_fair_comparison_n3.sh`、`compute_strict_fair_metrics.py`，并扩展 baseline attach/smoke/observer 支持 `mpc_local_planner`。
- 修正 runtime overlay：`mpc_local_planner` 使用既有 `/home/a/scout_ws/install_isolated_mpc/setup.bash` 提供 plugin library；Cartographer isolated workspace 仅在 `/data/a` proxy runtime 显式 source，未修改 map/world/URDF/spawn/定位参数/TF 语义。
- 完成 strict fresh-sim N=3 批次 `strict_fresh_fair_n3_20260611_202103`（12 case，30s pre-control settle，70s timeout，30s post-shutdown wait）：`mpc_local_planner`、TEB、DWA 均 3/3 `GOAL_REACHED`，SPMPC `B_ours` 三次均 `TRACKING_UNSAFE_PROJECTION`，批次 `failed_cases=3`、`invalid_cases=0`，整体 NO-GO。
- 已生成 strict aggregate 表与图：`/data/a/scout_sim_replacement/results/strict_fresh_fair_n3_20260611_202103/strict_fair_metrics_aggregate.json`、`.csv`、`strict_fair_metric_comparison.png/svg`；并把 NO-GO 结论写入实物前仿真 SOP 与 `20260611_SPMPC实物Smoke_P2P到固定路径步骤.md`。实物四 planner 正式对比仍不得开始，需先复核 SPMPC strict runtime 的 map->odom 跳变/unsafe projection。
- 复核“SPMPC 为什么突然不行”：上一轮成功 `fair_peak_20260611_190337` 使用 established map `/data/a/scout_sim_replacement/maps/proxy_world_manual_saved_20260611_154348.pbstream`，而初始 strict 两阶段 SPMPC 环境启动日志为 `MAP_FILE=<localization launch default>`，落回 `/home/a/scout_ws/src/scout_apps/scout_maps/maps/map_sim_empty.pbstream`；失败 bags 中 `map->odom` 跳变约 `4.47~4.50m`，上一轮成功 max jump 约 `0.0193m`。
- 按用户要求补跑 `B_slosh` strict fresh-sim N=3：`/data/a/scout_sim_replacement/results/strict_fresh_spmpc_B_slosh_n3_20260611_205842`，3/3 valid 但 0/3 到点，三次均 `TRACKING_UNSAFE_PROJECTION`，说明初始失败不是 `B_ours` 特有退化。
- 显式 established map 复测 SPMPC strict N=3：`/data/a/scout_sim_replacement/results/strict_fresh_spmpc_explicit_map_n3_20260611_211023`，`B_ours` 与 `B_slosh` 共 6 case 全部 `GOAL_REACHED`，`failed_cases=0`、`invalid_cases=0`；aggregate 已写入 `strict_spmpc_explicit_map_metrics_aggregate.json`。
- 修正 strict SPMPC runtime 口径：`run_proxy_spmpc_mainline_smoke.sh` 和 `run_strict_fresh_fair_comparison_n3.sh` 默认并记录 established `MAP_FILE`，防止 strict fresh-sim 两阶段启动静默使用 localization launch 默认 map；这只对齐运行口径，不修改 world/map 文件内容/URDF/spawn/Cartographer 参数/TF 语义。
- 已同步更新实物前仿真 SOP 与 `20260611_SPMPC实物Smoke_P2P到固定路径步骤.md`：初始 SPMPC NO-GO 保留为 map 口径不一致诊断证据，corrected strict evidence 记录为 SPMPC `B_ours`/`B_slosh` explicit-map N=3 PASS；实物仍必须先做 P0 straight SPMPC B0 与 P2 SPMPC smoke，不能直接上四 planner 正式对比。
- 新增可选全量白名单录包脚本 `src/scout_apps/control/spmpc_local_planner/scripts/record_spmpc_full_rgb_bag.sh`，用于实物 SPMPC smoke + 离线 RGB 分析；默认录 RGB 原图/相机内参、`/scan_front`、SPMPC debug、TF/odom/map/IMU、外部 `/slosh/*` 与诊断 sidecar，但只录不控、不发布 `/cmd_vel`。已在实物 smoke SOP 中补充可选使用方式。