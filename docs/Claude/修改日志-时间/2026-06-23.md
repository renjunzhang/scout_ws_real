# 2026-06-23 修改/运行日志

## 官方 LT-DWA 隔离复现与 wrapper 设计

### 安全边界

```text
主仓库代码：未修改 /home/a/scout_ws/src
官方源码：未修改 /data/a/lt_dwa_official_repro_ws/src/LT_DWA
仿真/控制：未启动 Scout 仿真；未发布 /cmd_vel 或 /benchmark/cmd_vel_raw
未触碰：SPMPC OCP、ROS DWA、slosh monitor/control 链路
```

### Phase 1：官方 LT-DWA 隔离复现

```text
workspace=/data/a/lt_dwa_official_repro_ws
upstream=https://github.com/flztiii/LT_DWA.git
commit=6f49cce70360c27d6b8afac4ae24271b40002b3c
```

结果：

```text
[PASS] official LT-DWA isolated clone/build
[PASS] declared rosdep check
[PASS] full catkin Release build
[PASS] static/orca launch --nodes dry check by full path
[PARTIAL] bounded official static demo: 22/22 observed successes before 180s timeout
```

记录：

```text
/data/a/lt_dwa_official_repro_ws/notes/phase1_reproduction_manifest.md
/data/a/lt_dwa_official_repro_ws/logs/static_demo_20260623_125353/
```

### Phase 2：interface audit

完成官方 LT-DWA interface audit，结论是 upstream 当前是 demo/batch harness，不是可直接接 Scout 的 runtime planner。

关键结论：

```text
local_planner_node 有 getchar() 和 300-case batch loop
无 odom/TF/lidar/global-path 订阅
无 /cmd_vel 发布
可复用 seam 是 Policy::forward / SeedPolicy::forward
frame 存在 odom/base_footprint/world 混用，必须 wrapper 显式规范
```

记录：

```text
/data/a/lt_dwa_official_repro_ws/notes/phase2_interface_audit.md
```

### Phase 3：wrapper 设计与隔离 skeleton

完成 Phase 3 wrapper 设计，并在 `/data/a` 创建隔离 wrapper workspace：

```text
/data/a/lt_dwa_wrapper_ws/src/lt_dwa_official_wrapper
```

已完成：

```text
[PASS] Phase 3A/3B wrapper package skeleton
[PASS] wrapper-owned types/status/config
[PASS] FrameValidator
[PASS] path/map/obstacle converters
[PASS] PlannerFacade skeleton
[PASS] worker isolation skeleton
[PASS] WorkerRequest file payload
[PASS] worker --mode plan-request --request <path>
[PASS] Phase 3F frozen offline worker request fixture
[PASS] worker supervisor fixture regression
[PASS] Phase 3G / #33 worker-only official core boundary design
[PASS] Phase 3H / #34 worker-only official-core-once mode implemented and compile-gated
[PASS] Phase 3I / #35 official core offline worker validation
[PASS] Phase 4A / #36 Scout-facing bridge shadow-safe design
[PASS] Phase 4B / #37 bridge shadow mode implementation
[SKIP] Phase 5A / #38 current-sim dry diagnostics: no ROS master/current sim available
[PASS] Phase 5B / #39 fresh-sim visible Scout SOP smoke: official LT-DWA worker core OK in shadow mode
```

当前仍未做：

```text
[PASS OFFLINE] official SeedPolicy::forward(...) completed inside worker with private local_map service
[PASS] Scout-facing ROS bridge shadow mode implemented under /data/a/lt_dwa_wrapper_ws
[PASS SHADOW] official LT-DWA worker core completed inside visible Scout isolated SOP with /baseline/official_lt_dwa/* outputs
[NOT STARTED] any command topic publication
[NOT CLAIMED] goal-reaching/tracking benchmark; command publication remained disabled
```

验证：

```text
catkin_make --directory /data/a/lt_dwa_wrapper_ws -DCMAKE_BUILD_TYPE=RelWithDebInfo  # PASS
catkin_make --directory /data/a/lt_dwa_wrapper_ws run_tests                         # PASS
catkin_test_results /data/a/lt_dwa_wrapper_ws/build/test_results
Summary: 66 tests, 0 errors, 0 failures, 0 skipped
```

Fresh-sim shadow smoke：

```text
SOP workspace: /data/a/scout_sim_replacement
MAP_FILE=/data/a/scout_sim_replacement/maps/proxy_world_manual_saved_20260611_154348.pbstream
Gazebo UI: visible, gzclient running
Bridge status: /baseline/official_lt_dwa/status = OK
Worker result: LT_DWA_WORKER_RESULT status=OK reason=official_core_ok command_v=0.198015 command_w=0.155553 core_return=0
Command safety: /cmd_vel Publishers: None; /benchmark/cmd_vel_raw absent
```

60s `/cmd_vel` benchmark：

```text
User-approved command publication: /baseline/official_lt_dwa/shadow_cmd_vel -> /cmd_vel for bounded 60s run
Criterion: 60s not reaching endpoint = FAIL
Start: x=-0.032064 y=0.003556 goal=(5.0, 0.0) distance=5.032065
Best observed distance: 1.524023 m
Final: x=-0.428727 y=1.832073 distance=5.729535
Result: FAIL reason=timeout_60s_not_reached elapsed=60.500
Post-run safety: /cmd_vel Publishers: None; zero Twist published on exit
```

失败诊断：

```text
Primary cause: smoke-gate bridge ran at 0.5 Hz, producing one worker request about every 2.0 s.
Official core returns opt_states[1] for time_step=0.2 s, so the benchmark held a first-step command about 10x too long.
Contributing cause: official LT-DWA cost follows nearest path-derived reference states; target_pose is only used for goal-reached check, not as a terminal attraction term.
Representative replay near closest point: t=2351.1 pose=(3.574,0.619) yaw=-1.641 dist=1.554 -> command_v=0.688737 command_w=-0.427996.
Conclusion: controller/bridge integration failure, not Gazebo startup or missing /cmd_vel failure.
```

#45 real-time command bridge：

```text
[PASS] 实现 30 Hz command publication loop（默认 shadow_cmd），planner cadence 默认 5 Hz
[PASS] 官方 LT-DWA core 仍只在 worker process 中调用，parent bridge 不 in-process 调 official core
[PASS] stale/missing/invalid/non-OK worker result -> zero command；默认 enable_actuated_output=false，不发布 /cmd_vel 或 /benchmark/cmd_vel_raw
[PASS] 私有 ROS master 离线诊断：diagnostics 显示 planner_rate_hz=5、command_publish_rate_hz=30、effective_cmd_vel=false、effective_benchmark_raw=false；/cmd_vel 与 /benchmark/cmd_vel_raw 均 Unknown topic
```

验证：

```text
catkin_make --directory /data/a/lt_dwa_wrapper_ws -DCMAKE_BUILD_TYPE=RelWithDebInfo -DLT_DWA_WRAPPER_ENABLE_OFFICIAL_CORE=ON  # PASS
catkin_make --directory /data/a/lt_dwa_wrapper_ws run_tests && catkin_test_results /data/a/lt_dwa_wrapper_ws/build/test_results
Summary: 74 tests, 0 errors, 0 failures, 0 skipped
```

记录：

```text
/data/a/lt_dwa_official_repro_ws/notes/phase3_wrapper_design.md
/data/a/lt_dwa_wrapper_ws/notes/phase3a3b_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3c_planner_facade_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3d_worker_isolation_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3e_worker_request_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3f_worker_fixture_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3g_worker_core_boundary.md
/data/a/lt_dwa_wrapper_ws/notes/phase3h_worker_core_mode_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase3i_official_core_offline_validation.md
/data/a/lt_dwa_wrapper_ws/notes/phase4a_scout_bridge_design.md
/data/a/lt_dwa_wrapper_ws/notes/phase4b_bridge_shadow_mode_manifest.md
/data/a/lt_dwa_wrapper_ws/notes/phase5a_current_sim_dry_diagnostics.md
/data/a/lt_dwa_wrapper_ws/notes/phase5b_fresh_sim_smoke_gate.md
/data/a/lt_dwa_wrapper_ws/notes/phase6_60s_cmd_vel_benchmark.md
/data/a/lt_dwa_wrapper_ws/notes/phase6_60s_benchmark_failure_diagnosis.md
```

### 下一步建议

```text
Phase 5C：把本次 map-frame odom 适配固化到 bridge/launch 层，避免依赖 /tmp 临时适配脚本。
Phase 6：若进入 benchmark-safe/actuated gate，必须另行明确批准命令发布；一般 60s 未到达终点即判 FAIL。
硬约束：parent 不直接调用 official core；缺少结构化响应必须视为 CORE_PROCESS_EXITED；不发布 /cmd_vel 或 /benchmark/cmd_vel_raw，除非进入后续明确批准的 benchmark-safe gate；upstream 保持只读。
```
