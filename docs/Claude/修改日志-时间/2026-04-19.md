# 修改日志 2026-04-19

> 今日类型：论文对比补全与基线规划
> 策略：先冻结当前仿真收敛版本，补齐文献级对比算法方案；不继续扩主线，不先改 MPC 主体。

---

## 1. 当前状态快照

今天开始时，当前版本已经完成：

- Day4 仿真主线阶段性收敛
- `v_plan / v_exec` 解耦
- speed profile 几何来源修正
- 当前版本已作为阶段性基线

同时确认了论文结构上的新缺口：

- 现有 `C0 ~ C5` 全部为自消融；
- 缺少外部对比算法 baseline；
- 按当前提纲，ISR 基线尚未实现。

---

## 2. 今日决策

### 2.1 先上传当前状态

已将当前工作区整体状态提交并推送：

- 分支：`refactor/idea-improvement`
- 提交：`1d28531`
- message：`Checkpoint current research state before ISR baseline plan`

目的：

- 冻结当前阶段性收敛版本；
- 作为补 ISR 之前的稳定基线。

### 2.2 不继续扩当前仿真主线

当前决定：

- 不继续在仿真里追 terminal/near-goal 细节；
- 不继续改 MPC 主约束；
- 先补论文需要的外部 baseline。

---

## 3. 今日新增方案

新增方案文档：

- `docs/Claude/修改方案-时间-简介/2026-04-19_对比算法ISR基线实现与验证.md`

核心内容：

1. 论文当前缺少外部文献级 baseline；
2. 选择 `ISR / ZV input shaping` 作为最小可实现对比算法；
3. ISR 只作用于参考速度层，不改 MPC 主体；
4. 后续对比组先收敛为：
   - `NOM`
   - `ISR`
   - `FAS`
   - `PROP`

---

## 4. 文档状态整理

已确认：

- `docs/Claude/修改方案-时间-简介/2026-04-17_Day4_集成测试与参数调试.md`
  在“仿真优先”缩减目标下可判定为完成
- 已在该文档中把仿真范围内的完成项显式标注为 `[x]`

当前文档口径为：

- Day4 仿真范围已完成
- 实物相关项目顺延

---

## 5. 当前结论

2026-04-19 这一步不再继续扩仿真补丁，而是转入论文对比准备阶段。

当前最合理的下一步是：

1. 实现 ISR baseline；
2. 保持与 PROP 主线解耦；
3. 先做仿真对比，不先做实物 ISR。

这一步的目标不是优化当前控制器，而是补齐论文对比结构缺口。
