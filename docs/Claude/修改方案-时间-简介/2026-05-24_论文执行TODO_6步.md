# 2026-05-24 RA-L 论文执行 TODO（当前收敛版）

> 2026-05-25 决策：不做 P1 `ω²·d / ω̇·d` 偏置耦合，不做 P2 terminal residual。P3 Ferrari ζ 已做离线对比，但不接在线默认。
> 当前主线：固定 SloshPriorityMPC 结构，完成 C/D/E/F 对比实验并锁定论文数据。

当前详细设计：`2026-05-25_SloshPriorityMPC对比实验设计.md`

## 1. 论文问题

```text
Q1: modal slosh cost 单独是否有效？
Q2: ax/jerk 平滑单独是否有效？
Q3: modal slosh cost + ax/jerk 平滑是否构成最好的 SloshPriorityMPC 目标重分配？
```

不再推进：

```text
P1 偏置耦合；
P2 terminal residual；
P3 Ferrari ζ 在线接入。
```

这些写入 limitation / future work。

## 2. 实验分组

| 组别 | 名称 | `Q_slosh` | `mpc_R_a` | `mpc_R_da` | 目的 |
|---|---|---:|---:|---:|---|
| C | Smooth baseline | 0 | 0.5 | 1.5 | 无晃动项 baseline |
| D | Slosh cost only | 5 | 0.5 | 1.5 | modal slosh cost 单独贡献 |
| E | Ax/jerk only | 0 | 1.0 | 2.0 | ax/jerk 平滑单独贡献 |
| F | Slosh + ax/jerk | 5 | 1.0 | 2.0 | 主方法 |

G 组 preview 暂不进主表。已有数据不支持它稳定改善 peak，只能作为 appendix / negative ablation。

## 3. 固定条件

```text
1. 不动 OSCRS / GeoRef / path post-processor。
2. 不改 prediction_v_max。
3. 不用极端 Q_slosh 制造效果。
4. 所有组使用同一 P2_s_curve 固定路径逻辑。
5. 所有组使用 d200 terminal 配置。
6. 主评价窗口固定为 TRACKING start -> first terminal - 1.0s。
7. terminal approach 单独诊断，不进入主效果统计。
8. RGB 真值使用 max(left, center, right)。
9. `/slosh/height` 只作为模型侧解释，不作为真实液面结论。
10. 每个 bag 必须保留 sidecar：topics / rosparam / nodes / info。
```

## 4. 录包 TODO

### 4.1 目录

```bash
mkdir -p /data/a/slosh_bags/real/20260525_fixed_path_cost_d200_factorial
```

### 4.2 样本量

最低可用：

```text
C/D/E/F 各 n=3
```

更稳妥：

```text
C/D/E/F 各 n=5
```

建议 block 交错顺序：

```text
Block 1: C1 -> D1 -> E1 -> F1
Block 2: D2 -> F2 -> C2 -> E2
Block 3: F3 -> E3 -> D3 -> C3
```

### 4.3 启动命令来源

所有启动命令以 `docs/重要文档/20260518_MPC终点收敛与固定路径验证方案.md` 第 3 节为准。

只允许改变分组参数：

```text
C: Q_slosh:=0.0  mpc_R_a:=0.5  mpc_R_da:=1.5
D: Q_slosh:=5.0  mpc_R_a:=0.5  mpc_R_da:=1.5
E: Q_slosh:=0.0  mpc_R_a:=1.0  mpc_R_da:=2.0
F: Q_slosh:=5.0  mpc_R_a:=1.0  mpc_R_da:=2.0
```

其余参数保持一致：

```text
global_path_topic:=/scout/global_path_fixed
terminal_slowdown_distance:=2.00
slosh_preview_factor:=0
damping_ratio:=0.05
```

## 5. 每包验收

每包录完先看：

```text
1. bag 能读取；
2. /camera/color/image_raw 有帧；
3. /mpc_status 有 TRACKING 和 REACHED；
4. /terminal/mode 有记录；
5. /mpc/cost_breakdown 有记录；
6. /reference/v_ref_horizon 有记录；
7. sidecar 四件套齐全；
8. 未出现明显 GOAL_PASSED / turnback；
9. 主窗口长度合理，不被 terminal 过早切断。
```

失败包：

```text
标为 smoke/debug，不进入正式统计；
补录同组同编号；
不要用人工挑选替代预设排除规则。
```

## 6. RGB 与指标分析 TODO

### 6.1 RGB 流程

按 `docs/重要文档/红色液体视觉验证固定流程.md`：

```text
1. 固定相机 exposure / gain / white_balance；
2. 做当天三标尺；
3. HSV 标注；
4. 对每个 bag 跑 red_liquid_infer_from_bag.py；
5. 主指标用 max(left, center, right)。
```

### 6.2 Ferrari-style 指标

```bash
python3 src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_ferrari_indices.py \
    --bag-dir /data/a/slosh_bags/real/20260525_fixed_path_cost_d200_factorial \
    --glob 'slosh_*_d200_*.bag' \
    --red-infer-dir /data/a/slosh_bags/real/20260525_fixed_path_cost_d200_factorial/red_infer \
    --out-csv /data/a/slosh_bags/real/20260525_fixed_path_cost_d200_factorial/ferrari_indices.csv \
    --baseline-bag <C_run01_文件名>.bag
```

### 6.3 ζ 离线对照

P3 已完成：

```text
输出目录：
/data/a/slosh_bags/real/20260522_fixed_path_cost_d200/zeta_fidelity_ablation_20260525

结论：
Ferrari ζ 提升 p95 A_rank，但 gamma/RMSE/corr 变差，不接在线默认。
```

## 7. 统计比较 TODO

必须输出：

```text
D - C: modal slosh cost 单独作用
E - C: ax/jerk 平滑单独作用
F - E: 同等强平滑下，modal slosh cost 的额外作用
F - D: 同等 slosh cost 下，ax/jerk 平滑的额外作用
F - C: 完整方法总收益
```

主表指标：

```text
RGB p95
RGB RMS
RGB peak
model p95
model peak
ax_p95
ay_p95
jerk_p95
duration
gamma_model
gamma_opt
```

## 8. 成功标准

最低成功：

```text
1. D 相对 C：RGB p95 下降；
2. F 相对 C：RGB p95 / RGB peak 下降；
3. F 相对 E：RGB p95 或 peak 继续下降；
4. F 的 duration 增加可接受，并明确报告 trade-off。
```

强成功：

```text
1. D-C 和 F-E 都支持 slosh cost 有增量作用；
2. F 的 RGB peak 最低；
3. ax/jerk 指标能解释 F 的收益来源；
4. terminal 诊断没有污染主窗口。
```

如果只满足 F-C，不满足 D-C 或 F-E：

```text
不能写“slosh cost 单独有效”；
只能写“slosh-priority objective rebalancing 有效”。
```

## 9. 输出交付物

```text
1. C/D/E/F 每组 n=3 或 n=5 bag + sidecar
2. red_infer CSV 和曲线图
3. ferrari_indices.csv
4. group_metrics_main_window.csv
5. reduction_vs_C_main_window.csv
6. terminal_approach_1s.csv
7. cost_breakdown_group_summary.csv
8. 导师汇报版 Markdown
9. 论文图 Figure 1-6
```

## 10. 当前进度

```text
[x] P3 ζ 离线对比完成，不接在线默认
[x] 对比实验设计完成
[ ] C 组 n=3
[ ] D 组 n=3
[ ] E 组 n=3
[ ] F 组 n=3
[ ] RGB infer 完成
[ ] Ferrari-style 指标表完成
[ ] C/D/E/F 差分统计完成
[ ] 导师汇报版结果更新
```
