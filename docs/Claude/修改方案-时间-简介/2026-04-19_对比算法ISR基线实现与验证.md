# 2026-04-19 对比算法方案：ISR 基线实现与验证

> 日期：2026-04-19
> 目标：补齐论文对比算法缺口，在不改 MPC 主体的前提下，实现经典 `ZV input shaping` 基线，用于后续论文对比实验。

---

## 0. 背景与约束

根据

- `docs/Claude/总结/论文提纲.md`

当前论文对比结构存在明显缺口：

- 现有 `C0 ~ C5` 基本都是自消融；
- 缺少外部文献级 baseline；
- 审稿时容易被质疑“仅与自己方法内部变体比较”。

当前最短、最稳的补位方案是：

- 增加 `ISR / ZV input shaping` 基线；
- 不改 MPC 主体；
- 只在参考速度层引入 shaping；
- 与 `NOM / Fixed-Q / PROP` 形成最小可发表对比组。

本次不扩展到第二个外部算法，不重开控制主线。

---

## 1. 方案目标

新增一个可独立开关的 ISR baseline，用于后续实验和论文表格：

- `NOM`：无 slosh 抑制
- `ISR`：经典 ZV input shaping
- `FAS`：固定 `Q_eta` MPC
- `PROP`：完整方法

本次目标不是“把 ISR 做得最优”，而是：

1. 实现一个定义清晰、可复现、可稳定运行的经典基线；
2. 保证它不污染当前 PROP 主线；
3. 后续能用同一条实验链直接跑对比。

---

## 2. ISR 基线定义

采用二脉冲 ZV shaper：

```text
v_shaped(t) = A1 * v_ref(t) + A2 * v_ref(t - t1)

A1 = 1 / (1 + K)
A2 = K / (1 + K)
K  = exp(-zeta * pi / sqrt(1 - zeta^2))
t1 = pi / omega_d
omega_d = omega_0 * sqrt(1 - zeta^2)
```

其中：

- `omega_0` 与 `zeta` 来自当前 slosh 模态参数；
- shaping 只作用于参考速度；
- 不修改内部 MPC 代价、约束、求解结构。

---

## 3. 实现边界

### 3.1 允许修改

- `PathHandler` 或参考速度生成链路
- 仿真/实验配置项
- 实验脚本与分析脚本
- 论文提纲里的对比表述

### 3.2 不修改

- MPC 状态维度
- MPC 约束形式
- risk scheduler 主逻辑
- settling / terminal recovery 主逻辑

### 3.3 开关要求

必须有明确开关，默认关闭：

- `input_shaping/enable`
- `input_shaping/type = zv`

且要求：

- `PROP` 默认不启用 ISR；
- ISR baseline 单独运行；
- 不与 adaptive Q、risk scheduler、额外补丁耦合成混合方法。

---

## 4. 代码落点

优先实现路径：

1. 在参考速度生成处，对 `v_ref` 做 shaping
2. 使用环形缓冲保存延迟项
3. 每周期输出 shaped 后的速度参考

建议落点：

- `src/scout_apps/control/scout_local_planner/src/path_handler.cpp`
- 或 `LocalPlannerROS` 中调用 `getReferencePoints()` 后、送入 MPC 前的参考速度处理层

选择原则：

- 尽量不入侵 MPC 主体；
- 尽量不破坏当前 `v_plan / v_exec` 解耦；
- 尽量保持 ISR 与 PROP 只在“参考层策略”上不同。

---

## 5. 实验设计

### 5.1 最小对比组

先做四组：

1. `NOM`
2. `ISR`
3. `FAS`
4. `PROP`

### 5.2 统一实验要求

- 同一条路径
- 同一终点
- 同一速度上限
- 同一 bag 分析口径

指标沿用当前主线：

- `spill / slosh height`
- `settling time`
- `tracking error`
- `success ratio`
- `solve time`

### 5.3 当前阶段优先级

先做仿真：

1. ISR 能稳定跑通
2. 对比 `NOM`
3. 对比 `PROP`

暂不先做实物 ISR。

---

## 6. 预计输出物

本阶段完成后应有：

1. 一套 ISR 可开关实现
2. 一组 ISR 仿真 baseline bag
3. 一份 ISR 与 NOM / PROP 的初步对比结论
4. 论文提纲中新增 ISR baseline 条目

---

## 7. 当前结论

本方案是当前最短、最稳、最符合论文缺口的下一步：

- 不改主控制方案；
- 不继续堆内部消融；
- 先补一个经典外部 baseline；
- 以最小实现代价换取论文对比完整性。

下一步按此方案实现 ISR，不再继续扩新的外部算法。
