# I0 与显式执行器 OCP 最小修复方案

日期：2026-09-03

> 当前决策基线：`diag/lt-dwa-collision-tracking @ 9b2cb14`
>
> 状态：`RUNTIME-FIX-IMPLEMENTED / SOFTWARE-VERIFIED / NO-MOTION-SMOKE-PASS / HARDWARE-BSLOSH-SMOKE-PENDING / SOURCE-UNCOMMITTED`
>
> 文档边界：本文是独立方案，不修改、取代或合并到 `代码改造方案_唯一主线.md`。

## 结论

当前最直接的最小可用修复定为：

> **放弃 `O0 + L22` 作为新主线，仅保留为历史基线；采用 `processed-IMU I0 + fail_closed + common_epoch + explicit actuator OCP`。**

目标不是一次建立完美底盘模型，而是先消除“命令速度等于实际速度”的结构错误，使 MPC 能有效降低执行延迟对 slosh 的影响。

## 显式执行器结构的实现状态

截至 2026-09-03，显式执行器结构已由提交 `5993ffa` 实现：

- 主线 acados 模型改为显式 command/actual 状态；B0 为 `23` 维、Bslosh 为 `27` 维。
- 30 Hz 下线速度和角速度分别使用 `5` 拍、`10` 拍 FIFO；两通道独立使用 `L/tau/K`。
- common epoch 到求解时刻由同一执行器模型和最终已发布命令历史前推。
- 车体与液体只使用 actual 状态；液体纵向激励直接使用执行器方程给出的 `a_actual`。
- `/cmd_vel` 取 horizon 的 `x1.v_cmd/x1.omega_cmd`，不再由 actual 状态重建。
- `explicit_actuator` 与 legacy `fixed_closed_loop/fixed_robot_only` 同时启用时启动失败。
- 新 ABBA 入口只选择新 profile，录制、现场门和分析继续复用已有公共 engine。

软件证据：

```text
generated B0       nx=23, np=28
generated Bslosh   nx=27, np=37
catkin tests       234 tests, 0 errors, 0 failures, 0 skipped
ABBA validate-only Row01..Row04 全部 PASS，未启动运动
无运动 smoke       explicit_actuator + delay_phase=off 正常初始化
冲突 smoke         explicit_actuator + fixed_closed_loop 以 FATAL 拒绝启动
```

随后完成的 development Block 1 表明：`B0` Row01 主后验通过；`B_slosh` Row02 虽到达终点，但出现 `47` 拍 common-epoch 插值失败和 `10` 拍 acados `MINSTEP`，不能作为降晃效果样本。完整证据见 [Block 1 分析](../实物对比试验分析/20260903_I0显式执行器OCP_B0_Bslosh_Block1分析.md)。

这次失败不否定 command/actual 分离。当前最先要修的是运行时调度和计算超期，不能直接用权重调整掩盖故障性零速。

## 判断依据

已确认：

- 三包离线结果中，I0 相对 O0 的平均 MAE 下降 `27.69%`，因此保留 processed-IMU I0。
- short100 实物结果中，solver 首拍加速度 P95 达到 `0.600 m/s²`，强正负翻转 `76` 次；顿挫由 solver 命令产生，不是发布链或底盘自行停顿。
- 底盘同时存在启动滞后和渐进建立，且响应随命令幅值变化；legacy `0.15/0.22 s` 纯平移不能表达这段动态。

尚未证明：一组固定执行器参数能够覆盖全部速度范围，也尚未证明执行器失配是 slosh 误差的唯一原因。因此首版只在主要工作速度附近验证结构，不先调 `w_slosh` 掩盖问题。

## MVP 只做四项修改

### 1. 完全关闭 legacy L22

关闭整个 `fixed_closed_loop/L22`，机器人状态和液体状态都不再按“命令速度等于实际速度”前推。

关闭 L22 不等于放弃时间补偿：从 common epoch 到 OCP 起点，仍使用最终已发布的 `/cmd_vel` 历史和同一套显式执行器模型前推。显式模型与 legacy L22 必须互斥，同时启用时直接启动失败。

### 2. OCP 区分 command 与 actual

命令状态由优化动作推进：

$$
\dot v_{\rm cmd}=a_{\rm cmd},\qquad
\dot\omega_{\rm cmd}=\alpha_{\rm cmd}
$$

线、角通道分别使用固定延迟、一阶惯性和增益：

$$
\dot v_{\rm actual}
=\frac{K_v v_{\rm cmd}(t-L_v)-v_{\rm actual}}{\tau_v}
$$

$$
\dot\omega_{\rm actual}
=\frac{K_\omega\omega_{\rm cmd}(t-L_\omega)-\omega_{\rm actual}}{\tau_\omega}
$$

不得再把线、角延迟统一取为 $\max(L_v,L_\omega)$。

### 3. 车体和液体统一使用 actual

车体位姿使用预测实际速度：

$$
\dot x=v_{\rm actual}\cos\theta,\qquad
\dot y=v_{\rm actual}\sin\theta,\qquad
\dot\theta=\omega_{\rm actual}
$$

液体激励使用：

$$
a_x=a_{\rm actual}=\dot v_{\rm actual},\qquad
a_y=v_{\rm actual}\omega_{\rm actual}
$$

其中 $a_{\rm actual}$ 直接由执行器方程计算，不再使用 $\Delta v_{\rm cmd}/\Delta t$，也不使用带噪声的 odom 速度差分。

### 4. 发布 command，而不是 actual

`/cmd_vel` 发布 `v_cmd/omega_cmd`。第一拍命令来自：

$$
v_{{\rm cmd},1}=v_{{\rm cmd},0}+a_{{\rm cmd},0}\Delta t
$$

角速度同理，不能再从 $v_{{\rm actual},0}+a_{{\rm cmd},0}\Delta t$ 生成命令。命令初态和延迟队列只由最终 emitted `/cmd_vel` 历史初始化；安全仲裁产生的零命令也必须回写历史。

## 完整闭环

```text
processed-IMU I0 ─┐
                  ├→ common epoch
odom/TF actual ───┘
                        ↓
最终 /cmd_vel 历史 + 显式执行器模型前推
                        ↓
command / actual / delay queue / liquid @ OCP 起点
                        ↓
explicit actuator OCP
   ├→ actual → 车体位姿预测
   ├→ actual → 液体激励与状态预测
   └→ command → 安全仲裁 → 最终 /cmd_vel
                                  ↓
                              回写命令历史
```

## 首版边界与验证顺序

- 在主要工作速度附近先冻结一组 `L_v/L_omega/tau_v/tau_omega/K_v/K_omega`，暂不做幅值调度。
- MVP 可先保留 `a_cmd/alpha_cmd` 为控制量；结构修正后若仍有跨周期翻转，再加入完整时域的 `Delta a_cmd/Delta alpha_cmd` 约束或 jerk 平滑。
- `short100` 只作诊断。模型验证通过后再比较 `167/267/333 ms` 液体代价窗口。

当前验证顺序：

1. 先按独立协议完成 literal `B0/Bslosh` ABBA，postflight 同时核对预测 actual 与 NOKOV/odom、命令状态和 FIFO 契约。
2. 若执行器预测本身不匹配实测，先修正固定 `L/tau/K`，不调 `w_slosh`。
3. 若预测已匹配但命令仍强烈翻转，再增加完整时域的 `Delta a_cmd/Delta alpha_cmd` 或 jerk 平滑。
4. 结构和连续性通过后，再扫描液体代价窗口；需要隔离液体代价贡献时再做 `matched0/matched5`。

## Block 1 后的最小运行时修复

总体顺序固定为：

```text
先消除运行时硬停车
    ↓
再处理 MPC 主动输出的速度锯齿
```

继续冻结以下主线，不回退到 legacy L22：

```text
processed-IMU I0 + fail_closed + common_epoch + explicit actuator OCP
legacy fixed_closed_loop/L22 = off
```

### 第一阶段：只修运行时

1. 将 `/odom` 放入独立 callback queue 和单线程 spinner，subscriber queue 深度设为 `10`。这样 acados 求解超期时，原始 odom 仍可持续进入 planner 历史。
2. 对 `last_odom`、odom history、接收时间和 timing 快照统一加锁；控制周期只复制短快照，不在持锁期间求解或等待 TF。`prev_odom` 只属于单线程 odom spinner，不与控制线程共享。
3. 缓存显式执行器 prefix rollout 的固定子步液体离散模型，避免每个 `10 ms` 子步重复构造矩阵和输出配置日志；非整步尾段仍按实际时长精确传播。
4. 对 acados 部分凝聚参数先做本机软件比较。候选为 `qp_solver_cond_N=5/10/15`，只在无新增求解失败且耗时更低的情况下冻结；生成脚本和 B0/Bslosh solver 必须使用同一取值。

### 第一阶段实现结果

截至 `9b2cb14` 之上的未提交工作区，以上四项已实现：

- `/odom` 使用独立 callback queue、单线程 `AsyncSpinner`、队列深度 `10` 和 `tcpNoDelay`；控制线程只读取受锁保护的快照。
- 显式执行器 prefix 的固定 `10 ms` 液体离散模型在启动时缓存；不足 `10 ms` 的尾段使用实际时长精确 ZOH 传播。
- 生成器新增 `--qp-cond-n`，默认读取 `common.yaml`；B0 和 Bslosh 当前生成 JSON 均为 `N=60`、`qp_solver_cond_N=10`。
- ABBA validate-only 会检查两个 solver JSON 和 odom 队列合同，避免配置已改但仍误用旧生成物。

用 Row02 的 `539` 个真实 pre-solve snapshot 顺序回放 Bslosh solver，软件比较为：

| `cond_N` | 成功 | mean | P95 | max |
|---:|---:|---:|---:|---:|
| 5 | 539/539 | 7.493 ms | 9.408 ms | 11.661 ms |
| **10** | **539/539** | **7.589 ms** | **9.077 ms** | **10.501 ms** |
| 15 | 539/539 | 8.150 ms | 10.045 ms | 12.144 ms |
| 60（原值） | 539/539 | 17.635 ms | 20.677 ms | 25.275 ms |

选择 `10`：均值只比 `5` 高约 `0.1 ms`，但 P95 和最大值更低。该结果只证明同机离线软件回放更快，不替代实车负载下的运行时验收。

当前软件验证：

```text
RelWithDebInfo 编译        PASS
catkin                    234 tests, 0 errors, 0 failures, 0 skipped
explicit actuator Python  6 tests PASS
ABBA contract Python      20 tests PASS
B0/Bslosh codegen check   PASS
Row02 validate-only       PASS，未启动运动
无运动 launch smoke       PASS，publish_cmd_vel=false
```

本阶段明确不做：

- 不改 `w_slosh`、`w_accel`、`w_du_a` 或液体代价窗口；
- 不放宽 `max_interpolation_gap_sec=0.050`；
- 不在对齐失败时保持上一条非零命令；仍保留 `fail_closed`；
- 不同时启用 legacy L22。

### 第一阶段验收

代码完成后只录一包低速 `B_slosh` smoke，现有有效 `B0` 暂不重录。运动窗内必须满足：

```text
common_epoch failure = 0
ACADOS failure       = 0
故障性零速            = 0
planner odom >50 ms  = 0
完整控制回调 P95      < 30 ms
>33.3 ms 不形成连续积压
```

完整控制回调耗时按同一条 `ControlCycleAudit` 的 `command_publish_stamp - cycle_start_stamp` 计算，不用 solver time 代替。

若 smoke 仍有 common-epoch 失败，先检查独立 odom 队列是否实际运行；若 odom 已连续但仍有 `MINSTEP`，再单独定位求解恢复和 warm-start，不进入权重调整。

### 第二阶段：运行时通过后再抑制锯齿

只有第一阶段验收通过，才按一次一个变量的顺序处理有效求解拍中的主动翻转：

1. 先单独设置 `w_accel=0.3`；
2. 若仍有明显正负翻转，再提高 stage-0 `w_du_a`；
3. 最后才调整液体代价窗口和 `w_slosh`。

第二阶段需要重新记录正常求解区间的 `|a_0|` 饱和占比、强正负翻转次数和 RGB 指标。未经这一阶段验证，不能声称已解决 MPC 主动速度锯齿。

## 相关证据

- [slosh 状态输入与短时延迟补偿三包分析](../实物对比试验分析/20260831_slosh状态输入与短时延迟补偿三包分析.md)
- [I0 fail-closed + fixed_closed_loop Block 1 分析](../实物对比试验分析/20260901_I0_fail_closed_fixed_closed_loop_B0_Bslosh_Block1分析.md)
- [底盘速度阶跃与执行器能力辨识分析](../实物对比试验分析/20260902_底盘速度阶跃与执行器能力辨识分析.md)
- [共同状态时刻与执行器延迟模型实施思路](./20260831_共同状态时刻与执行器延迟模型实施思路.md)
- [I0 + 显式执行器 OCP 的 B0/Bslosh ABBA 实物验证方案](../实物对比实验/20260903_I0显式执行器OCP_B0_Bslosh_ABBA验证方案.md)
