# 20260706 B0 当日 gate 复核记录

> 目的：在 2026-07-06 `B_smooth / B_ours` 正式补充矩阵前，先用 `B0` 复核当天 fixed-path tracking、delay compensation、topic 记录和底盘状态是否仍可作为 clean baseline。

## 1. bag

```text
/home/geist/slosh_bags/real/20260706_fixed_path_compare/B0/B0_fixed_150_220_0706_gate01.bag
```

## 2. 实验口径

```text
ALG=B0
RUN_LABEL=B0_fixed_150_220_0706_gate01
CMD_TOPIC=/cmd_vel
V_REF=0.20
ALPHA_MAX=1.2
SHARED_LINEAR_ACCEL_LIMIT_ENABLE=true
SHARED_LINEAR_ACCEL_MAX=0.6
SHARED_ANGULAR_LIMIT_ENABLE=true
SHARED_ANGULAR_RATE_MAX=1.2
SHARED_ANGULAR_ACCEL_MAX=1.2
DELAY_PHASE_MODE=fixed_closed_loop
DELAY_PHASE_LINEAR_DELAY_SEC=0.15
DELAY_PHASE_ANGULAR_DELAY_SEC=0.22
RECORD_TOPIC_INFO=false
RECORDER_STARTUP_SEC=8
RECORD_RGB=false
```

## 3. summary 结果

```text
GOAL_REACHED: yes
goal time: 33.81 s
delay_compensation_applied_frac: 1.000
history_complete_frac: 1.000
fixed_closed_loop_applied_frac: 1.000
critical missing topic: none
zero_due_to_tracking_safety: 0
solver failure: 0
```

summary red flags：

```text
slosh_cost_inactive
eta_dot_cost_inactive
published_zero_often = 0.313
sidecar_effective_config_mismatch
```

判读：

```text
1. slosh_cost_inactive / eta_dot_cost_inactive 对 B0 是正常现象，因为 B0 本来不开 slosh cost。
2. published_zero_often 主要来自 GOAL_REACHED 后停车段；zero_due_to_tracking_safety = 0，不能解释为 tracking safety 失败。
3. sidecar_effective_config_mismatch 中 B0 effective w_slosh=0 是正确 baseline 行为，不影响 gate 判定。
```

## 4. path progress 10%~90% 指标

统计窗口：

```text
first ACADOS_OK -> first GOAL_REACHED，
再按 /spmpc/debug/stage0_reference.s0 取 path progress 10%~90%。
```

结果：

| 指标 | 数值 |
|---|---:|
| projection p95 | 0.042 m |
| projection max_10_90 | 0.068 m |
| stage contour p95 | 0.042 m |
| stage contour max_10_90 | 0.047 m |
| yaw error p95 | 0.066 rad |
| yaw error max_10_90 | 0.105 rad |
| `/spmpc/slosh_height` p95 | 1.122 mm |
| `/spmpc/slosh_height` max_10_90 | 2.457 mm |
| `/spmpc/slosh_height` RMS | 0.564 mm |
| cmd_v p95 / max_10_90 | 0.412 / 0.433 m/s |
| cmd_omega p95 / max_10_90 | 0.231 / 0.365 rad/s |

## 5. 和 20260705 B0 baseline 的关系

简要对比：

```text
1. goal time 与 20260705 B0 formal N=3 接近；
2. internal slosh p95 接近；
3. tracking p95 略大，20260705 约 3cm，本次约 4.2cm；
4. 但本次无离轨、无 tracking safety、无 solver fail，delay compensation 全程生效。
```

因此本次 B0 gate 可视为通过。

## 6. 结论

```text
2026-07-06 当日 B0 fixed_closed_loop 0.15 / 0.22 baseline 复核通过。
可以进入 B_smooth / B_ours 正式补充矩阵。

若后续使用矩阵脚本，因为本次 B0 gate 已经手动完成，可设置：
MATRIX_INCLUDE_GATE=false
```
