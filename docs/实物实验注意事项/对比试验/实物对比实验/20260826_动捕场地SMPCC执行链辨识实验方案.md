# 动捕场地 S-MPCC 执行链辨识实验方案

日期：2026-08-26

协议 ID：`SMPCC_mocap_execution_chain_v1`

状态：`IMPLEMENTED_AWAITING_FIELD_MAP_AND_SMOKE`。路径冻结、单条 runner、postflight 和离线分析入口已经生成并通过离线测试；动捕场地尚未生成唯一命名的 Cartographer 地图，也尚未通过显式地图/SHA 的静止定位验收和现场 ROS/NOKOV smoke。完成地图前置门、现场 S 路径、S-MPCC variant、速度和录制检查项冻结后，才能开始计入本协议的数据采集。

相关文档：

- NOKOV 通过 Wi-Fi 接入 ROS 的现场操作，以 [NOKOV 动捕通过 WiFi 广播到 ROS 使用说明](../../动捕设备使用/NOKOV动捕通过WiFi广播到ROS使用说明.md) 为准；
- 动捕、IMU、odom 的坐标与时间标定原则，见 [动捕辅助 IMU 标定与标注方案](../../动捕设备使用/20260727_动捕辅助IMU标定与标注方案.md)；
- 动捕场地从建图、冻结、静止定位验收到切回大场地的命令，以 [动捕场地建图、定位验收与双场地切换 SOP](20260828_动捕场地建图定位与双场地切换SOP.md) 为准；
- 大场地液面效果实验，见 [G3R3 短可信时域公平配对实物验证方案](20260819_G3R3短可信时域公平配对实物验证方案.md)。

## 0. 一眼看懂

本实验不在动捕场地复现大场地 G3R3，也不直接证明减晃有效。它只利用一条紧凑、可重复的 S 路径，把 S-MPCC 从“计划”到“实车运动”的执行链测清楚。

```text
第一次人工点击终点，选择安全的紧凑 S 路径
                    ↓
              保存并冻结该路径
                    ↓
       同一配置、同一路径重复运行 3～5 次
                    ↓
同步记录 MPC horizon、控制审计、/cmd_vel、odom、IMU、动捕 pose
                    ↓
  分析计划覆盖 → 软件改命令 → 底盘执行延迟 → 传感器误差
                    ↓
为大场地 G3R3 提供执行链模型和可解释的误差边界
```

三层核心证据：

```text
上一周期计划 k=1 → 当前周期重新规划 k=0
        当前 solver command → 最终发布 /cmd_vel
                 最终 /cmd_vel → 动捕真实 v / omega
```

| 要回答的问题 | 主要数据 | 得到的结论 |
| --- | --- | --- |
| 原计划动作是否被下一周期覆盖 | previous `k=1`、current `k=0` | 滚动重规划替换量 |
| 控制器输出是否被软件层修改 | solver、terminal、post-gate、published command | terminal、安全门、限幅器介入量 |
| 最终命令是否被底盘及时实现 | published `/cmd_vel`、动捕 pose | 线/角通道延迟、增益和时间常数 |
| odom/IMU 能否在大场地代替动捕做诊断 | 动捕、odom、IMU | odom/IMU 的偏差、相位和可信边界 |

## 1. 为什么单独在动捕场地做

动捕场地不足以容纳 G3R3 的完整 H0 路径，但执行链辨识不要求复现大场地起终点。它只要求：

1. 路径在动捕有效区域内；
2. 同时包含左转和右转，使角速度出现正负变化；
3. 包含起步、正常跟踪和减速，使线速度有足够变化；
4. 路径、控制配置和载荷能够重复；
5. 动捕、控制、odom 和 IMU 的时间戳可追溯。

因此可以在 RViz 中人工点击一个终点，让 S-MPCC 跟踪紧凑 S 路径。终点的绝对坐标不是研究变量，但正式记录时不能每条随意选择不同终点。

本实验与 G3R3 的分工为：

| 实验 | 场地 | 回答的问题 |
| --- | --- | --- |
| 本协议 | 小型动捕场地 | 命令是否被修改、底盘是否迟滞、odom/IMU 是否可信 |
| G3R3 | 正常实物场地 | 短可信时域液体代价是否真实降低 RGB 液面响应 |

## 2. 实验边界

### 2.1 本实验负责

- 测量上一周期 `k=1` 计划与当前周期 `k=0` 动作的一致性；
- 测量 solver command 到最终 `/cmd_vel` 的软件层改变量；
- 测量最终 `/cmd_vel` 到动捕真实线速度、角速度的响应；
- 分别估计线速度与角速度通道的延迟、稳态增益和一阶时间常数；
- 用动捕校验 odom 与 IMU 的幅值、符号、时间偏移和相位；
- 给大场地 G3R3 的离线解释提供执行链参数及不确定性。

### 2.2 本实验不负责

- 不把动捕接入 S-MPCC 控制闭环；
- 不用动捕替换 `/odom`，也不发布控制用 TF；
- 不比较 Matched0/Matched5 的真实液面效果；
- 不把小场地数据计入 G3R3 的 6 条 development trial；
- 不用 IMU 加速度积分得到线速度真值；
- 不因一次运行的单个峰值就宣布延迟参数已经冻结；
- 不把 Wi-Fi 网络传输延迟全部误认为底盘执行延迟。

若后续把本实验辨识出的执行器模型接入 S-MPCC，必须建立新的代码 release 和协议 ID，不能继续沿用原 G3R3 release。

## 3. 冻结实验对象

第一条正式记录前冻结以下内容，并写入每条 trial 的 metadata：

| 项目 | 冻结要求 |
| --- | --- |
| S-MPCC variant | 只选一个代表当前主线的 variant；本协议内不混用不同 variant |
| 控制频率与 horizon | 固定，不在批次中调整 |
| `v_ref` 与公共权重 | 固定；优先与大场地主实验一致，场地不允许时使用更低安全值并明确不可直接外推 |
| 路径 | 探索运行后保存的一条紧凑 S 路径 |
| 起点区域 | 用胶带或动捕坐标定义一个允许误差的小门区 |
| 终点与终点朝向 | 固定；终点朝向尽量与路径末端切向一致，避免额外原地自旋 |
| 机器人与载荷 | 同一底盘、轮胎状态、固件、容器、液量和安装方式 |
| 地面 | 记录材质；与大场地不同时，将迁移误差列为限制 |
| 动捕刚体 | 固定刚体名、Marker 布局、刚体原点和轴向 |
| NOKOV 网络 | 固定 Windows IP、VRPN 端口、接收机、Wi-Fi/有线方式 |

### 3.1 路径选择规则

第一次人工点击终点只用于选择路径，不计入正式数据。候选路径必须满足：

- 全程位于动捕可靠覆盖区，并给停止距离和定位误差留出余量；
- 机器人、容器和 Marker 不会进入遮挡区；
- 至少包含一个左转段和一个右转段；
- 中间存在明确的角速度换向；
- 起步和停止之外，至少有一段正常 tracking；
- 不贴墙、不穿障碍、不要求临场人工修正；
- 试跑后保存实际 reference/global path，记录目标 pose 与路径哈希。

如果无法保存路径，最低要求是保存每条实际 global/reference path，并设置几何一致性门。路径不一致的 trial 只能作为探索数据，不能合并辨识。

## 4. 现场总体步骤

```text
P0 连接并检查 NOKOV
 → P-1 完成无地图 smoke、唯一地图冻结和显式地图/SHA 静止定位验收
 → P1 检查控制与安全边界
 → P2 人工点击一次终点并选择 S 路径
 → P3 保存路径、冻结配置、完成无运动录制 smoke
 → P4 重复 3～5 条正式 trial
 → P5 检查 bag 与时间有效性
 → P6 分三层离线分析
 → P7 输出执行链参数和是否可迁移到 G3R3 的结论
```

### 已准备的脚本

| 脚本 | 作用 | 是否可能让机器人运动 |
| --- | --- | --- |
| `mocap_field_mapping.env.example` | 小场地地图、Tracker、定位和静止门阈值模板 | 否 |
| `freeze_spmpc_mocap_field_map.sh` | 停止一条建图轨迹，唯一命名并冻结 pbstream/PGM/YAML/SHA | 否；必须显式 `ARM_MAP_FREEZE=YES` |
| `analysis/validate_mocap_field_map.py` | 离线校验地图三件套、分辨率、PGM 和 SHA | 否 |
| `analysis/validate_mocap_field_localization.py` | 无运动检查运行时地图身份、topic、TF 和静止稳定性 | 否 |
| `mocap_execution_chain_field.env.example` | 现场只改一处的参数模板；复制后 `source` | 否 |
| `prepare_spmpc_mocap_s_path.sh` | 等待人工点击终点，生成、校验并冻结紧凑 S 路径 | 否 |
| `run_spmpc_mocap_path_selection_trial.sh` | 低速重放候选路径并记录独立 `PATH_SELECTION` development bag | 只有显式设置 `ARM_MOTION=YES`；不占用 R01--R05 |
| `record_spmpc_mocap_static_smoke.sh` | 静止录制动捕/odom/IMU，检查频率、断帧、时间戳和抖动 | 否 |
| `analysis/validate_mocap_s_path.py` | 检查路径长度、左右转、换向、范围和 SHA-256 | 否 |
| `run_spmpc_mocap_execution_chain_trial.sh` | 执行一条冻结路径 trial，强制动捕录制并自动 postflight | 只有显式设置 `ARM_MOTION=YES` |
| `analysis/validate_mocap_execution_chain_bag.py` | 检查 topic、频率、时间戳、路径、控制周期和 S 激励 | 否 |
| `analysis/analyze_mocap_execution_chain.py` | 导出动捕速度、分析三层执行链并拟合延迟/一阶模型 | 否 |

以上路径均相对于：

```text
src/scout_apps/control/spmpc_local_planner/scripts/
```

建议到场后先把参数模板复制到仓库外，只修改这一个现场文件：

```bash
cd /home/geist/scout_ws
cp src/scout_apps/control/spmpc_local_planner/scripts/mocap_execution_chain_field.env.example \
  /tmp/mocap_execution_chain.env
${EDITOR:-vi} /tmp/mocap_execution_chain.env
source /tmp/mocap_execution_chain.env
```

至少核对 `MOCAP_HOST`、`MOCAP_TRACKER`、`PATH_FILE`、场地跨度门、`VARIANT` 和 `V_REF`。模板故意不设置 `ARM_MOTION`，避免一次 `source` 就获得运动权限。

### P0：连接 NOKOV/XINGYING

完整步骤按 NOKOV 使用说明执行。现场最短流程为：

1. Windows/XINGYING 与 Ubuntu 接入同一可互访网络；
2. XINGYING 选择 Windows Wi-Fi IPv4、刚体、单位“米”，开启 VRPN 并播放；
3. Ubuntu 检查 Windows IP 与 `3883` 端口；
4. 启动原始 VRPN client；
5. 启动项目 `/mocap/*` 监控桥；
6. 确认原始 pose 持续更新且 `/mocap/status` 为 `OK`。

已知现场成功参数仅作示例，实验前必须重新确认：

```text
MOCAP_HOST=192.168.203.85
MOCAP_PORT=3883
MOCAP_TRACKER=Tracker0
```

终端 1：

```bash
source /opt/ros/noetic/setup.bash
roslaunch vrpn_client_ros sample.launch server:=192.168.203.85
```

终端 2：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash

roslaunch nokov_mocap_monitor nokov_monitor.launch \
  start_vrpn:=false \
  server:=192.168.203.85 \
  tracker:=Tracker0
```

只读检查：

```bash
rostopic echo -n 1 /vrpn_client_node/Tracker0/pose
rostopic hz /vrpn_client_node/Tracker0/pose
rostopic echo -n 1 /mocap/status
```

### P-1：动捕场地地图与静止定位硬门

当前场地尚未建图，因此 P2 的 RViz 目标、map-frame 路径和 S-MPCC 运动均未放行。先严格执行双场地 SOP：

```text
无地图 mocap/odom/IMU smoke
→ 新 MAP_ID 建图
→ 停止 teleop，冻结 pbstream/PGM/YAML/SHA
→ 绝对 map_file + expected SHA 重启定位
→ validate_mocap_field_localization.py 静止验收 PASS
```

最低证据包括：

- 小场地地图 stem 与大场地 `map_carto_20260629_R0` 不同；
- 建图/定位时 `map -> odom` 只有 Cartographer 一个 authority；
- `${MAP_ID}_map_validation.json` 和静止定位 JSON 顶层均为 `pass=true`；
- 运行时 `/cartographer_node/frozen_map_file` 与冻结 pbstream 绝对路径一致，预期/实际 SHA 一致；
- `/map`、`map -> base_link`、scan、odom、IMU 和原始动捕均通过门；
- 验收全程没有 `/cmd_vel` publisher；
- NOKOV 与 Cartographer frame 保持隔离，不用动捕替换 `/odom` 或控制 TF。

完整命令及切回 G3R3 的旧地图恢复命令只在 [双场地 SOP](20260828_动捕场地建图定位与双场地切换SOP.md) 维护。本节通过后才允许 P2 的路径生成与 `PATH_SELECTION` 探索；路径和其余配置冻结后，再在 P3 执行正式 `record_spmpc_mocap_static_smoke.sh` timing smoke。

### P1：控制与安全检查

开始运动前确认：

- 急停在操作者手边，场地净空；
- Marker 与车架刚性连接，转弯时不会遮挡；
- 动捕节点只发布 `/mocap/*`，不发布 `/odom`、`/cmd_vel` 或控制 TF；
- S-MPCC 不订阅 `/mocap/scout_odom`；
- 最终 `/cmd_vel` 只有当前控制链一个 publisher；
- odom、IMU、TF、定位和 S-MPCC 状态正常；
- 容器、液量和载荷与计划保持一致；
- 已测试停止命令与急停。

### P2：选择紧凑 S 路径

1. 将机器人放在动捕覆盖区内的候选起点；
2. 在 RViz 人工点击一个安全终点；
3. 检查生成路径是否同时包含左、右转和角速度换向；
4. 低速进行一条探索运行；
5. 检查机器人、容器、Marker 和停止距离是否始终安全；
6. 保存这条 reference/global path、终点 pose 和实际起点 pose；
7. 探索 bag 标记为 `PATH_SELECTION`，不进入参数辨识。

终点可以由操作者选择，但一旦进入正式采集，就不得逐条重新点击不同终点。

脚本入口：

```bash
cd /home/geist/scout_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash

DATE=20260826 \
MOCAP_TRACKER=Tracker0 \
PATH_FILE=/home/geist/fixed_paths/real/20260826_spmpc_mocap_execution_chain/mocap_compact_s_path.json \
bash src/scout_apps/control/spmpc_local_planner/scripts/prepare_spmpc_mocap_s_path.sh
```

该脚本只等待 `/scout/goal`、生成并发布 reference path，不启动 planner，也不发布 `/cmd_vel`。生成后会同时写出：

```text
mocap_compact_s_path.json
mocap_compact_s_path.json.sha256
mocap_compact_s_path_validation.json
mocap_compact_s_path_freeze.env
```

如果现场 RViz 的点击目标不是 `/scout/goal`，只修改 `GOAL_TOPIC`，不要临时改 Python 源码。场地边界明确时可设置 `MAX_SPAN_X_M/MAX_SPAN_Y_M`。

候选路径先做独立 dry-run，再进行一条低速探索；不得借用 `R01`：

```bash
cd /home/geist/scout_ws
source /tmp/mocap_field_mapping.env
source "${MAP_STEM}_freeze.env"
source /tmp/mocap_execution_chain.env

SELECTION_ID=C01 V_REF=0.10 VALIDATE_ONLY=true \
  bash src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_mocap_path_selection_trial.sh

SELECTION_ID=C01 V_REF=0.10 ARM_MOTION=YES \
  bash src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_mocap_path_selection_trial.sh
```

该入口强制 `V_REF <= 0.15 m/s`、绑定小场地 pbstream/SHA、要求运行时 Cartographer 地图身份一致，并拒绝已有 `/cmd_vel` publisher。输出 metadata 固定为 `run_class=PATH_SELECTION`、`formal_trial_consumed=false`。只有低速运行安全且自动 postflight PASS，候选路径才可进入 P3；失败时保留 C01，换新路径文件和 `SELECTION_ID=C02`。

### P3：冻结与无运动 smoke

正式运行前：

- 冻结路径、variant、`v_ref`、权重、控制频率和安全配置；
- 显式启用动捕录制：`RECORD_MOCAP=true`；
- 固定 `MOCAP_TRACKER`；
- 确认 recorder 同时包含控制审计、horizon、命令、odom、IMU 和动捕；
- 静止录制至少 10 s，检查时间戳单调、动捕频率、最大间隔和 Marker 抖动；
- 确认 bag 中不存在意外的控制发布者或缺失的关键 topic。

当前通用 recorder 已支持 `RECORD_MOCAP=true`，但默认值为 `false`。本协议 runner 必须显式打开，postflight 也必须把动捕设为 required，不能只依赖操作者临时输入环境变量。执行链 runner 还固定设置 `RECORD_MOCAP_PATH=false`：保留原始/桥接 pose 和 status，但不录制不断累积的 `/mocap/scout_path`。

先做纯参数/路径校验；它不连接 ROS，也不录包：

```bash
cd /home/geist/scout_ws
source /tmp/mocap_field_mapping.env
source "${MAP_STEM}_freeze.env"
source /tmp/mocap_execution_chain.env

TRIAL_ID=R01 \
VALIDATE_ONLY=true \
bash src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_mocap_execution_chain_trial.sh
```

`VALIDATE_ONLY=true` 只检查 trial 编号、脚本、路径几何/哈希和小场地地图三件套/SHA，不连接控制器、不启动录包、不发布命令。真实 R01--R05 还会复核运行时 Cartographer map 参数，防止切错场地地图。

然后让机器人完全静止，真正录制一条至少 10 s 的动捕 smoke：

```bash
cd /home/geist/scout_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash
source /tmp/mocap_execution_chain.env

SMOKE_SEC=15 \
bash src/scout_apps/control/spmpc_local_planner/scripts/record_spmpc_mocap_static_smoke.sh
```

该脚本会拒绝在 `/cmd_vel` 已有 publisher 时启动，只录制数据，不启动 planner、不发 goal、不发速度。结束后自动检查：

- 原始和桥接动捕、odom、IMU 的频率与最大帧间隔；
- header 时间戳是否有效且单调；
- `/mocap/status` 是否全程为当前 Tracker 的 `OK`；
- 静止位置/航向 P95 抖动、单帧跳点和四元数范数；
- bag 是否闭包、是否误录图像流。

smoke postflight 未通过时不要进入运动 trial。阈值需要调整时，只能在 R01 前统一修改模板中的 `MIN_MOCAP_RATE_HZ`、`MAX_MOCAP_GAP_SEC`、`MAX_STATIC_POSITION_P95_M` 和 `MAX_STATIC_YAW_P95_RAD`，并保留失败报告。

### P4：正式重复运行

推荐运行 `5` 条；场地时间受限时最低 `3` 条：

| trial | 用途 |
| --- | --- |
| `R01` | 参数拟合 |
| `R02` | 参数拟合 |
| `R03` | 参数拟合 |
| `R04` | held-out 验证 |
| `R05` | held-out 验证 |

每条执行：

1. 人工将机器人放回冻结起点门；
2. 保持静止至少 10 s，确认动捕 `OK`、IMU/odom 正常；
3. 使用保存的同一 S 路径启动 S-MPCC；
4. 正常完成路径，不在中途修改终点、权重或速度；
5. 到达后继续静止记录至少 5 s；
6. 正常闭包 bag；
7. 立即执行 postflight，通过后才开始下一条。

任一条出现动捕丢失、命令链异常、solver failure、安全介入或人工急停，保留该 bag 并停止本批。不得删除失败数据后用一条成功重跑替代。

正式单条入口示例：

```bash
cd /home/geist/scout_ws
source /opt/ros/noetic/setup.bash
source devel/setup.bash
source /tmp/mocap_field_mapping.env
source "${MAP_STEM}_freeze.env"
source /tmp/mocap_execution_chain.env

TRIAL_ID=R01 \
ARM_MOTION=YES \
bash src/scout_apps/control/spmpc_local_planner/scripts/run_spmpc_mocap_execution_chain_trial.sh
```

按 `R01 → R02 → R03 → R04 → R05` 修改 `TRIAL_ID`。runner 会同时核对冻结路径和小场地地图的 SHA，并拒绝大场地 G3R3 地图。默认使用 `B_slosh_matched5`、`v_ref=0.20 m/s`；若现场需降低速度，必须在第一条前冻结统一的 `V_REF`，并在全部 trial 使用相同值。

## 5. 必须录制的数据

### 5.1 控制器计划与软件执行链

至少包括：

```text
/spmpc/debug/control_cycle_audit
/spmpc/debug/predicted_horizon
/spmpc/debug/pre_solve_snapshot
/spmpc/debug/cmd_vel_output
/spmpc/debug/cmd_vel_output_status
/spmpc/terminal/mode
/spmpc/terminal/debug
/spmpc/status
/cmd_vel
/cmd_vel_drive              # 现场存在时
```

`ControlCycleAudit` 已提供：

- `solver_u0_a / solver_u0_alpha`；
- `solver_cmd_v / solver_cmd_omega`；
- `terminal_cmd_* / post_gate_cmd_* / published_cmd_*`；
- `previous_shifted_plan_*` 与 `replanned_minus_shifted_*`；
- 每一层显式时间戳以及 odom/IMU 激励。

注意：`solver_u0_a/alpha` 是加速度/角加速度控制量，不能直接与速度形式的 `/cmd_vel` 相减。应先检查它到 `solver_cmd_v/omega` 的离散映射，再比较 solver、terminal、post-gate 和 published command。

### 5.2 路径、状态与传感器

至少包括：

```text
/scout/goal
/scout/current_goal
/scout/global_path
/scout/global_path_fixed
/mpc/reference_path
/odom
/scout/odom                 # 现场存在时
/imu/data
/tf
/tf_static
```

现场实际存在的 goal/reference path topic 必须在 smoke 时确认；不能因为 whitelist 中写了 topic 名就假定 bag 内一定有消息。

### 5.3 动捕

至少包括：

```text
/vrpn_client_node/Tracker0/pose
/mocap/scout_pose
/mocap/scout_odom
/mocap/status
```

可同时录制原始 VRPN `twist/accel` 作为辅助，但动捕速度真值统一从连续 pose 使用冻结的平滑与微分流程导出。

当前 `/mocap/scout_odom` 只携带 pose，twist 未填充，禁止直接把它的 twist 当作真实速度。

长时间录制默认不要求 `/mocap/scout_path`，避免不断累积的 `Path` 增大 bag。

## 6. 数据有效性门

每条 trial 至少满足：

- 原始 VRPN pose、桥接 pose、`/mocap/status`、控制审计、horizon、`/cmd_vel`、odom、IMU 和路径均存在；
- `/mocap/status` 在有效运动段持续为 `OK`；
- pose 时间戳单调，没有无法解释的跳点、刚体翻转或 Marker 丢失；
- 动捕频率满足现场冻结门，建议目标不低于 `60 Hz`；
- 记录实际 median/P95/max frame gap，不能只报告平均频率；
- 控制周期和 command publish timestamp 有效；
- 起点位于冻结门内，实际 reference path 与冻结路径一致；
- solver 成功，且没有未解释的安全门、fallback 或人工干预；
- bag 正常闭包，没有 `.bag.active` 残留。

若 Wi-Fi 帧间抖动与待估执行延迟处于同一量级，本条只能用于幅值和低频响应分析，延迟结论记为 `INCONCLUSIVE`。需要更精确的时延时，应优先改用有线网络，或启用经过验证的服务端时间并完成 Windows/Ubuntu 时钟同步。

## 7. 离线分析

### 7.1 先统一坐标与时间

1. 使用冻结的动捕刚体到 `base_link` 静态外参；
2. 将动捕位置和姿态统一到车体 `x` 前、`y` 左、`z` 上；
3. 以原始 VRPN pose 为可追溯真值输入；
4. 记录 raw pose 到 bridge pose 的时间差与网络帧间抖动；
5. 先估计 IMU—动捕时间偏移，再估计 command—真实运动延迟；
6. 禁止用 bag arrival order 代替消息时间戳对齐。

Wi-Fi VRPN 默认可能使用接收端 ROS 时间，因此动捕链路包含网络和调度延迟。本协议最终报告必须把以下两项分开：

```text
cmd → received mocap response 的观测延迟
Wi-Fi/VRPN 时间不确定性
```

只有时间同步和时间戳语义通过验证后，才能进一步解释为底盘物理执行延迟。

### 7.2 第一层：滚动计划是否被覆盖

对相邻有效 control cycle 比较：

```text
previous horizon k=1  ↔  current solver u0
```

报告：

- `a` 与 `alpha` 的替换量分布、RMSE 和 P95；
- 符号一致率；
- 左转、右转、角速度换向和直线段的分段结果；
- 计划更新时间与 control period；
- 禁止跨 solver failure、安全门或 terminal cycle 配对。

替换量大说明远期动作在真正到达第一控制步之前已被重新规划覆盖。这是滚动计划一致性问题，不是底盘延迟。

### 7.3 第二层：软件是否修改命令

逐周期比较：

```text
solver_cmd
  → terminal_cmd
  → post_gate_cmd
  → published_cmd
```

分别计算：

```text
Delta_v_terminal / Delta_omega_terminal
Delta_v_gate     / Delta_omega_gate
Delta_v_publish  / Delta_omega_publish
```

报告每一层介入率、最大改变量、P95 和发生阶段。正常 tracking 段与 terminal 段必须分开：接近终点时 terminal controller 可能有意接管，不能把该行为误判为 solver 输出错误。

### 7.4 第三层：底盘是否实现最终命令

从动捕 pose 使用固定平滑器导出：

```text
v_mocap(t)      = 车体纵向速度
omega_mocap(t)  = yaw rate
```

优先比较：

```text
published v_cmd      → v_mocap
published omega_cmd  → omega_mocap
```

第一候选模型为：

```text
tau_v     * d(v_mocap)/dt     + v_mocap     = K_v     * v_cmd(t - d_v)
tau_omega * d(omega_mocap)/dt + omega_mocap = K_omega * omega_cmd(t - d_omega)
```

至少比较：

- 纯延迟；
- 一阶；
- 一阶加纯延迟；
- 数据明确支持时，再考虑死区或饱和。

不能只用一次互相关峰值。每个方向至少保留命令变化时刻、首次明显响应、过零点、峰值和稳态段，并报告各重复 trial 的参数稳定性。

使用 `R01～R03` 拟合，`R04～R05` 做 held-out 验证。只有验证集上的时域误差、相位和参数稳定性通过，才能冻结执行器模型。

### 7.5 第四层：校验 odom 与 IMU

分别比较：

```text
odom v             ↔ v_mocap
odom omega         ↔ omega_mocap
IMU omega_z        ↔ omega_mocap
IMU a_x / a_y      ↔ 动捕导出的 IMU 点加速度
```

结论边界：

- 线速度以动捕导出速度为真值，odom 为待校验量；
- 角速度由动捕与 IMU `omega_z` 交叉验证；
- IMU acceleration 是 specific force，必须处理坐标、重力、零偏和杠臂；
- 不将 IMU 加速度积分为线速度真值；
- 主场地无动捕时，只能把校验后的 odom/IMU 作为迁移诊断量，并携带本实验给出的误差范围。

完成全部 bag 后运行：

```bash
python3 src/scout_apps/control/spmpc_local_planner/scripts/analysis/analyze_mocap_execution_chain.py \
  /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/S_path/*R01*.bag \
  /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/S_path/*R02*.bag \
  /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/S_path/*R03*.bag \
  /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/S_path/*R04*.bag \
  /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/S_path/*R05*.bag \
  --mocap-tracker Tracker0 \
  --training-count 3 \
  --plot \
  --output-dir /home/geist/slosh_bags/real/20260826_spmpc_mocap_execution_chain/analysis
```

前三条默认作为拟合数据，后两条作为 held-out。动捕刚体轴与 `base_link` 不重合时，通过 `--tracker-to-base-yaw-rad` 和 `--base-to-tracker-x-m/y-m` 传入冻结外参，禁止为了让结果更好而逐 bag 调参。

## 8. 预声明判定

| 观察结果 | 解释与下一步 |
| --- | --- |
| previous `k=1` 与 current `k=0` 差异大 | 滚动计划被覆盖；先处理计划一致性，不归因于底盘 |
| solver 与 published command 不一致 | 查 terminal、安全门、限幅器或发布链；不进入执行器辨识结论 |
| published command 与动捕响应有稳定延迟 | 支持辨识 `M_actuator`，用 held-out trial 验证 |
| 延迟随 trial 或方向剧烈变化 | 检查地面、轮胎、负载、网络时间、死区和饱和；暂不冻结单一延迟 |
| odom 与动捕稳定一致 | 大场地可把 odom 作为线/角速度主要诊断量，但仍报告校准误差 |
| IMU `omega_z` 与动捕一致 | 大场地可用 IMU 辅助判断角通道动态 |
| IMU 加速度与动捕不一致 | 先修 frame、重力、bias、杠臂或时间对齐，不直接修改液体模型 |
| Wi-Fi 时间抖动过大 | 延迟结论 `INCONCLUSIVE`；改用有线或可靠服务端时间同步 |

### 8.1 本协议完成条件

同时满足以下条件，才可写成“执行链辨识完成”：

1. 至少 `3` 条有效重复 trial，推荐 `5` 条；
2. 同一路径、同一 variant 和同一公共配置；
3. 三层命令链均能按 cycle/time stamp 联结；
4. 动捕数据连续，坐标、刚体和时间戳语义有记录；
5. software command intervention 已明确解释；
6. 线/角执行模型在 held-out trial 上仍有效；
7. odom/IMU 相对动捕的误差边界已报告；
8. 已明确哪些参数可以迁移到大场地，哪些只适用于当前地面和速度范围。

## 9. 输出物

本批结束后至少生成：

```text
protocol_and_frozen_config.yaml
frozen_s_path.json 或等价路径文件
trial_manifest.csv
每条 trial 的 bag、日志、metadata 与 postflight.json
control_chain_cycle_table.parquet/csv
mocap_derived_kinematics.parquet/csv
actuator_model_fit.json
heldout_validation_report.json
fig_plan_replacement.png
fig_solver_to_published.png
fig_published_to_mocap.png
20260826_动捕场地SMPCC执行链辨识结果.md
```

结果文档必须分别给出：

- 计划覆盖结论；
- 软件命令一致性结论；
- 底盘线/角响应模型；
- 动捕时间不确定性；
- odom/IMU 迁移可信度；
- 对 G3R3 的具体建议。

## 10. 现场速查清单

### 动捕与网络

- [ ] Windows/Ubuntu 网络互通，VRPN 端口可达；
- [ ] XINGYING 正在播放，单位为米，刚体名称正确；
- [ ] 原始 VRPN pose 持续更新；
- [ ] `/mocap/status` 为 `OK`；
- [ ] Marker 无松动、遮挡和跳变；
- [ ] 静止 smoke 已记录频率和 frame gap；

### 路径与控制

- [ ] 小场地唯一地图、三件套 SHA 和静止定位报告均已冻结并 PASS；
- [ ] `map -> odom` 只有 Cartographer 一个 authority，NOKOV 未接管控制 frame；
- [ ] 探索运行已完成且不计入正式数据；
- [ ] S 路径已保存并冻结；
- [ ] 路径同时包含左转、右转和换向；
- [ ] 起点门、终点 pose、variant、`v_ref` 和权重已冻结；
- [ ] `/cmd_vel` 只有一个最终 publisher；
- [ ] 急停和停止命令已确认；

### 录制

- [ ] `RECORD_MOCAP=true`，Tracker 名称正确；
- [ ] control audit、horizon、solver/published command 均有消息；
- [ ] goal/reference path、odom、IMU 均有消息；
- [ ] 原始 VRPN pose、bridge pose 和 status 均有消息；
- [ ] bag 正常闭包并通过 postflight；

### 分析边界

- [ ] `u0 a/alpha` 未被直接当作 `/cmd_vel`；
- [ ] tracking 与 terminal phase 分开；
- [ ] 动捕速度由 pose 使用冻结流程导出；
- [ ] 先估 IMU—动捕偏移，再估 command—motion 延迟；
- [ ] Wi-Fi 抖动作为不确定性报告；
- [ ] 本批未被写成 G3R3 液面效果证据。

---

一句话结论：

> 在动捕小场地用一条冻结的紧凑 S 路径重复运行 S-MPCC，沿“上一拍计划—当前求解—最终发布—动捕真实运动”逐层测量，先把计划覆盖、软件改命令和底盘迟滞区分开，再把校验后的执行链结论带到大场地 G3R3，而不是把两类实验混在一起。
