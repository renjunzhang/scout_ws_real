# 20260804 仿真源码隔离迁移：R8 起点与 R7 历史边界

## 结论

仿真控制器、运行工具和 H0 路径链已迁入
`src/scout_apps/simulation/spmpc_sim_local_planner`；其唯一允许的构建/运行
前缀是 `/data/a/scout_sim_replacement/r8_controller_ws`。实物控制器目录
`src/scout_apps/control/spmpc_local_planner` 保持零 diff，仿真构建不会写入
`/home/a/scout_ws/build` 或 `/home/a/scout_ws/devel`。

这建立的是新的**源码/二进制隔离 R8 起点**，不是对既有
`SIM-MECHANISM-40-64-88-R7` 的改名。R7 的 88 行仍是历史
`SIMULATION_MECHANISM_ONLY` 证据；它不能被标注为 source-separated，且 R7
执行入口已被隔离 gate 禁止创建新 attempt。

本次没有生成 R8 formal freeze、随机表、GO receipt、timing admission 或任何
R8 40/64/88 正式实验数据。已完成的 H0 是 development smoke，明确
`formal:false`，不消费 formal planned row，也不能作为 H1/L1 或
physical-primary 证据。

## 已隔离的边界

| 层 | 新仿真侧 | 实物侧/禁止项 |
| --- | --- | --- |
| 控制器源码与 ABI | 自有 `include/`、`src/`、ROS messages、C++ namespace、`libspmpc_sim_local_planner.so` | 不修改、不链接 `libspmpc_local_planner.so` |
| build/devel | `/data/a/scout_sim_replacement/r8_controller_ws/{build,devel}`；helper 用干净 `env -i` 且只 whitelist `spmpc_sim_local_planner` | 不写入、不选用主工作区 `build/devel` |
| ACADOS | 自有 `generated/acados/` 副本；node/library 使用 `DT_RPATH` 指向仿真 prefix 与仿真 codegen | 不从实物 planner 的 codegen 目录加载；不允许 `LD_LIBRARY_PATH` 覆盖为实物库 |
| ROS 图 | `/sim_spmpc_local_planner`、`/sim_spmpc/*` | 不发布/依赖实物 `/spmpc/*` |
| node admission | Python gate hash + C++ 二次校验：loopback master、`/use_sim_time`、R8 target/gate ACK、仿真环境 owner marker | 直接 `rosrun`、非 loopback 或伪装为实物环境均在创建 controller 前拒绝 |
| 在线入口 | `smpcc_sim_mechanism_r8.launch`、仿真自有 FixedProfile launch | 不 include 实物 planner launch |
| H0 path | `smpcc_sim_h0_fixed_path_publisher.py`；从 `/odom` 生成旧开阔场地 P2 s-curve，目标 `(5,0,0)` | 不调用 `proxy_spmpc` / `scout_mini_proxy_nav_adapter` 路径链 |
| 环境 | `launch_sim_environment.sh` 仅启动 fresh ROS、Gazebo proxy 与 localization | 不启动实物 controller；仅清理自身 PID/进程组 |

H0 path 的坐标系明确是 `/odom`，不是 H1/L1 frozen path，也不是实物坐标对齐证据。

R8 的 H_proxy monitor 与控制器 modal kernel 都在仿真包内实现；不再编译、
链接或运行实物侧 `control/slosh_models`。`/slosh/height` 仍只记录为
`H_proxy`，`/sim_spmpc/slosh_height` 仍只记录为 `H_modal`；源码隔离不改变
二者的科学语义，均不进入 physical-primary 字段。独立 plant/信息防火墙/保真
验证仍是 formal 的必要条件。

## H0 development live smoke（非正式）

干净 smoke 位于
`/data/a/scout_sim_replacement/results/r8_isolated_h0_live_smoke_20260804_2130`：

- fresh ROS/Gazebo 为 loopback `11890/11924`，pre/post master 均不可达；只清理本
  case 跟踪的进程组。
- H0 runtime s-curve 从 `(-4,0)` 到旧终点 `(5,0)`；不是 H1/L1 JSON replay。
- 30 s settle 后 recorder 已就绪才开始运动；`/odom` 首个有效运动为 ROS time
  `39.588 s`，`/sim_spmpc/status=GOAL_REACHED` 为 `69.420 s`，有效运动到达
  `29.832 s < 60 s`；随后完成 5 s frozen tail。
- `h0_runtime.bag` 为 10.35 MB；bag readback 覆盖 41 项 effective-config 字段且
  `declared_vs_observed_diff={}`。`/slosh/height` 仅写为 `H_proxy`，
  `/sim_spmpc/slosh_height` 仅写为 `H_modal`，没有 `H_plant`/physical-primary。
- `attempt_manifest.json`、`postflight.json` 与 append-only `dataset_index.jsonl`
  均为 `formal:false`、`METHOD_SUCCESS`。这是 H0 development 账本，不是正式
  40/64/88 账本。

此前目录 `r8_isolated_h0_live_smoke_20260804_2110` 保留为不可覆盖的开发证据：
它在目标到达后暴露了 shell `tf_echo|grep` 已看到 TF 却仍等到 timeout 的环境退出
问题（`h0_environment_unexpected_exit.json`，return code 124）。该尝试不用于干净
smoke 结论。随后将 TF probe 改为受控、仅清理自身 PID 的 Python probe，且 runner
要求 fresh environment launcher 在 settle、motion 和 tail 全程存活；上述 `2130`
尝试在此修复后通过且没有该异常文件。

## 自动验收

本次实际通过：

- `scripts/build_sim_controller_workspace.sh`：只遍历/构建
  `spmpc_sim_local_planner`，输出只在 R8 外部 prefix。
- C++ self-admission gtest：4/4；缺 gate token、非 loopback/非 sim-time、未绑定
  owner/ACK 均拒绝。
- active 仿真 package tests：42/42；active runtime-tool tests：108/108；独立
  liquid-plant tests：30/30。
- source-isolation 二进制检查确认 `DT_RPATH`、无 `DT_RUNPATH`，即使注入主工作区
  loader path，node 仍解析外部 sim library 与 sim-owned ACADOS，未解析
  `libspmpc_local_planner.so` 或 `libslosh_models.so`。
- 历史 R7/R1 archive 的显式 `python3` 调用也 fail-closed；它们不在 ROS/CMake
  install surface。
- `git diff --exit-code -- src/scout_apps/control`：PASS。

## R8 尚未放行的条件

R8 开跑之前必须全部满足：

1. 生成新的只读 R8 freeze/master，且二者 hash 绑定
   `source_separation`（自有 package/node/diagnostic root/fork provenance）。
2. 以新 binary、仿真自有 codegen、仿真自有环境入口产生并冻结 R8 GO receipt、timing
   admission。H0 development smoke 已通过，但不能替代该 formal admission。
3. 冻结 H1/L1 JSON replay、C1/C2、五个完整 effective config、FixedProfile、随机表和
   CRN bundle；不能使用 H0 runtime s-curve 替代。
4. 若要给仿真 formal primary：独立 liquid plant、controller subscriber firewall 和
   独立保真验证必须同时 PASS；否则仅能报告 H_proxy/H_modal 机制量。

在上述条件前，formal adapter 与通用 row runner 对 R1--R7 shared-source release 均
fail-closed；不会消耗 planned rows。

## 与实物流程的关系

实物源码、实物传感器/底盘/录包和实物 `FREEZE_ID` 不受本次迁移影响。仿真 R8 即使
将来完成，也只能作为独立仿真实验支撑论文的机制/鲁棒性部分；它不能替代实物 G0--G6、
实物冻结、视觉测量链或 physical-primary 结论。
