# 2026-09-17 Full 40 步单次验证

**40 步改善了启动，但本次仍未到点。** 小车起终点定位位移约 0.637 m，
成功接纳 42 次 OCP 命令，随后预算不足并持续停车，最终任务超时。

本次仅做 Full 求解器重新生成、受影响节点增量编译和一次独立 Gazebo 验证。
相对 `b2ee4596`，将预测步数从 60 改为 40，保持 30 Hz、原时间步、
QP 凝聚参数 10、任务、可行诊断计划及全部安全检查。
生成器同步更新了代价核中的步数及原有 `1/N` 归一化；实际求解快照确认 `horizon_steps=40`。
预测时长由 2 s 缩为约 1.33 s，节点增量编译通过。

| 检查项 | 本次结果 |
|---|---:|
| 成功接纳 OCP 命令 | 42 次 |
| 预测动力学检查拒绝 | 2 次 |
| 终止前 RTI，cycle 63 | 27.094 ms |
| 下一周期预计迭代耗时，cycle 64 | 27.090 ms |
| cycle 64 剩余预算 / 实际 RTI 次数 | 25.316 ms / 0 |
| 起始 / 最终定位 x | 0.0192 / 0.6561 m |
| 观察时间 | 60.118 s（仿真） |
| 到点 | false |
| 闭合 bag / 独立启停有效 | true / true |

另出现各一次 `STOP_ACTUAL_MOTION_BOUND_VIOLATION` 和
`STOP_JERK_HISTORY_INFEASIBLE`，以及两次定位传播时限拒绝；均保留在原始包和分析中。
此前同代码的 60 步无界面记录 `20260917_timing_repair/full_terminal_r01` 为零次接纳；
本次缩短窗口后能启动，但单次迭代耗时尖峰仍可触发持续停车，不能据此认为问题已解决。
这是单次开发验证，执行模型失配仍在，计划仍为复用的可行诊断计划，`formal=false`。

证据目录：`/data/a/scout_sim_replacement/results/20260917_full_n40/`。

- `case/run.bag`、`case/analysis.json`、`result.json`：原始数据及结果。
- `case/source.diff`、`tested_common.yaml`、`generated_n40/`：测试改动与 40 步生成物。
- `case/runtime/`、`case/live_params.yaml`：本次运行库与实际参数。
- `codegen_slosh.log`、`build.log`：生成与编译记录。首次生成因未设置 acados_template 搜索路径退出，补齐现有安装路径后成功，未安装依赖。
- `baseline_manifest.json`、`baseline_restored.json`：原配置和运行库恢复记录。

测试后已归档 40 步生成物，并恢复原 60 步配置、生成求解器、代价核及节点运行库，
恢复文件的 SHA-256 与备份一致。仅提交本说明，不 push。
