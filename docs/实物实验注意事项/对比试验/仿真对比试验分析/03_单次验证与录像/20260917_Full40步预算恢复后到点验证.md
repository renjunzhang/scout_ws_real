# 2026-09-17 Full 40 步：预算恢复后单次到点验证

**本次 Full 40 步跑通，约 16.13 秒到点，未出现命令结果过期。**
结合现有预算恢复修复与执行器适配，缩短预测窗口显著改善了本次在线计算余量。
仍存在停车检查拒绝，不能把一次到点等同于全过程无异常或正式防晃效果验证。

本次只跑一次无界面 Full，源码基于 `769bb3c6`。保留 30 Hz、同一任务、区域、
Full 可行诊断计划、液体功能、代价权重和全部时效/动力学/停车检查；
采用与最新 B0 录像相同的 `--identified-actuator`，`delay_phase=off`。
仅将 `common.yaml` 的预测步数 60→40，重新生成 Full 求解器并增量编译节点；
生成器同步更新代价核的 N 和原有 `1/N` 归一化，QP 凝聚参数保持 10。
预测时长由 2 s 缩为约 1.33 s，没有修改控制器逻辑或扩大预算。

## 结果

| 指标 | 本次结果 |
|---|---:|
| 实际快照步数 / dt | 40 / 0.0333333333 s（389 次调用） |
| GOAL_REACHED / passed | true / true |
| 到点时间 | 16.130 s |
| RTI 段墙钟耗时中位数 / P95 / 最大 | 7.69 / 16.35 / 26.93 ms |
| 成功接纳 OCP 命令 | 383 次 |
| 预算预判拒绝 / 随后恢复求解 | 2 / 2 次 |
| COMMAND_RESULT_EXPIRED | 0 次 |
| SOLVE_BUDGET_STOPPING | 43 个周期 |
| STOP_ACTUAL_MOTION_BOUND_VIOLATION | 19 次 |
| STOP_JERK_HISTORY_INFEASIBLE | 5 次 |
| PREDICTION_DYNAMICS_VIOLATION | 4 次 |
| 最终位置误差 / 朝向误差 | 0.04842 m / 0.01642 rad |
| 仿真观察时间（包含到点后约 5 s） | 21.083 s |
| bag 闭合 / 独立启停有效 | true / true |

cycle 24 预算拒绝，34 恢复并接纳命令；cycle 44 预算拒绝，79 恢复并接纳命令。
实际执行 RTI 的 387 个周期中，385 个执行 1 次，另各有 1 个周期执行 2、3 次。
最终位置 `(5.04811, -0.00542)`、朝向 `0.01642 rad`，落在既定目标容差内；
位置接近 5 cm 容差边缘，后续稳定性检查需保留这一事实。

相较[60 步预算恢复验证](20260917_Full预算恢复单次验证.md)，本次 RTI 段中位耗时
由约 25.04 ms 降至 7.69 ms，预算拒绝由 127 次降至 2 次，并成功到点。
这是不同闭环轨迹下的单次开发对照，不是同输入的求解器性能测量；不能据此声称
防晃收益或全工况稳定性。此前[40 步单次验证](20260917_Full40步单次验证.md)
使用旧停车锁存逻辑且没有执行器适配，其失败记录保留，不与本次混为同一配置。

## 归档与恢复

证据目录：`/data/a/scout_sim_replacement/results/20260917_full_n40_recovery/`。

- `case/run.bag`、`case/analysis.json`、`verification.json`：原始数据、逐周期统计和实际步数核验。
- `case/runtime/`、`case/live_params.yaml`、`tested_common.yaml`：40 步实际运行库及参数。
- `tested_generated_slosh/`、`tested_generated_cost/`、`tested_source.diff`：40 步生成物与测试差异。
- `codegen_slosh.log`、`build.log`、`run.log`、`run_once.py`：本次生成、编译、运行记录与入口。
- `baseline_backup/`、`baseline_manifest.json`、`baseline_restored.json`：原 60 步基线及恢复校验。

测试后已恢复 **60 步配置、生成求解器、共享代价源码、构建对象和运行库**；
SHA-256 清单逐文件一致。当前工作区默认仍是 60 步，40 步结果及运行物已独立归档。
本次仍为 `formal=false`，计划身份为 `REPLAYED_FEASIBLE_SEED_NOT_REOPTIMIZED`。
阶段性中文提交，不 push；下一步剩余问题是停车历史/实际运动边界拒绝及重复运行稳定性。
