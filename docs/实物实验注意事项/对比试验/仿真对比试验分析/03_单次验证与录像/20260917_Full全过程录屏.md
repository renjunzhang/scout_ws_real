# 2026-09-17 Full 全过程录屏

[完整原速视频：full_process.mp4](/data/a/scout_sim_replacement/results/20260917_full_video_r02/full_process.mp4)

[兼容本机“视频”播放器的 WebM 版本](/data/a/scout_sim_replacement/results/20260917_full_video_r02/full_process.webm)

播放兼容性补充：本机默认播放器为 Totem，其 GStreamer 缺少 H.264 解码器；
原 MP4 在 FFmpeg 下解码正常，但在默认播放器下会报缺少插件。
已补充 VP8 WebM 版本，保留完整 1649 帧、109.934 秒、1600×900、15 fps，约 5.27 MB。
使用本机 GStreamer 完整解码通过，并已打开到 Totem；无需安装额外插件。
诊断、转换及播放验证记录分别保存在 `mp4_default_decoder_check.log`、
`webm_conversion.log`、`webm_playback_review.json`，原 MP4 保留。

视频长 **109.934 秒（约 1 分 50 秒）**，1600×900、15 fps、H.264，约 3.24 MB。
这是本次新启动 Gazebo 的实时 RViz 画面，包含环境初始化、30 秒静置、
Full 任务下发、完整 60 秒观察和关闭提示。画面同时显示机器人、地图、
参考路线、Full 计划、速度反馈、求解次数和第一次拒绝原因；没有剪掉停车过程。

| 视频位置（约） | 本次现象 |
|---|---|
| 00:00–00:44 | 环境启动、地图加载和定位静置 |
| 00:45 | 下发任务；先发生预测动力学检查拒绝，短暂接纳两次 OCP 命令后触发预算不足 |
| 00:46–01:29 | 持续 `TERMINAL_DRAINING`，机器人基本停在起点 |
| 01:30 | 达到 45 秒任务期限，状态变为 `TASK_DEADLINE_MISSED` |
| 01:46–01:50 | 观察结束、环境关闭和录制结束提示 |

视频时间为墙钟时间，任务期限为 ROS 仿真时间。面板时钟先于录屏启动约 0.4 秒；
事件回调与 15 fps 画面之间也有采样间隔，表中时间仅用于定位。

本次共尝试求解 5 次：2 次预测拒绝、2 次成功接纳、1 次预算拒绝。
周期 19 的单次 RTI 为 **27.918 ms**；周期 20 的迭代耗时估计为 **27.914 ms**，
剩余预算为 **23.527 ms**，因此执行 0 次 RTI 并进入持续停车。
`GOAL_REACHED=false`，观察时间 60.144 秒（仿真）/ 60.477 秒（墙钟）。
这些数据说明本次录像中的 Full 仍未跑通；录屏进程也占用计算资源，
本次耗时用于解释录像中的事件，不作为独立的实时性能验收。

执行版本为 `9066d18178735aaf63a2e7e832c77d57043310a2`，沿用上一轮冻结任务、
生产控制器配置和可行诊断计划，未修改控制器。计划身份仍为
`REPLAYED_FEASIBLE_SEED_NOT_REOPTIMIZED`，本次没有重新求解上层最优计划。
仿真实际执行器与控制器预测模型的失配仍存在，所有结果为 `formal=false`。

证据目录：`/data/a/scout_sim_replacement/results/20260917_full_video_r02/`。

- `full_process.mp4`：完整录像。
- `video_manifest.json`：视频参数、SHA-256、代码身份和录制进程退出状态。
- `video_events.json` / `video_timeline.json`：实时状态变化及视频近似位置。
- `case/run.bag`、`case/analysis.json`：闭合原始包及逐周期结果。
- `case/runtime/`、`case/live_params.yaml`：运行库副本及实际配置。
- `01_start.png`、`02_failure.png`、`03_deadline.png`、`04_end.png`：视频抽帧。

已核查 MP4 参数并完整解码，解码器退出码为 0；人工查看实时画面和录像抽帧，
确认机器人、路线、中文面板和结束状态可见。ROS/Gazebo 前后端口均为空，
`closed_bag=true`、`valid_fresh_lifecycle=true`；录屏、观察界面及嵌套显示进程均已关闭。
两个新增 Python 脚本通过语法检查；本次未改控制器，无需重复整编译。

第一次录制曾因观察界面缺少机器人模型资源路径而中断，保留在
`/data/a/scout_sim_replacement/results/20260917_full_video/`，其
`recording_complete=false`，不作为本次完整录像。修正只为 RViz 补充模型资源搜索路径，
随后重新独立启停并完成上述录像；外部仿真的地图、world、launch 和脚本未修改。

复用录制入口时，输出目录必须尚不存在：

```bash
source /tmp/scout_handoff_20260916_6XlFYy/ros_devel/setup.bash
export ACADOS_SOURCE_DIR=/home/a/acados
python3 scripts/record_full_trajectory_video.py \
  --output /data/a/scout_sim_replacement/results/full_video_next \
  --setup /tmp/scout_handoff_20260916_6XlFYy/ros_devel/setup.bash \
  --task /data/a/scout_sim_replacement/results/20260917_startup_repair/raw_r01/task.json \
  --plan /data/a/scout_sim_replacement/results/20260917_startup_repair/feasible_seed_plan.json
```

视频和原始包留在证据目录，不进入 Git；本阶段只提交录制脚本和说明，不 push。
