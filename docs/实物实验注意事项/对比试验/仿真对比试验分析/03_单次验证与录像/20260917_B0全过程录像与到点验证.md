# 2026-09-17 B0 全过程录像与到点验证

**B0 本次跑通，约 27.1 秒到点。** 这是当前控制器与新执行器适配下的一次实际运行，
与此前约 24.16 秒、未接入执行器适配的历史 B0 记录分开保留。

[观看完整 WebM 视频（本机播放器兼容）](/data/a/scout_sim_replacement/results/20260917_b0_video/b0_process_annotated.webm)

视频长 83.467 秒，1600×900、15 fps、VP8，约 4.85 MB；完整保留环境启动、
30 秒静置、运动、停车重试和到点后保持。**约 00:46 开始任务，01:13 显示到点。**
事件时间与视频帧存在采样差异；后台轮询统计到点为 27.071 秒，状态事件间隔为 26.961 秒。

## 本次条件与结果

- 控制器源码 `75125f5f`，运行库 SHA-256 与上一轮 Full 预算恢复验证一致；未改控制器、未重编译。
- `raw_mpcc / B0`，30 Hz、60 步、原任务和区域；`delay_phase=off`。
- 开启与最近 Full 相同的 `--identified-actuator`，按控制器 delay/tau/gain 输出到原 Gazebo 驱动。
- B0 使用 cruise 参考，不加载 Full 上层计划；计划身份为 `NO_PLAN_CRUISE`。
- 只跑一次；外部地图、world、launch 和脚本保持原样。录屏入口仅扩展 B0 profile 和对应画面标签。

| 指标 | 本次结果 |
|---|---:|
| GOAL_REACHED / passed | true / true |
| 到点时间（运行器统计） | 27.071 s |
| 最终定位 | (4.99717, -0.00247, 0.06708 rad) |
| 最终位置误差 | 约 3.76 mm |
| 成功接纳 OCP 命令 | 679 次 |
| 实际 RTI 段墙钟耗时中位数 / P95 | 7.34 / 18.06 ms |
| 预算预判拒绝 / 结果过期 | 8 / 2 次 |
| 停车 jerk 历史不可行 / 动力学拒绝 | 4 / 5 次 |
| 闭合 bag / 独立启停有效 | true / true |

本次能到点，但全过程仍有拒绝和停车恢复，不能宣称完全连续或零异常。
录像与 RViz 自身占用资源；本次为 `formal=false`，不用于严格实时性能或防晃效果比较。

## 画面说明与文件

原始录像的橙色线直接使用了 proxy 里程计位置，带有世界出生点偏移，未与定位地图对齐。
因此不能用它与绿色参考线之间的距离判断跟踪误差；机器人模型使用定位 TF，可见其沿
绿色路线运动并到达终点。交付 WebM 已在画面上明确标注这个显示问题；未剪辑或改写
运动、诊断状态和时间，原始 MP4 完整保留。随后已将观察脚本改为用地图中的定位 TF
绘制橙线；该显示修复完成语法检查，未为它重新运行仿真。

目录：`/data/a/scout_sim_replacement/results/20260917_b0_video/`。
`b0_process_annotated.webm` 是交付视频，`b0_process.mp4` 是原始录屏；
`video_manifest.json`、`video_timeline.json`、`webm_playback_review.json` 记录身份、时间和播放检查。
`case/run.bag`、`case/analysis.json`、`case/runtime/`、`case/live_params.yaml` 保留原始数据和运行条件。

WebM 共 1252 帧，已用本机 GStreamer 完整解码通过；检查了跟踪、到点和最终标注画面，
并启动本机播放器。录屏、观察窗口和独立仿真进程均已关闭，前后 ROS/Gazebo 端口均为空。
本阶段只提交观察脚本和说明，不 push。
