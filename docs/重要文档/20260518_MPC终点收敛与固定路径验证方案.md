# 20260518 MPC 终点收敛与固定路径验证方案

目标：

```text
先验证当前 MPC terminal 是否稳定收敛；
terminal 收敛通过后，进入固定路径 MPC cost 对比；
本方案只验证 MPC cost / terminal，不涉及 OSCRS / GeoRef / path post-processor。
```

核心原则：

```text
1. terminal smoke 已通过；后续固定路径包仍需检查 terminal 不污染主窗口。
2. 固定路径对比只改变 MPC cost 参数，不改变路径、不打开 OSCRS。
3. 真实液面结论以 RGB 视觉为准，/slosh/height 只作为模型调试量。
4. terminal_slowdown / TERMINAL_MPC_STOP 段只用于判断终点是否平稳，不作为 slosh cost 主效果窗口。
```

当前状态（2026-05-18 17:25 实物 smoke）：

```text
已通过 terminal 进入第二阶段的最低门槛。

通过包：
  /home/geist/slosh_bags/real/20260518_terminal_smoke/
    slosh_Q0_20260518_172357_terminal_smoke_rate_limited_run01.bag
    slosh_Q0_20260518_172513_terminal_smoke_rate_limited_run02.bag

结果：
  两包均进入 TERMINAL_MPC_STOP；
  两包均 REACHED；
  两包均未 GOAL_PASSED；
  min_dist 约 0.073 m / 0.080 m；
  terminal_slowdown_v_max:=0.80 + terminal 输出限加速度为当前主线。

限制：
  terminal 内仍有 odom_ax 脉冲；
  terminal 只作为安全收敛机制；
  Slosh cost 有效性只统计 TRACKING_PRE_TERMINAL。
```

## 1. 实物系统启动

### 1.1 传感器 / 底盘 / 定位

终端 A：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

src/scout_apps/control/scout_local_planner/scripts/launch_real_sensors_stack.sh
```

该脚本会按顺序启动：

```text
scout_mini_robot_base.launch
nanoscan3_front.launch
scout_nanoscan3_cartographer_localization.launch
scout_imu_with_tf.launch
rs_camera.launch
```

脚本内部会检查：

```bash
rostopic hz /camera/color/image_raw
rostopic hz /imu/data
```

人工确认：

```text
/odom 正常；
/scan_front 正常；
/imu/data 正常；
/camera/color/image_raw 正常；
Cartographer 定位在 RViz 中稳定。
```

### 1.2 MBF 全局规划器

终端 B：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_global_planner mbf_global.launch
```

检查：

```bash
rostopic list | grep /scout/global_path
rostopic list | grep /scout/mbf_costmap_nav
```

## 2. 第一阶段：terminal 收敛 smoke（已通过）

目的：

```text
确认当前 terminal_slowdown + v_des_rate_limit + TERMINAL_MPC_STOP 能让小车稳定停车；
确认不会冲过终点后调头；
确认 terminal 附近没有明显 ax / jerk 脉冲。
```

本轮优先测试对象：

```text
先测试当前新结构：
  terminal capture 只进入 TERMINAL_MPC_STOP；
  不再由外层 CAPTURE_BRAKE 直接发制动速度；
  MPC 继续负责最后一段减速。

不要先回到旧命令做主验证。
旧命令只适合作为对照包，用来证明新结构是否降低 terminal 附近 ax / jerk。
```

当前 terminal 结构：

```text
1. terminal_slowdown_distance 内启用运动学停车包络：
   v_env(d) = min(terminal_slowdown_v_max, sqrt(2 * max_tan_decel * max(0, d - goal_tolerance)))

2. pre-MPC：
   v_des = min(raw_v_des, v_env)

3. post-MPC：
   cmd_v = min(MPC_cmd_v, v_env)
   如果 GoalInfo.dx <= 0，cmd_v 强制为 0。
   未越过 goal 时，用 path_handler/max_tan_decel 对 cmd_v 做变化率限制。
   terminal phase 下会同步 cmd_v EMA 滤波状态，避免上一帧高速被滤波器带回输出。

4. REACHED：
   必须同时满足 speed_low 和 goal_position_reached；
   冲过终点后停在容差外，不再假阳性 REACHED。
```

注意 `terminal_slowdown_v_max` 的含义：

```text
terminal_slowdown_v_max 是 envelope 的上限。

如果 terminal_slowdown_v_max:=0.18：
  envelope 大部分时候被 0.18 截断；
  实测不会过冲，但容易停在 goal tolerance 外，无法 REACHED；
  现在只作为保守不越界参考，不作为优先验证参数。

如果 terminal_slowdown_v_max:=0.80：
  envelope 更多由 sqrt(2ad) 决定；
  实测可以 REACHED，但 terminal 内 ax 脉冲偏大；
  当前优先在 0.80 基础上验证输出层限加速度。
```

后续如需复查 terminal，建议录包顺序：

```text
1. 输出限加速度 smoke：
   terminal_slowdown_v_max:=0.80
   录 terminal_smoke_rate_limited_run01/02

2. 如果第 1 步仍有明显 ax 脉冲，再考虑降低 terminal_slowdown_v_max 到 0.60-0.70，
   或单独调小 path_handler/max_tan_decel。

3. 如果第 1 步仍然冲过终点，先停固定路径 cost 主实验；
   回看 /terminal/v_envelope、/terminal/cmd_v_pre_clamp、/terminal/cmd_v_post_clamp 和 /cmd_vel。
```

### 2.1 MPC 启动命令：输出限加速度 smoke（复查用）

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=32.0 \
  mpc_Q_etheta:=15.0 \
  mpc_Q_v:=9.0 \
  mpc_R_a:=0.4 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=0.5 \
  mpc_R_domega:=4.0 \
  Q_slosh:=0.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=0.0 \
  terminal_factor_slosh_eta_dot:=0.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

该命令中的：

```text
terminal_slowdown_v_max:=0.80
```

表示以已通过 REACHED 的 envelope 设置为基础，验证输出层限加速度是否能降低 terminal 内 ax 脉冲。
这个包只用于验证 terminal，不进入晃动效果统计。

如果仍然有明显 terminal ax 脉冲，可临时只改：

```bash
terminal_slowdown_v_max:=0.60
```

### 2.2 录包

终端 D：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260518_terminal_smoke \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 0 terminal_smoke_rate_limited_run01
```

说明：

```text
record_slosh_experiment.sh 当前默认 SLOSH_RECORD_ALL=true，会使用 rosbag record -a。
因此新跑 terminal smoke 时，正常情况下不需要手动追加话题。

如果使用 SLOSH_RECORD_ALL=false，当前白名单已经补齐 terminal envelope 诊断话题。

但每个 bag 的 sidecar 文件必须保留：
  *_topics.txt
  *_rosparam.yaml
  *_nodes.txt
  *_info.txt

如果之后为了省磁盘改成 SLOSH_RECORD_ALL=false，必须确认下面“终点过冲根因排查话题”全部在白名单内。
```

终点过冲根因排查必需话题：

```text
控制与执行：
  /cmd_vel
  /odom
  /scout/cmd_vel
  /scout/odom

终点状态：
  /terminal/mode
  /terminal/recovery_latched
  /terminal/goal_info
  /terminal/v_envelope
  /terminal/envelope_active
  /terminal/phase_active
  /terminal/cmd_v_pre_clamp
  /terminal/cmd_v_post_clamp

参考速度链路：
  /reference/v_des_raw
  /reference/v_des_target
  /reference/v_des_eff
  /reference/v_des_rate_limited
  /reference/v_ref
  /reference/v_ref_horizon
  /reference/s_horizon
  /reference/v_path
  /reference/kappa
  /reference/s
  /reference/implied_ax
  /reference/implied_ay
  /reference/implied_jerk
  /reference/implied_ax_abs_p95
  /reference/implied_ay_abs_p95
  /reference/implied_jerk_abs_p95

MPC 与 horizon：
  /mpc/status_val
  /mpc_status
  /mpc/solve_ms
  /mpc/solve_time_ms
  /mpc/cost_breakdown
  /mpc/slosh_horizon_summary
  /local_path
  /mpc/reference_path

晃动模型连续性：
  /slosh/state
  /slosh/height
  /slosh/height_pred_max
  /slosh/eta_norm
  /slosh/eta_dot_norm
  /slosh/modal_energy
  /slosh/modal_energy_norm
  /slosh/ax_est
  /slosh/ay_est
  /slosh/alpha_est
  /slosh/omega_est_used

定位与路径：
  /scout/goal
  /scout/current_goal
  /scout/global_path
  /scout/global_path_fixed
  /scan_front
  /tf
  /tf_static
  /tracked_pose
  /submap_list
  /trajectory_node_list
  /diagnostics
  /rosout
```

### 2.3 发送普通导航 goal

终端 E：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

rosrun scout_local_planner send_fixed_goal.py \
  --goal-topic /scout/goal \
  --x 3.0 \
  --y 0.0 \
  --yaw 0.0 \
  --repeat-count 10 \
  --repeat-rate 5
```

也可以用 RViz 点一个车头前方 2.5-4.0 m 的普通 goal。

### 2.4 离线诊断

把 `xxx.bag` 换成实际 bag 文件名。

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash

python3 /home/geist/scout_ws/src/scout_apps/control/scout_local_planner/scripts/analysis/diagnose_terminal_overshoot.py \
  --bag /home/geist/slosh_bags/real/20260518_terminal_smoke/xxx.bag

python3 /home/geist/scout_ws/src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_terminal_transition.py \
  --bag /home/geist/slosh_bags/real/20260518_terminal_smoke/xxx.bag \
  --out-dir /home/geist/slosh_bags/real/20260518_terminal_smoke/terminal_analysis \
  --plots
```

PASS 标准：

```text
diagnose_terminal_overshoot.py:
  不出现 GOAL_PASSED；
  不出现 GOAL_PASSED_THEN_TERMINAL_TURNBACK；
  注意该脚本还不是 envelope-aware；
  若它报 REFERENCE_SPEED_TOO_HIGH_NEAR_GOAL / EXECUTION_SPEED_TOO_HIGH_NEAR_GOAL，
  需要结合 /terminal/v_envelope 和 /terminal/cmd_v_post_clamp 判读。

analyze_terminal_transition.py:
  first_terminal_mode = TERMINAL_MPC_STOP 或 REACHED；
  capture_stop_triggered = 1；
  recovery_triggered = 0；
  terminal_pulse_verdict 可作为 ax/jerk 风险提示；
  不再只用 terminal_ref_v_verdict 判定失败，因为 terminal 输出现在由 envelope + rate limit 接管。

envelope-aware 判据：
  /terminal/phase_active 应在 terminal 附近为 1；
  /terminal/cmd_v_post_clamp 应不高于 /terminal/v_envelope；
  /terminal/cmd_v_post_clamp 一阶差分 p95 应接近 path_handler/max_tan_decel；
  /cmd_vel 一阶差分不应出现大幅 step；
  /odom 不应出现持续高 ax 脉冲。
```

如果 terminal smoke 不通过：

```text
停止固定路径 cost 主实验；
先看 /terminal/v_envelope、/terminal/cmd_v_pre_clamp、/terminal/cmd_v_post_clamp；
再看 /reference/v_des_*、/reference/v_ref_horizon、/cmd_vel、/odom；
不要通过增大 Q_slosh 解决终点过冲。
```

建议至少录：

```text
terminal_smoke_rate_limited_run01
terminal_smoke_rate_limited_run02

如需保守一点：
terminal_smoke_rate_limited_vmax06_run01
```

当前 run01/run02 已通过，可以进入固定路径对比。

## 3. 第二阶段：固定路径 MPC cost / ax-jerk 调参对比

目的：

```text
固定路径形状，比较 C/D/E/F/G 五组 MPC cost；
验证 slosh cost 已经进入优化后，进一步确认显式提高 a / Δa 权重是否能压住早段 ax 和 RGB peak。
```

本阶段仍然不启动 OSCRS / GeoRef post-processor。

20260518 fixed_path_cost 已有结论：

```text
D 组相对 C 组：
  v_p95、ay_p95、model_p95 有下降；
  pct_slosh_total 约 20%，说明 slosh cost 已经参与优化；
  但 model_peak 没下降，RGB h_abs_peak / p95 也没有下降。

峰值定位：
  model_peak 多发生在 first terminal 前 0.03s 左右，和 terminal 前 ax 减速脉冲相关；
  RGB peak 在 D 组三次都集中在 TRACKING 后约 7s、路径里程 s≈1.5m；
  该处 D 组 ay 降了，但 odom_ax 没有明显降。

因此下一轮不是继续堆 Q_slosh，而是验证 ax/jerk 优先级。
```

本轮不做的事：

```text
1. 不改固定路径速度剖面来单独照顾 D/F 组；
2. 不启用 energy_profile / input_shaping / risk_scheduler / speed_governor；
3. 不打开 OSCRS / GeoRef / anti_slosh_path_post_processor；
4. 不通过降低 prediction_v_max 或改 slosh gate 制造效果；
5. 不把 terminal 段作为 slosh cost 有效性的主证据。
```

固定路径主实验的 terminal 规则：

```text
允许使用当前 terminal envelope + 输出限加速度结构保证安全停车；
但 terminal_slowdown / TERMINAL_MPC_STOP / REACHED 附近不进入 C/D/E/F/G 主效果统计。

C/D/E/F/G 比较只看 TRACKING_PRE_TERMINAL：
  这段内还没有 terminal envelope 接管；
  更能反映 MPC cost 和 slosh cost 对轨迹/速度/加速度的影响。
```

### 3.1 固定路径选择

优先顺序：

```text
P2_s_curve:
  首选。主要考察 ay / omega / domega / eta_y / RGB 液面。

P0_straight:
  用于复查 ax / terminal，不作为第一条 slosh cost 主路径。

P3_mixed:
  P2_s_curve 有结果后再跑，作为综合路径。
```

先只做：

```text
P2_s_curve
```

固定路径生成方式保持所有组完全一致：

```text
template_fixed_path_generator.py 负责生成 /scout/global_path_fixed；
send_fixed_goal.py 或 rostopic pub 只负责提供终点 goal；
所有组使用同一模板、同一 goal、同一生成参数；
每包前尽量回到同一起点姿态后，可以重新生成 P2_s_curve；
不按组修改路径形状或速度剖面。
```

不要把“固定路径”理解成完全复用同一个 JSON：

```text
template_fixed_path_generator.py 的输入包含当前小车起点；
如果每包起点有小偏差，重新生成的 P2_s_curve 也会有小差异。

因此现场主流程是：
  1. 每包前把车回到同一物理起点和朝向；
  2. 用同一个 goal；
  3. 用同一套 template_fixed_path_generator.py 参数重新生成 /scout/global_path_fixed；
  4. 录包后用 path_len、trajectory_overlay、起始 tracking error 检查路径一致性。
```

更严格但风险更高的做法：

```text
第一次在标准起点生成 P2_s_curve.json；
后续所有组直接重复发布同一个 JSON。

只有当小车每次都能回到几乎同一定位起点时才推荐；
否则初始 tracking error 会变大，反而污染 C/D/E/F/G 对比。
```

### 3.2 分组参数

本轮建议先跑 5 组，每组 3 包：

```text
C: SMOOTH_SPEED_RELAXED
   不加 slosh，作为平滑/降速基础对照。

D: SLOSH_PRIORITY
   在 C 的速度和平滑设置上加 Q_slosh=5，复现 20260518 D 组边界。

E: AX_JERK_PRIORITY
   不加 slosh，提高 R_a / R_da，验证单纯纵向加速度/jerk 平滑能否压住 RGB peak。

F: SLOSH_PLUS_AX_JERK
   在 D 基础上提高 R_a / R_da，验证 slosh + ax/jerk 是否优于单纯平滑。

G: SLOSH_PLUS_AX_JERK_PREVIEW
   在 F 基础上打开 slosh_preview_factor，验证 one-step preview slosh 加权是否优于 F。
```

参数表：

```text
组别  Q_v  R_a  R_da  R_omega  R_domega  Q_slosh  preview  terminal slosh
C     3.0  0.5  1.5   2.0      6.0       0        0.0      off
D     3.0  0.5  1.5   2.0      6.0       5        0.0      eta=5, eta_dot=3
E     3.0  1.0  2.0   2.0      6.0       0        0.0      off
F     3.0  1.0  2.0   2.0      6.0       5        0.0      eta=5, eta_dot=3
G     3.0  1.0  2.0   2.0      6.0       5        0.5      eta=5, eta_dot=3
```

说明：

```text
E/F 只提高 a 和 Δa 的权重，不降低 R_a/R_omega。
如果 E/F 仍不能压住早段 RGB peak，再考虑更强一档 R_a=1.5、R_da=3.0；
不要第一轮直接大幅调参。
```

G 组说明：

```text
slosh_preview_factor 是可回退开关：
  0.0 = 完全旧行为；
  0.5 = 对预测状态 x_1..x_N 额外加 0.5 份 slosh 权重。

G 组不是默认主线，只用于回答：
  在 F 的 slosh + ax/jerk 结构上，进一步强调一步后的预测液体状态是否更有效。

第一轮只用 slosh_preview_factor:=0.5。
如果行为过慢、pct_slosh_total 过高或 tracking 变差，立即回退到 0.0。
```

### 3.3 MPC 启动：C 组 SMOOTH_SPEED_RELAXED

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path_fixed \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=28.0 \
  mpc_Q_etheta:=12.0 \
  mpc_Q_v:=3.0 \
  mpc_R_a:=0.5 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=1.5 \
  mpc_R_domega:=6.0 \
  Q_slosh:=0.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=0.0 \
  terminal_factor_slosh_eta_dot:=0.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 3.4 MPC 启动：D 组 SLOSH_PRIORITY

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path_fixed \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=28.0 \
  mpc_Q_etheta:=12.0 \
  mpc_Q_v:=3.0 \
  mpc_R_a:=0.5 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=1.5 \
  mpc_R_domega:=6.0 \
  Q_slosh:=5.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=5.0 \
  terminal_factor_slosh_eta_dot:=3.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 3.5 MPC 启动：E 组 AX_JERK_PRIORITY

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path_fixed \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=28.0 \
  mpc_Q_etheta:=12.0 \
  mpc_Q_v:=3.0 \
  mpc_R_a:=1.0 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=2.0 \
  mpc_R_domega:=6.0 \
  Q_slosh:=0.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=0.0 \
  terminal_factor_slosh_eta_dot:=0.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 3.6 MPC 启动：F 组 SLOSH_PLUS_AX_JERK

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path_fixed \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=28.0 \
  mpc_Q_etheta:=12.0 \
  mpc_Q_v:=3.0 \
  mpc_R_a:=1.0 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=2.0 \
  mpc_R_domega:=6.0 \
  Q_slosh:=5.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=5.0 \
  terminal_factor_slosh_eta_dot:=3.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 3.7 MPC 启动：G 组 SLOSH_PLUS_AX_JERK_PREVIEW

G 组只比 F 组多一个参数：

```text
slosh_preview_factor:=0.5
```

回退方式：

```text
slosh_preview_factor:=0.0
```

终端 C：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws

roslaunch scout_local_planner slosh_experiment.launch \
  global_path_topic:=/scout/global_path_fixed \
  mpc_Q_lag:=0.5 \
  mpc_Q_contour:=28.0 \
  mpc_Q_etheta:=12.0 \
  mpc_Q_v:=3.0 \
  mpc_R_a:=1.0 \
  mpc_R_omega:=2.0 \
  mpc_R_da:=2.0 \
  mpc_R_domega:=6.0 \
  Q_slosh:=5.0 \
  slosh_height_ref:=0.005 \
  slosh_eta_dot_ratio:=0.3 \
  slosh_preview_factor:=0.5 \
  Q_slosh_eta_dot:=0.0 \
  terminal_factor_slosh_eta:=5.0 \
  terminal_factor_slosh_eta_dot:=3.0 \
  terminal_recovery_enable:=false \
  terminal_slowdown_enable:=true \
  terminal_slowdown_distance:=1.20 \
  terminal_slowdown_v_max:=0.80 \
  terminal_slowdown_Q_v:=40.0 \
  terminal_slowdown_terminal_factor_v:=5.0 \
  terminal_capture_stop_enable:=true \
  terminal_capture_stop_distance:=0.70 \
  v_des_rate_limit_enable:=true \
  v_des_accel_limit:=0.60 \
  v_des_decel_limit:=0.80 \
  enable_slosh_box_constraint:=false \
  energy_profile_enable:=false \
  input_shaping_enable:=false \
  risk_scheduler_enable:=false \
  slosh_speed_governor_enable:=false \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 3.8 生成并发布 S 弯固定路径

终端 F：

```bash
source /opt/ros/noetic/setup.bash
source /home/geist/scout_ws/devel/setup.bash
cd /home/geist/scout_ws
mkdir -p /home/geist/fixed_paths/real/20260520

rosrun scout_local_planner template_fixed_path_generator.py \
  --template s_curve \
  --goal-topic /scout/goal \
  --output-topic /scout/global_path_fixed \
  --path-file /home/geist/fixed_paths/real/20260520/P2_s_curve.json \
  --start-heading current \
  --spacing 0.05 \
  --amplitude-ratio 0.18 \
  --min-amplitude 0.25 \
  --max-amplitude 1.20 \
  --publish-count 0
```

该脚本会等待 `/scout/goal`。启动后在终端 E 发 goal：

```bash
rosrun scout_local_planner send_fixed_goal.py \
  --goal-topic /scout/goal \
  --x 7.164488315582275 \
  --y 9.307367324829102 \
  --yaw 1.0808 \
  --repeat-count 1 \
  --repeat-rate 1

```

说明：

```text
template_fixed_path_generator.py 生成的是 /scout/global_path_fixed；
MPC 订阅 global_path_topic:=/scout/global_path_fixed；
MBF 可以保持运行，但该固定路径实验不依赖 MBF 重新规划。
```

### 3.9 固定路径录包

C 组：

```bash
SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260520_fixed_path_cost \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 0 P2_s_curve_C_run01
```

D 组：

```bash
SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260520_fixed_path_cost \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 5 P2_s_curve_D_run01
```

E 组：

```bash
SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260520_fixed_path_cost \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 0 P2_s_curve_E_axjerk_run01
```

F 组：

```bash
SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260520_fixed_path_cost \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 5 P2_s_curve_F_slosh_axjerk_run01
```

G 组：

```bash
SLOSH_BAG_DIR=/home/geist/slosh_bags/real/20260520_fixed_path_cost \
SLOSH_RECORD_ALL=true \
src/scout_apps/control/scout_local_planner/scripts/record_slosh_experiment.sh 5 P2_s_curve_G_slosh_axjerk_preview_run01
```

推荐最小数量：

```text
C 组 3 包：
  P2_s_curve_C_run01
  P2_s_curve_C_run02
  P2_s_curve_C_run03

D 组 3 包：
  P2_s_curve_D_run01
  P2_s_curve_D_run02
  P2_s_curve_D_run03

E 组 3 包：
  P2_s_curve_E_axjerk_run01
  P2_s_curve_E_axjerk_run02
  P2_s_curve_E_axjerk_run03

F 组 3 包：
  P2_s_curve_F_slosh_axjerk_run01
  P2_s_curve_F_slosh_axjerk_run02
  P2_s_curve_F_slosh_axjerk_run03

G 组 3 包：
  P2_s_curve_G_slosh_axjerk_preview_run01
  P2_s_curve_G_slosh_axjerk_preview_run02
  P2_s_curve_G_slosh_axjerk_preview_run03
```

每包之间：

```text
小车回到同一起点；
液体静止 >= 5 s；
相机视野、红液 ROI、光照不变；
固定路径 JSON 不变。
```

## 4. 固定路径结果分析

分析输出建议统一放到：

```text
/home/geist/slosh_bags/real/20260520_fixed_path_cost/analysis
```

拷贝到工作站后可对应放到：

```text
/data/a/slosh_bags/real/20260520_fixed_path_cost
```

先做 terminal 截窗：

```bash
python3 /home/geist/scout_ws/src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_terminal_transition.py \
  --bag-dir /home/geist/slosh_bags/real/20260520_fixed_path_cost \
  --glob "*.bag" \
  --out-dir /home/geist/slosh_bags/real/20260520_fixed_path_cost/analysis/terminal_analysis \
  --plots
```

再做 cost contribution：

```bash
python3 /home/geist/scout_ws/src/scout_apps/control/scout_local_planner/scripts/analysis/extract_mpc_cost_breakdown.py \
  /home/geist/slosh_bags/real/20260520_fixed_path_cost \
  --phase TRACKING \
  --out-dir /home/geist/slosh_bags/real/20260520_fixed_path_cost/analysis/cost_breakdown
```

再做固定路径行为分析：

```bash
python3 /home/geist/scout_ws/src/scout_apps/control/scout_local_planner/scripts/analysis/analyze_fixed_path_cost_effect.py \
  --bag-dir /home/geist/slosh_bags/real/20260520_fixed_path_cost \
  --glob "*.bag" \
  --out-dir /home/geist/slosh_bags/real/20260520_fixed_path_cost/analysis/fixed_path_cost_effect
```

再做 RGB 视觉液面分析，按：

```text
docs/重要文档/红色液体视觉验证固定流程.md
docs/重要文档/论文参考总结/OSCRS模型保真度指标整理.md
```

注意：

```text
这里只借用“模型保真度/视觉评价指标”的分析方法；
实验对象是 MPC cost，不是 OSCRS。

20260520 建议使用 1920x1080 RGB；
如果现场掉帧或 rosbag 压力过大，最低降到 1280x720；
不建议再用 640x480 做正式液面幅值结论。
```

主判断窗口：

```text
TRACKING_PRE_TERMINAL:
  mpc_status = TRACKING
  terminal/mode 为空或 NONE
  goal_dist > terminal_slowdown_distance
```

该窗口是 C/D/E/F/G 组 cost 效果的主统计窗口。
任何靠近终点的 terminal_slowdown / TERMINAL_MPC_STOP 段都不能用于证明 slosh cost 有效。

本轮新增两个局部窗口：

```text
EARLY_AX_WINDOW:
  从 TRACKING start 开始积分 odom_v 得到路径里程 s；
  重点统计 s = 1.0-2.0 m，另报告 s = 1.2-1.8 m；
  该窗口用于判断早段 RGB peak 是否来自速度建立 / ax 脉冲。

PRE_TERMINAL_1S:
  first terminal/capture 前 1.0 s；
  用于判断 model_peak 是否仍被 terminal 前减速脉冲拉高；
  不作为主效果窗口，只作为终点污染诊断。
```

NEAR_GOAL / TERMINAL_MPC_STOP：

```text
只用于说明 terminal 是否平稳；
不用于证明 slosh cost 主效果。
```

原因：

```text
terminal phase 内实际输出由：
  MPC solution
  + terminal envelope
  + terminal 输出限加速度
  + REACHED gate
共同决定。

因此 terminal 内 /mpc/cost_breakdown 可以作为调试信息，
但不能解释为“slosh cost 主导了实际执行速度”。
```

主指标：

```text
控制行为：
  v_p95 / cmd_v_p95；
  odom_ax_p95、odom_ax_peak；
  odom_ay_p95、odom_ay_peak；
  jerk_p95；
  tracking duration；
  path_len。

早段 ax 诊断：
  EARLY_AX_WINDOW 内的 v_ref_mean / cmd_v_mean / odom_v_mean；
  implied_ax_p95_abs / odom_ax_p95_abs；
  implied_ay_p95_abs / odom_ay_p95_abs；
  RGB peak 的路径里程 rgb_peak_s_m。

RGB 真值：
  abs(h_smooth_corr) p95；
  abs(h_smooth_corr) peak；
  abs(h_smooth_corr) rms；
  range_lcr p95；
  high-slosh frame ratio。

模型侧：
  /slosh/height p95/rms/peak；
  /mpc/slosh_horizon_summary；
  pct_slosh_total mean/median/p95；
  Ferrari-style U_p95 / U_peak / A_rank。
```

明天最重要的对比不是 D vs C，而是：

```text
E vs C:
  只看 ax/jerk 优先级是否压住早段 RGB peak。

F vs D:
  在同样 Q_slosh 下，提高 R_a/R_da 是否进一步降低早段 ax 和 RGB peak。

F vs E:
  判断 slosh cost 在 ax/jerk 已经更平滑的前提下是否仍有额外收益。

G vs F:
  在同样 slosh + ax/jerk 参数下，判断 slosh_preview_factor 是否带来额外收益；
  重点看 pct_slosh_total 是否只是变大，以及 RGB p95/RMS 是否真的同步下降。
```

验收标准：

```text
如果 E/F 相比 C/D：
  EARLY_AX_WINDOW 的 odom_ax_p95 明显下降；
  RGB h_abs_peak 或 h_abs_p95 不再升高，最好下降；
  ay 不大幅反弹；
  tracking duration 不过分变长；
  first terminal 前 1s 的 model_peak 不再被 ax 脉冲拉高；
则说明“显式 ax/jerk 优先级”是下一步主线。

如果 E/F 只让速度变慢、时间变长，但 RGB peak 不降：
  说明问题可能不只是 ax 权重；
  需要复查 RGB 指标/模型相位/视觉 ROI，而不是继续堆 R_a/R_da。

如果 G 相比 F：
  pct_slosh_total 明显升高，但 RGB p95/RMS 不降，或 completion time 明显变长；
  则 preview 不进入主线，回退 slosh_preview_factor:=0.0。

如果 G 相比 F：
  RGB p95/RMS 下降，model p95 同向下降，ay/ax 不反弹，tracking duration 只小幅增加；
  则 preview 可以作为 slosh-state penalty 的结构增强进入下一轮。
```

## 5. 后续结构改进：preview slosh penalty

当前已加入可回退开关：

```text
slosh_preview_factor
```

本节原本作为 E/F 后的结构候选；现在在第二阶段中新增 G 组小剂量测试：

```text
G = F + slosh_preview_factor:=0.5
```

回退方式：

```text
slosh_preview_factor:=0.0
```

当前 slosh state cost 可以理解为：

```text
J_slosh,k = Q_eta * eta_k^2 + Q_eta_dot * eta_dot_k^2
```

其中：

```text
x_slosh,k = [eta_x, eta_dot_x, eta_y, eta_dot_y]
x_slosh,k+1 = A_d * x_slosh,k + B_d * a_k
```

问题：

```text
eta / eta_dot 是二阶系统状态，带记忆和相位延迟；
当前 a_k 对 eta_k 的影响不直接；
如果只惩罚 eta_k，优化器不一定能及时压住当前 ax 脉冲。
```

可选结构改进：

```text
one-step-ahead slosh penalty:
  先用当前候选控制 a_k 预测 x_slosh,k+1；
  再对 eta_{k+1} / eta_dot_{k+1} 计 slosh cost。

形式：
  x_slosh,k+1 = A_d * x_slosh,k + B_d * a_k
  J_slosh,k += Q_eta * eta_{k+1}^2
             + Q_eta_dot * eta_dot_{k+1}^2
```

也可以扩展成 multi-step preview：

```text
J_slosh,k += sum_i w_i * ||x_slosh,k+i||_Q^2

例如：
  i = 1, 2, 3
  w = 1.0, 0.8, 0.5
```

预期作用：

```text
让当前控制 a_k 更直接地影响 slosh cost；
减少“slosh 项有占比，但压不住当前 ax 脉冲”的现象；
作为 slosh-state penalty 的结构增强，而不是单纯堆 Q_slosh。
```

边界：

```text
preview slosh penalty 仍然不是 ax hard limiter；
如果模型相位/幅值与 RGB 不一致，它仍可能压不住真实峰值；
因此它不能替代 R_a / R_da / ax constraint。
```

建议推进顺序：

```text
1. 先跑 C/D/E/F/G；
2. 先确认 E/F 是否能压住早段 ax/RGB p95；
3. 再看 G vs F，判断 preview 是否提供额外收益；
4. preview 改动后必须重新检查 cost_breakdown、/mpc/slosh_horizon_summary 和 RGB 真值。
```

论文表述建议：

```text
The slosh-aware MPC can combine a liquid-state penalty with an excitation-shaping penalty.
The liquid-state penalty may be evaluated on previewed slosh states to increase the coupling
between the current acceleration command and the predicted liquid response.
```

中文口径：

```text
不是只惩罚液体当前晃起来后的状态；
也可以惩罚当前控制一步以后将造成的液体预测状态；
但纵向激励源头仍需要 R_a/R_da 或 ax constraint 共同约束。
```

## 6. 是否进入下一步

进入 P3_mixed 的条件：

```text
1. terminal smoke 已通过，无 GOAL_PASSED，且固定路径包 terminal 不污染主窗口；
2. P2_s_curve 的 C/D/E/F/G 对比中 terminal 没有污染主窗口；
3. cost_breakdown 显示 D/F 组 slosh cost 有可见占比；
4. E/F 能证明 ax/jerk 优先级对早段 RGB peak 或 p95 有方向一致的改善；
5. F 组相对 D 组不能只靠“更慢”解释全部收益。
```

如果 P2_s_curve 看不出差异：

```text
先查 horizon 内 eta 是否真的被预测出来；
再查 Q_slosh_eta 归一化尺度；
再查 EARLY_AX_WINDOW 的 v_ref/cmd_v/odom_v/implied_ax；
不要直接大幅堆 Q_slosh，也不要降低 R_a/R_omega 制造效果。
```
