# 2026-03-20 测试流程命令

## 目的

当前主线对应：

- `/home/geist/scout_ws/docs/重要文档/20260319进一步修改方案.md`
- `Todo 1：做 lateral_accel 的低风险导航 A/B`

当前这轮测试要回答的问题只有一个：

- `slosh_use_imu_lateral_accel:=true` 是否值得进入默认正式配置

因此当前只做：

- A 组：`yaw=true, lateral=false, alpha=false`
- B 组：`yaw=true, lateral=true, alpha=false`

不要在这一轮里做：

- `run_imu_stage2_sequence.py`
- `run_imu_stage4_sequence.py`
- `alpha_z` A/B
- 同时修改 `Q_slosh / v_max / near-goal / tracking` 参数

---

## 启动顺序

### 1. 底盘 + CAN

```bash
sudo modprobe gs_usb
sudo ip link set can0 down 2>/dev/null || true
sudo ip link set can0 up type can bitrate 500000
roslaunch scout_bringup scout_mini_robot_base.launch
```

### 2. 雷达

```bash
roslaunch nanoscan3_bringup nanoscan3_front.launch use_rviz:=false
```

### 3. 定位

```bash
roslaunch nanoscan3_localization scout_nanoscan3_amcl.launch use_rviz:=true
```

### 4. IMU

```bash
roslaunch scout_bringup scout_imu_with_tf.launch \
  imu_frame:=imu_link \
  imu_x:=0.10 \
  imu_y:=-0.045 \
  imu_z:=0.0 \
  imu_roll:=0.0 \
  imu_pitch:=0.0 \
  imu_yaw:=0.0
```

启动后检查：

```bash
rostopic echo -n1 /imu/data
rostopic hz /imu/data
```

### 5. 全局规划

```bash
roslaunch scout_global_planner mbf_global.launch
```

说明：

- 当前实物测试使用 `mbf_global.launch`
- 不使用这里之前误写的 `move_base_global.launch mode:=mpc`

---

## 固定 Goal

如果复现 bag

- `/home/geist/slosh_bags/slosh_Q5_20260320_151158_lateral_ab_A_run1.bag`

则按 `map` 坐标系依次发送下面两个 goal：

### Goal 1

```bash
rosrun scout_local_planner send_fixed_goal.py \
  --goal-topic /scout/goal \
  --frame map \
  --x 3.012886 \
  --y 2.004346 \
  --yaw 0.248438
```

### Goal 2

```bash
rosrun scout_local_planner send_fixed_goal.py \
  --goal-topic /scout/goal \
  --frame map \
  --x 0.027128 \
  --y 1.021683 \
  --yaw -2.670031
```

建议：

- 先发 `Goal 1`
- 等第一个目标点 `REACHED` 后，再发 `Goal 2`

---

## A 组

### 6A. 启动局部规划

```bash
roslaunch scout_local_planner slosh_experiment.launch \
  Q_slosh:=5 \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=false \
  slosh_use_imu_alpha_z:=false
```

### 7A. 开始录包

```bash
cd $(rospack find scout_local_planner)
./scripts/record_slosh_experiment.sh 5 lateral_ab_A_run1
```

### 8A. 静止 5 秒后发送固定 goal

按上面的“固定 Goal”发送。复现这次双目标流程时：

- 先发 `Goal 1`
- 等 `REACHED` 后再发 `Goal 2`

结束后：

- `Ctrl+C` 停掉录包
- `Ctrl+C` 停掉这一路 `scout_local_planner`

---

## B 组

### 6B. 启动局部规划

```bash
roslaunch scout_local_planner slosh_experiment.launch \
  Q_slosh:=5 \
  slosh_use_imu_yaw_rate:=true \
  slosh_use_imu_lateral_accel:=true \
  slosh_use_imu_alpha_z:=false
```

### 7B. 先检查 bias 是否锁定

```bash
rostopic echo -n5 /slosh/imu_ay_bias_ready
rostopic echo -n5 /slosh/imu_ay_bias
```

要求：

- 先看到 `/slosh/imu_ay_bias_ready = 1`
- 再开始发 goal

### 8B. 开始录包

```bash
cd $(rospack find scout_local_planner)
./scripts/record_slosh_experiment.sh 5 lateral_ab_B_run1
```

### 9B. 发送与 A 组完全相同的 goal

按上面的“固定 Goal”发送，顺序与 A 组保持完全一致：

- 先发 `Goal 1`
- 等 `REACHED` 后再发 `Goal 2`

结束后：

- `Ctrl+C` 停掉录包
- `Ctrl+C` 停掉这一路 `scout_local_planner`

---

## 重复次数

每组至少 2 包：

- `lateral_ab_A_run1`
- `lateral_ab_A_run2`
- `lateral_ab_B_run1`
- `lateral_ab_B_run2`

要求：

- 路线开阔、低速、无复杂障碍
- 每次只改 `slosh_use_imu_lateral_accel`
- A/B 用同一个 goal
- 每次切组都重启 `scout_local_planner`

---

## 录完后的分析命令

### 1. 汇总 slosh / MPC 指标

```bash
cd $(rospack find scout_local_planner)
python3 scripts/extract_slosh_metrics.py \
  /home/geist/slosh_bags/lateral_ab_A_run1*.bag \
  /home/geist/slosh_bags/lateral_ab_A_run2*.bag \
  /home/geist/slosh_bags/lateral_ab_B_run1*.bag \
  /home/geist/slosh_bags/lateral_ab_B_run2*.bag
```

### 2. 必要时单独核对 ay 切源

重点话题：

```bash
rostopic echo /slosh/ay_est
rostopic echo /slosh/imu_ay_filtered
rostopic echo /slosh/imu_ay_bias
rostopic echo /slosh/imu_ay_bias_ready
```

---

## 当前判断口径

如果 B 组同时满足下面条件，才考虑把 `slosh_use_imu_lateral_accel:=true` 提升为默认正式配置：

- 不引入明显 solver failure
- 不引入明显 tracking 回归
- `/slosh/ay_est` 切换逻辑正确
- `/slosh/height`、`/slosh/height_pred_max` 或控制更平稳
