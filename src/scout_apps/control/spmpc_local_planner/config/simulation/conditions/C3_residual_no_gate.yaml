schema: spmpc_closed_loop_trial_condition_v1
condition_id: C3
implementation_id: delay_augmented_phase_acados_22d_empirical_monitor_v1
implementation_complete: true
mode: residual_no_gate
offline_nominal: true
online_residual: true
recovery_gate: false
execution_compatibility_gate: true
stored_recovery_action: true
input_shaping: false
# Strict C3 shares C4's 22D Full SQP, V3, B_exec, frozen bounded recovery
# feedback and final command transaction. The legacy stored_recovery_action
# key means that the V3 recovery path is enabled; it is not a fixed action.
# The empirical terminal/current gate remains monitor-only.
pilot_only: false
formal_c3_c4_causal_comparison_ready: true
residual_feedback:
  max_residual_v: 0.08
  max_residual_omega: 0.20
trial:
  control_rate_hz: 30.0
  max_motion_sec: 45.0
  fixed_tail_sec: 4.0
  publish_latency_sec: 0.01
