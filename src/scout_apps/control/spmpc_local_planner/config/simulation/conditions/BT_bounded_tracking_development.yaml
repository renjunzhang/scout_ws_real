schema: spmpc_closed_loop_trial_condition_v1
condition_id: BT
implementation_id: bounded_tracking_development_v1
implementation_complete: true
mode: bounded_tracking
offline_nominal: true
online_residual: true
recovery_gate: false
execution_compatibility_gate: false
stored_recovery_action: false
input_shaping: false
# Development-only diagnostic.  It replays the same strict V3 artifact clock,
# but replaces the 22D solver and all gates with the code-frozen v1 bounded
# tracking policy followed by the shared rate-safe command transaction.
pilot_only: true
formal_c3_c4_causal_comparison_ready: false
bounded_tracking_policy_contract_id: bounded_tracking_recovery_policy_v1
trial:
  control_rate_hz: 30.0
  max_motion_sec: 45.0
  fixed_tail_sec: 4.0
  publish_latency_sec: 0.01
