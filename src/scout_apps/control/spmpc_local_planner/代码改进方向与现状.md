# 代码改进方向与现状

更新：2026-09-16。分支 `feat/spmpc-liquid-control`，实现与软件验证基线 `f235e11`。当前主线为**区域内自主缓弯 + 全程几何/速度/液体规划 + 在线 MPCC 协同**，跟踪误差仅作诊断。实物脚本从 [scripts README](scripts/README.md) 索引；模型与接口见 [方法说明](README_METHOD.md)，配置见 [四组 profile](config/experiments/trajectory_mpcc/README.md)。

`diag/lt-dwa-collision-tracking` 是此前一直使用的实物分支。其历史 Full 尚无可重复的全程降晃收益；旧 bag 和新模型内结果保持各自版本，不能互相替代。

## 当前结论

区域、曲率目标、全程计划、完整阶段参考、持续任务时钟和离线后缀检查**已经实现**。15/15 个 C++ 测试程序、62 项 Python 定向检查通过；24 次模型闭环完成 22 次，失败均为无上层的单弯 `geometry_mpcc`。缓弯已观察到，但完整方法相对 raw 尚未稳定降低模型液面峰值，最坏周期约 100 ms，未满足 30 Hz 要求。

本包仍是 ROS1/catkin；本次 ROS2 Jazzy 主机只验证独立生产核心和真实 acados，没有整节点编译、录制或实物验收。[软件验证记录](../../../../docs/实物实验注意事项/对比试验/解决问题的思路/20260916_两层协同实现与软件验证记录.md)保存任务、结果和负例，本文只维护当前状态。

## 已完成的主线

| 模块 / 提交 | 实际状态与边界 |
| --- | --- |
| V1 共源液体 / `ac18949` | 容器旋转液体、actual 传播和热启动已统一；传感外参、激励精度和实物模型误差仍需验证 |
| V2–V4 / `ba4a9c2` | 代价 scaling/重构、有界恢复、完整停车和发布历史已有实现；物理 freeboard/margin 未校准 |
| 区域与自主几何 / `7202d97`、`0165583` | 凸 cell、车体包络/运动余量、弱 contour、曲率/曲率变化及真实目标项；区域显式输入，不自动提取障碍物 |
| 参考与时钟 / `345d1bf`、`0165583` | N+1 阶段参考、time/progress 采样、速度/进度速度节点；重复同路线不续期，计时任务换路线明确失败 |
| 上层与后缀 / `0ab2dde` | IPOPT 全状态优化及独立重新传播；从真实28维末端接续；后缀只离线诊断，不影响控制 |
| 停车与数值合同 / `5df2cf2` | 进度容差统一为 1e-6，普通 MPCC 停车可不消费液体，命令/FIFO/记忆完整释放 |
| ROS 与录制 / `c81cae9` | 四组配置、完整状态导出、schema8、配置冻结与 live 参数核对；未完成 ROS1 整链验证 |
| 可复现验证 / `f235e11` | native 测试与24组模型闭环入口、结果身份和失败证据已入库 |

当前模型为 `continuous_mpcc_acados + explicit_actuator`，NX 24/28、NP 92/104、NU 3；液体版本 1、代价版本 3、snapshot/horizon schema 8。B0/slosh 的执行器、延迟队列和加速度记忆同构。旧 6/10 维模型、NP34/46 和 schema7 仅适用于相应历史提交。

物理核、上层任务、参考/区域、规划适配、恢复/停车和 ROS 参数模块已分开。ROS 编排与 acados 包装层仍较大；后续拆分应由确定的问题驱动。

## 实验比较与配置

主比较为原路线直接进入普通 `raw_mpcc`，对比完整 `planned_slosh`。`geometry_mpcc` 检查只有下层自主几何时的变化；`planned_mpcc` 与 `planned_slosh` 共用同一上层液体计划，隔离下层液体项。上层是否考虑液体需另生成目标权重关闭的计划。

四组共同区域、运动限制、目标容差、期限、jerk 和评价窗口。raw 的新几何/目标软代价、全部液体决策关闭；其 contour 为 1，另外三组为 0.02。几何权重 0.05、曲率变化权重 0.01、目标权重 2 和 RTI 5 次均为软件候选。`trajectory_common.yaml` 开 MPC 停车交接和 jerk，`terminal.complete_stop.enable=false`；此前 `model_v234_development.yaml` 是历史开发覆盖层，不能无条件叠到新四组上。

默认四组使用普通停车，均未开启完整液体稳定等待；raw 的液体决策必须保持关闭，液体组可另用覆盖层启用并单独记录。基础 observer 默认仍为 odom，实物 I0/processed-IMU 来源须用覆盖层明确配置并核对实际 selection。没有新相机融合估计器，内部 modal 高度不等于实测液面。

## 已知未关闭问题

[本轮复审及避障接入梳理](../../../../docs/实物实验注意事项/后续改进/20260916_局部规划器复审与避障接入梳理.md)新增确认5项：终端制动绕过未来区域检查、液体关闭组TF/odom时刻混用、投影跳分支、普通到达锁存漏查位置、不发布速度模式缺少命令历史。3个核心反例（停车、投影、到达锁存）已实际复现，ROS两项为静态确认；本轮仅梳理，均未修复。

| 问题 | 当前证据与下一步 |
| --- | --- |
| 缓弯但未稳定降晃 | 单弯和连续弯变短、变快，模型液面峰值高于 raw；须平衡真实目标、速度/进度和液体项，固定评价窗分辨几何与减速贡献 |
| 无上层单弯失败 | 两个执行器条件下 geometry 返回 `ACADOS_SOLVE_FAILED_4`；失败前的低曲率不算成功 |
| 剩余任务可接续性 | 6 个真实在线末端接名义后缀均失败，一个后缀重优化未找到可行解；后缀尚未参与在线目标 |
| 实时性 / RUN-02 | 完整组 P95 约21–34 ms，最坏约100 ms；计算后运输延迟仍有残差，不能只统计 solver time |
| 静止噪声 / RUN-01 | 新配置 actual_v_min=-0.002 缓解原非负边界与微负测量冲突；尚无静止/真回退标定，不能认定实物已解决 |
| 低速执行器 / RUN-03 | 固定 FOPDT 尚未覆盖死区、摩擦、换向与现场传输差异；quiet 阈值和停车应按测量噪声复核 |
| 物理液面边界 / LIQ-01 | freeboard/margin、液量、安装和传感作用点须实测；恢复目标/预算不能从旧模型直接移植 |
| 空间与避障 | 显式区域依赖人工/任务输入；动态障碍、地图到区域和在线拓扑选路没有完整主线链路 |
| ROS 与新实物 | 消息及 solver 整体重建、frame/时间/实际参数/不发布速度回放和执行链检查仍须完成 |

后续顺序：先核查实物入口与隐藏缺陷，再复盘速度/目标/防晃权衡和可接续性，解决最坏周期，最后冻结新实物版本。避障先梳理现有能力、数据链和缺失接口，不直接把旧 obstacle 开关视为完整避障能力。

## 历史问题如何追溯

V1 定义及测量边界见 [9月13日记录](../../../../docs/实物实验注意事项/对比试验/解决问题的思路/20260913_液体模型一致性V1实现与离线验证.md)；V2–V4 的旧测试、恢复与停车定义见 [9月14日记录](../../../../docs/实物实验注意事项/对比试验/解决问题的思路/20260914_代价核准液面恢复与完整停车实现验证.md)。

8月30日的 SPD/TRK/COST/FEAS/REF/CFG/TERM/SAFE 等问题保存在[历史审查](../../../../docs/实物实验注意事项/对比试验/解决问题的思路/20260830_基础跟踪代码审查历史存档.md)。本轮已修改参考拟合、完整计划、状态传播与停车，旧反例须在当前实现重新核对；未复现的问题不能直接算现存 bug，也不能因已有测试通过一概关闭。
