#pragma once

// Compatibility facade.  The authoritative backend interface lives in
// solver/api so implementations do not depend on the historical core path.
#include "spmpc_local_planner/solver/api/solver.h"
