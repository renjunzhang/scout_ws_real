<launch>
  <!-- ===== Cartographer 纯定位 Launch 文件 (实物) ===== -->
  <!-- 用途：加载已有的 .pbstream 地图进行定位，不更新地图 -->

  <!-- ===== 地图配置 ===== -->
  <arg name="map_file" default="$(find scout_maps)/maps/map_carto_20260629_R0.pbstream" />

  <!-- ===== 传感器配置 ===== -->
  <arg name="scan_topic" default="/scan_front" />
  <arg name="odom_topic" default="/odom" />

  <!-- ===== 可视化配置 ===== -->
  <arg name="use_rviz" default="true" />

  <!-- 1) Cartographer 定位节点 -->
  <node name="cartographer_node" pkg="cartographer_ros" type="cartographer_node" 
        args="-configuration_directory $(find nanoscan3_localization)/config
              -configuration_basename scout_2d_localization.lua
              -load_state_filename $(arg map_file)"
        output="screen">
    <remap from="scan" to="$(arg scan_topic)" />
    <remap from="odom" to="$(arg odom_topic)" />
  </node>

  <!-- 2) 栅格地图发布（用于 RViz 显示和导航） -->
  <node name="cartographer_occupancy_grid_node" pkg="cartographer_ros" 
        type="cartographer_occupancy_grid_node" args="-resolution 0.02"
        output="screen" />

  <!-- 3) RViz 可视化 -->
  <node if="$(arg use_rviz)" name="rviz" pkg="rviz" type="rviz"
        args="-d $(find nanoscan3_localization)/rviz/cartographer_localization.rviz" 
        output="screen" />
</launch>
