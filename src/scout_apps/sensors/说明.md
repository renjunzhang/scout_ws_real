# Codex
codex resume 019c0a43-c32b-71f1-b6e9-938e78ce16dc
# sensors

本目录包含 Nanoscan3 前置激光雷达相关的三个功能包：

- **nanoscan3_bringup**：驱动与前置雷达启动。启动 `sick_safetyscanners`，发布 `/scan_front` 和 `/scan_front_filtered`，并发布 `base_link -> nanoscan3_front` 静态 TF。入口：`launch/nanoscan3_front.launch`。
- **nanoscan3_mapping**：基于 gmapping 的建图包，订阅 `/scan_front` 生成地图，可选 RViz。入口：`launch/scout_nanoscan3_gmapping.launch`。
- **nanoscan3_localization**：基于 AMCL 的定位包，加载 `scout_maps` 包内地图进行定位，可选 RViz。入口：`launch/scout_nanoscan3_amcl.launch`（`map_file` 参数可覆盖）。

## 快速使用

### 1. 建图（Gmapping）
```bash
# 终端 1：底盘
roslaunch scout_bringup scout_mini_robot_base.launch

# 终端 2：雷达
roslaunch nanoscan3_bringup nanoscan3_front.launch use_rviz:=false

# 终端 3：建图
roslaunch nanoscan3_mapping scout_nanoscan3_gmapping.launch use_rviz:=true

# 终端 4：键盘控制
rosrun teleop_twist_keyboard teleop_twist_keyboard.py

# 保存地图到 scout_maps 包内
rosrun map_server map_saver -f $(rospack find scout_maps)/maps/map122
```

### 2. 定位（AMCL）
```bash
# 终端 1：底盘
roslaunch scout_bringup scout_mini_robot_base.launch

# 终端 2：雷达
roslaunch nanoscan3_bringup nanoscan3_front.launch use_rviz:=false

# 终端 3：定位（可按需指定 map_file）
roslaunch nanoscan3_localization scout_nanoscan3_amcl.launch use_rviz:=true
```

## 话题与 TF 约定
- 话题：`/scan_front`（原始）与 `/scan_front_filtered`（滤波后）
- TF：`map -> odom -> base_link -> nanoscan3_front`
- 雷达坐标系：`nanoscan3_front`（默认）

## 地图管理
- 地图统一放在 `scout_maps` 包：`$(rospack find scout_maps)/maps/`
- 推荐命名：`mapXXX.yaml` / `mapXXX.pgm`

## 设备与参数
- 雷达 IP、主机 IP、外参见 `nanoscan3_bringup/launch/nanoscan3_front.launch`。
- 定位参数（AMCL）见 `nanoscan3_localization/launch/scout_nanoscan3_amcl.launch`。

---

## 仿真使用

### 1. 启动仿真环境
```bash
roslaunch scout_description scout_mini_gazebo.launch use_rviz:=false
```

### 2. 启动键盘控制
```bash
roslaunch scout_bringup scout_teleop_keyboard.launch
```

### 3. 启动激光雷达（仿真）
```bash
roslaunch nanoscan3_bringup nanoscan3_front_sim.launch use_rviz:=false
```

### 4. 建图（仿真）
```bash
roslaunch nanoscan3_mapping scout_nanoscan3_gmapping_sim.launch use_rviz:=true
```

---

## 修改记录

### 2026-01-30 仿真 gmapping 参数优化

**问题现象**：
- 仿真中 gmapping 建图时，RViz 中 base_link axes 明显抖动/跳变
- 建图效果差，地图容易偏移

**根本原因**：
- `libgazebo_ros_skid_steer_drive.so` 使用理想运动学模型计算里程计
- 但 Gazebo 物理引擎模拟了滑移转向的轮子打滑
- 两者不一致导致里程计累积误差，尤其转弯时
- gmapping 默认参数对里程计信任过高

**修改文件**：`nanoscan3_mapping/launch/scout_nanoscan3_gmapping_sim.launch`

**参数调整**：

| 参数 | 默认值 | 修改值 | 说明 |
|------|--------|--------|------|
| `srr` | 0.1 | 0.2 | 平移引起的平移误差容忍度 |
| `srt` | 0.2 | 0.2 | 平移引起的旋转误差 |
| `str` | 0.1 | 0.2 | 旋转引起的平移误差 |
| `stt` | 0.2 | **0.4** | 旋转引起的旋转误差（关键！滑移转向重点） |
| `particles` | 30 | 80 | 粒子数，越多越稳定 |
| `minimumScore` | 0 | 100 | 扫描匹配最低分，防止错误匹配 |
| `delta` | 0.05 | 0.05 | 地图分辨率 5cm |
| `maxUrange` | - | 8.0 | 激光最大可用距离 |
| `maxRange` | - | 10.0 | 激光最大测量距离 |
| `linearUpdate` | 1.0 | 0.2 | 移动 0.2m 更新一次 |
| `angularUpdate` | 0.5 | 0.25 | 旋转 14° 更新一次 |
| `temporalUpdate` | -1 | 2.0 | 静止 2 秒也更新 |

**效果**：
- ✅ axes 不再跳变
- ⚠️ 建图仍有一定漂移（滑移转向仿真的固有问题）

**使用建议**：
- 慢速移动（0.2~0.3 m/s）
- 避免原地旋转，尽量大弧度转弯
- 如仍不稳定，可尝试增大 `stt` 到 0.5~0.6 或 `particles` 到 100~150

---

## Cartographer 安装（源码编译）

Cartographer 是 Google 开源的激光 SLAM 算法，比 gmapping 对里程计依赖更低，更适合滑移转向机器人。

### 安装目录

```
/home/a/scout_ws/src/scout_apps/sensors/cartographer_ws/
├── src/
│   ├── cartographer/           # 核心库
│   └── cartographer_ros/       # ROS 封装
├── build_isolated/
├── devel_isolated/
└── install_isolated/           # 安装目录
```

### 安装步骤（可移植到工控机）

#### 步骤 1：安装系统依赖

```bash
sudo apt-get update
sudo apt-get install -y \
    cmake g++ git google-mock \
    libboost-all-dev \
    libcairo2-dev \
    libceres-dev \
    libcurl4-openssl-dev \
    libeigen3-dev \
    libgflags-dev \
    libgoogle-glog-dev \
    liblua5.2-dev \
    libprotobuf-dev \
    libsuitesparse-dev \
    lsb-release \
    ninja-build \
    protobuf-compiler \
    python3-sphinx \
    python3-wstool \
    stow
```

#### 步骤 2：创建工作空间并下载源码

```bash
CARTO_WS=/home/a/scout_ws/src/scout_apps/sensors/cartographer_ws
mkdir -p ${CARTO_WS}/src
cd ${CARTO_WS}/src

# 使用镜像加速下载（国内网络）
git clone https://ghproxy.cc/https://github.com/cartographer-project/cartographer.git
git clone https://ghproxy.cc/https://github.com/cartographer-project/cartographer_ros.git

# 或直接从 GitHub 下载（需科学上网）
# git clone https://github.com/cartographer-project/cartographer.git
# git clone https://github.com/cartographer-project/cartographer_ros.git
```

#### 步骤 3：安装 abseil-cpp

```bash
cd ${CARTO_WS}/src/cartographer/scripts
chmod +x install_abseil.sh
./install_abseil.sh

# 如果 stow 有冲突，执行：
sudo rm -f /usr/local/lib/pkgconfig/absl_*.pc
cd /usr/local/stow && sudo stow absl
```

#### 步骤 4：禁用 Sphinx 文档（避免 jinja2 冲突）

```bash
sed -i 's/find_package(Sphinx)/# find_package(Sphinx)/' \
    ${CARTO_WS}/src/cartographer/CMakeLists.txt
```

#### 步骤 5：安装 ROS 依赖

```bash
cd ${CARTO_WS}
rosdep install --from-paths src --ignore-src -r -y
# 忽略 libabsl-dev 错误（已从源码安装）
```

#### 步骤 6：编译

```bash
cd ${CARTO_WS}
catkin_make_isolated --install --use-ninja \
    -DCMAKE_BUILD_TYPE=Release \
    -DBUILD_GRPC=OFF \
    -DBUILD_TESTING=OFF
```

编译时间约 10-30 分钟，取决于机器性能。

#### 步骤 7：配置环境

```bash
# 添加到 ~/.bashrc
echo 'source /home/a/scout_ws/src/scout_apps/sensors/cartographer_ws/install_isolated/setup.bash' >> ~/.bashrc
source ~/.bashrc
```

### 使用方法

```bash
# 1. 启动仿真
roslaunch scout_description scout_mini_gazebo.launch use_rviz:=false

# 2. 键盘控制
roslaunch scout_bringup scout_teleop_keyboard.launch

# 3. 激光雷达
roslaunch nanoscan3_bringup nanoscan3_front_sim.launch use_rviz:=false

# 4. Cartographer 建图
roslaunch nanoscan3_mapping scout_nanoscan3_cartographer_sim.launch
```

### 配置文件

- Launch 文件：`nanoscan3_mapping/launch/scout_nanoscan3_cartographer_sim.launch`
- Lua 配置：`nanoscan3_mapping/config/scout_2d_sim.lua`

### 关键配置说明

| 参数 | 值 | 说明 |
|------|-----|------|
| `use_online_correlative_scan_matching` | true | 纯激光匹配，减少对 odom 依赖 |
| `use_imu_data` | false | 仿真无 IMU |
| `provide_odom_frame` | false | 使用 Gazebo 提供的 odom |
| `angular_search_window` | 30° | 较大搜索窗口容忍航向误差 |

### 注意事项

1. **编译需要较大内存**：建议至少 8GB RAM
2. **abseil-cpp 全局安装**：安装到 `/usr/local`，这是官方推荐方式
3. **环境隔离**：Cartographer 使用独立工作空间，不影响主工作空间
4. **source 顺序**：先 source 主工作空间，再 source cartographer_ws

### 编译问题排查

#### 问题 1：Sphinx/jinja2 冲突
```
Extension error: Could not import extension sphinx.builders.latex 
(exception: cannot import name 'contextfunction' from 'jinja2')
```
**解决**：禁用 Sphinx 文档编译（步骤 4）

#### 问题 2：glog 库冲突
```
undefined reference to `lzma_index_uncompressed_size'
```
**原因**：系统中有 Conan 安装的 glog 与系统 glog 冲突  
**解决**：
```bash
mv ~/.conan/data/glog ~/.conan/data/glog.bak
# 然后重新编译
```

#### 问题 3：包名重复错误
```
Multiple packages found with the same name "cartographer"
```
**解决**：在源码目录添加 CATKIN_IGNORE
```bash
touch ${CARTO_WS}/src/cartographer/CATKIN_IGNORE
```

#### 问题 4：stow 链接冲突
```
existing target is neither a link nor a directory: lib/pkgconfig/absl_*.pc
```
**解决**：删除旧文件后重新链接
```bash
sudo rm -f /usr/local/lib/pkgconfig/absl_*.pc
cd /usr/local/stow && sudo stow absl
```

#### 问题 5：运行时 libglog.so.1 找不到（重要！）
```
error while loading shared libraries: libglog.so.1: cannot open shared object file
```

**根本原因**：
- 系统安装的是 glog 0.4.0，提供 `libglog.so.0`
- 如果 `~/.bashrc` 中有 Conan 安装的 glog (0.6.0)、gflags、ceres-solver 的 `LD_LIBRARY_PATH`，这些库依赖 `libglog.so.1`
- 程序运行时会从 `LD_LIBRARY_PATH` 优先加载 Conan 版本的 ceres，而 Conan ceres 依赖 `libglog.so.1`

**诊断方法**：
```bash
# 检查 LD_LIBRARY_PATH 是否包含 conan 路径
echo $LD_LIBRARY_PATH | tr ':' '\n' | grep conan

# 检查 cartographer_node 的依赖
ldd ~/scout_ws/src/scout_apps/sensors/cartographer_ws/install_isolated/lib/cartographer_ros/cartographer_node | grep -E "glog|ceres|not.found"
```

**解决方案**：

1. **注释掉 `~/.bashrc` 中冲突的 Conan 路径**（推荐）：
   ```bash
   # 编辑 ~/.bashrc，找到并注释掉以下路径（如果存在）：
   # export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/a/.conan/data/gflags/...
   # export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/a/.conan/data/glog/...
   # export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:/home/a/.conan/data/ceres-solver/...
   
   # 保留 osqp 和 libunwind（如果 MPC 模块需要）
   ```

2. **或者使用干净环境启动脚本**：
   ```bash
   # 使用已创建的脚本
   ~/scout_ws/src/scout_apps/sensors/nanoscan3_mapping/scripts/start_cartographer_clean.sh
   ```

3. **如果修改了 .bashrc，需要打开新终端**（或重新登录）才能生效

**验证修复**：
```bash
# 在新终端中执行
ldd ~/scout_ws/src/scout_apps/sensors/cartographer_ws/install_isolated/lib/cartographer_ros/cartographer_node | grep "not found"
# 应该没有任何输出
```

**Conan 路径冲突的背景**：
- 如果之前安装过 MPC 相关的 Conan 包（如 osqp、ceres-solver），可能会在 `~/.bashrc` 中添加这些路径
- Conan 版本的 glog 是 0.6.0（导出 libglog.so.1），与系统 glog 0.4.0 冲突
- 解决方法是只保留必要的 Conan 路径（osqp、libunwind），注释掉与系统库冲突的路径（gflags、glog、ceres-solver）

---

### 完整编译命令参考（重新编译时使用）

如果需要从头重新编译 Cartographer（例如修复库冲突后）：

```bash
# 1. 确保使用干净的 ROS 环境（不 source 任何 workspace）
source /opt/ros/noetic/setup.bash

# 2. 进入 cartographer 工作空间
cd ~/scout_ws/src/scout_apps/sensors/cartographer_ws

# 3. 删除 CATKIN_IGNORE（如果存在）
rm -f src/cartographer/CATKIN_IGNORE src/cartographer_ros/CATKIN_IGNORE

# 4. 清理旧的构建（可选，完全重新编译时使用）
# rm -rf build_isolated devel_isolated install_isolated

# 5. 编译
catkin_make_isolated --install --use-ninja -j4 -DCMAKE_BUILD_TYPE=Release \
    --source src --build build_isolated --devel devel_isolated --install-space install_isolated

# 6. 恢复 CATKIN_IGNORE（防止主工作空间扫描到这些包）
touch src/cartographer/CATKIN_IGNORE src/cartographer_ros/CATKIN_IGNORE
```

---

### 移植到新机器的检查清单

1. ✅ 安装系统依赖（步骤 1）
2. ✅ 安装 abseil-cpp（步骤 3）
3. ✅ 禁用 Sphinx（步骤 4）
4. ✅ 编译 Cartographer（步骤 6）
5. ✅ 检查 `~/.bashrc` 中是否有 Conan 的 glog/gflags/ceres 路径，如有则注释掉
6. ✅ 验证 `ldd cartographer_node | grep "not found"` 无输出
7. ✅ 测试启动：`roslaunch nanoscan3_mapping scout_nanoscan3_cartographer_sim.launch`
